---
phase: 15-journal-gate-hook-context7-mcp
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/npm/src/steps/journal-gate-hook.integration.test.ts
  - packages/pip/tests/test_journal_gate_hook.py
  - templates/.claude/settings.json
  - .claude/settings.json
  - packages/npm/templates/.claude/settings.json
autonomous: true
gap_closure: true
requirements: [HOOK-01, HOOK-02]

must_haves:
  truths:
    - "A `git commit` whose command text includes `-C <path>` targeting a repo DIFFERENT from the hook's own cwd is checked against the TARGET repo's actual staged-file/merge/rebase state, not the hook process's cwd"
    - "cwd=repoA (JOURNAL.md staged), command `git -C <repoB, JOURNAL.md unstaged> commit -am \"fix\"` -> BLOCKED (exit 2) — no bypass"
    - "cwd=repoB (JOURNAL.md unstaged), command `git -C <repoA, JOURNAL.md staged> commit -am \"fix\"` -> ALLOWED (exit 0) — no false block"
    - "All 12 pre-existing scenarios in journal-gate-hook.integration.test.ts and test_journal_gate_hook.py still pass unchanged"
    - "templates/.claude/settings.json and .claude/settings.json carry byte-identical hooks.PreToolUse blocks (dogfood parity preserved) after the fix"
  artifacts:
    - path: "packages/npm/src/steps/journal-gate-hook.integration.test.ts"
      provides: "14 total scenario tests (12 existing + 2 new cross-repo -C adversarial cases)"
      min_lines: 110
    - path: "packages/pip/tests/test_journal_gate_hook.py"
      provides: "14 total scenario tests (12 existing + 2 new cross-repo -C adversarial cases), mirrored"
      min_lines: 110
    - path: "templates/.claude/settings.json"
      provides: "hooks.PreToolUse[0].hooks[0].command extracts a -C <path> target from UNQUOTED and routes GITDIR/MERGE_HEAD/rebase/staged-file checks through it via a GIT() wrapper function"
      contains: "TARGETDIR"
  key_links:
    - from: "templates/.claude/settings.json"
      to: ".claude/settings.json"
      via: "identical hooks.PreToolUse block (dogfood D-01), each retaining its own distinct permissions block"
      pattern: "TARGETDIR"
    - from: "hook command UNQUOTED variable"
      to: "TARGETDIR extraction"
      via: "sed -nE capture of the argument following a `-C` token, empty string if absent"
      pattern: "TARGETDIR=\\$\\(printf"
    - from: "TARGETDIR"
      to: "GITDIR resolution / MERGE_HEAD-rebase checks / staged-file diff"
      via: "GIT() wrapper function that prepends `-C \"$TARGETDIR\"` to every git invocation when TARGETDIR is non-empty"
      pattern: "GIT\\(\\) \\{"
---

<objective>
Close the sole remaining BLOCKER gap from `15-VERIFICATION.md` (independently reproduced from `15-REVIEW.md` CR-01, a NEW defect found after plan 15-04 already closed a different, earlier gap): the journal-gate hook's git-state checks (`GITDIR` resolution, the `MERGE_HEAD`/`rebase-merge`/`rebase-apply` exemption checks, and `git diff --cached --name-only`) always run against the hook process's own cwd and never honor a `-C <path>` argument present in the intercepted command text. This produces both a bypass (a commit to an unstaged target repo is wrongly allowed) and a false block (a commit to a correctly-staged target repo is wrongly blocked). `-C` handling was an explicit, named goal during original phase planning (15-CONTEXT.md, 15-01-PLAN.md Test 8: "the `-C` variant must still be caught") — this closes an incomplete implementation of a stated requirement, not new scope.

**Chosen approach — full `-C <path>` support, not fail-closed-on-detection:** 15-REVIEW.md CR-01 offered two options: (1) extract the `-C` target and route every git invocation through it, or (2) fail closed (always block) whenever `-C`/`--git-dir=`/`--work-tree=` is detected, if full support is judged too risky. This plan takes option (1) because this gap's own mandated test scenario — cwd unstaged, `-C` target staged, must ALLOW (exit 0) — cannot be satisfied by a fail-closed policy: any policy that blocks on `-C` detection alone would incorrectly block that required scenario. Only routing the checks to the actual target directory satisfies both mandated directions (block when the real target is unstaged, allow when it is staged), so full `-C` support is the only approach that passes the test scenarios named in `15-VERIFICATION.md`.

**Scope boundary for this fix:** only `-C <path>` is parsed and honored, matching the exact flag named in the original phase's goal (15-01-PLAN.md Test 8) and in both of this gap's required test scenarios. `--git-dir=`/`--work-tree=` are explicitly NOT parsed by this fix (documented as an accepted limitation in `<threat_model>` T-15-09, mirroring the T-15-05 precedent from plan 15-04) — adding two more capture-group branches to an already fragile triple-escaped one-liner (15-REVIEW.md WR-02) for flags neither named in the original goal nor exercised by either required test scenario is out of scope for this gap-closure round.

**Explicitly out of scope for this plan** (separate warnings in 15-REVIEW.md, not part of this VERIFICATION.md gap — noted, no tasks created): WR-01 (no automated parity check between the two hand-duplicated hook copies), WR-02 (single triple-escaped-string maintainability concern), WR-03 (pre-existing `rm -rf *`/hardcoded path in `.claude/settings.json`), WR-04 (update/upgrade docs conflation), WR-05 (stale TDD-era comment).

Purpose: The journal-gate hook is the phase's core deliverable — a commit-time guardrail enforcing "update JOURNAL.md every task". A hook that checks the wrong repository's state under a trivially-available flag (`-C <path>`) provides false confidence in both directions, which is worse than no guardrail — the exact defect class this plan exists to remove.
Output: Fixed hook command (adds `TARGETDIR` extraction + a `GIT()` wrapper function) embedded in `templates/.claude/settings.json`, `.claude/settings.json`, and the regenerated npm prebuild mirror; 2 new adversarial cross-repo test scenarios (both bypass directions) added to both existing test suites, all 14 scenarios (12 original + 2 new) green in both packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-journal-gate-hook-context7-mcp/15-VERIFICATION.md
@.planning/phases/15-journal-gate-hook-context7-mcp/15-REVIEW.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cross-repo -C adversarial regression tests (RED)</name>
  <files>packages/npm/src/steps/journal-gate-hook.integration.test.ts, packages/pip/tests/test_journal_gate_hook.py</files>
  <read_first>
    packages/npm/src/steps/journal-gate-hook.integration.test.ts (full file — reuse the `runHook`/`getHookCommand` helpers, the `beforeEach`/`afterEach` `repoDir` lifecycle, and the exact `it(...)` naming style; note `beforeEach` creates `repoDir` as a fresh git repo with an unstaged `JOURNAL.md`, and `afterEach` recursively removes `repoDir` — a nested subdirectory created inside `repoDir` is cleaned up automatically, no new teardown needed)
    packages/pip/tests/test_journal_gate_hook.py (full file — reuse the `_run_hook`/`_get_hook_command` helpers and the `repo_dir` fixture, which is `tmp_path` directly per CLAUDE.md's "The tmp_dir pytest fixture" convention — a nested subdirectory under `repo_dir` is cleaned up automatically by pytest's `tmp_path` teardown)
    .planning/phases/15-journal-gate-hook-context7-mcp/15-VERIFICATION.md (Behavioral Spot-Checks table, the two NEW rows — exact scenario definitions and expected/actual exit codes)
    .planning/phases/15-journal-gate-hook-context7-mcp/15-REVIEW.md (CR-01, lines 42-75 — root cause and the exact reproduction transcript)
    CLAUDE.md ("Regression tests" and "Test naming" conventions — sentence-style names, RED committed before GREEN)
  </read_first>
  <behavior>
    Add exactly 2 new test cases per file, appended after the existing 12 in each file, using the existing `runHook`/`_run_hook` helpers unchanged (no new helper functions required, no mocking). Each test creates a SECOND, independent git repo nested inside the existing fixture directory (`join(repoDir, 'other-repo')` for npm, `repo_dir / "other-repo"` for pip) via a real `git init` — do not stage/unstage `JOURNAL.md` in this second repo through the hook; set it up directly with `git add`/plain file write, exactly like the existing `repoDir`/`repo_dir` setup in `beforeEach`/the `repo_dir` fixture. No git user.email/user.name config is needed in the second repo since the hook only runs `git rev-parse`/`git diff --cached` against it, never `git commit`.
    - Test D (npm: `it(...)`, pip: `def test_...`): the hook's own cwd (`repoDir`/`repo_dir`) HAS `JOURNAL.md` staged; the second repo (`other-repo`) has `JOURNAL.md` present but NOT staged. Run the hook with cwd = the hook's own repo and command = `git -C <other-repo-absolute-path> commit -am "fix"`. Expected: exit code 2, stderr (trimmed) equals `BLOCKED: JOURNAL.md not staged. Update JOURNAL.md, then: git add JOURNAL.md`. This is 15-VERIFICATION.md's bypass direction (a commit to an unstaged target repo, reached via `-C`, must not be silently allowed just because the hook's own cwd happens to be staged).
    - Test E (npm: `it(...)`, pip: `def test_...`): the hook's own cwd HAS `JOURNAL.md` present but NOT staged (the fixture's default state — do not stage it); the second repo (`other-repo`) has `JOURNAL.md` staged. Run the hook with cwd = the hook's own repo and command = `git -C <other-repo-absolute-path> commit -am "fix"`. Expected: exit code 0. This is 15-VERIFICATION.md's false-block direction (a commit to a correctly-staged target repo, reached via `-C`, must not be blocked just because the hook's own cwd happens to be unstaged).
    Name the npm tests: `it('blocks a git -C commit targeting a different, unstaged repo even when the hook's own cwd has JOURNAL.md staged', ...)` and `it('allows a git -C commit targeting a different, staged repo even when the hook's own cwd has JOURNAL.md unstaged', ...)`.
    Name the pip tests: `def test_blocks_dash_c_commit_targeting_different_unstaged_repo_when_cwd_staged(repo_dir):` and `def test_allows_dash_c_commit_targeting_different_staged_repo_when_cwd_unstaged(repo_dir):`.
    At this point (before Task 2's fix), Test D must FAIL (the currently-shipped hook resolves `GITDIR`/staged-check against the hook's own cwd only, which IS staged in this scenario, so it currently returns exit 0 — a bypass) and Test E must FAIL (the currently-shipped hook resolves against the hook's own cwd, which is NOT staged in this scenario, so it currently returns exit 2 — a false block) — this is the RED step, confirmed against the live, currently-shipped (unfixed) `templates/.claude/settings.json`. Do not modify any of the 12 existing test cases.
  </behavior>
  <action>
    Append the 2 new test cases to each of the two existing files exactly as described above. Do not touch `templates/.claude/settings.json`, `.claude/settings.json`, or `packages/npm/templates/.claude/settings.json` in this task — the fix lands in Task 2, and these new tests must be observed failing first for the correct (documented) reason.
  </action>
  <acceptance_criteria>
    - `packages/npm/src/steps/journal-gate-hook.integration.test.ts` contains 14 total `it(...)` blocks (12 unchanged + 2 new)
    - `packages/pip/tests/test_journal_gate_hook.py` contains 14 total `def test_...():` functions (12 unchanged + 2 new)
    - Running `cd packages/npm && npx vitest run src/steps/journal-gate-hook.integration.test.ts` reports exactly 2 failing tests (the 2 new ones) and 12 passing
    - Running `cd packages/pip && uv run pytest tests/test_journal_gate_hook.py` reports exactly 2 failing tests (the 2 new ones) and 12 passing
    - Neither new test mocks `execa`/`subprocess.run`/`sh` (grep confirms no `vi.mock` or `mocker.patch` added: `grep -c 'vi.mock\|mocker.patch' packages/npm/src/steps/journal-gate-hook.integration.test.ts packages/pip/tests/test_journal_gate_hook.py` reports 0 for both)
  </acceptance_criteria>
  <verify>
    <automated>cd packages/npm && npx vitest run src/steps/journal-gate-hook.integration.test.ts 2>&1 | tail -25; cd ../pip && uv run pytest tests/test_journal_gate_hook.py 2>&1 | tail -25</automated>
  </verify>
  <done>Both test files have 14 total scenarios each, the 2 new cross-repo adversarial tests fail against the current (unfixed) hook for the documented reason (bypass in Test D, false block in Test E), and the 12 pre-existing tests remain green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route git-state checks through the -C target directory (GREEN)</name>
  <files>templates/.claude/settings.json, .claude/settings.json, packages/npm/templates/.claude/settings.json</files>
  <read_first>
    templates/.claude/settings.json (current hook command — full JSON, confirm the exact current string, i.e. plan 15-04's `UNQUOTED`-stripping fix, before editing)
    .claude/settings.json (confirm `hooks.PreToolUse` is byte-identical to `templates/.claude/settings.json`, and that `permissions.allow`'s 3 existing entries — `Bash(rm -rf *)`, the node dist/index.js entry, the echo entry — are left untouched)
    .planning/phases/15-journal-gate-hook-context7-mcp/15-REVIEW.md (CR-01 fix suggestion, lines 65-75)
    .planning/phases/15-journal-gate-hook-context7-mcp/15-04-PLAN.md Task 2 (precedent for the exact editing method: `node -e` `JSON.parse`/`JSON.stringify` round-trip, never hand-typed/re-escaped)
  </read_first>
  <behavior>
    After this task, all 14 tests in both `packages/npm/src/steps/journal-gate-hook.integration.test.ts` and `packages/pip/tests/test_journal_gate_hook.py` must pass, including the 2 new cross-repo cases from Task 1, with zero regressions in the 12 pre-existing scenarios.
  </behavior>
  <action>
    Set the value of `hooks.PreToolUse[0].hooks[0].command` in both `templates/.claude/settings.json` and `.claude/settings.json` to the exact JSON string value given below — copy it literally, including its surrounding double quotes, as the JSON value of that key. Do not hand-derive, retype, or re-escape it — this exact string has already been execution-verified: `JSON.parse()`-ing it and running the result with `sh -c` against all 14 scenarios (the 12 pre-existing plus Test D and Test E from Task 1) in two hand-built throwaway git repos produced the correct exit code for every scenario, including both new cross-repo directions.

    The exact JSON string value to write as `hooks.PreToolUse[0].hooks[0].command` is:
    "IN=$(cat); CMD=$(printf \"%s\" \"$IN\" | sed -E \"s/.*\\\"command\\\"[[:space:]]*:[[:space:]]*\\\"((\\\\\\\\.|[^\\\"\\\\\\\\])*)\\\".*/\\\\1/\"); UNQUOTED=$(printf \"%s\" \"$CMD\" | sed -E \"s/\\\\\\\\\\\"[^\\\"]*\\\\\\\\\\\"/ /g; s/'[^']*'/ /g\"); printf \"%s\" \"$UNQUOTED\" | grep -qE \"(^|[[:space:]])git([[:space:]]+[^[:space:]]+)*[[:space:]]+commit([[:space:]]|$)\" || exit 0; printf \"%s\" \"$UNQUOTED\" | grep -qE -- \"(^|[[:space:]])--amend([[:space:]]|$)\" && exit 0; TARGETDIR=$(printf \"%s\" \"$UNQUOTED\" | sed -nE \"s/.*(^|[[:space:]])git[[:space:]]+-C[[:space:]]+([^[:space:]]+).*/\\\\2/p\"); GIT() { if [ -n \"$TARGETDIR\" ]; then git -C \"$TARGETDIR\" \"$@\"; else git \"$@\"; fi; }; GITDIR=$(GIT rev-parse --git-dir 2>/dev/null) || exit 0; { [ -f \"$GITDIR/MERGE_HEAD\" ] || [ -d \"$GITDIR/rebase-merge\" ] || [ -d \"$GITDIR/rebase-apply\" ]; } && exit 0; GIT diff --cached --name-only | grep -qx \"JOURNAL.md\" && exit 0; echo \"BLOCKED: JOURNAL.md not staged. Update JOURNAL.md, then: git add JOURNAL.md\" >&2; exit 2"

    Recommended editing method (matches 15-04's precedent exactly): write a small `node -e` script that reads each settings.json with `JSON.parse`, sets `obj.hooks.PreToolUse[0].hooks[0].command` to that string's *parsed* (unescaped) form — i.e. the result of `JSON.parse('<the exact JSON string value above>')` — and writes the object back with `JSON.stringify(obj, null, 2)`. This guarantees the on-disk file is valid JSON and the resulting escaped command is byte-identical to the verified string above, without any manual re-escaping. Do not hand-edit the escaped string directly into the file with a text editor.

    What changed vs. the currently-shipped (15-04) command: two new statements were inserted immediately after the existing `--amend` exemption check and before the existing `GITDIR=$(git rev-parse --git-dir ...)` line: (1) `TARGETDIR=$(printf "%s" "$UNQUOTED" | sed -nE "s/.*(^|[[:space:]])git[[:space:]]+-C[[:space:]]+([^[:space:]]+).*/\2/p")` — extracts the argument immediately following a `-C` token from the unquoted command text, or an empty string if no `-C` is present; (2) `GIT() { if [ -n "$TARGETDIR" ]; then git -C "$TARGETDIR" "$@"; else git "$@"; fi; }` — a shell function that prepends `-C "$TARGETDIR"` to every git invocation when a target was found, otherwise behaves as plain `git`. The three subsequent git invocations — `GITDIR=$(git rev-parse --git-dir ...)`, the `MERGE_HEAD`/`rebase-merge`/`rebase-apply` exemption block (unchanged, still reads `$GITDIR`-relative paths, which are now target-repo-relative), and `git diff --cached --name-only` — were each changed from bare `git` to `GIT` (the new wrapper). No other line changed: the `$CMD` extraction, the `UNQUOTED` quote-stripping, the commit-subcommand detection, the `--amend` exemption, the `JOURNAL.md` staged-check logic itself, and the final `BLOCKED:` stderr message and `exit 2` all stay exactly as they are today.

    Confirm both files' `hooks.PreToolUse` blocks are byte-identical to each other after editing (`diff <(node -e "console.log(JSON.stringify(require('./templates/.claude/settings.json').hooks))") <(node -e "console.log(JSON.stringify(require('./.claude/settings.json').hooks))")` outputs nothing), and confirm neither file's `permissions` block changed. After editing `templates/.claude/settings.json`, run `cd packages/npm && npm run prebuild` to regenerate the gitignored `packages/npm/templates/.claude/settings.json` mirror (required — the npm test suite's `resolveTemplatesDir()` prefers the already-present `packages/npm/templates/` copy over the repo-root `templates/` dir, so skipping this step leaves the npm suite reading the stale, unfixed hook even after `templates/.claude/settings.json` is correctly updated).
  </action>
  <acceptance_criteria>
    - `cd packages/npm && npx vitest run src/steps/journal-gate-hook.integration.test.ts` exits 0 (all 14 tests pass, including Test D and Test E)
    - `cd packages/pip && uv run pytest tests/test_journal_gate_hook.py` exits 0 (all 14 tests pass, including Test D and Test E)
    - Reproducing the bypass direction manually in two hand-built temp repos (repoA: `JOURNAL.md` staged; repoB: `JOURNAL.md` unstaged) — running the extracted hook command with cwd=repoA and command text `git -C <repoB> commit -am "fix"` — exits 2 with the exact `BLOCKED:` stderr message (previously exited 0)
    - Reproducing the false-block direction manually — same two repos, cwd=repoB and command text `git -C <repoA> commit -am "fix"` — exits 0 (previously exited 2)
    - `node -e "JSON.parse(require('fs').readFileSync('templates/.claude/settings.json','utf-8'))"` and the same for `.claude/settings.json` both exit 0 (valid JSON)
    - `templates/.claude/settings.json` and `.claude/settings.json` have byte-identical `hooks` values (diff empty per the command in `<action>`)
    - `.claude/settings.json`'s `permissions.allow` array still contains all 3 original entries, unchanged by this plan (out of scope per 15-REVIEW.md WR-03)
    - `packages/npm/templates/.claude/settings.json` (post-prebuild) contains the same `hooks` block as `templates/.claude/settings.json`
    - Full regression suites remain green: `cd packages/npm && npm test` and `cd packages/pip && uv run pytest tests/`
  </acceptance_criteria>
  <verify>
    <automated>cd packages/npm && npx vitest run src/steps/journal-gate-hook.integration.test.ts && cd ../pip && uv run pytest tests/test_journal_gate_hook.py</automated>
  </verify>
  <done>Both settings.json files route `GITDIR` resolution, the merge/rebase exemption checks, and the staged-file diff through the `-C` target directory when one is present in the intercepted command; both cross-repo adversarial directions now resolve to the correct exit code; all 14 integration tests pass in both packages; the npm prebuild mirror is regenerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Claude-generated Bash command -> hook stdin | Same boundary as 15-01/15-04: the hook parses a JSON payload built from `tool_input.command`, ultimately model-generated text |
| Hook shell script -> real git state | Unchanged from 15-01/15-04: reads `.git` state (`MERGE_HEAD`, `rebase-merge`, `rebase-apply`, `diff --cached`) — now potentially against a DIFFERENT repo than the hook's cwd |
| Hook shell script -> attacker/model-controlled `-C` path argument | NEW boundary introduced by this fix: `$TARGETDIR` is extracted directly from untrusted command text and passed to `git -C "$TARGETDIR"` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Tampering | `TARGETDIR` extraction / `GIT()` wrapper (the exact bug this plan fixes) | mitigate | Extract a `-C <path>` argument from `UNQUOTED` via an anchored `sed -nE ... p` capture group (empty string if absent); route `GITDIR` resolution, the `MERGE_HEAD`/`rebase-merge`/`rebase-apply` exemption checks, and the staged-file diff through `git -C "$TARGETDIR"` when present via the `GIT()` wrapper function. Closes CR-01's cross-repo bypass (unstaged target wrongly allowed) and false-block (staged target wrongly blocked) in both directions; verified against both new adversarial scenarios plus all 12 pre-existing scenarios in two hand-built temp repos before being written into this plan |
| T-15-09 | Tampering | `--git-dir=`/`--work-tree=` flags (deliberately NOT parsed by this fix) | accept | Only `-C <path>` is extracted and honored. A commit issued with `--git-dir=`/`--work-tree=` instead of `-C` is still checked against the hook's own cwd — same class of defect as CR-01, undiminished for those two flags. Full support for all three flags was considered and rejected: it would add two more capture-group branches to an already-fragile triple-escaped one-liner (15-REVIEW.md WR-02) for flags not named in the original phase's explicit goal (15-01-PLAN.md Test 8 names only `-C`) and not exercised by either of this gap's mandated test scenarios (both use `-C`). Accepted as a known, documented limitation, mirroring the T-15-05 precedent set by plan 15-04 |
| T-15-10 | Injection | `$TARGETDIR` (attacker/model-controlled path text) passed to `git -C "$TARGETDIR"` | mitigate | `$TARGETDIR` is always double-quoted in the `GIT()` wrapper (`git -C "$TARGETDIR" "$@"`), so it cannot be word-split or glob-expanded into extra arguments. A `$TARGETDIR` value that is not a real, existing git working tree simply makes `git -C "$TARGETDIR" rev-parse --git-dir` fail, which the existing `\|\| exit 0` on that line already treats as "cannot verify, let it through" — this fail-open-on-non-repo behavior is unchanged from the original hook design (pre-dates this plan) and is not a new risk introduced by this fix |
| T-15-11 | Elevation of Privilege | fail-closed-on-`-C`-detection (rejected alternative) | accept | 15-REVIEW.md CR-01 offered "fail closed whenever `-C` is detected" as a simpler alternative to full support. This plan rejects it because this gap's own required test scenario (cwd unstaged, `-C` target staged, must ALLOW) cannot be satisfied by any policy that blocks on `-C` detection alone — only routing the checks to the real target directory satisfies both mandated directions |

</threat_model>

<verification>
Run `cd packages/npm && npm test` (full suite) and `cd packages/pip && uv run pytest tests/` (full suite) — both must be green (14/14 in journal-gate-hook tests, all other pre-existing tests unaffected). Manually re-run both cross-repo directions (bypass and false-block) from Task 1's read_first/acceptance criteria against the live extracted command from `templates/.claude/settings.json` using two hand-built temp git repos and confirm both now resolve to the correct exit code. Diff `templates/.claude/settings.json` against `.claude/settings.json` to confirm both carry an identical `hooks.PreToolUse` block while each retains its own distinct `permissions` block. Confirm `packages/npm/templates/.claude/settings.json` (gitignored prebuild mirror) matches after `npm run prebuild`.
</verification>

<success_criteria>
- HOOK-01: the shipped hook reliably blocks `git commit` without `JOURNAL.md` staged in the ACTUAL target repository, including when that repository is reached via `-C <path>` and differs from the hook's own cwd
- HOOK-02: the `MERGE_HEAD`/`rebase-merge`/`rebase-apply` exemption checks and the staged-file check are evaluated against the correct target repository in both same-cwd and cross-repo (`-C`) cases
- The two mandated adversarial directions from `15-VERIFICATION.md` both resolve correctly: cwd-staged/target-unstaged -> BLOCK, cwd-unstaged/target-staged -> ALLOW
- 14/14 integration tests pass in both npm and pip packages (12 original + 2 new cross-repo adversarial), zero mocking of the shell subprocess
- `templates/.claude/settings.json` and `.claude/settings.json` remain in dogfood parity (identical `hooks` block)
- `--git-dir=`/`--work-tree=` remain a documented, accepted limitation (T-15-09) — not silently claimed as fixed
</success_criteria>

<output>
Create `.planning/phases/15-journal-gate-hook-context7-mcp/15-05-SUMMARY.md` when done
</output>
