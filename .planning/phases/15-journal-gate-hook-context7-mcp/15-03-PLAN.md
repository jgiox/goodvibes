---
phase: 15-journal-gate-hook-context7-mcp
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/docs/getting-started.md
  - docs/getting-started.md
autonomous: true
requirements: [HOOK-04, CTX7-02, CTX7-03]

must_haves:
  truths:
    - "A developer reading docs/getting-started.md learns the journal-gate hook only gates Claude Code's own Bash tool — not manual git commit, not other agents/IDEs"
    - "A developer reading docs/getting-started.md learns how to add a context7 API key later via ${CONTEXT7_API_KEY} without ever committing a literal key"
    - "A developer reading docs/getting-started.md learns Claude Code will show a one-time 'trust this project's MCP servers' prompt on first use"
  artifacts:
    - path: "docs/getting-started.md"
      provides: "two new sections appended after 'What is headroom?': hook-scope caveat, then 'What is context7?'"
      contains: "What is context7?"
    - path: "templates/docs/getting-started.md"
      provides: "identical two new sections shipped to every scaffolded project"
      contains: "What is context7?"
  key_links:
    - from: "docs/getting-started.md"
      to: "HOOK-04 scope caveat"
      via: "explicit 'only gates' wording naming Claude Code's Bash tool"
      pattern: "only gates"
    - from: "docs/getting-started.md"
      to: "CTX7-02 upgrade path"
      via: "CONTEXT7_API_KEY env-var reference, never a literal key"
      pattern: "CONTEXT7_API_KEY"
    - from: "docs/getting-started.md"
      to: "CTX7-03 trust prompt"
      via: "mention of Claude Code's one-time project-MCP trust approval"
      pattern: "trust"
---

<objective>
Add two short documentation sections to `docs/getting-started.md` (and the shipped `templates/docs/getting-started.md`): one stating the journal-gate hook's exact scope (HOOK-04), and one titled "What is context7?" covering the optional `${CONTEXT7_API_KEY}` upgrade path (CTX7-02) and the one-time MCP trust prompt (CTX7-03).

Purpose: Users and other agents picking up a goodvibes-scaffolded project need to understand what the hook does and does not cover, and how context7 works, without reading source code.
Output: Two new `##` sections appended to both copies of `getting-started.md`, mirroring the existing "What is headroom?" section's terse, 2-3-sentence style.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-journal-gate-hook-context7-mcp/15-CONTEXT.md
@.planning/phases/15-journal-gate-hook-context7-mcp/15-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add hook-scope caveat and "What is context7?" sections to getting-started.md</name>
  <files>templates/docs/getting-started.md, docs/getting-started.md</files>
  <read_first>
    docs/getting-started.md (current full content — the existing "What is headroom?" section at the end is the exact style/structure to mirror: `##` heading, 2 short sentences, no bullets, no code block)
    templates/docs/getting-started.md (byte-identical to docs/getting-started.md today — confirm both stay in sync)
    .planning/phases/15-journal-gate-hook-context7-mcp/15-CONTEXT.md (D-05, D-06 — doc placement decisions)
    .planning/phases/15-journal-gate-hook-context7-mcp/15-RESEARCH.md (Code Examples "upgrade path documentation snippet" — the exact JSON to embed showing the headers block with `${CONTEXT7_API_KEY}`; Sources section — code.claude.com/docs/en/mcp trust-prompt mechanics)
  </read_first>
  <action>
    Append two new `##` sections to the end of both `templates/docs/getting-started.md` and `docs/getting-started.md`, after the existing "What is headroom?" section, in this order:

    Section 1, heading "## About the journal-gate hook" (HOOK-04, per D-05 doc placement): 2-3 sentences stating plainly that the hook only gates `git commit` when it runs through Claude Code's own Bash tool — it does not intercept a manual `git commit` typed directly in a terminal, and it does not apply to other AI coding tools or IDEs (Cursor, Copilot, etc.) that lack an equivalent hook mechanism. Match the terse, no-bullet, no-code-block style of "What is headroom?".

    Section 2, heading "## What is context7?" (CTX7-02, CTX7-03, per D-06 doc placement): Open with one sentence explaining what context7 does (an MCP server that gives Claude Code live, up-to-date library documentation lookups, mirroring the "X does Y so you get Z" structure of the headroom section's opening sentence). Then state that it works out of the box with no signup or API key. Then add a short paragraph covering the optional upgrade path: users who hit rate limits can set a `CONTEXT7_API_KEY` environment variable and add a `headers` block referencing `${CONTEXT7_API_KEY}` to their `.mcp.json` for higher limits — include the exact JSON snippet from RESEARCH.md's "upgrade path documentation snippet" (the `mcpServers.context7` object with `type`, `url`, and a `headers.Authorization` value of `Bearer ${CONTEXT7_API_KEY}`) inside a fenced ```json code block, and state explicitly that no literal key should ever be committed — only the `${CONTEXT7_API_KEY}` reference. Close with one sentence mentioning that Claude Code shows a one-time "trust this project's MCP servers" prompt the first time it loads a project with an `.mcp.json`, and that approving it is what enables context7 tool calls.

    Apply the identical two sections to both files (this file is not template-sync-mirrored automatically for the pip package the way `packages/npm/templates/` is — both `templates/docs/getting-started.md` and repo-root `docs/getting-started.md` need the manual matching edit).
  </action>
  <acceptance_criteria>
    - `docs/getting-started.md` and `templates/docs/getting-started.md` are byte-identical after this task
    - Both files contain the heading `## About the journal-gate hook` followed by prose containing the phrase "only gates" and naming Claude Code's Bash tool
    - Both files contain the heading `## What is context7?`
    - Both files contain the literal string `CONTEXT7_API_KEY`
    - Both files contain a fenced ```json code block with `"Authorization": "Bearer ${CONTEXT7_API_KEY}"`
    - Neither file contains any string matching a real-looking API key (no literal secret value — only the `${CONTEXT7_API_KEY}` reference)
    - Both files mention the word "trust" in the context of the one-time MCP approval prompt
    - Both new sections appear after the existing "What is headroom?" section (grep line number of "About the journal-gate hook" is greater than line number of "What is headroom?")
  </acceptance_criteria>
  <verify>
    <automated>diff docs/getting-started.md templates/docs/getting-started.md && grep -q "only gates" docs/getting-started.md && grep -q "CONTEXT7_API_KEY" docs/getting-started.md && grep -q "What is context7?" docs/getting-started.md && echo DOCS_OK</automated>
  </verify>
  <done>Both getting-started.md files carry the two new sections in the required order, with the exact required phrases and JSON snippet, and remain byte-identical to each other.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation text -> committed repo | Docs must reference `${CONTEXT7_API_KEY}` only as an env-var placeholder, never a literal secret |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-06 | Information Disclosure | docs/getting-started.md upgrade-path snippet | mitigate | Acceptance criteria explicitly forbid any literal-looking key string in the doc; only the `${CONTEXT7_API_KEY}` env-var reference is permitted, matching Claude Code's documented `${VAR}` expansion mechanism |
</threat_model>

<verification>
`diff docs/getting-started.md templates/docs/getting-started.md` produces no output (byte-identical). Manual read-through confirms both new sections accurately describe scope (HOOK-04) and behavior (CTX7-02, CTX7-03) without overstating or understating what the hook/context7 actually do.
</verification>

<success_criteria>
- HOOK-04: docs state the hook only gates Claude Code's own Bash tool, not manual commits or other agents/IDEs
- CTX7-02: docs cover the optional `${CONTEXT7_API_KEY}` upgrade path with no literal key ever committed
- CTX7-03: docs mention the one-time "trust this project's MCP servers" prompt
</success_criteria>

<output>
Create `.planning/phases/15-journal-gate-hook-context7-mcp/15-03-SUMMARY.md` when done
</output>
