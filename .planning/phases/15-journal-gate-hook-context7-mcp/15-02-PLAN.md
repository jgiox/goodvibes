---
phase: 15-journal-gate-hook-context7-mcp
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - templates/.mcp.json
  - .mcp.json
  - packages/npm/src/steps/mcp-json.test.ts
  - packages/pip/tests/test_mcp_json.py
autonomous: true
requirements: [CTX7-01]

must_haves:
  truths:
    - "A fresh `goodvibes init` (npm or pip) ships `.mcp.json` with context7 configured at the free/public HTTP endpoint, no key required"
    - "The goodvibes repo's own root has a `.mcp.json` with context7 configured (dogfooding, D-02)"
  artifacts:
    - path: "templates/.mcp.json"
      provides: "context7 mcpServers entry, type=http, no headers block by default"
      contains: "mcp.context7.com/mcp"
    - path: ".mcp.json"
      provides: "identical context7 entry at repo root (dogfood D-02)"
      contains: "mcp.context7.com/mcp"
    - path: "packages/npm/src/steps/mcp-json.test.ts"
      provides: "static shape assertion on the shipped .mcp.json"
      min_lines: 8
    - path: "packages/pip/tests/test_mcp_json.py"
      provides: "mirrored static shape assertion (pytest)"
      min_lines: 8
  key_links:
    - from: "templates/.mcp.json"
      to: "mcpServers.context7.url"
      via: "static JSON value"
      pattern: "https://mcp\\.context7\\.com/mcp"
    - from: "templates/.mcp.json"
      to: "absence of headers key"
      via: "no default Authorization header (avoids unset-env-var literal-text pitfall)"
      pattern: "^(?!.*headers).*$"
---

<objective>
Ship a static `.mcp.json` template shipping `context7` wired to its free/public HTTP endpoint (CTX7-01) — no key, no signup, no `headers` block by default. Dogfood the identical file into goodvibes' own repo root (D-02).

Purpose: Give every goodvibes-scaffolded project (and this repo itself) instant access to up-to-date library documentation via context7's MCP server, with zero setup friction.
Output: `templates/.mcp.json` (new, canonical source), `.mcp.json` (new, repo root dogfood), plus static-shape unit tests in both packages.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-journal-gate-hook-context7-mcp/15-CONTEXT.md
@.planning/phases/15-journal-gate-hook-context7-mcp/15-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing shape tests for .mcp.json (RED)</name>
  <files>packages/npm/src/steps/mcp-json.test.ts, packages/pip/tests/test_mcp_json.py</files>
  <read_first>
    .planning/phases/15-journal-gate-hook-context7-mcp/15-RESEARCH.md (Code Examples ".mcp.json — context7, no key, default template")
    .planning/phases/15-journal-gate-hook-context7-mcp/15-PATTERNS.md (mcp-json.test.ts and test_mcp_json.py pattern sections)
    packages/npm/src/steps/write-manifest.test.ts (small self-contained unit test style to mirror — imports, no temp dir needed)
    packages/npm/src/steps/copy-templates.ts (resolveTemplatesDir — dual dev/build path resolution)
    packages/pip/src/goodvibes_cli/steps/copy_templates.py (resolve_templates_dir — note: this resolves an INSTALLED WHEEL's bundled templates and raises FileNotFoundError in dev/test mode; confirmed by running it locally against the current checkout)
  </read_first>
  <behavior>
    Both test files read `.mcp.json` from the canonical `templates/` source (which does not exist yet — this is the RED step) and assert its shape:
    - Test 1: `mcpServers.context7.type` equals `"http"`
    - Test 2: `mcpServers.context7.url` equals `"https://mcp.context7.com/mcp"`
    - Test 3: `mcpServers.context7.headers` is absent (undefined in TS / `"headers" not in ...` in Python) — this is the CTX7-02 safety property (no unset `${CONTEXT7_API_KEY}` literal text shipped by default)
  </behavior>
  <action>
    In `packages/npm/src/steps/mcp-json.test.ts`, import `resolveTemplatesDir` from `./copy-templates.js`, `JSON.parse(readFileSync(join(resolveTemplatesDir(), '.mcp.json'), 'utf-8'))`, and assert the three properties above. In `packages/pip/tests/test_mcp_json.py`, do NOT use `resolve_templates_dir()` (it targets an installed wheel and fails in dev/test mode) — instead resolve the canonical repo-root templates dir directly via `pathlib.Path(__file__).resolve().parents[3] / "templates"` (four levels up from `packages/pip/tests/test_mcp_json.py` reaches the repo root), read `.mcp.json` from there with `json.loads`, and assert the same three properties. Name tests as sentences per CLAUDE.md's Testing convention, e.g. `it('ships context7 at the free/public HTTP endpoint with no headers block')` / `def test_mcp_json_ships_context7_http_endpoint_with_no_headers():`.
  </action>
  <acceptance_criteria>
    - `packages/npm/src/steps/mcp-json.test.ts` exists and imports `resolveTemplatesDir` from `./copy-templates.js`
    - `packages/pip/tests/test_mcp_json.py` exists and does NOT import or call `resolve_templates_dir`
    - `cd packages/npm && npx vitest run src/steps/mcp-json.test.ts` fails (non-zero exit) because `templates/.mcp.json` does not exist yet
    - `cd packages/pip && uv run pytest tests/test_mcp_json.py` fails (non-zero exit) with `FileNotFoundError`
  </acceptance_criteria>
  <verify>
    <automated>cd packages/npm && npx vitest run src/steps/mcp-json.test.ts; test $? -ne 0 && echo RED_CONFIRMED</automated>
  </verify>
  <done>Both test files exist, assert the 3-property shape, and both fail because templates/.mcp.json does not exist yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create .mcp.json and dogfood it into repo root (GREEN)</name>
  <files>templates/.mcp.json, .mcp.json</files>
  <read_first>
    .planning/phases/15-journal-gate-hook-context7-mcp/15-RESEARCH.md (Code Examples ".mcp.json — context7, no key, default template" and Common Pitfalls #5 — why headers must be omitted by default; Assumptions Log A1 — recommends verifying shape against `claude mcp add --transport http` output if available)
    CLAUDE.md ("Template sync (npm package)" convention)
  </read_first>
  <behavior>
    After this task, Task 1's tests must pass (GREEN): `mcpServers.context7.type === "http"`, `mcpServers.context7.url === "https://mcp.context7.com/mcp"`, no `headers` key present.
  </behavior>
  <action>
    Create `templates/.mcp.json` (net-new file — no existing `.mcp.json` exists anywhere in this repo) with a single top-level `mcpServers` object containing one entry, `context7`, with exactly two keys: `type` set to the string `http`, and `url` set to the string `https://mcp.context7.com/mcp`. Do not add a `headers` key — per RESEARCH.md Pitfall 5, an unset `${CONTEXT7_API_KEY}` would ship as literal unexpanded text and silently break every anonymous request. Copy this exact file content, byte-for-byte, to `.mcp.json` at the repo root (dogfood D-02 — this is a net-new file, not a merge, since nothing exists there yet). After creating `templates/.mcp.json`, run `cd packages/npm && npm run prebuild` to regenerate the gitignored `packages/npm/templates/.mcp.json` mirror (idempotent — safe even if another plan in this wave also triggers it).
  </action>
  <acceptance_criteria>
    - `templates/.mcp.json` and `.mcp.json` (repo root) are byte-identical
    - Both files parse as valid JSON with exactly the shape `mcpServers.context7 = { type: "http", url: "https://mcp.context7.com/mcp" }` (key order and whitespace may differ, values must match exactly)
    - Neither file contains the string `headers`
    - `cd packages/npm && npx vitest run src/steps/mcp-json.test.ts` exits 0
    - `cd packages/pip && uv run pytest tests/test_mcp_json.py` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd packages/npm && npx vitest run src/steps/mcp-json.test.ts && cd ../pip && uv run pytest tests/test_mcp_json.py</automated>
  </verify>
  <done>templates/.mcp.json and .mcp.json both exist with the exact context7 http entry, no headers key, and both packages' shape tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.mcp.json` -> remote context7 server | Claude Code establishes an outbound HTTP connection to `mcp.context7.com` on the user's behalf, gated by a one-time interactive trust prompt |
| Doc/template text -> committed repo | Any secret referenced by `${CONTEXT7_API_KEY}` must never appear as a literal value in a committed file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | Information Disclosure | `.mcp.json` (templates + root) | mitigate | Ship with no `headers` key by default — a key is never present to leak, and the optional upgrade path (documented separately in Plan 03) only ever references `${CONTEXT7_API_KEY}` via env-var expansion, never a literal |
| T-15-05 | Spoofing | remote MCP endpoint | accept | `https://mcp.context7.com/mcp` is Upstash's documented, official public endpoint per RESEARCH.md Sources (github.com/upstash/context7); Claude Code's own one-time project-MCP trust prompt (CTX7-03) is the existing control surface for this, not something goodvibes needs to duplicate |
</threat_model>

<verification>
Run `cd packages/npm && npm test` and `cd packages/pip && uv run pytest tests/` — both full suites must be green. Manually diff `templates/.mcp.json` against `.mcp.json` to confirm byte-identical dogfood copy.
</verification>

<success_criteria>
- CTX7-01: `.mcp.json` ships context7 at the free/public HTTP endpoint, no key, no signup
- Dogfood (D-02): repo-root `.mcp.json` exists with the identical entry
- Both packages' shape tests pass with zero mocking (static file read only)
</success_criteria>

<output>
Create `.planning/phases/15-journal-gate-hook-context7-mcp/15-02-SUMMARY.md` when done
</output>
