---
phase: 10-vibe-coder-completeness
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - templates/docs/getting-started.md
  - templates/docs/platform-setup/cursor.md
  - templates/docs/platform-setup/windsurf.md
  - templates/docs/platform-setup/kiro.md
  - templates/docs/platform-setup/replit.md
  - templates/docs/platform-setup/bolt.md
  - CHANGELOG.md
  - JOURNAL.md
  - packages/npm/package.json
  - packages/pip/pyproject.toml
autonomous: true
requirements:
  - VCC-04
  - VCC-06

must_haves:
  truths:
    - "templates/docs/getting-started.md exists and walks a beginner through first vibe-coded feature in under 5 minutes"
    - "Cursor, Windsurf, Kiro, Replit, and Bolt.new each have a platform-setup guide in templates/docs/platform-setup/"
    - "Each platform-setup guide has a 'Ponytail is already active' section explaining how the embedded rule file works"
    - "Version bumped to 1.6.0 in both packages/npm/package.json and packages/pip/pyproject.toml"
    - "CHANGELOG.md has a v1.6.0 entry listing all six VCC items"
  artifacts:
    - path: "templates/docs/getting-started.md"
      provides: "VCC-04 newbie flow guide"
      contains: "goodvibes init"
    - path: "templates/docs/platform-setup/cursor.md"
      provides: "VCC-06 Cursor ponytail guidance"
      contains: "ponytail"
    - path: "templates/docs/platform-setup/windsurf.md"
      provides: "VCC-06 Windsurf ponytail guidance"
      contains: "ponytail"
    - path: "templates/docs/platform-setup/kiro.md"
      provides: "VCC-06 Kiro ponytail guidance"
      contains: "ponytail"
    - path: "templates/docs/platform-setup/replit.md"
      provides: "VCC-06 Replit ponytail guidance"
      contains: "ponytail"
    - path: "templates/docs/platform-setup/bolt.md"
      provides: "VCC-06 Bolt.new ponytail guidance"
      contains: "ponytail"
  key_links:
    - from: "templates/docs/getting-started.md"
      to: "target project docs/ directory"
      via: "existing copyTemplates machinery — docs/ prefix already copied"
      pattern: "docs/"
    - from: "templates/docs/platform-setup/*.md"
      to: "target project docs/platform-setup/ directory"
      via: "existing copyTemplates machinery"
      pattern: "platform-setup"
---

<objective>
Author the five platform-setup guides (VCC-06) and the getting-started guide (VCC-04), then bump versions, update CHANGELOG and JOURNAL. No CLI code changes.

Purpose: The existing copyTemplates machinery already copies everything under templates/docs/ to the target project. These are pure static content files — no wiring needed.

Output: 6 new template files, updated CHANGELOG.md, JOURNAL.md, version fields bumped to 1.6.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-vibe-coder-completeness/10-RESEARCH.md
@templates/docs/platform-setup/base44.md
@templates/docs/platform-setup/chatgpt.md
@templates/docs/onboarding.md
@CHANGELOG.md
@JOURNAL.md
@packages/npm/package.json
@packages/pip/pyproject.toml

# Prior wave summaries
@.planning/phases/10-vibe-coder-completeness/10-01-SUMMARY.md
@.planning/phases/10-vibe-coder-completeness/10-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author getting-started.md and five IDE ponytail guides</name>
  <files>
    templates/docs/getting-started.md,
    templates/docs/platform-setup/cursor.md,
    templates/docs/platform-setup/windsurf.md,
    templates/docs/platform-setup/kiro.md,
    templates/docs/platform-setup/replit.md,
    templates/docs/platform-setup/bolt.md
  </files>
  <read_first>
    - templates/docs/platform-setup/base44.md — content structure and tone to match (beginner-first, imperative, no assumed terminal knowledge)
    - templates/docs/platform-setup/chatgpt.md — second example for structure reference
    - templates/docs/onboarding.md — see what's already covered so getting-started.md does not duplicate it
    - templates/CLAUDE.md — the ponytail rules to reference in the IDE guides
    - .kiro/steering/goodvibes.md or templates/.kiro/steering/goodvibes.md — Kiro's actual rule file path to reference
    - templates/.cursor/rules/goodvibes.mdc — Cursor rule file path for accurate instructions
    - templates/.windsurfrules — Windsurf rule file for accurate instructions
  </read_first>
  <action>
    Create templates/docs/getting-started.md:
    - H1: "Getting started with goodvibes"
    - Brief intro: "You ran `goodvibes init`. Here is what happens next."
    - Section "What was set up": bullet list of what goodvibes wrote (CLAUDE.md, skills, CI workflows). One paragraph max.
    - Section "Your first change": step-by-step — (1) Open your project in your AI coding tool. (2) Tell the AI what you want to build. (3) Review the diff before you commit. (4) Run `git add . && git commit -m "feat: ..."`. (5) Run `git push`. Each step is one sentence of instruction + one sentence of why.
    - Section "Check your setup": "Run `goodvibes doctor` to verify everything is working. It checks that headroom is installed, your git identity is configured, and the goodvibes rules are in place."
    - Section "Useful commands": short table — `goodvibes update` (re-sync goodvibes files), `goodvibes doctor` (check setup), `goodvibes upgrade --dry-run` (preview changes). No more than 3 rows.
    - Section "What is headroom?": One paragraph: headroom compresses the AI's memory of your project so you spend fewer tokens per session. It runs automatically in the background.
    - Keep the whole file under 60 lines. Beginner-first: no assumed terminal knowledge beyond running goodvibes init already.

    Create templates/docs/platform-setup/cursor.md:
    - H1: "Cursor — goodvibes setup"
    - Opening: "goodvibes init wrote `.cursor/rules/goodvibes.mdc` into your project. This file activates the ponytail minimalism rules automatically when you open the project in Cursor."
    - Section "Ponytail is already active": Explain that the rules enforce simplicity-first, fail-loud, and surgical changes. "You do not need to paste anything. The rules are in `.cursor/rules/goodvibes.mdc`."
    - Section "Verify activation": "Open Cursor. In the AI chat, ask: 'What are your active rules?' You should see a reference to goodvibes or ponytail in the response."
    - Section "If you use .cursorrules (legacy)": "If your project has a `.cursorrules` file from before goodvibes, the `.cursor/rules/goodvibes.mdc` file is independent. Both will apply. You may want to merge them to avoid duplication."
    - Section "Headroom (context compression)": "Cursor does not use Claude Code's MCP protocol. If you want context compression, install headroom separately and use it via its CLI. See [headroom docs](https://headroom-docs.vercel.app/)."
    - Keep under 50 lines. Tone: friendly, non-technical.

    Create templates/docs/platform-setup/windsurf.md:
    - H1: "Windsurf — goodvibes setup"
    - Opening: "goodvibes init wrote `.windsurfrules` into your project. Windsurf reads this file automatically and applies the goodvibes engineering rules to every AI interaction."
    - Section "Ponytail is already active": Same concept as Cursor guide — rules are in `.windsurfrules`, no paste needed.
    - Section "Verify activation": Ask Cascade (Windsurf's AI) what rules are active; expect to see goodvibes or ponytail reference.
    - Section "Headroom": Same note as Cursor — not MCP-connected; use headroom CLI if desired.
    - Keep under 40 lines.

    Create templates/docs/platform-setup/kiro.md:
    - H1: "Kiro — goodvibes setup"
    - Opening: "goodvibes init wrote `.kiro/steering/goodvibes.md` into your project. Kiro applies steering files automatically to all AI interactions in the project."
    - Section "Ponytail is already active": Rules in `.kiro/steering/goodvibes.md`, no further setup needed.
    - Section "Verify activation": Open Kiro, check that the steering file appears in the project settings panel.
    - Section "Headroom": Same note as Cursor.
    - Keep under 40 lines.

    Create templates/docs/platform-setup/replit.md:
    - H1: "Replit Agent — goodvibes setup"
    - Opening: "Replit Agent does not read files from your project automatically. Instead, you provide the rules as a system prompt for the Agent."
    - Section "One-time setup":
      1. Open your Repl.
      2. Find "System prompt" or "AI instructions" in the Agent settings.
      3. Copy the text from `.replit-agent-instructions.md` (if goodvibes wrote it) or from CLAUDE.md section between `<!-- goodvibes:start -->` and `<!-- goodvibes:end -->`.
      4. Paste into the system prompt field and save.
    - Section "Ponytail on Replit": "Replit Agent will follow the ponytail rules for every task in this project once the system prompt is set. The rules encourage minimal code, no over-engineering, and explicit error handling."
    - Section "Headroom": Replit manages its own context. Headroom is not applicable inside Replit — skip the headroom install step if you are Replit-only.
    - Keep under 50 lines. Use numbered steps for anything requiring user action.

    Create templates/docs/platform-setup/bolt.md:
    - H1: "Bolt.new — goodvibes setup"
    - Opening: "Bolt.new does not read project files for AI rules. You activate ponytail discipline by pasting the rules into the chat as a one-time instruction."
    - Section "One-time setup":
      1. Open your Bolt.new project.
      2. In the chat, paste: "Please follow these engineering rules for all code you write: [copy the text from CLAUDE.md between <!-- goodvibes:start --> and <!-- goodvibes:end -->]"
      3. Bolt.new will acknowledge and apply the rules for the rest of the session.
      4. Repeat this at the start of each new session, or save it as a pinned message if Bolt.new supports it.
    - Section "Ponytail on Bolt.new": Same spirit as Replit — minimal code, surgical changes, fail loud.
    - Section "Headroom": Not applicable inside Bolt.new.
    - Keep under 50 lines.

    Content quality check after writing:
    - Each file must mention "ponytail" at least once.
    - Each file must explain what the goodvibes-managed rule file is and where it lives.
    - No file should have TODO placeholders or lorem ipsum.
    - getting-started.md must mention `goodvibes doctor` and `goodvibes update`.
  </action>
  <verify>
    <automated>ls /home/ygiokas/GoodVibes/templates/docs/getting-started.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/cursor.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/windsurf.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/kiro.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/replit.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/bolt.md && grep -l "ponytail" /home/ygiokas/GoodVibes/templates/docs/platform-setup/cursor.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/windsurf.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/kiro.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/replit.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/bolt.md | wc -l</automated>
  </verify>
  <done>
    - 6 files exist (getting-started.md + 5 IDE guides)
    - All 5 IDE guides contain the word "ponytail"
    - getting-started.md contains "goodvibes doctor" and "goodvibes update"
    - No file has "TODO" or placeholder text
  </done>
</task>

<task type="auto">
  <name>Task 2: Bump version to 1.6.0, update CHANGELOG and JOURNAL</name>
  <files>
    packages/npm/package.json,
    packages/pip/pyproject.toml,
    CHANGELOG.md,
    JOURNAL.md
  </files>
  <read_first>
    - packages/npm/package.json — current version field (1.5.0); update to 1.6.0
    - packages/pip/pyproject.toml — current version field; update to 1.6.0
    - CHANGELOG.md — read the most recent entry to follow the format exactly; add a new [1.6.0] section above [1.5.0]
    - JOURNAL.md — read the most recent entry to follow additive format; add today's entry (2026-07-01)
  </read_first>
  <action>
    In packages/npm/package.json: change "version": "1.5.0" to "version": "1.6.0".

    In packages/pip/pyproject.toml: find the `version = "..."` line and update to "1.6.0". Also check `[tool.hatch.version]` or `dynamic = ["version"]` — if version is dynamic from __init__.py, update __init__.py instead. Read the file to confirm the version mechanism before editing.

    In CHANGELOG.md: add a new section immediately after the header block (before the existing latest entry). Follow the Keep a Changelog format already in use. Entry content:
    ```
    ## [1.6.0] — 2026-07-01

    ### Added
    - `goodvibes update` command — alias for `goodvibes upgrade` (VCC-01)
    - `goodvibes doctor` command — checks headroom, git identity, CLAUDE.md, and sentinel block; exits non-zero on any failure (VCC-02)
    - `goodvibes --version` now reads version from package.json dynamically (VCC-03)
    - `docs/getting-started.md` template — beginner flow guide from init to first commit (VCC-04)
    - Headroom install step now shows "what is headroom?" description and skips gracefully if already installed (VCC-05)
    - Platform setup guides for Cursor, Windsurf, Kiro, Replit Agent, and Bolt.new with ponytail activation instructions (VCC-06)
    ```

    In JOURNAL.md: add a new entry at the bottom (additive only, never rewrite history):
    ```
    ## 2026-07-01 — Phase 10: Vibe Coder Completeness (v1.6.0)

    **Task:** Phase 10 — ship `goodvibes update`, `goodvibes doctor`, `--version` fix, headroom transparency, getting-started guide, and IDE ponytail guides.

    **Files changed:**
    - packages/npm/src/index.ts — dynamic version via createRequire, registerDoctorCommand wired
    - packages/npm/src/commands/doctor.ts — new: five-check doctor command
    - packages/npm/src/commands/doctor.test.ts — new: 6+ unit tests
    - packages/npm/src/commands/upgrade.ts — .alias('update') added
    - packages/npm/src/steps/install-headroom.ts — description log + idempotency probe
    - packages/pip/src/goodvibes_cli/main.py — update alias + doctor command registered
    - packages/pip/src/goodvibes_cli/commands/doctor_cmd.py — new: Python doctor command
    - packages/pip/tests/test_doctor_cmd.py — new: 11+ unit tests
    - packages/pip/src/goodvibes_cli/steps/install_headroom.py — description log + shutil.which probe
    - templates/docs/getting-started.md — new: beginner flow guide
    - templates/docs/platform-setup/{cursor,windsurf,kiro,replit,bolt}.md — new: IDE ponytail guides
    - packages/npm/package.json, packages/pip/pyproject.toml — version bumped to 1.6.0
    - CHANGELOG.md — v1.6.0 entry added

    **Why:** Completes Phase 10 (Vibe Coder Completeness). Gives users tools to verify their setup and guides for non-Claude Code IDEs.

    **Tests run:** cd packages/npm && npm test (all pass); cd packages/pip && uv run pytest tests/ (all pass)

    **Docs updated:** CHANGELOG.md, JOURNAL.md, templates/docs/getting-started.md, templates/docs/platform-setup/*.md
    ```
  </action>
  <verify>
    <automated>grep '"version"' /home/ygiokas/GoodVibes/packages/npm/package.json && grep -E '^version' /home/ygiokas/GoodVibes/packages/pip/pyproject.toml && grep '1.6.0' /home/ygiokas/GoodVibes/CHANGELOG.md | head -3</automated>
  </verify>
  <done>
    - packages/npm/package.json "version" is "1.6.0"
    - packages/pip/pyproject.toml version is "1.6.0"
    - CHANGELOG.md has [1.6.0] section with 6 Added items
    - JOURNAL.md has 2026-07-01 entry
    - Both package versions match
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Template file content | Static markdown authored here; copied verbatim to user projects by copyTemplates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03-01 | Information Disclosure | getting-started.md content | accept | Pure documentation; no secrets, no user data |
| T-10-03-02 | Tampering | Version bump (package.json/pyproject.toml) | accept | Version bump is intentional; npm/PyPI publish is a separate tagged step |
| T-10-03-SC | Tampering | npm/pip installs | accept | No new packages in this plan |
</threat_model>

<verification>
- All 6 template files exist and contain "ponytail" (5 IDE guides) or "goodvibes doctor" (getting-started)
- Version is 1.6.0 in both package.json and pyproject.toml
- CHANGELOG.md has [1.6.0] entry
- JOURNAL.md has 2026-07-01 entry
- Both test suites remain green (run after this plan, no code changed)
</verification>

<success_criteria>
- templates/docs/getting-started.md: exists, mentions goodvibes doctor and goodvibes update, no TODOs, under 60 lines
- templates/docs/platform-setup/cursor.md + windsurf.md + kiro.md + replit.md + bolt.md: all exist and mention ponytail
- packages/npm/package.json: "version" == "1.6.0"
- packages/pip/pyproject.toml: version == "1.6.0"
- CHANGELOG.md: [1.6.0] section present with 6 Added items
- JOURNAL.md: 2026-07-01 entry present and additive
</success_criteria>

<output>
Create `.planning/phases/10-vibe-coder-completeness/10-03-SUMMARY.md` when done.
</output>
