---
phase: 10-vibe-coder-completeness
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/npm/src/commands/doctor.test.ts
  - packages/npm/src/commands/upgrade.test.ts
  - packages/npm/src/steps/install-headroom.test.ts
  - packages/npm/src/index.test.ts
  - packages/pip/tests/test_doctor_cmd.py
  - packages/pip/tests/test_install_headroom.py
  - packages/pip/tests/test_upgrade_cmd.py
  - packages/pip/tests/test_main.py
autonomous: true
requirements:
  - VCC-01
  - VCC-02
  - VCC-03
  - VCC-05

must_haves:
  truths:
    - "doctor.test.ts covers all five checks: headroom, git user.name, git user.email, CLAUDE.md present, sentinel intact"
    - "doctor.test.ts verifies all-pass exits 0 and any-fail exits 1 with remediation in output"
    - "upgrade.test.ts has at least one test asserting 'update' alias routes to upgrade action"
    - "install-headroom.test.ts tests that already-installed probe logs skip and returns without calling installer"
    - "install-headroom.test.ts tests that description log appears before installer calls"
    - "index.test.ts verifies --version output is not '1.0.0' (reads dynamically)"
    - "Python test equivalents exist in test_doctor_cmd.py and test_install_headroom.py"
    - "test_main.py verifies --version output contains the installed package version and is not '1.0.0'"
  artifacts:
    - path: "packages/npm/src/commands/doctor.test.ts"
      provides: "VCC-02 unit test coverage"
      contains: "describe('doctor'"
    - path: "packages/pip/tests/test_doctor_cmd.py"
      provides: "VCC-02 Python parity test coverage"
      contains: "def test_"
    - path: "packages/pip/tests/test_main.py"
      provides: "VCC-03 Python --version test coverage"
      contains: "def test_version_output_includes_installed_version"
  key_links:
    - from: "packages/npm/src/commands/doctor.test.ts"
      to: "packages/npm/src/commands/doctor.ts"
      via: "vi.mock + import registerDoctorCommand"
      pattern: "registerDoctorCommand"
    - from: "packages/pip/tests/test_doctor_cmd.py"
      to: "packages/pip/src/goodvibes_cli/commands/doctor_cmd.py"
      via: "import + mocker.patch"
      pattern: "from goodvibes_cli.commands.doctor_cmd import"
    - from: "packages/pip/tests/test_main.py"
      to: "packages/pip/src/goodvibes_cli/main.py"
      via: "typer.testing.CliRunner + importlib.metadata.version"
      pattern: "from goodvibes_cli.main import app"
---

<objective>
Add all Phase 10 unit tests covering VCC-01 through VCC-05 behavior. This plan runs in Wave 1 parallel with 10-01, so it creates test files that will initially fail until 10-01 ships; but since both plans run in the same wave, the executor should complete 10-01 first, then 10-02.

Purpose: Validation contract — every VCC requirement has an automated test that fails if the behavior regresses.

Output: New doctor.test.ts (npm), new test_doctor_cmd.py (pip), additions to upgrade.test.ts, install-headroom.test.ts, index.test.ts, test_install_headroom.py, test_upgrade_cmd.py, test_main.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-vibe-coder-completeness/10-RESEARCH.md
@.planning/phases/10-vibe-coder-completeness/10-VALIDATION.md
@packages/npm/src/commands/upgrade.test.ts
@packages/npm/src/steps/install-headroom.test.ts
@packages/pip/tests/test_upgrade_cmd.py
@packages/pip/tests/test_install_headroom.py
@packages/pip/tests/conftest.py

# Only needed if prior plan summary is available
@.planning/phases/10-vibe-coder-completeness/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add npm unit tests — doctor, version, alias, headroom transparency</name>
  <files>
    packages/npm/src/commands/doctor.test.ts,
    packages/npm/src/commands/upgrade.test.ts,
    packages/npm/src/steps/install-headroom.test.ts,
    packages/npm/src/index.test.ts
  </files>
  <read_first>
    - packages/npm/src/commands/upgrade.test.ts — existing mock pattern: vi.mock('@clack/prompts'), vi.mock('../steps/copy-templates.js'), vi.mock('../utils/sentinel-merge.js'), vi.mock('execa'); follow the same structure
    - packages/npm/src/steps/install-headroom.test.ts — existing vi.mock('execa') and vi.mock('../utils/detect-python.js') pattern; read fully to see what tests already exist before adding
    - packages/npm/src/commands/doctor.ts — read after 10-01 completes to understand exact function names and CheckResult shape being tested
    - packages/npm/src/index.ts — read after 10-01 to verify createRequire path and .version() call to write a correct version test
  </read_first>
  <action>
    Create packages/npm/src/commands/doctor.test.ts:
    - Mock all external calls: `vi.mock('execa', ...)`, `vi.mock('node:fs', ...)`, `vi.mock('@clack/prompts', () => ({ note: vi.fn(), outro: vi.fn() }))`.
    - Mock process.exit: `const exitSpy = vi.spyOn(process, 'exit').mockImplementation(() => { throw new Error('process.exit') })`.
    - Write a `describe('doctor command')` block with these named tests:
      - `it('exits 0 and calls outro when all checks pass')` — mock execa to resolve, existsSync to return true, readFileSync to return content with both sentinel strings; invoke the action; assert exitSpy not called and outro called with 'All checks passed.'
      - `it('exits 1 when headroom is not on PATH')` — mock execa('headroom') to throw with code 'ENOENT'; assert exitSpy called with 1 and note called with text containing 'uv tool install'
      - `it('exits 1 when git user.name is not configured')` — mock headroom probe to pass, git user.name execa to throw; assert exitSpy(1) and note containing 'git config --global user.name'
      - `it('exits 1 when CLAUDE.md does not exist')` — mock existsSync to return false; assert exitSpy(1) and note containing 'goodvibes init'
      - `it('exits 1 when CLAUDE.md is missing sentinel block')` — mock existsSync to return true, readFileSync to return 'no sentinel here'; assert exitSpy(1) and note containing 'sentinel'
      - `it('collects all failures before exiting — does not short-circuit')` — mock headroom to fail AND git to fail; assert both failure labels appear in note calls before exitSpy
    - Use `beforeEach(() => vi.resetAllMocks())`.
    - Test names must be sentences (no 'test_happy_path', no 'test_1').

    Add to packages/npm/src/commands/upgrade.test.ts:
    - Read the existing file first to find the describe block and existing test structure.
    - Add `it('registers update as an alias for upgrade')` — after mocking program.command, assert the mock chain included `.alias('update')`. Pattern: mock `program.command` to return a chainable mock `{ description: vi.fn().mockReturnThis(), option: vi.fn().mockReturnThis(), alias: vi.fn().mockReturnThis(), action: vi.fn().mockReturnThis() }`, call `registerUpgradeCommand(program)`, assert `aliasMock` was called with 'update'.

    Add to packages/npm/src/steps/install-headroom.test.ts:
    - Read the existing file first to understand what describe blocks and mocks already exist.
    - Add `it('logs description and skips installer when headroom is already on PATH')` — mock detectPython to return 'python3', mock execa so that `execa('headroom', ['--version'])` resolves successfully; call installHeadroom(log); assert log was called with text containing 'compresses AI context'; assert log was called with 'headroom already installed — skipping'; assert execa was NOT called with 'uv' or 'pipx' or 'pip'.
    - Add `it('logs description before the idempotency probe and ONNX warning')` — assert log call order: description first, then ONNX warning.

    Add to packages/npm/src/index.test.ts (create if it does not exist — check first):
    - `it('version flag output matches package.json version and is not the hardcoded 1.0.0 string')` — import the actual package.json version using `createRequire`, spawn the built CLI with `--version`, assert output includes the real version string and does NOT include '1.0.0'.
    - Note: if index.test.ts does not exist, create a minimal file with the one test above. If it exists, add to the existing describe block.
    - Since this test runs the built CLI, add a guard: if `dist/index.js` does not exist, skip the test with `it.skip(...)` and a comment 'run npm run build first'. This avoids breaking CI on clean checkouts.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | tail -30</automated>
  </verify>
  <done>
    - doctor.test.ts exists with 6+ named tests all passing
    - upgrade.test.ts has alias test passing
    - install-headroom.test.ts has 2 new headroom-transparency tests passing
    - index.test.ts has version test (or skipped with build guard)
    - `npm test` exits 0 with all tests green
  </done>
</task>

<task type="auto">
  <name>Task 2: Add pip unit tests — doctor, install_headroom transparency, update alias, --version</name>
  <files>
    packages/pip/tests/test_doctor_cmd.py,
    packages/pip/tests/test_install_headroom.py,
    packages/pip/tests/test_upgrade_cmd.py,
    packages/pip/tests/test_main.py
  </files>
  <read_first>
    - packages/pip/tests/test_install_headroom.py — existing mocker.patch patterns and test structure; read fully before adding
    - packages/pip/tests/test_upgrade_cmd.py — existing test structure before adding alias test
    - packages/pip/tests/conftest.py — check for shared fixtures (tmp_dir, etc.)
    - packages/pip/src/goodvibes_cli/commands/doctor_cmd.py — read after 10-01 to verify exact function names and CheckResult field names (passed vs pass)
    - packages/pip/tests/test_main.py — check if it already exists; if so, read fully before adding
  </read_first>
  <action>
    Create packages/pip/tests/test_doctor_cmd.py:
    - Import `pytest`, `pathlib`, `from unittest.mock import patch, MagicMock`.
    - Import `from goodvibes_cli.commands.doctor_cmd import _check_headroom, _check_git_config, _check_claude_md, _check_sentinel, doctor_cmd`.
    - Write a function-level test file (no class) following the existing test_upgrade_cmd.py pattern.
    - Tests (all names are sentences):
      - `def test_check_headroom_returns_pass_when_headroom_on_path(mocker)` — patch `shutil.which` to return '/usr/bin/headroom'; assert result.passed is True.
      - `def test_check_headroom_returns_fail_with_remedy_when_not_on_path(mocker)` — patch `shutil.which` to return None; assert result.passed is False and 'uv tool install' in result.remedy.
      - `def test_check_git_config_returns_pass_when_name_set(mocker)` — patch `subprocess.run` to return a MagicMock with stdout='Jane Doe', returncode=0; assert result.passed is True.
      - `def test_check_git_config_returns_fail_with_remedy_when_not_set(mocker)` — patch `subprocess.run` to raise CalledProcessError; assert result.passed is False and 'git config --global user.name' in result.remedy.
      - `def test_check_claude_md_returns_pass_when_file_exists(tmp_path)` — create tmp_path/'CLAUDE.md'; assert _check_claude_md(tmp_path).passed is True.
      - `def test_check_claude_md_returns_fail_when_file_absent(tmp_path)` — no file; assert _check_claude_md(tmp_path).passed is False.
      - `def test_check_sentinel_returns_pass_when_both_markers_present(tmp_path)` — write CLAUDE.md with '<!-- goodvibes:start -->' and '<!-- goodvibes:end -->'; assert _check_sentinel(tmp_path).passed is True.
      - `def test_check_sentinel_returns_fail_when_sentinel_missing(tmp_path)` — write CLAUDE.md without sentinel strings; assert _check_sentinel(tmp_path).passed is False.
      - `def test_doctor_cmd_raises_exit_1_when_any_check_fails(mocker, tmp_path)` — patch all checks so headroom returns fail, others return pass; patch pathlib.Path.cwd to return tmp_path; assert typer.Exit raised with code 1.
      - `def test_doctor_cmd_does_not_raise_when_all_checks_pass(mocker, tmp_path)` — all checks return pass; assert no typer.Exit raised.
      - `def test_doctor_cmd_collects_all_failures_before_exiting(mocker, tmp_path)` — headroom fails AND git user.name fails; capture console.print calls; assert both failure labels appear before exit.

    Add to packages/pip/tests/test_install_headroom.py:
    - `def test_install_headroom_skips_installer_when_already_on_path(mocker)` — patch `shutil.which` to return '/usr/bin/headroom' for 'headroom'; call install_headroom(log); assert log called with text containing 'already installed'; assert subprocess.run NOT called with any installer.
    - `def test_install_headroom_logs_description_before_probe(mocker)` — patch shutil.which to return None (not installed); capture log call order; assert description log (containing 'compresses AI context') appears before any subprocess calls.

    Add to packages/pip/tests/test_upgrade_cmd.py:
    - `def test_update_alias_is_registered_in_app()` — import app from goodvibes_cli.main; assert any(c.name == 'update' for c in app.registered_commands).

    Add to packages/pip/tests/test_main.py (create if it does not exist; if it exists, read fully first and add to existing structure):
    - `def test_version_output_includes_installed_version()` — use `typer.testing.CliRunner` to invoke the app with `["--version"]`; get the installed version via `importlib.metadata.version('jgiox-goodvibes')`; assert the version string appears in the CLI output and the output does NOT contain '1.0.0'. Import: `from typer.testing import CliRunner`, `import importlib.metadata`, `from goodvibes_cli.main import app`.

    Run `cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/ -x -q` after each file to catch import errors immediately.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/ -x -q 2>&1 | tail -30</automated>
  </verify>
  <done>
    - test_doctor_cmd.py exists with 11+ named tests all passing
    - test_install_headroom.py has 2 new transparency tests passing
    - test_upgrade_cmd.py has update alias test passing
    - test_main.py has test_version_output_includes_installed_version passing (asserts version != '1.0.0')
    - `uv run pytest tests/` exits 0 with all tests green
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test mocks | All external calls mocked — subprocess, shutil.which, execa — no real processes spawned in unit tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02-01 | Tampering | Test file creation | accept | Tests are dev-only artifacts; no runtime trust boundary |
| T-10-02-SC | Tampering | npm/pip installs | accept | No new packages — only test additions to existing files |
</threat_model>

<verification>
Both suites green with new tests included:
- `cd packages/npm && npm test`
- `cd packages/pip && uv run pytest tests/`
</verification>

<success_criteria>
- doctor.test.ts: 6+ tests covering all five doctor checks, all-pass, any-fail, collect-all behavior
- test_doctor_cmd.py: 11+ tests covering same behaviors in Python
- upgrade.test.ts and test_upgrade_cmd.py: alias test present and passing
- install-headroom tests: description-log and already-installed skip tests passing in both TS and Python
- index.test.ts: version test present (skipped if build absent)
- test_main.py: test_version_output_includes_installed_version present and passing
- Both full test suites exit 0
</success_criteria>

<output>
Create `.planning/phases/10-vibe-coder-completeness/10-02-SUMMARY.md` when done.
</output>
