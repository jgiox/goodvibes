---
phase: 10-vibe-coder-completeness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/npm/src/index.ts
  - packages/npm/src/commands/doctor.ts
  - packages/npm/src/steps/install-headroom.ts
  - packages/pip/src/goodvibes_cli/main.py
  - packages/pip/src/goodvibes_cli/commands/doctor_cmd.py
  - packages/pip/src/goodvibes_cli/steps/install_headroom.py
autonomous: true
requirements:
  - VCC-01
  - VCC-02
  - VCC-03
  - VCC-05

must_haves:
  truths:
    - "'goodvibes update' runs the full upgrade logic (sentinel merge + template copy)"
    - "'goodvibes doctor' runs all five checks and exits 1 when any fail, 0 when all pass"
    - "'goodvibes --version' prints the version from package.json, not the hardcoded '1.0.0'"
    - "Headroom install logs a one-sentence description and 'already installed' skip before any subprocess"
  artifacts:
    - path: "packages/npm/src/commands/doctor.ts"
      provides: "doctor command module — registerDoctorCommand(program)"
      exports: ["registerDoctorCommand"]
    - path: "packages/pip/src/goodvibes_cli/commands/doctor_cmd.py"
      provides: "doctor command function — doctor_cmd()"
      exports: ["doctor_cmd"]
  key_links:
    - from: "packages/npm/src/index.ts"
      to: "packages/npm/src/commands/doctor.ts"
      via: "registerDoctorCommand(program)"
      pattern: "registerDoctorCommand"
    - from: "packages/pip/src/goodvibes_cli/main.py"
      to: "packages/pip/src/goodvibes_cli/commands/doctor_cmd.py"
      via: "app.command('doctor')(doctor_cmd)"
      pattern: "doctor_cmd"
---

<objective>
Wire `goodvibes update` alias, add `goodvibes doctor` command, fix `--version` hardcoded string, and add headroom install transparency in both the npm (TypeScript) and pip (Python) CLIs.

Purpose: VCC-01 through VCC-03 and VCC-05 are pure CLI code changes with no new dependencies. All building blocks exist in the codebase. This plan delivers the minimum diffs.

Output: Modified index.ts, new doctor.ts, modified install-headroom.ts (npm); modified main.py, new doctor_cmd.py, modified install_headroom.py (pip).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-vibe-coder-completeness/10-RESEARCH.md
@packages/npm/src/index.ts
@packages/npm/src/commands/upgrade.ts
@packages/npm/src/steps/install-headroom.ts
@packages/npm/src/utils/sentinel-merge.ts
@packages/pip/src/goodvibes_cli/main.py
@packages/pip/src/goodvibes_cli/steps/install_headroom.py
@packages/pip/src/goodvibes_cli/utils/sentinel_merge.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire `update` alias, fix `--version`, add `doctor` command (npm TypeScript)</name>
  <files>
    packages/npm/src/index.ts,
    packages/npm/src/commands/doctor.ts,
    packages/npm/src/steps/install-headroom.ts
  </files>
  <read_first>
    - packages/npm/src/index.ts — see current .version('1.0.0') and registerUpgradeCommand import
    - packages/npm/src/commands/upgrade.ts — line 144: program.command('upgrade') — add .alias('update') here
    - packages/npm/src/steps/install-headroom.ts — current install loop; add idempotency probe after Python null check
    - packages/npm/src/utils/sentinel-merge.ts — SENTINEL_START/SENTINEL_END constants are NOT exported; doctor.ts must define its own (same strings)
    - packages/npm/src/commands/init.ts — import pattern and clack usage to follow
  </read_first>
  <behavior>
    - `goodvibes update` invokes registerUpgradeCommand handler (alias, not new logic)
    - `goodvibes --version` prints the version field from packages/npm/package.json (currently "1.5.0"), not "1.0.0"
    - Doctor with headroom on PATH returns pass result for headroom check
    - Doctor with ENOENT on headroom returns fail result with remedy "Run: uv tool install "headroom-ai[all]"  (or re-run goodvibes init)"
    - Doctor with git user.name set returns pass result
    - Doctor with git user.name missing returns fail result with remedy "Run: git config --global user.name "Your Name""
    - Doctor with CLAUDE.md present returns pass for both CLAUDE.md and sentinel checks (when sentinel is in file)
    - Doctor with CLAUDE.md absent returns fail for both CLAUDE.md and sentinel checks
    - Doctor with CLAUDE.md present but missing sentinel strings returns fail for sentinel check only
    - Doctor collects all results before exiting; calls process.exit(1) only after printing all failures
    - Headroom install logs description "headroom compresses AI context to save tokens — this keeps your costs down and sessions faster." before any subprocess
    - Headroom install with headroom already on PATH logs "headroom already installed — skipping" and returns without running any installer
  </behavior>
  <action>
    In packages/npm/src/index.ts:
    - Add `import { createRequire } from 'node:module'` at top (after existing imports).
    - Add `const _require = createRequire(import.meta.url)` and `const _pkg = _require('../package.json') as { version: string }`.
    - Replace `.version('1.0.0')` with `.version(_pkg.version)`.
    - Add `import { registerDoctorCommand } from './commands/doctor.js'` after the registerUpgradeCommand import.
    - Add `registerDoctorCommand(program)` after `registerUpgradeCommand(program)`.

    In packages/npm/src/commands/upgrade.ts:
    - On the `.command('upgrade')` call (line 146), chain `.alias('update')` — single line change. No other modifications.

    Create packages/npm/src/commands/doctor.ts:
    - Import `type { Command }` from 'commander', `{ note, outro }` from '@clack/prompts', `{ existsSync, readFileSync }` from 'node:fs', `{ join }` from 'node:path', `{ execa }` from 'execa'.
    - SENTINEL_START and SENTINEL_END are NOT exported from sentinel-merge.ts (npm) or sentinel_merge.py (pip) — define them as local constants in doctor.ts and doctor_cmd.py respectively. Do not attempt to import them.
    - Define `SENTINEL_START = '<!-- goodvibes:start -->'` and `SENTINEL_END = '<!-- goodvibes:end -->'` as module-level constants (not imported — sentinel-merge.ts does not export them).
    - Define `type CheckResult = { label: string; pass: boolean; remedy?: string }`.
    - Implement `async function checkHeadroom(): Promise<CheckResult>` — calls `await execa('headroom', ['--version'])`, returns pass on success. On catch: if code is 'ENOENT' return fail with remedy 'Run: uv tool install "headroom-ai[all]"  (or re-run goodvibes init)'; rethrow for any other error.
    - Implement `async function checkGit(): Promise<CheckResult[]>` — iterates `['user.name', 'user.email']`, calls `execa('git', ['config', key])` for each, returns pass if stdout.trim().length > 0, fail with remedy `Run: git config --global ${key} "Your Value"` on error or empty.
    - Implement `function checkClaudeMd(cwd: string): CheckResult` — uses `existsSync(join(cwd, 'CLAUDE.md'))`, returns pass/fail with remedy 'Run: goodvibes init'.
    - Implement `function checkSentinel(cwd: string): CheckResult` — reads CLAUDE.md if exists, checks `content.includes(SENTINEL_START) && content.includes(SENTINEL_END)`, returns fail with 'Run: goodvibes init (will merge sentinel block)' if missing.
    - Implement `export function registerDoctorCommand(program: Command): void` — registers `.command('doctor').description('Check goodvibes setup is complete').action(async () => { ... })`.
    - In the action: collect results from all checks (run sequentially, not short-circuiting). Call `note(lines.join('\n'), 'goodvibes doctor')`. If any failures, call `note(remediation, 'How to fix')` then `process.exit(1)`. If all pass, call `outro('All checks passed.')`.
    - Do NOT use Promise.all for git checks — `checkGit()` already returns both results in sequence.

    In packages/npm/src/steps/install-headroom.ts:
    - After the `pythonCmd === null` early return, add the one-sentence description log: `log('headroom compresses AI context to save tokens — this keeps your costs down and sessions faster.')`.
    - After the description log, add an idempotency probe: try `await execa('headroom', ['--version'])`, on success log 'headroom already installed — skipping' and return. On catch: if code is 'ENOENT' fall through to installer loop; if code is NOT 'ENOENT' log the error message and proceed to installer loop (non-fatal probe failure).
    - Leave the existing ONNX warning and installer loop unchanged after the idempotency block.

    Create packages/pip/src/goodvibes_cli/commands/doctor_cmd.py:
    - Use `import pathlib`, `import shutil`, `import subprocess`, `from dataclasses import dataclass`, `import typer`, `from rich.console import Console`, `from rich.panel import Panel`.
    - Define `SENTINEL_START = "<!-- goodvibes:start -->"` and `SENTINEL_END = "<!-- goodvibes:end -->"` as module-level constants (not imported — sentinel_merge.py does not export them).
    - Define `console = Console()`.
    - Define `@dataclass class CheckResult: label: str; passed: bool; remedy: str = ""`.
    - Implement `def _check_headroom() -> CheckResult` using `shutil.which("headroom")` — not None means pass.
    - Implement `def _check_git_config(key: str) -> CheckResult` using `subprocess.run(["git", "config", key], capture_output=True, text=True, check=True)` — pass if stdout.strip() is truthy; remedy is `Run: git config --global {key} "Your Value"` on CalledProcessError or FileNotFoundError.
    - Implement `def _check_claude_md(cwd: pathlib.Path) -> CheckResult` using `(cwd / "CLAUDE.md").exists()`.
    - Implement `def _check_sentinel(cwd: pathlib.Path) -> CheckResult` — reads CLAUDE.md text and checks both sentinel strings; if file absent, returns fail with "Run: goodvibes init".
    - Implement `def doctor_cmd() -> None` (no decorators — registered from main.py) that collects all five check results, prints a Panel with pass/fail lines, prints remediation Panel if any failures, then `raise typer.Exit(1)` if any failures.

    In packages/pip/src/goodvibes_cli/main.py:
    - Add `from goodvibes_cli.commands.doctor_cmd import doctor_cmd` import.
    - Add `app.command("upgrade")(upgrade_cmd)` remains unchanged.
    - Add `app.command("update")(upgrade_cmd)` after the upgrade line (alias — same function).
    - Add `app.command("doctor")(doctor_cmd)` after the update line.

    In packages/pip/src/goodvibes_cli/steps/install_headroom.py:
    - After the `python_cmd is None` early return, add: `log("headroom compresses AI context to save tokens — this keeps your costs down and sessions faster.")`.
    - After the description log, add idempotency: `if shutil.which("headroom") is not None: log("headroom already installed — skipping"); return`. Import `shutil` is already used in other files; add it here.
    - Leave the existing ONNX warning and installer loop unchanged after the idempotency block.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `goodvibes --version` path uses createRequire('../package.json').version in index.ts
    - `.alias('update')` present on the upgrade command in upgrade.ts
    - registerDoctorCommand imported and called in index.ts
    - doctor.ts exports registerDoctorCommand with all five check functions
    - install-headroom.ts logs description before idempotency probe, probe before installer loop
    - Python main.py registers 'update' alias and 'doctor' command
    - doctor_cmd.py implements all five checks with collect-all behavior
    - install_headroom.py has shutil.which idempotency check after description log
    - All npm tests pass (cd packages/npm && npm test)
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire doctor and verify pip CLI (Python)</name>
  <files>
    packages/pip/src/goodvibes_cli/commands/doctor_cmd.py,
    packages/pip/src/goodvibes_cli/main.py,
    packages/pip/src/goodvibes_cli/steps/install_headroom.py
  </files>
  <read_first>
    - packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py — function signature and rich/typer usage pattern to mirror
    - packages/pip/src/goodvibes_cli/main.py — current app.command() registrations; update alias and doctor command added here
    - packages/pip/src/goodvibes_cli/steps/install_headroom.py — current install loop; add shutil import and idempotency block
    - packages/pip/tests/test_upgrade_cmd.py — test pattern to mirror for doctor tests
  </read_first>
  <action>
    This task verifies the pip-side files created in Task 1 are correct and the pip test suite passes.

    Run the pip test suite: `cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/ -x -q`.

    If any test fails related to the new files (import errors, missing shutil, etc.):
    - Fix the specific import or structural error in the relevant file.
    - Do not change test assertions — fix the implementation to match what the tests expect.

    Verify doctor_cmd.py is importable and registers correctly in main.py:
    `cd /home/ygiokas/GoodVibes/packages/pip && uv run python -c "from goodvibes_cli.main import app; print('ok')"`.

    Verify the update alias registers without error:
    `cd /home/ygiokas/GoodVibes/packages/pip && uv run python -c "from goodvibes_cli.main import app; cmds = [c.name for c in app.registered_commands]; print(cmds)"`.

    If Typer raises on duplicate app.command('update')(upgrade_cmd) registration (assumption A1 from RESEARCH.md), use a thin wrapper instead:
    `def _update_cmd() -> None: upgrade_cmd()` and register that.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/ -x -q 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `cd packages/pip && uv run pytest tests/` exits 0 with all existing tests passing
    - `from goodvibes_cli.main import app` succeeds without error
    - 'doctor', 'update', and 'upgrade' all appear in app.registered_commands
    - install_headroom.py has shutil.which idempotency check present
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor subprocess calls | execa/subprocess.run reads git config and probes headroom — no user input in args |
| install-headroom probe | execa('headroom', ['--version']) — no user input; read-only probe |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | doctor sentinel check (readFileSync) | accept | Reads only CLAUDE.md in cwd; cwd is trusted (user's project directory) |
| T-10-02 | Information Disclosure | git config user.name/user.email in logs | accept | Values are already public in commits; CLI is local-only |
| T-10-03 | Spoofing | headroom PATH probe (ENOENT vs broken install) | accept | Probe is read-only; a broken headroom binary would be caught at actual use |
| T-10-SC | Tampering | npm/pip installs | accept | Phase 10 adds no new packages; all deps already in package.json/pyproject.toml |
</threat_model>

<verification>
Both test suites must be green before marking this plan complete:
- `cd packages/npm && npm test` — all existing + new tests pass
- `cd packages/pip && uv run pytest tests/ -x -q` — all existing tests pass (new doctor tests added in 10-02-PLAN.md)
</verification>

<success_criteria>
- `goodvibes update` and `goodvibes upgrade` both invoke the upgrade action (alias confirmed)
- `goodvibes --version` output matches packages/npm/package.json "version" field
- `goodvibes doctor` is a registered command in both CLIs
- Headroom install logs description + idempotency skip in both npm and pip implementations
- All 106 npm unit tests + all 109 pip unit tests remain green
</success_criteria>

<output>
Create `.planning/phases/10-vibe-coder-completeness/10-01-SUMMARY.md` when done.
</output>
