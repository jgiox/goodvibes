---
phase: "07-readme-demo"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - packages/npm/package.json
  - packages/pip/pyproject.toml
autonomous: true
requirements:
  - README-01
  - README-02
  - README-03
  - README-04

must_haves:
  truths:
    - "A first-time visitor to the GitHub repo sees the npx goodvibes init command before any prerequisites table"
    - "Four live Shields.io badges appear between the tagline and the demo GIF"
    - "The README Flags section explicitly states that --minimal skips .github/ and docs/"
    - "npm package.json keywords include ai-coding, claude-code, and copilot"
    - "PyPI pyproject.toml keywords include ai-coding, claude-code, and copilot, and [project.urls] Homepage is set"
  artifacts:
    - path: "README.md"
      provides: "Hero README with tagline, badges, GIF embed, Quick start, and restructured sections"
      contains: "One command. Production-grade project. No config."
    - path: "packages/npm/package.json"
      provides: "Updated npm package metadata"
      contains: "ai-coding"
    - path: "packages/pip/pyproject.toml"
      provides: "Updated PyPI package metadata with [project.urls] section"
      contains: "ai-coding"
  key_links:
    - from: "README.md"
      to: "docs/demo.gif"
      via: "inline GIF embed"
      pattern: "demo.*docs/demo.gif"
    - from: "README.md"
      to: "img.shields.io"
      via: "badge markdown"
      pattern: "img.shields.io/npm/v/%40jgiox"
---

<objective>
Full hero redesign of README.md and metadata sync for npm and PyPI packages.

Purpose: Give the goodvibes repo a compelling first impression — tagline, live badges, GIF placeholder, Quick start command above the fold — and sync package discovery metadata so the npm and PyPI pages match the README.

Output: Rewritten README.md following D-01 section order; packages/npm/package.json with 3 new keywords; packages/pip/pyproject.toml with 3 new keywords and a [project.urls] Homepage entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-readme-demo/07-CONTEXT.md
@.planning/phases/07-readme-demo/07-RESEARCH.md
@README.md
@packages/npm/package.json
@packages/pip/pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full README hero redesign (D-01 through D-06, D-18)</name>
  <files>README.md</files>
  <read_first>
    README.md — read the full current file before editing; preserve existing content (What you get, Platform support, Docs sections) in the new structure
    .planning/phases/07-readme-demo/07-RESEARCH.md §Pattern 3 — README hero structure reference
    .planning/phases/07-readme-demo/07-CONTEXT.md §decisions D-01 through D-06 and D-18
  </read_first>
  <action>
    Rewrite README.md with the following exact section order (per D-01):

    1. `# goodvibes` title
    2. Blockquote tagline: `> One command. Production-grade project. No config.` (per D-02)
    3. Four badges on consecutive lines (per D-03, D-08):
       - `[![npm](https://img.shields.io/npm/v/%40jgiox%2Fgoodvibes?style=flat-square)](https://www.npmjs.com/package/@jgiox/goodvibes)`
       - `[![PyPI](https://img.shields.io/pypi/v/jgiox-goodvibes?style=flat-square)](https://pypi.org/project/jgiox-goodvibes/)`
       - `[![CI](https://img.shields.io/github/actions/workflow/status/jgiox/goodvibes/ci.yml?style=flat-square)](https://github.com/jgiox/goodvibes/actions/workflows/ci.yml)`
       - `[![License](https://img.shields.io/badge/license-Apache%202.0-blue?style=flat-square)](LICENSE)`
    4. GIF embed (per D-04): `[![demo](docs/demo.gif)](docs/demo.gif)` — place on its own line with a blank line above and below; the GIF will be populated by Plan 2 / CI.
    5. `## Quick start` section (per D-06) containing the npx command block and pip alternative, exactly as in the current README's opening two code blocks but under a named heading.
    6. `## What you get` — lift content from current "What happens when you run it" section. Rename heading. Preserve all four bullet points.
    7. `## Flags` section (per D-18) — update the `--minimal` description line to explicitly state: "`--minimal` skips: `.github/` (workflows, issue templates, PR template, dependabot) and `docs/`". Keep the two existing code block lines (`--dry-run` and `--minimal`).
    8. `## What you need first` — move the prerequisites table here (per D-05); content unchanged.
    9. `## Platform support` — unchanged content.
    10. `## Docs` — unchanged content.

    Do NOT add `<div align="center">` wrappers. Left-align all content. Left-align badges (no centering HTML). Do not add any new sections beyond those listed.
  </action>
  <verify>
    <automated>
      grep -F '> One command. Production-grade project. No config.' README.md
      grep -F 'img.shields.io/npm/v/%40jgiox%2Fgoodvibes?style=flat-square' README.md
      grep -F 'img.shields.io/pypi/v/jgiox-goodvibes?style=flat-square' README.md
      grep -F 'img.shields.io/github/actions/workflow/status/jgiox/goodvibes/ci.yml?style=flat-square' README.md
      grep -F 'img.shields.io/badge/license-Apache%202.0-blue?style=flat-square' README.md
      grep -F '[![demo](docs/demo.gif)](docs/demo.gif)' README.md
      grep -F '## Quick start' README.md
      grep -F '## What you need first' README.md
      grep -F '## Flags' README.md
      grep -F ".github/" README.md
      grep -F "docs/" README.md
    </automated>
  </verify>
  <done>
    README.md contains: tagline blockquote, all four exact badge URLs (flat-square style), GIF embed on its own line, `## Quick start` before `## What you need first`, and `## Flags` section that explicitly names `.github/` and `docs/` as what `--minimal` skips.
  </done>
</task>

<task type="auto">
  <name>Task 2: Package metadata sync — npm keywords (D-16) and PyPI keywords + Homepage (D-17)</name>
  <files>packages/npm/package.json, packages/pip/pyproject.toml</files>
  <read_first>
    packages/npm/package.json — read current keywords array before editing
    packages/pip/pyproject.toml — read current keywords and full file structure before editing
    .planning/phases/07-readme-demo/07-CONTEXT.md §decisions D-16 and D-17
    .planning/phases/07-readme-demo/07-RESEARCH.md §Code Examples (pyproject.toml [project.urls] pattern)
  </read_first>
  <action>
    In packages/npm/package.json: add three keywords to the existing `keywords` array — `"ai-coding"`, `"claude-code"`, `"copilot"` — appended after the existing six entries. Do not replace existing keywords. The resulting array should have 9 entries: scaffold, vibe-coding, claude, llm, starter-kit, cli, ai-coding, claude-code, copilot. Per D-16.

    In packages/pip/pyproject.toml: add three keywords to the existing `keywords` list — `"ai-coding"`, `"claude-code"`, `"copilot"` — appended after the existing six entries. Do not replace existing keywords. Per D-17.

    Also in packages/pip/pyproject.toml: add a `[project.urls]` table after the `[project]` block and before the `[build-system]` block. The table must contain exactly one entry: `Homepage = "https://github.com/jgiox/goodvibes"`. Per D-17.

    After editing both files, run the npm test suite (`cd packages/npm && npm test`) and the pip test suite (`cd packages/pip && uv run --extra dev pytest tests/ -x -q`) to confirm the edits did not break anything.
  </action>
  <verify>
    <automated>
      node -e "const p = JSON.parse(require('fs').readFileSync('/home/ygiokas/GoodVibes/packages/npm/package.json', 'utf8')); const kw = p.keywords; console.assert(kw.includes('ai-coding'), 'missing ai-coding'); console.assert(kw.includes('claude-code'), 'missing claude-code'); console.assert(kw.includes('copilot'), 'missing copilot'); console.log('npm keywords ok:', kw);"
      grep -F '"ai-coding"' packages/npm/package.json
      grep -F '"claude-code"' packages/npm/package.json
      grep -F '"copilot"' packages/npm/package.json
      grep -F '"ai-coding"' packages/pip/pyproject.toml
      grep -F 'Homepage = "https://github.com/jgiox/goodvibes"' packages/pip/pyproject.toml
      grep -F '[project.urls]' packages/pip/pyproject.toml
    </automated>
  </verify>
  <done>
    packages/npm/package.json keywords array contains ai-coding, claude-code, and copilot in addition to the original six entries. packages/pip/pyproject.toml keywords contains the same three new entries and has a [project.urls] table with Homepage = "https://github.com/jgiox/goodvibes". Both test suites pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → GitHub CDN | Static Markdown rendered by GitHub; no user input processed |
| img.shields.io → reader's browser | Shields.io fetches live version data; no secrets in badge URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | Badge URLs in README.md | accept | URLs are static Shields.io CDN links with no auth; worst case is a wrong version displayed — no data integrity risk |
| T-07-02 | Information Disclosure | pyproject.toml [project.urls] Homepage | accept | Public GitHub repo URL; intended to be public |
| T-07-03 | Spoofing | img.shields.io CDN | accept | Shields.io is a trusted third-party CDN; no credentials flow through it; degradation is a gray "error" shield — no security impact |
</threat_model>

<verification>
After Task 1 and Task 2 complete:
- grep for all four exact badge markdown strings in README.md
- grep for tagline blockquote in README.md
- grep for `## Quick start` appearing before `## What you need first` in README.md (line number check)
- grep for `.github/` and `docs/` in the Flags section
- Confirm npm keywords array has 9 entries
- Confirm pyproject.toml has [project.urls] and Homepage
- Run `cd packages/npm && npm test` — passes
- Run `cd packages/pip && uv run --extra dev pytest tests/ -x -q` — passes
</verification>

<success_criteria>
1. README.md opens with: title → tagline blockquote → four flat-square badges → GIF embed → `## Quick start`
2. All four badge URLs match the exact strings specified in D-08
3. `## Flags` section names `.github/` and `docs/` explicitly
4. `## What you need first` appears after `## Flags` in the file
5. npm and PyPI packages each have ai-coding, claude-code, copilot in keywords
6. pyproject.toml has `[project.urls]` with Homepage pointing to the GitHub repo
7. Both test suites pass without modification
</success_criteria>

<output>
Create `.planning/phases/07-readme-demo/07-01-SUMMARY.md` when done
</output>
