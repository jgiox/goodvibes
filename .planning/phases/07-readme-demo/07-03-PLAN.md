---
phase: "07-readme-demo"
plan: "03"
type: execute
wave: 2
depends_on:
  - "07-02"
files_modified:
  - .github/workflows/vhs.yml
autonomous: true
requirements:
  - DEMO-02

must_haves:
  truths:
    - "A push to main that changes scripts/demo.tape triggers the VHS Demo workflow automatically"
    - "The workflow produces docs/demo.gif using vhs-action@v2.1.0 and commits it back with git-auto-commit-action@v7.1.0"
    - "The workflow does NOT trigger again when the GIF commit is pushed (no infinite loop)"
    - "The workflow uses permissions: contents: write on the job so git-auto-commit-action can push"
  artifacts:
    - path: ".github/workflows/vhs.yml"
      provides: "CI workflow that auto-regenerates docs/demo.gif when scripts/demo.tape changes"
      contains: "charmbracelet/vhs-action@v2.1.0"
  key_links:
    - from: ".github/workflows/vhs.yml"
      to: "scripts/demo.tape"
      via: "paths filter on push trigger"
      pattern: "paths.*scripts/demo.tape"
    - from: ".github/workflows/vhs.yml"
      to: "docs/demo.gif"
      via: "git-auto-commit-action file_pattern"
      pattern: "file_pattern.*docs/demo.gif"
---

<objective>
Create .github/workflows/vhs.yml — the CI workflow that auto-regenerates docs/demo.gif whenever scripts/demo.tape changes on main.

Purpose: Ensure the demo GIF stays in sync with the tape file. Any contributor who updates the tape triggers an automatic CI re-record without any manual VHS install or git push of binary files.

Output: .github/workflows/vhs.yml using charmbracelet/vhs-action@v2.1.0 and stefanzweifel/git-auto-commit-action@v7.1.0, with correct path filter and permissions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-readme-demo/07-CONTEXT.md
@.planning/phases/07-readme-demo/07-RESEARCH.md
@.github/workflows/ci.yml
@.github/workflows/publish-npm.yml
@.planning/phases/07-readme-demo/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create .github/workflows/vhs.yml (D-13, D-14, D-15)</name>
  <files>.github/workflows/vhs.yml</files>
  <read_first>
    .github/workflows/ci.yml — this repo's workflow structure pattern; note actions/checkout@v7 convention and job-level structure
    .github/workflows/publish-npm.yml — reference for trigger structure and step ordering conventions
    .planning/phases/07-readme-demo/07-RESEARCH.md §Pattern 2 (vhs.yml Workflow) — the complete verified workflow YAML
    .planning/phases/07-readme-demo/07-RESEARCH.md §Common Pitfalls (Pitfall 2: missing permissions, Pitfall 7: infinite loop)
    .planning/phases/07-readme-demo/07-CONTEXT.md §decisions D-13, D-14, D-15
  </read_first>
  <action>
    Create .github/workflows/vhs.yml. Follow the repo's established workflow conventions (version-tag action pinning as in ci.yml, job-level structure).

    Workflow name: `VHS Demo` (matches the badge CI display name pattern in this repo)

    Trigger: `on.push.branches: [main]` with `on.push.paths: ['scripts/demo.tape']` — per D-13. No `workflow_dispatch`. The path filter to `scripts/demo.tape` is what prevents infinite loop: the auto-commit of `docs/demo.gif` does NOT touch `scripts/demo.tape`, so the workflow will not re-trigger.

    Single job named `vhs`, runs-on: `ubuntu-latest`.

    Job-level permissions: `permissions: contents: write` — REQUIRED for stefanzweifel/git-auto-commit-action@v7.1.0 to push. Per research Pitfall 2.

    Steps:
    1. `uses: actions/checkout@v7` — match repo version-tag convention from ci.yml
    2. `uses: charmbracelet/vhs-action@v2.1.0` with `path: 'scripts/demo.tape'` — vhs-action installs VHS, ttyd, and ffmpeg automatically; no manual install steps needed
    3. `uses: stefanzweifel/git-auto-commit-action@v7.1.0` with `commit_message: 'chore(demo): regenerate demo.gif'` and `file_pattern: 'docs/demo.gif'` — per D-14. GITHUB_TOKEN is used implicitly (no `token:` field needed; default behavior of the action). `file_pattern` scopes the commit to docs/demo.gif only.

    Do NOT add a header comment citing SHA — the repo uses version-tag pinning, not SHA pinning (research Pattern 2, note 4). Add a brief inline comment on the permissions line explaining why it is required.

    Add an inline comment on the paths filter explaining that the path filter prevents the GIF commit from re-triggering the workflow.
  </action>
  <verify>
    <automated>
      grep -F 'name: VHS Demo' .github/workflows/vhs.yml
      grep -F "branches: [main]" .github/workflows/vhs.yml
      grep -F "scripts/demo.tape" .github/workflows/vhs.yml
      grep -F "contents: write" .github/workflows/vhs.yml
      grep -F "charmbracelet/vhs-action@v2.1.0" .github/workflows/vhs.yml
      grep -F "path: 'scripts/demo.tape'" .github/workflows/vhs.yml
      grep -F "stefanzweifel/git-auto-commit-action@v7.1.0" .github/workflows/vhs.yml
      grep -F "commit_message: 'chore(demo): regenerate demo.gif'" .github/workflows/vhs.yml
      grep -F "file_pattern: 'docs/demo.gif'" .github/workflows/vhs.yml
      python3 -c "import yaml, sys; yaml.safe_load(open('.github/workflows/vhs.yml')); print('YAML valid')"
    </automated>
  </verify>
  <done>
    .github/workflows/vhs.yml exists and passes YAML validation. The file contains: name VHS Demo, push trigger on main with paths filter for scripts/demo.tape, job-level `permissions: contents: write`, vhs-action@v2.1.0 with path input, git-auto-commit-action@v7.1.0 with commit_message and file_pattern inputs.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → git push | git-auto-commit-action pushes docs/demo.gif using GITHUB_TOKEN with contents: write |
| Push trigger → workflow | Path filter on scripts/demo.tape gates when the workflow runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Tampering | vhs-action@v2.1.0 supply chain | mitigate | Pinned to exact version tag v2.1.0 (not @latest); Dependabot in repo will surface version updates |
| T-07-08 | Tampering | git-auto-commit-action@v7.1.0 supply chain | mitigate | Pinned to exact version tag v7.1.0; confirmed SHA 04702edda442b2e678b25b537cec683a1493fcb9 in research if SHA pinning is later desired |
| T-07-09 | Denial of Service | Recursive workflow trigger (infinite loop) | mitigate | paths: ['scripts/demo.tape'] filter — GIF commit never touches demo.tape, so no re-trigger; confirmed safe in research Pitfall 7 |
| T-07-10 | Elevation of Privilege | GITHUB_TOKEN contents: write scope | accept | Scope limited to job level (not workflow level); only writes docs/demo.gif; no other repo contents modified; standard pattern for asset auto-commit workflows |
| T-07-11 | Information Disclosure | Secrets in GIF recording | accept | Tape uses --dry-run --minimal; no filesystem writes, no home directory access, no credentials in captured output |
</threat_model>

<verification>
After Task 1 completes:
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/vhs.yml'))"` — valid YAML, no parse error
- `grep 'charmbracelet/vhs-action@v2.1.0' .github/workflows/vhs.yml` — correct action version
- `grep 'stefanzweifel/git-auto-commit-action@v7.1.0' .github/workflows/vhs.yml` — correct action version
- `grep 'contents: write' .github/workflows/vhs.yml` — permissions present
- `grep "paths:" .github/workflows/vhs.yml && grep "scripts/demo.tape" .github/workflows/vhs.yml` — path filter present
- `grep "file_pattern.*docs/demo.gif" .github/workflows/vhs.yml` — scoped commit
- `grep -v 'v5' .github/workflows/vhs.yml` — no reference to the non-existent v5 tag (per research Pitfall 1)
</verification>

<success_criteria>
1. .github/workflows/vhs.yml is valid YAML and passes `python3 -c "import yaml; yaml.safe_load(open(...))"` 
2. Workflow triggers on push to main when scripts/demo.tape changes (path filter present)
3. Job has `permissions: contents: write`
4. Uses vhs-action@v2.1.0 with `path: 'scripts/demo.tape'`
5. Uses git-auto-commit-action@v7.1.0 with `commit_message: 'chore(demo): regenerate demo.gif'` and `file_pattern: 'docs/demo.gif'`
6. No reference to @v5 (non-existent tag) in the file
7. Does NOT have `workflow_dispatch` trigger (per D-13)
</success_criteria>

<output>
Create `.planning/phases/07-readme-demo/07-03-SUMMARY.md` when done
</output>
