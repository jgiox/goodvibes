---
phase: 17-cross-tool-governance-directive-wording
plan: 01
type: execute
requirements: [AGENT-01, AGENT-02, AGENT-03, AGENT-04, AGENT-05, AGENT-06, CAVE-01, CAVE-02]
---

# Plan 17-01: directive wording and cross-agent handoff

## Decisions

- One "Start of every session" block (read JOURNAL.md first and treat it as binding; never re-ask for what README.md, CLAUDE.md, AGENTS.md, JOURNAL.md or the codebase answers; never state a guess as fact) in every rule file.
- Budget: the CLAUDE.md goodvibes block must not grow in lines; new rules are paid for by merging "Proof of work" into "Definition of done" and compressing wording. The vendored ponytail section is untouched.
- The six AGENTS.md copies stay byte-identical, so their opening sentence must be true in every copy (they are each tool's own file): it names Copilot's and Claude Code's files and calls AGENTS.md the fallback, "not guaranteed to be read by all".
- JOURNAL.md template fields now match the CLAUDE.md Journal rule (files changed, why, tests run, docs updated) and its header states the Claude-Code-only scope of the commit check once.
- caveman is MIT; the default change is marked as a goodvibes change in SKILL.md and README.md.
- Rule 15 needs no code: `mergeClaude` writes the template verbatim when CLAUDE.md is absent, so the stub sits above the sentinel in templates/CLAUDE.md.

## Verification

`packages/npm/src/steps/rule-files.test.ts`: hedge-word grep over all 13 files, read-JOURNAL-first in all 13 plus JOURNAL.md, the never-ask source list, Copilot authority and AGENTS.md fallback wording, byte-identity of the six copies, project stub outside the block, caveman default ultra.
