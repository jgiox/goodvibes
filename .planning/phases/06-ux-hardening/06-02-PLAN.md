---
phase: 06-ux-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - packages/npm/src/steps/copy-templates.ts
  - packages/npm/src/commands/init.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - MIN-01
  - MIN-02

must_haves:
  truths:
    - "copyTemplates returns { written: string[], skipped: string[] } — not string[]"
    - "Running init in non-empty dir prints an informational note before tasks() runs (UX-01)"
    - "Completion summary shows two separate notes: one for written files, one for skipped (UX-02)"
    - "ci.yml in destination is never overwritten by the rename step (UX-04)"
    - "--minimal skips all of .github/ (not just workflows) and all of docs/ (MIN-01)"
    - "--dry-run --minimal shows only CLAUDE.md and skills files, not CI or docs files (MIN-02)"
    - "EACCES / EPERM errors from tasks() call cancel() with plain-English message and exit 1 (UX-03)"
    - "npm test exits 0 (all tests GREEN)"
    - "--minimal run appends a note to next-steps explaining how to add CI/docs later"
  artifacts:
    - path: "packages/npm/src/steps/copy-templates.ts"
      provides: "Updated copyTemplates with {written,skipped} return, ci.yml guard, expanded minimal filter"
      exports: ["copyTemplates", "listTemplateFiles", "resolveTemplatesDir"]
    - path: "packages/npm/src/commands/init.ts"
      provides: "Updated init command with non-empty notice, split summary, error handling, dry-run fix"
  key_links:
    - from: "packages/npm/src/commands/init.ts"
      to: "packages/npm/src/steps/copy-templates.ts"
      via: "copyTemplates destructuring { written, skipped }"
      pattern: "\\{ written, skipped \\}"
    - from: "packages/npm/src/steps/copy-templates.ts"
      to: "rename guard"
      via: "existsSync(ciPath) check before rename"
      pattern: "existsSync.*ci\\.yml|ciPath"
---

<objective>
Implement all Phase 6 TypeScript changes in the npm package, making the RED tests from Plan 01 go GREEN.
Two files change: copy-templates.ts (structural return type + ci.yml guard + minimal filter) and
init.ts (non-empty notice + split completion summary + error wrapping + dry-run minimal fix).

Purpose: Deliver UX-01 through MIN-02 for the npm CLI, enabling accurate written/skipped reporting,
safe re-runs against existing projects, correct --minimal scope, and beginner-safe error output.
Output: Updated copy-templates.ts and init.ts; all npm tests passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-ux-hardening/06-RESEARCH.md
@.planning/phases/06-ux-hardening/06-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement copy-templates.ts changes (UX-02, UX-04, MIN-01)</name>
  <files>packages/npm/src/steps/copy-templates.ts</files>
  <read_first>
    packages/npm/src/steps/copy-templates.ts
    packages/npm/src/steps/copy-templates.test.ts
    packages/npm/src/commands/init.ts
  </read_first>
  <action>
    Apply these four changes to packages/npm/src/steps/copy-templates.ts. Make them in sequence,
    preserving all existing logic that is not explicitly changed.

    **Change 1 — Return type declaration (UX-02):**
    Change the function signature return type from `Promise<string[]>` to
    `Promise<{ written: string[]; skipped: string[] }>`.
    The dry-run branch still returns `string[]` internally — keep that branch returning a plain
    array but wrap it as `{ written: <the array>, skipped: [] }` before returning, so the return
    type is consistent for all callers.
    Specifically: in the dry-run branch, replace `return all.filter(...)` with:
    `return { written: all.filter(...), skipped: [] }`.

    **Change 2 — Minimal filter expansion (MIN-01):**
    Locate the filter callback inside `copy()`. Find the line:
    `if (minimal && src.includes('.github/workflows')) return false`
    Replace it with two lines using relative-path comparison (per RESEARCH.md Pitfall 3 —
    Windows path separator safety):
    ```
    const rel = relative(templateDir, src)
    if (minimal && (rel.startsWith('.github') || rel.startsWith('docs'))) return false
    ```
    The `relative` import is already present. Note: `sep` import is NOT needed — use string
    prefix comparison. Add a `ponytail:` comment: `// ponytail: MIN-01 — .github/ and docs/ both skipped`

    **Change 3 — ci.yml rename guard (UX-04):**
    In the rename block after `copy()`, find:
    ```
    if (existsSync(variantPath)) {
      await rename(variantPath, ciPath)
    }
    ```
    Change to:
    ```
    if (existsSync(variantPath)) {
      if (existsSync(ciPath)) {
        skippedFiles.push('.github/workflows/ci.yml') // ponytail: UX-04
      } else {
        await rename(variantPath, ciPath)
      }
    }
    ```
    Note: `skippedFiles` is introduced in Change 4 below. Add it before this block.

    **Change 4 — Written/skipped tracking and return (UX-02):**
    Before the `copy()` call, add:
    `const skippedFiles: string[] = []`

    After CLAUDE.md sentinel merge and the `walkDir(destDir, destDir)` call, split the result
    into written and skipped by comparing against a pre-copy snapshot of destDir entries.

    Strategy: snapshot destDir before copy (or use a different approach). The RESEARCH.md
    recommended pattern is: snapshot existing files before copy, then classify post-copy files.

    Implementation:
    1. Before `copy()`, snapshot existing dest paths into a Set:
       ```
       const existingBefore = new Set<string>()
       if (existsSync(destDir)) {
         const beforeFiles = await walkDir(destDir, destDir).catch(() => [])
         for (const f of beforeFiles) existingBefore.add(f)
       }
       ```
    2. After walkDir(destDir) call, classify:
       ```
       const allDestFiles = destFiles.sort()
       const written = allDestFiles.filter(f => !existingBefore.has(f))
       // CLAUDE.md is always in 'written' — sentinel merge runs regardless (per RESEARCH.md note)
       const writtenWithClaude = written.includes('CLAUDE.md') ? written : ['CLAUDE.md', ...written]
       const skipped = [...allDestFiles.filter(f => existingBefore.has(f) && f !== 'CLAUDE.md'), ...skippedFiles]
       return { written: writtenWithClaude.sort(), skipped: skipped.sort() }
       ```

    **Error wrapping (UX-03 partial — step-level):**
    Wrap the `copy()` call in a try/catch:
    ```
    try {
      await copy(templateDir, destDir, { overwrite: false, errorOnExist: false, filter })
    } catch (e) {
      const err = e as NodeJS.ErrnoException
      const hint = err.code === 'EACCES' || err.code === 'EPERM'
        ? 'Check directory permissions.'
        : 'Check available disk space.'
      throw new Error(`Cannot copy template files: ${err.message}. ${hint}`)
    }
    ```
    This re-throws a plain Error so init.ts catches it without a raw Node.js stack trace.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test -- --run copy-templates 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm test for copy-templates exits 0. All existing tests pass. New tests added in Plan 01
    (return shape, ci.yml guard, minimal filter) also pass. TypeScript compiler reports no errors
    when running `npx tsc --noEmit` from packages/npm/.
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement init.ts changes (UX-01, UX-02, UX-03, MIN-02)</name>
  <files>packages/npm/src/commands/init.ts</files>
  <read_first>
    packages/npm/src/commands/init.ts
    packages/npm/src/steps/copy-templates.ts
    packages/npm/src/commands/init.test.ts
  </read_first>
  <action>
    Apply these five changes to packages/npm/src/commands/init.ts. Preserve all existing logic
    not explicitly changed.

    **Change 1 — Import readdirSync and cancel (UX-01, UX-03):**
    Add `readdirSync` to the node:fs import: `import { readdirSync } from 'node:fs'`
    Add `cancel` to the @clack/prompts import: `import { intro, outro, note, tasks, cancel } from '@clack/prompts'`

    **Change 2 — Non-empty directory notice (UX-01):**
    After `intro('goodvibes init')`, before `if (dryRun)`, add:
    ```
    const existingEntries = readdirSync(cwd).filter(e => e !== '.git' && e !== '.DS_Store')
    if (existingEntries.length > 0) {
      note('Existing files will not be overwritten.', 'Non-empty project detected')
    }
    ```
    This is a notice only — NOT a prompt. Zero-config is preserved (no user input required).

    **Change 3 — Fix dry-run + minimal (MIN-02):**
    In the dry-run branch, replace:
    ```
    const files = await listTemplateFiles(templateDir)
    ```
    with:
    ```
    const allFiles = await listTemplateFiles(templateDir)
    const files = minimal
      ? allFiles.filter(f => !f.startsWith('.github') && !f.startsWith('docs'))
      : allFiles.filter(f => !ciVariants.some((v: string) => f.endsWith(v) && v !== selectedVariant))
    ```
    This requires that `ciVariants` and `selectedVariant` are defined before the dry-run branch.
    Move these two variable declarations to BEFORE the `if (dryRun)` block:
    ```
    const ciVariants = ['ci-node.yml', 'ci-python.yml', 'ci-both.yml']
    const selectedVariant = `ci-${projectType}.yml`
    ```
    (They currently live only inside copyTemplates — add them here as local variables for the
    dry-run path only. They are purely for filtering display output and do not affect the copy step.)

    Also update the dry-run next-steps note when minimal=true: add a line below the existing 3
    next-steps items:
    `minimal ? '\n4. Run without --minimal to also add CI workflows and docs.' : ''`
    Include it only if minimal is true.

    **Change 4 — Destructure copyTemplates return and show split summary (UX-02):**
    In the 'Copying template files' task, replace:
    ```
    const files = await copyTemplates(templateDir, cwd, false, minimal, projectType)
    createdFiles.push(...files)
    return `Copied ${files.length} files`
    ```
    with:
    ```
    const { written, skipped } = await copyTemplates(templateDir, cwd, false, minimal, projectType)
    createdFiles.push(...written)
    skippedFiles.push(...skipped)
    return `Copied ${written.length} files`
    ```
    Add `const skippedFiles: string[] = []` alongside `const createdFiles: string[] = []`.

    After `await tasks(taskList)`, replace the single note call:
    `note(createdFiles.join('\n'), 'Files created')`
    with two conditional notes:
    ```
    note(createdFiles.join('\n') || '(none)', `Files written (${createdFiles.length})`)
    if (skippedFiles.length > 0) {
      note(skippedFiles.join('\n'), `Files skipped (${skippedFiles.length})`)
    }
    ```

    **Change 5 — Error surfacing (UX-03):**
    Wrap `await tasks(taskList)` in a try/catch:
    ```
    try {
      await tasks(taskList)
    } catch (e) {
      const err = e as NodeJS.ErrnoException
      const msg = err.code === 'EACCES' || err.code === 'EPERM'
        ? `Cannot write files to ${cwd}.\nWhy: You do not have write permission.\nFix: chmod u+w ${cwd}  (macOS/Linux) or check folder properties (Windows)`
        : `Setup failed: ${err.message ?? String(e)}\nFix: Run npx goodvibes@latest init`
      cancel(msg)
      process.exit(1)
    }
    ```

    **Change 6 — --minimal next-steps note (MIN-01, per ROADMAP success criterion #3):**
    After the next-steps note, add a conditional note when minimal is true:
    ```
    if (minimal) {
      note(
        'CI workflows and docs were skipped.\nRun goodvibes init without --minimal to add them.',
        'Skipped layers'
      )
    }
    ```
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test -- --run 2>&1 | tail -20</automated>
  </verify>
  <done>
    Full npm test suite exits 0. All tests GREEN (copy-templates.test.ts and init.test.ts both pass).
    TypeScript compiler produces no errors (`npx tsc --noEmit` from packages/npm/).
    The `skippedFiles` variable is declared, populated, and used correctly in init.ts.
    The dry-run --minimal path filters out .github and docs files before display.
    JOURNAL.md updated with date, task summary, files changed, why, tests run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cwd filesystem → init.ts | `readdirSync(cwd)` reads user's project directory; one-level read, no path traversal |
| template file → destDir | Existing path-traversal guard `relative(templateDir, src).includes('..')` preserved in filter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02 | Tampering | error message in cancel() | mitigate | Error messages include only err.message (not stack trace); UX-03 requirement enforced |
| T-06-03 | Tampering | minimal filter path separator | mitigate | Use `relative(templateDir, src).startsWith('.github')` for cross-platform safety (per RESEARCH.md Pitfall 3) |
| T-06-04 | Tampering | template path traversal (preserved) | mitigate | Existing `relative(templateDir, src).includes('..')` guard preserved unchanged in filter |
| T-06-SC | Tampering | npm/pip installs | accept | No new installs — plan uses only already-installed @clack/prompts and node:fs stdlib |
</threat_model>

<verification>
Full verification after both tasks:
1. `cd packages/npm && npm test -- --run` exits 0
2. `cd packages/npm && npx tsc --noEmit` exits 0
3. Running `goodvibes init` in a directory with existing files shows a note before spinner tasks
4. Running `goodvibes init --minimal` produces a completion note about skipped CI/docs layers
5. Running `goodvibes init --dry-run --minimal` does not list .github or docs files in preview
</verification>

<success_criteria>
All npm package tests pass (GREEN). copyTemplates returns { written, skipped }. init.ts
destructures and displays both lists separately. ci.yml is never silently overwritten.
--minimal correctly excludes all of .github/ and docs/. --dry-run --minimal preview is accurate.
EACCES/EPERM errors produce plain-English messages with cancel() + process.exit(1).
</success_criteria>

<output>
Create `.planning/phases/06-ux-hardening/06-02-SUMMARY.md` when done.
</output>
