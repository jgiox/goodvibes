---
phase: 06-ux-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/npm/src/steps/copy-templates.test.ts
  - packages/npm/src/commands/init.test.ts
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - MIN-01
  - MIN-02

must_haves:
  truths:
    - "copy-templates.test.ts has a failing test asserting copyTemplates returns { written, skipped } object shape"
    - "copy-templates.test.ts has a failing test asserting existing ci.yml is not overwritten by rename"
    - "copy-templates.test.ts has failing tests asserting --minimal skips .github/ISSUE_TEMPLATE and docs/"
    - "init.test.ts has a failing test asserting note() is called when cwd is non-empty"
    - "init.test.ts has a failing test asserting note() is called with both 'written' and 'skipped' title variants"
    - "init.test.ts has a failing test asserting EACCES causes cancel() and process.exit(1)"
    - "init.test.ts has a failing test asserting --dry-run --minimal output excludes .github and docs files"
    - "npm test exits non-zero (tests are RED) — implementation not done yet"
  artifacts:
    - path: "packages/npm/src/steps/copy-templates.test.ts"
      provides: "RED test cases for UX-02, UX-04, MIN-01"
      contains: "written"
    - path: "packages/npm/src/commands/init.test.ts"
      provides: "RED test cases for UX-01, UX-02, UX-03, MIN-02"
      contains: "skipped"
  key_links:
    - from: "copy-templates.test.ts"
      to: "copy-templates.ts copyTemplates return type"
      via: "mock shape { written, skipped }"
      pattern: "written.*skipped"
    - from: "init.test.ts"
      to: "init.ts non-empty notice"
      via: "note() spy assertion"
      pattern: "non-empty|Non-empty"
---

<objective>
Write all failing (RED) unit tests for the TypeScript npm package before any implementation changes.
These tests document the exact behavior changes required by UX-01 through MIN-02 and drive
the implementation in Plan 02.

Purpose: TDD discipline — tests define the contract; implementation is written to make them pass.
Output: Updated test files with new failing test cases. All new tests must fail before Plan 02 runs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-ux-hardening/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED tests for copy-templates.ts (UX-02, UX-04, MIN-01)</name>
  <files>packages/npm/src/steps/copy-templates.test.ts</files>
  <read_first>
    packages/npm/src/steps/copy-templates.test.ts
    packages/npm/src/steps/copy-templates.ts
  </read_first>
  <behavior>
    Existing tests that call `copyTemplates(...)` and then do `expect(files.length).toBeGreaterThan(0)`
    or `expect(files).toContain('CLAUDE.md')` will BREAK once the return type changes from
    `string[]` to `{ written: string[]; skipped: string[] }`. These must be updated to destructure
    `{ written }` before asserting on the array.

    New test cases to add (all must fail until Plan 02 implementation):

    - "returns object with written and skipped arrays": call copyTemplates on an empty dest, assert result
      has `written` and `skipped` keys, `written.length > 0`, `skipped.length === 0`,
      `written` contains `'CLAUDE.md'`

    - "skipped array contains files that already existed in dest": first call copyTemplates to an empty
      dir (written set). Then call copyTemplates again to same dir. On second call, assert
      `result.skipped.length > 0` and `result.written.length === 0` (or near-zero — only CLAUDE.md
      always counts as written per the RESEARCH.md open question resolution)

    - "does not rename ci.yml when it already exists in destination (UX-04)": copy once to tmpDir,
      then assert ci.yml exists, then call copyTemplates again; assert that ci.yml still exists and
      ci-node.yml does NOT exist (the rename guard must not clobber an existing ci.yml)

    - "--minimal skips .github/ISSUE_TEMPLATE/ not just workflows (MIN-01)": pass minimal=true,
      assert that a file at `.github/ISSUE_TEMPLATE/bug_report.yml` was NOT written to dest

    - "--minimal skips docs/ (MIN-01)": pass minimal=true, assert `docs/onboarding.md` was NOT
      written to dest

    - "--minimal keeps CLAUDE.md and skills (MIN-01)": pass minimal=true, assert `written` contains
      'CLAUDE.md' and at least one `.claude/skills/` file
  </behavior>
  <action>
    Modify packages/npm/src/steps/copy-templates.test.ts:

    1. Update all existing `copyTemplates` call sites in the `copyTemplates` describe block that
       directly use the return value as an array: change them to destructure `{ written }` from the
       result and use `written` in place of the former `files` variable. Specifically:
       - "copies all template files to empty destination": change `const files = await copyTemplates(...)`
         to `const { written } = await copyTemplates(...)`, then replace `files.length` and
         `files.toContain` with `written.length` and `written`.
       - "dry-run returns file list without writing anything": Plan 02 changes the dry-run return type
         to `{ written: string[], skipped: [] }` (same shape as non-dry-run). Update this test NOW to
         destructure `{ written }` from the result and assert on `written` instead of the flat array.
         This makes the test go RED in Plan 01 (current implementation still returns string[]) and
         GREEN in Plan 02 when the return type is unified. Change the assertion from
         `expect(files.length).toBeGreaterThan(0)` / `expect(files).toContain(...)` to
         `expect(written.length).toBeGreaterThan(0)` / `expect(written).toContain(...)`.
       - "second call is idempotent" test: the return value is not asserted here — leave as-is.
       - "skips existing files" test: the return value is not asserted here — leave as-is.

    2. Add a new `describe('copyTemplates — written/skipped tracking', ...)` block with its own
       `beforeEach`/`afterEach` using `mkdtempSync` / `rmSync` for tmpDir. Inside:

       a. Test "returns object with written and skipped arrays (empty dest)":
          Call `copyTemplates(templateDir, tmpDir, false, false)`.
          Assert `typeof result === 'object' && result !== null`.
          Assert `Array.isArray(result.written)`.
          Assert `Array.isArray(result.skipped)`.
          Assert `result.written.length > 0`.
          Assert `result.skipped.length === 0`.
          Assert `result.written` contains `'CLAUDE.md'`.

       b. Test "skipped array contains pre-existing files on second run":
          Run copyTemplates twice to same tmpDir. On second call: assert `result.skipped.length > 0`.

    3. Add a new `describe('copyTemplates — ci.yml rename guard', ...)` block with tmpDir:

       a. Test "does not overwrite existing ci.yml in destination (UX-04)":
          Run copyTemplates once to populate tmpDir (projectType 'node' so ci.yml is created via rename).
          Confirm `existsSync(join(tmpDir, '.github/workflows/ci.yml'))` is true.
          Write custom content to that ci.yml: `writeFileSync(ciPath, '# custom CI\n')`.
          Run copyTemplates again to same tmpDir.
          Assert ci.yml still contains `# custom CI` (not overwritten).
          Assert `result.skipped` includes a string matching `.github/workflows/ci.yml` or
          `result.written` does NOT include it.

    4. Add a new `describe('copyTemplates — minimal filter scope', ...)` block with tmpDir:

       a. Test "--minimal skips .github/ISSUE_TEMPLATE (MIN-01)":
          Run copyTemplates(templateDir, tmpDir, false, true).
          Assert `!existsSync(join(tmpDir, '.github/ISSUE_TEMPLATE'))`.

       b. Test "--minimal skips docs/ (MIN-01)":
          Run copyTemplates(templateDir, tmpDir, false, true).
          Assert `!existsSync(join(tmpDir, 'docs'))`.

       c. Test "--minimal keeps CLAUDE.md (MIN-01)":
          Run copyTemplates(templateDir, tmpDir, false, true).
          Destructure `{ written }`.
          Assert `written.includes('CLAUDE.md')` OR `existsSync(join(tmpDir, 'CLAUDE.md'))`.

    All new tests are integration-style (using real tmpDir + resolveTemplatesDir()) — they do NOT use mocks.
    Existing test structure uses `mkdtempSync`/`rmSync` pattern — follow the same pattern.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test -- --run copy-templates 2>&1 | tail -20</automated>
  </verify>
  <done>
    npm test exits non-zero (tests FAIL) because copyTemplates still returns string[] but new tests
    expect { written, skipped }. Existing tests updated to use `{ written }` destructuring (including
    the dry-run test) also fail because the return type has not changed yet. The test file compiles
    without TS errors (or only runtime assertion errors — not syntax errors).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED tests for init.ts (UX-01, UX-02, UX-03, MIN-02)</name>
  <files>packages/npm/src/commands/init.test.ts</files>
  <read_first>
    packages/npm/src/commands/init.test.ts
    packages/npm/src/commands/init.ts
    packages/npm/src/steps/copy-templates.ts
  </read_first>
  <behavior>
    Existing mocks in init.test.ts return `string[]` from copyTemplates (e.g. line 78, 119, 172).
    Once the real function's return type changes, TypeScript will flag these mocks. Update them now
    to the new shape so the test file is type-correct after Plan 02 lands.

    Existing test "prints file list completion note with 'Files created' title" will break because
    after the return type change the note title changes from "Files created" to "Files written"
    (written count) and a second note is added for "Files skipped". Update this test accordingly.

    New test cases to add (all must fail until Plan 02 implementation):

    - "non-empty dir: note() is called with non-empty notice before tasks (UX-01)": mock
      readdirSync (or inject a directory with files) so cwd reports non-empty. Assert note()
      was called with content matching 'non-empty' or 'Existing files' before tasks() runs.

    - "completion summary: note() called with 'Files written' title showing written count (UX-02)":
      normal run, copyTemplates mock returns { written: ['CLAUDE.md', 'README.md'], skipped: [] }.
      Assert note() was called with a title containing 'written' (case-insensitive) and content
      containing 'CLAUDE.md'.

    - "completion summary: note() called with 'Files skipped' title when skipped > 0 (UX-02)":
      copyTemplates mock returns { written: ['CLAUDE.md'], skipped: ['JOURNAL.md'] }. Assert
      note() was called with a title containing 'skipped' and content containing 'JOURNAL.md'.

    - "EACCES error from tasks(): cancel() called and process exits 1 (UX-03)": tasks() mock
      throws an error with code 'EACCES'. Assert cancel() was called with a message containing
      'permission' or 'EACCES' or 'write'. Assert process.exit was called with 1.

    - "--dry-run --minimal: note shows only minimal files, excludes .github and docs (MIN-02)":
      listTemplateFiles mock returns a list including 'CLAUDE.md', '.github/workflows/ci.yml',
      'docs/onboarding.md', '.claude/skills/caveman/SKILL.md'. After calling with --dry-run --minimal,
      assert the note() content does NOT contain '.github' and does NOT contain 'docs/onboarding.md'
      but DOES contain 'CLAUDE.md'.
  </behavior>
  <action>
    Modify packages/npm/src/commands/init.test.ts:

    1. Update existing mock return shapes for copyTemplates from `string[]` to
       `{ written: string[], skipped: [] }`:
       - Line 78 (--minimal test): change `.mockResolvedValue(['CLAUDE.md', 'README.md'])` to
         `.mockResolvedValue({ written: ['CLAUDE.md', 'README.md'], skipped: [] })`
       - Line 119 (normal run test): change `.mockResolvedValue(['CLAUDE.md', '.github/workflows/ci.yml', 'README.md'])`
         to `.mockResolvedValue({ written: ['CLAUDE.md', '.github/workflows/ci.yml', 'README.md'], skipped: [] })`
       - Line 172 (file list title test): change `.mockResolvedValue(['CLAUDE.md', 'README.md'])`
         to `.mockResolvedValue({ written: ['CLAUDE.md', 'README.md'], skipped: [] })`
       - Line 203 (next-steps test): change `.mockResolvedValue(['CLAUDE.md'])`
         to `.mockResolvedValue({ written: ['CLAUDE.md'], skipped: [] })`

    2. Update existing test "prints file list completion note with 'Files created' title":
       Change the expected title string from `'Files created'` to `'written'` (the new note title
       will be something like "Files written (2)"). The test should use `String(c[1]).includes('written')`
       instead of `.includes('Files created')`.

    3. Add `cancel` and `process.exit` to the vi.mock for `@clack/prompts`. At the top of the file,
       update the mock factory to include `cancel: vi.fn()`. Also add `vi.spyOn(process, 'exit').mockImplementation(() => { throw new Error('process.exit') })` inside the relevant test case (or use `beforeEach`).

    4. Add new describe block "UX-01: non-empty directory notice":

       a. Test "note() is called with non-empty notice when cwd has files":
          Mock `readdirSync` from 'node:fs' to return `['some-file.txt']`.
          Add `'node:fs'` to the vi.mock list at top: `vi.mock('node:fs', () => ({ readdirSync: vi.fn(), existsSync: vi.fn() }))`.
          Set `vi.mocked(readdirSync).mockReturnValue(['some-file.txt'] as any)`.
          Mock copyTemplates to return `{ written: ['CLAUDE.md'], skipped: [] }`.
          Run parseAsync with `['node', 'goodvibes', 'init', '--minimal']`.
          Assert note() was called with a second argument containing 'non-empty' or 'Non-empty' or
          'Existing files'.

    5. Add new describe block "UX-02: written/skipped split in completion":

       a. Test "note() called with 'written' title and written file list":
          copyTemplates mock returns `{ written: ['CLAUDE.md', 'README.md'], skipped: [] }`.
          Assert note() called with content containing 'CLAUDE.md' and title containing 'written'.

       b. Test "note() called with 'skipped' title when skipped non-empty":
          copyTemplates mock returns `{ written: ['CLAUDE.md'], skipped: ['JOURNAL.md'] }`.
          Assert note() called with content containing 'JOURNAL.md' and title containing 'skipped'.

    6. Add new describe block "UX-03: error surfacing":

       a. Test "EACCES from tasks() calls cancel() and exits 1":
          Add `cancel` to the vi.mock for `@clack/prompts`: `cancel: vi.fn()`.
          Spy on process.exit: `const exitSpy = vi.spyOn(process, 'exit').mockImplementation((...args) => { throw new Error('exit ' + args[0]) })`.
          Mock tasks() to throw: `vi.mocked(tasks).mockRejectedValue(Object.assign(new Error('EACCES'), { code: 'EACCES' }))`.
          Wrap parseAsync in try/catch (it will throw from process.exit mock).
          Assert `vi.mocked(cancel)` was called.
          Assert exitSpy was called with `1`.

    7. Add new describe block "MIN-02: dry-run + minimal":

       a. Test "--dry-run --minimal excludes .github and docs from preview":
          Mock listTemplateFiles to return:
          `['CLAUDE.md', '.github/workflows/ci.yml', 'docs/onboarding.md', '.claude/skills/caveman/SKILL.md']`
          Run parseAsync with `['node', 'goodvibes', 'init', '--dry-run', '--minimal']`.
          Assert note() was called with content that does NOT include `.github` and does NOT include
          `docs/onboarding.md` but DOES include `CLAUDE.md`.

    Keep all existing tests intact — only update mock return shapes where noted above.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test -- --run init 2>&1 | tail -30</automated>
  </verify>
  <done>
    npm test for init.test.ts exits non-zero with clear assertion failures (not syntax errors).
    The new test cases fail because init.ts has not been updated yet.
    The updated mock shapes for existing tests may cause TypeScript type errors (acceptable at RED stage).
    All new describe blocks are present in the file.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test → implementation | Tests import from source; mocks isolate external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | test mock shapes | accept | Tests use local mocks only; no external input |
| T-06-SC | Tampering | npm/pip installs | accept | No new installs in this plan — test-only file changes |
</threat_model>

<verification>
After both tasks complete:
- npm test for copy-templates exits non-zero (RED)
- npm test for init exits non-zero (RED)
- No syntax errors — only assertion failures
- All new describe blocks present in both test files
</verification>

<success_criteria>
Both test files updated with RED test cases. Running `cd packages/npm && npm test -- --run` exits
non-zero. The failure output shows assertion errors for the new shape expectations, not TypeScript
compilation errors. Existing passing tests may also fail due to mock shape updates — this is expected
and correct at the RED stage.
</success_criteria>

<output>
Create `.planning/phases/06-ux-hardening/06-01-SUMMARY.md` when done.
</output>
