---
phase: 02-npm-cli
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/verify-phase2.sh
  - packages/npm/package.json
  - packages/npm/tsconfig.json
  - packages/npm/tsup.config.ts
  - packages/npm/src/index.ts
  - packages/npm/vitest.config.ts
  - packages/npm/src/index.test.ts
autonomous: true
requirements:
  - NPM-08
  - NPM-09

must_haves:
  truths:
    - "scripts/verify-phase2.sh is executable and runs without errors against the scaffold"
    - "packages/npm/package.json has name=goodvibes, bin.goodvibes pointing to dist/index.js, engines.node>=20.0.0"
    - "packages/npm/src/index.ts has the Node version check as its first executable statement before any imports"
    - "npm run build in packages/npm/ succeeds and produces dist/index.js with shebang"
    - "Test files covering Node version check and init flag stubs exist (Wave 0 gap closure)"
  artifacts:
    - path: "scripts/verify-phase2.sh"
      provides: "Phase 2 smoke test runner for all NPM-01..NPM-11 and HDR-01..HDR-06 checks"
      min_lines: 60
    - path: "packages/npm/package.json"
      provides: "npm CLI package manifest"
      contains: "goodvibes"
    - path: "packages/npm/src/index.ts"
      provides: "CLI entry point with Node version guard"
      contains: "process.version"
    - path: "packages/npm/tsup.config.ts"
      provides: "tsup build configuration"
    - path: "packages/npm/vitest.config.ts"
      provides: "vitest test framework config"
  key_links:
    - from: "packages/npm/src/index.ts"
      to: "packages/npm/dist/index.js"
      via: "npm run build (tsup)"
      pattern: "defineConfig.*entry.*src/index"
    - from: "packages/npm/package.json"
      to: "packages/npm/dist/index.js"
      via: "bin field"
      pattern: "\"goodvibes\".*dist/index"
---

<objective>
Create the Wave 0 foundation: the verify-phase2.sh smoke-test harness and the complete npm package scaffold. All subsequent Wave 1 plans depend on a buildable npm package existing here.

Purpose: Without this plan, no other plan can run `npm run build` or verify their implementation. This is the unblocking prerequisite for all Wave 1 work.
Output: scripts/verify-phase2.sh (35+ shell checks), packages/npm/ scaffold (package.json, tsconfig.json, tsup.config.ts, vitest.config.ts, src/index.ts entry stub with Node version guard), and test stubs that Wave 1 plans will fill in.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-npm-cli/02-RESEARCH.md
@.planning/phases/02-npm-cli/02-VALIDATION.md
@.planning/phases/01-template-content-repo-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create scripts/verify-phase2.sh smoke-test harness</name>
  <files>scripts/verify-phase2.sh</files>
  <read_first>
    - .planning/phases/02-npm-cli/02-VALIDATION.md — full per-requirement shell command list; use these exact commands as the basis for each check
    - scripts/verify-phase1.sh — use the same pass/fail counter pattern ($((pass + 1)), $((fail + 1)) — never ((var++)) due to set -e arithmetic behavior confirmed in 01-01-SUMMARY.md)
  </read_first>
  <action>
Create scripts/verify-phase2.sh as an executable bash smoke-test runner. Follow the exact same counter and color pattern from scripts/verify-phase1.sh (the $((var + 1)) arithmetic form, not ((var++))).

The script must accept a --quick flag: when passed, only run static checks (no filesystem writes, no npm builds). Without the flag, run the full suite including build verification.

Include one check per requirement from the VALIDATION.md per-task verification map, plus these additional static checks:

Static checks (always run, --quick safe):
1. packages/npm/package.json exists
2. packages/npm/package.json has "name" field equal to "goodvibes"
3. packages/npm/package.json has "bin.goodvibes" pointing to "./dist/index.js"
4. packages/npm/package.json has "engines.node" containing ">=20"
5. packages/npm/package.json does NOT contain "scripts.postinstall" (HDR-06 gate)
6. packages/npm/src/index.ts exists
7. packages/npm/src/index.ts starts with "#!/usr/bin/env node" on line 1
8. packages/npm/src/index.ts contains "process.version" (Node version check)
9. packages/npm/tsup.config.ts exists
10. packages/npm/tsup.config.ts references "src/index.ts"
11. packages/npm/tsconfig.json exists
12. packages/npm/vitest.config.ts exists

Build checks (only without --quick):
13. npm install runs successfully in packages/npm/
14. npm run build runs successfully in packages/npm/ and produces dist/index.js
15. dist/index.js starts with "#!/usr/bin/env node" on line 1 (shebang preserved by tsup)
16. node packages/npm/dist/index.js --help exits 0 and prints "goodvibes"
17. node packages/npm/dist/index.js init --help exits 0 and prints "dry-run"
18. node packages/npm/dist/index.js init --help exits 0 and prints "minimal"

HDR-06 static check (always):
19. npm pkg get scripts.postinstall (run from packages/npm/) returns empty string — no postinstall hook

Node version gate static check:
20. packages/npm/src/index.ts version check appears BEFORE the first import statement (use grep -n to confirm line numbers)

Summary line: "PASS: N checks passed, M failed" — exit 0 if M=0, exit 1 if M>0.

Make the script executable (chmod +x in the Write step or note that the executor must run chmod +x scripts/verify-phase2.sh after creating it).
  </action>
  <verify>
    <automated>bash -n scripts/verify-phase2.sh && bash scripts/verify-phase2.sh --quick && echo "SYNTAX+QUICK OK"</automated>
  </verify>
  <acceptance_criteria>
    - bash -n scripts/verify-phase2.sh exits 0 with no output (clean syntax)
    - scripts/verify-phase2.sh is executable (ls -la shows -rwxr-xr-x or similar)
    - bash scripts/verify-phase2.sh --quick exits 0 (no pipe — direct exit code)
    - The script contains exactly one check labeled for each of NPM-01 through NPM-11 and HDR-01 through HDR-06 in comments
  </acceptance_criteria>
  <done>scripts/verify-phase2.sh exists, passes bash -n syntax check, is executable, and contains labeled checks for all 17 phase requirements.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold packages/npm/ — package.json, tsconfig, tsup, entry stub, vitest, test stubs</name>
  <files>
    packages/npm/package.json
    packages/npm/tsconfig.json
    packages/npm/tsup.config.ts
    packages/npm/src/index.ts
    packages/npm/vitest.config.ts
    packages/npm/src/index.test.ts
    packages/npm/src/commands/init.test.ts
    packages/npm/src/steps/copy-templates.test.ts
    packages/npm/src/utils/detect-python.test.ts
    packages/npm/src/steps/install-headroom.test.ts
    packages/npm/src/steps/configure-mcp.test.ts
  </files>
  <read_first>
    - .planning/phases/02-npm-cli/02-RESEARCH.md — Pattern 6 (tsup config), Pattern 7 (Node version check), "Recommended Project Structure" section, "Standard Stack" section for exact version pins
    - .planning/phases/01-template-content-repo-foundation/01-03-SUMMARY.md — confirms packages/npm/ directory exists (was created as .gitkeep placeholder)
  </read_first>
  <action>
Create all files listed under files. Delete packages/npm/.gitkeep if it exists before writing new files.

PACKAGE.JSON — packages/npm/package.json:
Write with these exact values (do not deviate — all locked by RESEARCH.md and CLAUDE.md):
- name: "goodvibes"
- version: "1.0.0"
- description: "One-command bootstrap for vibe coding projects"
- type: "module"
- bin: { "goodvibes": "./dist/index.js" }
- main: "./dist/index.js"
- engines: { "node": ">=20.0.0" }
- files: ["dist", "templates"]
- license: "Apache-2.0"
- scripts:
  - "prebuild": "node -e \"const{cpSync}=require('fs');cpSync('../../templates','./templates',{recursive:true,force:true})\""
  - "build": "tsup"
  - "test": "vitest run"
  - "test:watch": "vitest"
- dependencies: commander ^13, @clack/prompts ^0.9, fs-extra ^11, execa ^9
- devDependencies: tsup ^8, typescript ^5.5, @types/node ^20, vitest ^2

Do NOT include cross-spawn as a dependency — execa v9 depends on cross-spawn internally (RESEARCH.md: "NPM-10's cross-spawn requirement is met by using execa, not by adding cross-spawn separately").
Do NOT include scripts.postinstall — it is explicitly prohibited by npm v12 (HDR-06).

TSCONFIG — packages/npm/tsconfig.json:
{
  "compilerOptions": {
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "target": "ES2022",
    "strict": true,
    "esModuleInterop": true,
    "outDir": "dist",
    "rootDir": "src"
  },
  "include": ["src"]
}

TSUP CONFIG — packages/npm/tsup.config.ts:
Use defineConfig from tsup. Single entry: ['src/index.ts']. format: ['esm']. platform: 'node'. target: 'node20'. clean: true. minify: false. No CJS output needed — CLI only, no library consumers.

ENTRY POINT — packages/npm/src/index.ts:
Line 1 MUST be: #!/usr/bin/env node
Lines 2-10 MUST be the Node version check — this check runs before any dynamic imports or async code:

  const [major] = process.version.replace('v', '').split('.').map(Number)
  if (major < 20) {
    process.stderr.write(
      `goodvibes requires Node.js 20 or higher.\nYou are running Node.js ${process.version}.\nInstall the latest LTS from https://nodejs.org\n`
    )
    process.exit(1)
  }

After the version check, add stub Commander.js wiring:
- Import Command from 'commander'
- Create program with name 'goodvibes', version '1.0.0', description 'One-command bootstrap for vibe coding projects'
- Register an 'init' subcommand with --dry-run and --minimal options pointing to a stub async action that logs "init stub — Wave 1 will implement" and exits 0
- Call program.parseAsync(process.argv) at the end

This stub allows the harness checks (`node dist/index.js --help`, `node dist/index.js init --help`) to pass without Wave 1 work.

VITEST CONFIG — packages/npm/vitest.config.ts:
{
  test: {
    environment: 'node',
    include: ['src/**/*.test.ts'],
    coverage: { provider: 'v8' }
  }
}
Use defineConfig from 'vitest/config'.

TEST STUBS (Wave 0 gap closure per VALIDATION.md):
Create these files as minimal passing stubs — they exist so `npm test` does not fail on missing files. Wave 1 plans will add real assertions.

packages/npm/src/index.test.ts:
  import { describe, it } from 'vitest'
  describe('index: Node version check', () => {
    it.todo('exits 1 with message when Node < 20')
    it.todo('continues when Node >= 20')
  })

packages/npm/src/commands/init.test.ts:
  import { describe, it } from 'vitest'
  describe('init command', () => {
    it.todo('--dry-run: prints files, writes nothing')
    it.todo('--minimal: skips headroom and CI files')
    it.todo('prints file list on completion (NPM-04)')
    it.todo('prints next-steps block with max 3 steps (NPM-05)')
    it.todo('zero-config: runs without stdin input (NPM-02)')
  })

packages/npm/src/steps/copy-templates.test.ts:
  import { describe, it } from 'vitest'
  describe('copy-templates', () => {
    it.todo('copies all template files to target directory')
    it.todo('skips existing files without overwriting (idempotent)')
    it.todo('routes CLAUDE.md through sentinel merge, not bulk copy')
    it.todo('--dry-run returns file list without writing')
  })

packages/npm/src/utils/detect-python.test.ts:
  import { describe, it } from 'vitest'
  describe('detectPython', () => {
    it.todo('returns python3 command when Python 3.10+ found')
    it.todo('returns null when no Python found (HDR-02)')
    it.todo('skips python command if version output does not match regex (Windows Store guard)')
  })

packages/npm/src/steps/install-headroom.test.ts:
  import { describe, it } from 'vitest'
  describe('install-headroom', () => {
    it.todo('installs via uv when uv is available (HDR-01)')
    it.todo('falls back to pipx when uv absent')
    it.todo('falls back to pip --user when pipx absent')
    it.todo('prints ONNX model warning before installing (HDR-03)')
    it.todo('skips install when Python absent, exits 0 with warning')
  })

packages/npm/src/steps/configure-mcp.test.ts:
  import { describe, it } from 'vitest'
  describe('configure-mcp', () => {
    it.todo('uses claude mcp add -s user with absolute headroom path (HDR-04)')
    it.todo('falls back to headroom mcp install when claude CLI absent')
    it.todo('skips registration if headroom already in mcp list (HDR-05)')
    it.todo('uses absolute path from which headroom, not bare command name')
  })

After writing all files, run in packages/npm/:
  npm install
  npm run build

Both must succeed for Task 2 to be complete.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm run build 2>&1 | grep -q "dist/index.js" && node dist/index.js --help 2>&1 | grep -q "goodvibes" && node dist/index.js init --help 2>&1 | grep -q "dry-run" && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - packages/npm/package.json: node -e "const p=JSON.parse(require('fs').readFileSync('./packages/npm/package.json','utf8'));process.exit(p.name==='goodvibes'&&p.bin?.goodvibes==='./dist/index.js'&&p.engines?.node==='>=20.0.0'?0:1)" exits 0
    - packages/npm/package.json: node -e "const p=JSON.parse(require('fs').readFileSync('./packages/npm/package.json','utf8'));process.exit(p.scripts?.postinstall?1:0)" exits 0 (no postinstall)
    - packages/npm/src/index.ts: head -1 packages/npm/src/index.ts | grep -q "#!/usr/bin/env node" — shebang on line 1
    - packages/npm/src/index.ts: grep -n "process.version" packages/npm/src/index.ts — must appear on a line BEFORE the first "import" statement line number
    - npm run build in packages/npm/ exits 0 and dist/index.js exists
    - head -1 packages/npm/dist/index.js | grep -q "#!/usr/bin/env node" — shebang preserved in output
    - node packages/npm/dist/index.js --help exits 0 and output contains "goodvibes"
    - node packages/npm/dist/index.js init --help exits 0 and output contains "dry-run" and "minimal"
    - npm test in packages/npm/ exits 0 (stubs with it.todo pass)
    - All 6 test stub files exist under packages/npm/src/
  </acceptance_criteria>
  <done>The npm package scaffold is complete: package.json with correct manifest, tsup builds dist/index.js with preserved shebang, node dist/index.js init --help shows --dry-run and --minimal flags, npm test exits 0, and all test stub files exist for Wave 1 to populate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer workstation → npm registry | npm install pulls packages from registry; pinned semver ranges |
| tsup compiler → dist/index.js | Build output is generated code executed by users via npx |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01-A | Tampering | npm install (commander, @clack/prompts, fs-extra, execa, tsup) | mitigate | All 5 packages are [VERIFIED] via npm registry + source repos per RESEARCH.md Package Legitimacy Audit; no postinstall scripts found; pinned to ^major version ranges |
| T-02-01-B | Tampering | packages/npm/package.json scripts.postinstall | mitigate | postinstall field MUST NOT exist; enforced by verify-phase2.sh check 19 which runs `npm pkg get scripts.postinstall` and asserts empty |
| T-02-01-C | Information Disclosure | dist/index.js published artifact | mitigate | verify-phase2.sh --quick static check confirms no secrets, tokens, or env var values are embedded in src/index.ts stub |
| T-02-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck table present in RESEARCH.md; all packages [VERIFIED]; no [ASSUMED] or [SUS] packages — no blocking checkpoint required |
</threat_model>

<verification>
After completing both tasks:
1. bash scripts/verify-phase2.sh --quick — must report 0 failures
2. cd packages/npm && npm run build — exits 0, dist/index.js produced
3. node packages/npm/dist/index.js --help | grep "goodvibes" — PASS
4. node packages/npm/dist/index.js init --help | grep -E "dry-run|minimal" — PASS
5. node packages/npm/dist/index.js init --help | grep "dry-run" && node packages/npm/dist/index.js init --help | grep "minimal" — both PASS
6. cd packages/npm && npm test — exits 0 (todo stubs pass)
7. head -1 packages/npm/dist/index.js | grep -q "#!/usr/bin/env node" — shebang preserved by tsup
</verification>

<success_criteria>
- scripts/verify-phase2.sh exists, is executable, passes bash -n, and contains a labeled check for each of the 17 Phase 2 requirement IDs
- packages/npm/ scaffold is complete: package.json (goodvibes, bin, engines, files, no postinstall), tsconfig.json, tsup.config.ts, vitest.config.ts, src/index.ts (shebang line 1, Node version check before imports)
- npm run build in packages/npm/ exits 0 and produces dist/index.js with shebang on line 1
- node packages/npm/dist/index.js init --help shows --dry-run and --minimal flags
- npm test in packages/npm/ exits 0 (all it.todo stubs pass)
- All 6 test stub files exist under packages/npm/src/ ready for Wave 1 implementations
</success_criteria>

<output>
Create `.planning/phases/02-npm-cli/02-01-SUMMARY.md` when done.
</output>
