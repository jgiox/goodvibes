---
phase: 02-npm-cli
plan: 05
type: execute
wave: 3
depends_on:
  - 02-04
files_modified:
  - packages/npm/.npmignore
  - packages/npm/package.json
  - scripts/verify-phase2.sh
autonomous: false
requirements:
  - NPM-10
  - NPM-11

must_haves:
  truths:
    - "npm pack --dry-run in packages/npm/ lists dist/ and templates/ but NOT src/, node_modules/, or test files"
    - "The packed tarball contains a shebang-enabled dist/index.js"
    - "npm publish (when run by the user) succeeds and resolves as 'goodvibes' on the npm registry"
    - "cross-spawn is satisfied by execa's internal dependency — no standalone cross-spawn in dependencies"
    - "The postinstall field is absent from package.json (HDR-06 gate passes)"
    - "verify-phase2.sh --quick reports 0 failures after all Wave 1 work is complete"
  artifacts:
    - path: "packages/npm/.npmignore"
      provides: "Excludes src/, *.test.ts, vitest.config.ts, tsup.config.ts, tsconfig.json from npm pack"
    - path: "packages/npm/package.json"
      provides: "Finalized publish manifest with correct files field, publishConfig, and repository fields"
  key_links:
    - from: "packages/npm/package.json"
      to: "npm registry (goodvibes)"
      via: "npm publish"
      pattern: "\"name\".*goodvibes"
    - from: "packages/npm/package.json"
      to: "packages/npm/dist/index.js"
      via: "files field + bin field"
      pattern: "\"files\".*dist"

user_setup:
  - service: npm registry
    why: "Publishing goodvibes package requires npm account login"
    env_vars: []
    dashboard_config:
      - task: "Log in to npm — run: npm login"
        location: "Terminal (packages/npm/ directory)"
      - task: "Verify package name is available — run: npm view goodvibes"
        location: "Terminal — should return E404 if name is still unclaimed"
      - task: "Run: npm publish --access public"
        location: "Terminal (packages/npm/ directory)"
---

<objective>
Finalize the npm package for publishing: write .npmignore to exclude dev artifacts, verify the files field captures dist/ and templates/, run npm pack --dry-run to confirm the manifest, and guide the user through npm publish.

Purpose: NPM-10 (cross-spawn via execa) and NPM-11 (published as 'goodvibes') are gated on this plan. The publish step is a human action because it requires npm registry credentials.
Output: .npmignore, finalized package.json, passing npm pack --dry-run, and a human checkpoint to publish.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-npm-cli/02-RESEARCH.md
@.planning/phases/02-npm-cli/02-01-SUMMARY.md
@.planning/phases/02-npm-cli/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Write .npmignore and finalize package.json for publishing</name>
  <files>
    packages/npm/.npmignore
    packages/npm/package.json
  </files>
  <read_first>
    - packages/npm/package.json — MUST read current state before modifying; was last written in Wave 0 Task 2 of plan 02-01
    - .planning/phases/02-npm-cli/02-RESEARCH.md — Pattern 6 (tsup config for CLI package) has the exact package.json publish block; Pitfall 6 (npm files field and templates/ directory); "Standard Stack" section for confirmed versions; NPM-10 cross-spawn note ("NPM-10's cross-spawn requirement is met by using execa, not by adding cross-spawn separately")
  </read_first>
  <action>
Read packages/npm/package.json first to see its current state.

Create packages/npm/.npmignore with these exclusions:
  src/
  *.test.ts
  vitest.config.ts
  tsup.config.ts
  tsconfig.json
  node_modules/
  .gitignore
  *.tsbuildinfo

Update packages/npm/package.json to add or verify these fields (merge with existing content — do NOT drop any field already present):
  - "repository": { "type": "git", "url": "https://github.com/jgiox/goodvibes" }
  - "keywords": ["scaffold", "vibe-coding", "claude", "llm", "starter-kit", "cli"]
  - "publishConfig": { "access": "public" }
  - "files": ["dist", "templates"] — confirm this is already present; if so, leave it; if missing, add it

Do NOT add cross-spawn to dependencies. Verify that execa ^9 is listed in dependencies (NPM-10 is satisfied by execa's internal cross-spawn dependency — this is explicitly documented in RESEARCH.md).

Verify that scripts.postinstall does NOT exist in the file (HDR-06). If it somehow appeared during Wave 1, remove it now.

After updating package.json, run npm install in packages/npm/ to regenerate the lockfile with final dependencies (this also verifies that npm resolves the dependency graph correctly).

Then run npm run build in packages/npm/ to confirm the final package state builds cleanly.

Then run npm pack --dry-run in packages/npm/ and capture the output. Verify:
  - dist/ entries are present in the pack list
  - templates/ entries are present (confirming prebuild script copied them)
  - src/ entries are NOT present (excluded by .npmignore)
  - *.test.ts files are NOT present
  - node_modules/ is NOT present

If templates/ is absent from the pack output: run the prebuild script manually (node -e "...cpSync..." from package.json scripts.prebuild) and re-run npm pack --dry-run.

Also verify NPM-10 by running:
  npm ls execa cross-spawn 2>&1 | grep "cross-spawn"
This must show cross-spawn as a transitive dep of execa, confirming the requirement is met without a direct dependency.

After all pack checks pass, update .planning/phases/02-npm-cli/02-VALIDATION.md frontmatter:
  - Set nyquist_compliant: true
  - Set wave_0_complete: true
This signals to gsd-verifier that the Nyquist validation contract is satisfied.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm pack --dry-run 2>&1 | grep -E "dist/|templates/" | head -10 && npm pack --dry-run 2>&1 | grep -c "src/" | grep "^0$" && echo "PACK OK"</automated>
  </verify>
  <acceptance_criteria>
    - packages/npm/.npmignore exists and contains "src/" and "*.test.ts"
    - npm pack --dry-run in packages/npm/ exits 0
    - npm pack --dry-run output: grep -q "dist/index.js" — dist entry present
    - npm pack --dry-run output: grep -q "templates/" — templates present in pack
    - npm pack --dry-run output: grep -c "src/" returns 0 — src excluded
    - npm pack --dry-run output: grep -c "*.test.ts" returns 0 — test files excluded
    - node -e "const p=JSON.parse(require('fs').readFileSync('./packages/npm/package.json','utf8'));process.exit(p.scripts?.postinstall?1:0)" exits 0 — no postinstall
    - npm ls cross-spawn 2>&1 | grep -q "cross-spawn" — present as execa transitive dep
    - npm run build exits 0 in packages/npm/ after package.json update
  </acceptance_criteria>
  <done>.npmignore excludes all dev artifacts. package.json is finalized with repository, keywords, publishConfig. npm pack --dry-run confirms dist/ and templates/ included, src/ excluded. cross-spawn confirmed as execa transitive dep satisfying NPM-10.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Human gate: full phase smoke test and npm publish</name>
  <what-built>
    The complete goodvibes npm CLI:
    - scripts/verify-phase2.sh — full smoke-test harness (NPM-01 through NPM-11, HDR-01 through HDR-06)
    - packages/npm/dist/index.js — compiled CLI entry
    - All step modules: copy-templates, install-headroom, configure-mcp, sentinel-merge
    - .npmignore + finalized package.json ready for publish
  </what-built>
  <how-to-verify>
    Step 1 — Run the full test suite:
      cd /home/ygiokas/GoodVibes
      cd packages/npm && npm test && cd ../..

    Step 2 — Run the full smoke harness:
      bash scripts/verify-phase2.sh
    Expected: "PASS: N checks passed, 0 failed"

    Step 3 — End-to-end dry-run smoke test:
      mkdir -p /tmp/gv-e2e-test && cd /tmp/gv-e2e-test
      node /home/ygiokas/GoodVibes/packages/npm/dist/index.js init --dry-run
    Expected: Named steps appear (Copying template files, Installing headroom, Configuring headroom MCP), file list printed showing CLAUDE.md and skill files, "Next steps" block shows 3 items, exits 0

    Step 4 — End-to-end real init in empty directory:
      mkdir -p /tmp/gv-e2e-real && cd /tmp/gv-e2e-real
      node /home/ygiokas/GoodVibes/packages/npm/dist/index.js init
    Expected: Files created (CLAUDE.md, .claude/skills/caveman/ etc), headroom installed (or skipped with message if Python absent), exits 0

    Step 5 — Idempotency check:
      cd /tmp/gv-e2e-real
      echo "# My custom rule" >> CLAUDE.md
      node /home/ygiokas/GoodVibes/packages/npm/dist/index.js init
    Expected: Custom content outside sentinel preserved; second run exits 0

    Step 6 — Verify npm pack:
      cd /home/ygiokas/GoodVibes/packages/npm
      npm pack --dry-run
    Confirm: dist/ and templates/ present; src/ absent

    Step 6b — Verify NPM-09 Windows documentation (best-effort):
      grep -i "windows\|wsl" /home/ygiokas/GoodVibes/README.md
    Confirm: README.md mentions WSL as the recommended path for Windows users.
    If absent, add one line to README.md: "**Windows:** Use WSL2 for the best experience — native Windows support is best-effort."

    Step 7 — Publish (when ready):
      cd /home/ygiokas/GoodVibes/packages/npm
      npm login   (if not already logged in)
      npm view goodvibes   (confirm still returns E404)
      npm publish --access public

    Step 8 — Verify publish (after publish):
      npx goodvibes@latest init --dry-run
    Expected: Downloads and runs goodvibes, shows dry-run output, exits 0
  </how-to-verify>
  <resume-signal>
    Type "approved" when Steps 1-6 all pass and you are ready to proceed.
    If you have published (Step 7-8), type "published" instead.
    If any step fails, describe the failure and Claude will create a gap-closure plan.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packages/npm/ → npm registry | Publishing the compiled package; registry is public |
| npm registry → end-user machine (npx goodvibes) | Package downloaded and executed by users |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05-A | Tampering | npm publish — supply chain integrity | mitigate | Publish only from clean git working tree (no uncommitted changes); run npm pack --dry-run inspection before every publish to confirm no secrets or unexpected files included |
| T-02-05-B | Information Disclosure | Package tarball — secrets in published artifact | mitigate | .npmignore excludes src/, test files, config files; npm pack --dry-run review in Task 1 acceptance criteria confirms no src/ in the pack; no .env files exist in packages/npm/ |
| T-02-05-C | Tampering | npm name squatting — 'goodvibes' name claimed before publish | accept | RESEARCH.md confirms name available as of 2026-06-23; human checkpoint (Step 7) verifies E404 immediately before publish; scoped fallback @jgiox/goodvibes available if needed |
| T-02-05-D | Elevation of Privilege | npm install --user (via headroom install) | accept | install-headroom.ts uses pip install --user — no sudo, no system-level elevation. Already mitigated in T-02-03-D. |
  T-02-05-SC | Tampering | npm/pip/cargo installs during publish workflow | mitigate | Publish flow does not install additional packages; existing packages verified in T-02-01-A; human reviews npm pack output before publishing |
</threat_model>

<verification>
After completing Task 1:
1. cd packages/npm && npm pack --dry-run | grep "dist/index.js" — PASS
2. cd packages/npm && npm pack --dry-run | grep "templates/" — PASS
3. cd packages/npm && npm pack --dry-run | grep -c "src/" | grep "^0$" — PASS (no src/ in pack)
4. npm ls cross-spawn | grep "cross-spawn" — PASS (NPM-10 verified)
5. bash scripts/verify-phase2.sh — full suite green

After checkpoint approved:
6. npx goodvibes@latest init --dry-run (post-publish) — resolves and runs
</verification>

<success_criteria>
- .npmignore correctly excludes all dev artifacts from the published package
- npm pack --dry-run confirms dist/ and templates/ included, src/ and *.test.ts excluded
- cross-spawn confirmed as execa transitive dep (NPM-10 satisfied without direct dependency)
- No scripts.postinstall in package.json (HDR-06 confirmed)
- Human checkpoint: full smoke test passes, end-to-end init works in empty directory, idempotency confirmed
- npm publish --access public succeeds; npx goodvibes@latest init --dry-run resolves from registry (NPM-11)
</success_criteria>

<output>
Create `.planning/phases/02-npm-cli/02-05-SUMMARY.md` when done.
</output>
