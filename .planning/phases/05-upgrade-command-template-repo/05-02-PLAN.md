---
phase: "05"
plan: "02"
type: execute
wave: 1
depends_on:
  - "05-01"
files_modified:
  - packages/npm/src/commands/upgrade.ts
  - packages/npm/src/index.ts
  - packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py
  - packages/pip/src/goodvibes_cli/main.py
autonomous: true
requirements:
  - UPG-01
  - UPG-02
  - UPG-03
  - UPG-04

must_haves:
  truths:
    - "Running `cd packages/npm && npm test` is fully GREEN (all tests pass including upgrade.test.ts)"
    - "Running `cd packages/pip && uv run --extra dev pytest -x -q` is fully GREEN"
    - "`npx goodvibes upgrade` in a stale project updates CLAUDE.md sentinel block and skill files without touching JOURNAL.md or CHANGELOG.md"
    - "`goodvibes upgrade --dry-run` prints a ~/=/+ change summary and exits 0 without writing any files"
    - "User content before and after the sentinel block in CLAUDE.md is preserved after upgrade"
    - "Running `scripts/verify-phase5.sh --quick` exits 0 (all file-existence and content checks pass)"
  artifacts:
    - path: "packages/npm/src/commands/upgrade.ts"
      provides: "TypeScript upgrade subcommand"
      contains: "registerUpgradeCommand"
      exports: ["registerUpgradeCommand"]
    - path: "packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py"
      provides: "Python upgrade subcommand"
      contains: "upgrade_cmd"
    - path: "packages/npm/src/index.ts"
      provides: "Wired upgrade subcommand into CLI"
      contains: "registerUpgradeCommand"
    - path: "packages/pip/src/goodvibes_cli/main.py"
      provides: "Wired upgrade subcommand into Typer app"
      contains: "upgrade_cmd"
  key_links:
    - from: "packages/npm/src/commands/upgrade.ts"
      to: "packages/npm/src/utils/sentinel-merge.ts"
      via: "import extractVersion, versionGte, mergeClaude"
      pattern: "extractVersion|versionGte|mergeClaude"
    - from: "packages/npm/src/commands/upgrade.ts"
      to: "packages/npm/src/steps/copy-templates.ts"
      via: "import resolveTemplatesDir + upgradeTemplates inline"
      pattern: "resolveTemplatesDir"
    - from: "packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py"
      to: "packages/pip/src/goodvibes_cli/utils/sentinel_merge.py"
      via: "from goodvibes_cli.utils.sentinel_merge import extract_version, version_gte, merge_claude"
      pattern: "extract_version|version_gte|merge_claude"
    - from: "packages/pip/src/goodvibes_cli/main.py"
      to: "packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py"
      via: "app.command('upgrade')(upgrade_cmd)"
      pattern: "upgrade_cmd"
---

<objective>
Wave 1: Implement the upgrade subcommand in both TypeScript (upgrade.ts) and Python (upgrade_cmd.py), and wire each into the respective CLI entry points (index.ts and main.py). This turns the RED tests from Plan 01 GREEN.

Purpose: Deliver UPG-01 through UPG-04 — a working `goodvibes upgrade` command that re-syncs managed files, preserves user content outside sentinel blocks (UPG-03), prints a diff summary before applying (UPG-04), and uses version stamps to detect currency (UPG-02).

Output: upgrade.ts, upgrade_cmd.py, updated index.ts, updated main.py — all tests GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-upgrade-command-template-repo/05-RESEARCH.md
@.planning/phases/05-upgrade-command-template-repo/05-PATTERNS.md
@.planning/phases/05-upgrade-command-template-repo/05-01-SUMMARY.md
@packages/npm/src/commands/init.ts
@packages/npm/src/steps/copy-templates.ts
@packages/npm/src/utils/sentinel-merge.ts
@packages/npm/src/index.ts
@packages/pip/src/goodvibes_cli/commands/init_cmd.py
@packages/pip/src/goodvibes_cli/steps/copy_templates.py
@packages/pip/src/goodvibes_cli/utils/sentinel_merge.py
@packages/pip/src/goodvibes_cli/main.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement upgrade.ts (TypeScript) and wire into index.ts</name>
  <files>packages/npm/src/commands/upgrade.ts, packages/npm/src/index.ts</files>

  <read_first>
    - packages/npm/src/commands/init.ts — registerInitCommand full structure; tasks() array pattern; intro/outro/note usage
    - packages/npm/src/utils/sentinel-merge.ts — extractVersion (line 7), versionGte (line 12), mergeClaude (line 31); function signatures and what they import (outputFile, pathExists from fs-extra; readFile, writeFile from node:fs/promises)
    - packages/npm/src/steps/copy-templates.ts — resolveTemplatesDir (line 10), copy() call with filter on line 71, path traversal guard on line 78, CI variant rename pattern on lines 87–94; imports: copy from fs-extra, readFile/rename from node:fs/promises, readdir from fs/promises, existsSync from fs, join/relative from path, fileURLToPath from url; listTemplateFiles export (signature and return type)
    - packages/npm/src/commands/upgrade.test.ts — exactly what each test mocks and asserts; the function names the tests expect (registerUpgradeCommand, and internal helpers via module-boundary mocks)
    - packages/npm/src/index.ts — current lines 10–22; two lines to add: import registerUpgradeCommand and call it
    - .planning/phases/05-upgrade-command-template-repo/05-RESEARCH.md — Pattern 1 (upgrade structure), Pattern 2 (version detection), Pattern 3 (overwrite-mode copy), Pattern 4 (diff summary format ~/=/+), Pattern 5 (managed vs unmanaged files table)
  </read_first>

  <behavior>
    - Test "skips upgrade when already up to date": extractVersion returns same version for installed and bundled; versionGte returns true; outro called with 'up to date'
    - Test "--dry-run: prints change summary without writing files": dryRun=true; note() called with change summary; tasks() NOT called; outro called with '--dry-run' or 'apply'
    - Test "runs full upgrade when CLAUDE.md is absent": pathExists=false; versionGte returns false; tasks() called and upgrade proceeds
    - Test "prints diff summary before applying changes": versionGte=false; tasks() executes task functions; note() called at least once with change info
    - Test "upgrade --help exits zero and contains dry-run": help output includes '--dry-run'
  </behavior>

  <action>
  Create packages/npm/src/commands/upgrade.ts implementing registerUpgradeCommand.

  Imports (mirror init.ts imports, replacing step-level imports with the required ones):
    - type Command from 'commander'
    - intro, outro, note, tasks from '@clack/prompts'
    - resolveTemplatesDir, listTemplateFiles from '../steps/copy-templates.js'
    - detectProjectType from '../utils/detect-project-type.js'
    - extractVersion, versionGte, mergeClaude from '../utils/sentinel-merge.js'
    - copy, pathExists from 'fs-extra'
    - readFile, rename from 'node:fs/promises'
    - existsSync from 'fs'
    - join, relative from 'path'

  Architectural note: upgradeTemplates is defined inline in upgrade.ts rather than as an exported function in copy-templates.ts. This is intentional — it has a single consumer (the upgrade command) and introducing a new export surface for a single call site would violate Ponytail. RESEARCH.md Architectural Responsibility Map lists it under the copy-templates step layer conceptually, but the inline placement is the correct implementation choice here.

  Internal helpers (not exported — Ponytail: no new public API surface):

  1. detectInstalledVersion(cwd: string): Promise<string | null>
     - claudePath = join(cwd, 'CLAUDE.md')
     - if !(await pathExists(claudePath)) return null
     - content = await readFile(claudePath, 'utf-8')
     - return extractVersion(content)

  2. detectBundledVersion(templateDir: string): Promise<string | null>
     - claudeSrc = join(templateDir, 'CLAUDE.md')
     - if !(existsSync(claudeSrc)) return null
     - content = await readFile(claudeSrc, 'utf-8')
     - return extractVersion(content)

  3. computeChanges(templateDir: string, destDir: string, projectType: string): Promise<Array<{path: string, status: 'changed' | 'unchanged' | 'new'}>>
     - Use listTemplateFiles(templateDir) (already imported via resolveTemplatesDir's module) to enumerate managed files, then filter to the managed prefix set (per RESEARCH.md Pattern 5). This avoids hand-rolling a directory walk.
     - MANAGED_PATHS: filter listTemplateFiles output to paths under '.claude/skills/', plus fixed paths: 'CLAUDE.md', '.github/workflows/ci.yml', '.github/workflows/security.yml', '.github/workflows/dependency-review.yml', '.github/dependabot.yml'
     - For CLAUDE.md: compare sentinel block from template vs sentinel block from dest (string equality on the extracted block)
     - For ci.yml: compare template's ci-{projectType}.yml content to dest's .github/workflows/ci.yml content
     - For each path: if dest file absent → status='new'; else readFile both, === → 'unchanged', else 'changed'
     - Return sorted list of FileChange objects

  4. formatChangeSummary(changes: Array<{path: string, status: string}>): string
     - Map status to symbol: 'changed' → '~', 'unchanged' → '=', 'new' → '+'
     - Return joined string: one line per file like "~ CLAUDE.md" or "= .claude/skills/caveman/SKILL.md"
     - If changes is empty: return "(no managed files found)"

  5. upgradeTemplates(templateDir: string, destDir: string, projectType: string): Promise<string[]>
     - CI variant selection: selectedVariant = "ci-" + projectType + ".yml"; ciVariants = ['ci-node.yml','ci-python.yml','ci-both.yml']
     - Call copy(templateDir, destDir, { overwrite: true, errorOnExist: false, filter: (src) => {
         if (src.endsWith('CLAUDE.md')) return false  // handled by mergeClaude
         if (relative(templateDir, src).includes('..')) return false  // path traversal guard per T-05-02
         if (src.includes('.claude/skills/')) return true
         for (const v of ciVariants) { if (src.endsWith(v) && v !== selectedVariant) return false }
         if (src.includes('.github/workflows/')) return true
         return false  // default: do not touch unknown files — managed set is explicit
       }
     })
     - Rename selectedVariant to ci.yml (same pattern as copyTemplates in copy-templates.ts lines 87–94)
     - Call await mergeClaude(join(destDir, 'CLAUDE.md'), await readFile(join(templateDir, 'CLAUDE.md'), 'utf-8'))
     - Return list of updated file paths (walk destDir for .claude/skills/ and .github/workflows/ only — not full walk)

  registerUpgradeCommand(program: Command) body:
    program.command('upgrade')
      .description('Update goodvibes-managed files to the latest version')
      .option('--dry-run', 'Preview what would change without writing')
      .action(async (options: { dryRun: boolean }) => {
        const dryRun = options.dryRun ?? false
        const cwd = process.cwd()
        const templateDir = resolveTemplatesDir()
        const projectType = detectProjectType(cwd)

        intro('goodvibes upgrade')

        const installedVersion = await detectInstalledVersion(cwd)
        const bundledVersion = await detectBundledVersion(templateDir)

        if (installedVersion && bundledVersion && versionGte(installedVersion, bundledVersion)) {
          outro(`Already up to date (v${installedVersion})`)
          return
        }

        const changes = await computeChanges(templateDir, cwd, projectType)
        note(formatChangeSummary(changes), 'What will change')

        if (dryRun) {
          outro('Run without --dry-run to apply these changes.')
          return
        }

        const updated: string[] = []
        await tasks([
          {
            title: 'Upgrading managed files',
            task: async () => {
              const files = await upgradeTemplates(templateDir, cwd, projectType)
              updated.push(...files)
              return `Updated ${files.length} files`
            },
          },
        ])

        note(updated.join('\n') || '(no files changed)', 'Files updated')
        outro('Upgrade complete!')
      })

  Key constraints:
    - Do NOT re-install headroom during upgrade (RESEARCH.md Anti-Patterns section)
    - Do NOT touch JOURNAL.md, CHANGELOG.md, CONTRIBUTING.md, SECURITY.md, docs/onboarding.md (RESEARCH.md Pattern 5)
    - Path traversal guard must be preserved in upgradeTemplates filter (T-05-02)
    - No try/catch wrappers — errors propagate (fail loud per CLAUDE.md)
    - No shell:true in any subprocess (none needed here)

  Update packages/npm/src/index.ts — add two lines (surgical change, touch nothing else):
    Line after "import { registerInitCommand } from './commands/init.js'":
      import { registerUpgradeCommand } from './commands/upgrade.js'
    Line after "registerInitCommand(program)":
      registerUpgradeCommand(program)
  </action>

  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - upgrade.ts exists at packages/npm/src/commands/upgrade.ts and exports registerUpgradeCommand
    - index.ts contains "registerUpgradeCommand" (both import and call)
    - `cd packages/npm && npm test` — all tests pass (GREEN), including the 5 upgrade tests
    - upgrade.ts contains 'dry-run' option (verify-phase5.sh check)
    - upgrade.ts contains '.claude/skills' string (managed path in filter)
    - upgradeTemplates filter uses `relative(templateDir, src).includes('..')` path traversal guard
    - mergeClaude is called for CLAUDE.md (not a direct write)
    - computeChanges calls listTemplateFiles() to enumerate managed files (no hand-rolled directory walk)
    - No imports from packages not in package.json
    - No fenced code blocks in action (prose task description)
  </acceptance_criteria>

  <done>upgrade.ts implemented; all npm tests GREEN including upgrade.test.ts 5 tests; index.ts wired; JOURNAL.md updated with: date, task summary, files changed, reason, tests run</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement upgrade_cmd.py (Python) and wire into main.py</name>
  <files>packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py, packages/pip/src/goodvibes_cli/main.py</files>

  <read_first>
    - packages/pip/src/goodvibes_cli/commands/init_cmd.py — full file; console.status() pattern, Panel usage, console.rule() for intro/outro, Annotated[bool, typer.Option] signature
    - packages/pip/src/goodvibes_cli/steps/copy_templates.py — resolve_templates_dir, copy_templates, list_template_files; shutil.copytree ignore_fn pattern (lines 52–77); CI variant rename pattern (lines 79–84); merge_claude call (lines 86–91)
    - packages/pip/src/goodvibes_cli/utils/sentinel_merge.py — extract_version, version_gte, merge_claude, _extract_sentinel_block; all signatures
    - packages/pip/src/goodvibes_cli/utils/detect_project_type.py — detect_project_type signature
    - packages/pip/tests/test_upgrade_cmd.py — what each test patches and asserts; exact patch paths like "goodvibes_cli.commands.upgrade_cmd.<name>"
    - packages/pip/src/goodvibes_cli/main.py — current 16 lines; two lines to add
    - .planning/phases/05-upgrade-command-template-repo/05-PATTERNS.md — Python upgrade_cmd.py pattern section (lines 118–178), Python shutil.copytree ignore pattern
  </read_first>

  <behavior>
    - test_upgrade_help_has_dry_run: app ['upgrade','--help'] exits 0, 'dry-run' in output
    - test_dry_run_shows_summary_without_writing: with mocked compute_changes=[], invoke ['upgrade','--dry-run'], exit_code==0, 'dry-run' or 'apply' in output
    - test_skips_when_already_up_to_date: _detect_installed_version='1.0.0', version_gte=True, 'up to date' in output
    - test_runs_upgrade_when_claude_md_absent: _detect_installed_version=None, upgrade_templates called once
    - test_diff_summary_printed_before_apply: compute_changes returns [('CLAUDE.md','changed')], output contains '~' or 'changed'
  </behavior>

  <action>
  Create packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py — Python port of upgrade.ts.

  Module docstring: "goodvibes upgrade command — port of upgrade.ts."

  Architectural note: upgrade_templates is defined inline in upgrade_cmd.py (single consumer — Ponytail). The RESEARCH.md Architectural Responsibility Map associates it with the copy_templates step layer conceptually, but adding a new export to copy_templates.py for a single call site would be over-engineering. Inline placement is the correct choice here.

  Imports:
    - from __future__ import annotations
    - import pathlib
    - import shutil
    - from typing import Annotated
    - import typer
    - from rich.console import Console
    - from rich.panel import Panel
    - from goodvibes_cli.steps.copy_templates import resolve_templates_dir, list_template_files
    - from goodvibes_cli.utils.detect_project_type import detect_project_type
    - from goodvibes_cli.utils.sentinel_merge import extract_version, version_gte, merge_claude, _extract_sentinel_block (if needed), SENTINEL_START, SENTINEL_END

  console = Console() at module level.

  Internal helpers (not exported):

  1. _detect_installed_version(cwd: pathlib.Path) -> str | None
     - claude_path = cwd / "CLAUDE.md"
     - if not claude_path.exists(): return None
     - return extract_version(claude_path.read_text(encoding="utf-8"))

  2. _detect_bundled_version(template_dir: pathlib.Path) -> str | None
     - claude_src = template_dir / "CLAUDE.md"
     - if not claude_src.exists(): return None
     - return extract_version(claude_src.read_text(encoding="utf-8"))

  3. compute_changes(template_dir: pathlib.Path, dest_dir: pathlib.Path, project_type: str) -> list[tuple[str, str]]
     - Returns list of (relative_path, status) where status is 'changed', 'unchanged', or 'new'
     - Use list_template_files(template_dir) to enumerate managed files, then filter to managed prefixes (.claude/skills/, plus fixed paths: 'CLAUDE.md', '.github/workflows/ci.yml', '.github/workflows/security.yml', '.github/workflows/dependency-review.yml', '.github/dependabot.yml'). This avoids hand-rolling a directory walk.
     - For CLAUDE.md: if dest absent → 'new'; else compare sentinel blocks from template vs dest (extract SENTINEL_START..SENTINEL_END substring, string equality)
     - For ci.yml: compare template's ci-{project_type}.yml content to dest's .github/workflows/ci.yml content
     - For other files: read both, == → 'unchanged', else 'changed' or 'new' if absent
     - Return sorted list by path

  4. format_change_summary(changes: list[tuple[str, str]]) -> str
     - Map: 'changed' → '~', 'unchanged' → '=', 'new' → '+'
     - Return "\n".join(f"{symbol} {path}" for symbol, path in mapped_pairs) or "(no managed files found)"

  5. upgrade_templates(template_dir: pathlib.Path, dest_dir: pathlib.Path, project_type: str) -> list[str]
     - ci_variants = {"ci-node.yml", "ci-python.yml", "ci-both.yml"}
     - selected_variant = f"ci-{project_type}.yml"
     - def ignore_fn(directory: str, contents: list[str]) -> set[str]:
         ignored: set[str] = set()
         for name in contents:
           full = pathlib.Path(directory) / name
           try: rel = full.relative_to(template_dir)
           except ValueError: ignored.add(name); continue
           if name == "CLAUDE.md": ignored.add(name)  # handled by merge_claude
           if ".." in pathlib.Path(str(rel)).parts: ignored.add(name)  # path traversal guard T-05-03
           rel_str = str(rel)
           # Skip unmanaged files
           if not any([".claude/skills" in rel_str, ".github/workflows" in rel_str]):
             if name not in {"CLAUDE.md"}:
               ignored.add(name)
           # Skip non-selected CI variants
           if name in ci_variants and name != selected_variant:
             ignored.add(name)
           # Never skip managed .claude/skills or .github/workflows files — overwrite allowed
         return ignored
     - shutil.copytree(str(template_dir), str(dest_dir), ignore=ignore_fn, dirs_exist_ok=True)
     - Rename selectedVariant to ci.yml (same pattern as copy_templates.py lines 79–84)
     - Call merge_claude(dest_dir / "CLAUDE.md", (template_dir / "CLAUDE.md").read_text(encoding="utf-8"))
     - Return sorted list of relative paths written (walk dest_dir .claude/skills/ and .github/workflows/ only)

  upgrade_cmd function signature:
    def upgrade_cmd(
        dry_run: Annotated[bool, typer.Option("--dry-run", help="Preview changes without writing")] = False,
    ) -> None:
        """Re-sync goodvibes-managed files to the latest version."""

  upgrade_cmd body — follow init_cmd.py pattern:
    template_dir = resolve_templates_dir()
    cwd = pathlib.Path.cwd()
    project_type = detect_project_type(cwd)
    console.rule("[bold]goodvibes upgrade[/bold]")

    installed_version = _detect_installed_version(cwd)
    bundled_version = _detect_bundled_version(template_dir)

    if installed_version and bundled_version and version_gte(installed_version, bundled_version):
        console.rule(f"[green]Already up to date (v{installed_version})[/green]")
        return

    changes = compute_changes(template_dir, cwd, project_type)
    console.print(Panel(format_change_summary(changes), title="What will change"))

    if dry_run:
        console.rule("Run without --dry-run to apply these changes.")
        return

    updated: list[str] = []
    with console.status("Upgrading managed files"):
        updated = upgrade_templates(template_dir, cwd, project_type)

    file_list = "\n".join(updated) if updated else "(no files changed)"
    console.print(Panel(file_list, title="Files updated"))
    console.rule("[green]Upgrade complete![/green]")

  Constraints:
    - Do NOT re-install headroom (RESEARCH.md Anti-Patterns)
    - Do NOT touch JOURNAL.md, CHANGELOG.md, CONTRIBUTING.md, SECURITY.md
    - ".." path traversal guard in ignore_fn (T-05-03)
    - No shell=True
    - No empty except blocks
    - Import only from goodvibes_cli — no new packages

  Update packages/pip/src/goodvibes_cli/main.py — two lines only (surgical):
    After "from goodvibes_cli.commands.init_cmd import init_cmd":
      from goodvibes_cli.commands.upgrade_cmd import upgrade_cmd
    After "app.command('init')(init_cmd)":
      app.command("upgrade")(upgrade_cmd)
  </action>

  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run --extra dev pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - upgrade_cmd.py exists at packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py and defines upgrade_cmd
    - main.py contains both "from goodvibes_cli.commands.upgrade_cmd import upgrade_cmd" and "app.command('upgrade')(upgrade_cmd)"
    - `cd packages/pip && uv run --extra dev pytest tests/ -x -q` — all tests pass (GREEN), including 5 test_upgrade_cmd.py tests
    - upgrade_cmd.py contains '--dry-run' in the typer.Option call
    - upgrade_cmd.py does NOT import shutil for headroom or call install_headroom
    - Path traversal guard ".." in ignore_fn present in upgrade_templates
    - merge_claude called for CLAUDE.md (not shutil.copy directly)
    - compute_changes calls list_template_files() to enumerate files (no hand-rolled directory walk)
    - Running `bash scripts/verify-phase5.sh --quick` exits 0 (all file-existence and content checks pass)
  </acceptance_criteria>

  <done>upgrade_cmd.py implemented; all pip tests GREEN including test_upgrade_cmd.py; main.py wired; verify-phase5.sh --quick exits 0; JOURNAL.md updated with: date, task summary, files changed, reason, tests run</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → command action | User-supplied --dry-run is boolean only (Commander/Typer enforce types) |
| templateDir → destDir file writes | All writes via join(destDir, rel) where rel comes from bundled template tree only |
| destDir | Always process.cwd() / pathlib.Path.cwd() — no user-supplied path |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02 | Tampering | upgradeTemplates filter in upgrade.ts | mitigate | `relative(templateDir, src).includes('..')` guard in copy() filter; bundled templates are repo-controlled but belt-and-suspenders retained per copy-templates.ts precedent |
| T-05-03 | Tampering | upgrade_templates ignore_fn in upgrade_cmd.py | mitigate | `".." in pathlib.Path(str(rel)).parts` guard in ignore_fn; mirrors copy_templates.py T-03-02-01 |
| T-05-04 | Tampering | Managed file scope (upgrade only touches declared paths) | mitigate | Explicit allowlist in filter/ignore_fn: `.claude/skills/` and `.github/workflows/` only; default=skip unknown files |
| T-05-05 | Information Disclosure | CLAUDE.md user content | mitigate | mergeClaude/merge_claude sentinel-aware merge preserves before/after user content; never full-file overwrite |
| T-05-SC | Tampering | npm/pip installs | accept | No new package installs in this plan; all packages pre-audited in Phases 2 and 3 |
</threat_model>

<verification>
After both tasks complete:
- `cd packages/npm && npm test` — all suites GREEN
- `cd packages/pip && uv run --extra dev pytest tests/ -x -q` — all suites GREEN
- `bash scripts/verify-phase5.sh --quick` — exits 0, "Phase 5 gate: PASS"
- `goodvibes upgrade --help` shows '--dry-run' option in help text
</verification>

<success_criteria>
- All npm tests pass (including 5 upgrade.test.ts tests)
- All pip tests pass (including 5 test_upgrade_cmd.py tests)
- verify-phase5.sh --quick exits 0
- upgrade.ts exports registerUpgradeCommand and is wired into index.ts
- upgrade_cmd.py defines upgrade_cmd and is wired into main.py via app.command("upgrade")
- CLAUDE.md update path always goes through mergeClaude/merge_claude (never direct write)
- Path traversal guard present in both TS and Python upgrade file copy helpers
</success_criteria>

<output>
Create `.planning/phases/05-upgrade-command-template-repo/05-02-SUMMARY.md` when done
</output>
