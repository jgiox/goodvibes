---
phase: "05"
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/npm/src/commands/upgrade.test.ts
  - packages/pip/tests/test_upgrade_cmd.py
  - scripts/verify-phase5.sh
autonomous: true
requirements:
  - UPG-01
  - UPG-02
  - UPG-03
  - UPG-04

must_haves:
  truths:
    - "Running `cd packages/npm && npm test` shows upgrade.test.ts RED (failing — implementation absent)"
    - "Running `cd packages/pip && uv run --extra dev pytest tests/test_upgrade_cmd.py -x -q` shows test_upgrade_cmd.py RED"
    - "Running `scripts/verify-phase5.sh --quick` exits non-zero (upgrade files absent)"
    - "Each RED test name is a full English sentence describing expected behavior"
  artifacts:
    - path: "packages/npm/src/commands/upgrade.test.ts"
      provides: "vitest RED tests for upgrade command (TS)"
      min_lines: 60
    - path: "packages/pip/tests/test_upgrade_cmd.py"
      provides: "pytest RED tests for upgrade command (Python)"
      min_lines: 50
    - path: "scripts/verify-phase5.sh"
      provides: "Phase 5 smoke harness"
      contains: "Phase 5 gate"
  key_links:
    - from: "packages/npm/src/commands/upgrade.test.ts"
      to: "packages/npm/src/commands/upgrade.js"
      via: "dynamic import after vi.mock setup"
      pattern: "import\\('./upgrade\\.js'\\)"
    - from: "packages/pip/tests/test_upgrade_cmd.py"
      to: "goodvibes_cli.main:app"
      via: "CliRunner.invoke(app, ['upgrade', ...])"
      pattern: "runner\\.invoke\\(app"
---

<objective>
Wave 0: Write RED (failing) unit tests for the upgrade command in both TypeScript and Python, plus the verify-phase5.sh smoke harness. No implementation is written here — tests must fail because upgrade.ts and upgrade_cmd.py do not yet exist.

Purpose: Establish the test contract before implementation, per TDD discipline and CLAUDE.md conventions. Wave 1 turns these RED tests GREEN.

Output: upgrade.test.ts, test_upgrade_cmd.py, verify-phase5.sh — all committed, tests failing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-upgrade-command-template-repo/05-RESEARCH.md
@.planning/phases/05-upgrade-command-template-repo/05-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write RED unit tests for upgrade command (TypeScript)</name>
  <files>packages/npm/src/commands/upgrade.test.ts</files>

  <read_first>
    - packages/npm/src/commands/init.test.ts — exact mock setup pattern, beforeEach reset, dynamic import pattern, vi.mocked().mockImplementation for tasks()
    - packages/npm/src/utils/sentinel-merge.ts — exports: extractVersion, versionGte, mergeClaude
    - packages/npm/src/steps/copy-templates.ts — exports: resolveTemplatesDir, listTemplateFiles, copyTemplates
    - packages/npm/src/utils/detect-project-type.ts — exports: detectProjectType
    - packages/npm/package.json — vitest version and test command
  </read_first>

  <behavior>
    - Test: "--dry-run: prints change summary without writing files" — mock resolveTemplatesDir, mock computeChanges (or the modules it uses), invoke with ['node','goodvibes','upgrade','--dry-run'], assert outro called with text containing 'dry-run' or 'apply', assert upgradeTemplates NOT called
    - Test: "skips upgrade when already up to date" — mock pathExists=true, extractVersion returns '1.0.0' for both installed and bundled, versionGte returns true, assert outro called with text containing 'up to date'
    - Test: "runs full upgrade when CLAUDE.md is absent" — mock pathExists=false (no installed CLAUDE.md), assert upgradeTemplates IS called (null version treated as 0.0.0)
    - Test: "prints diff summary before applying changes" — mock tasks() to execute task functions, assert note() is called with content containing at least one of '~', '=', '+', or "change"
    - Test: "upgrade --help exits zero and contains dry-run" — invoke with ['node','goodvibes','upgrade','--help'], assert process exits without error and output includes 'dry-run'
  </behavior>

  <action>
  Create packages/npm/src/commands/upgrade.test.ts using the exact pattern from init.test.ts.

  Mock setup at file top (before describe block):
    - vi.mock('@clack/prompts', ...) — mock intro, outro, note, tasks (same as init.test.ts)
    - vi.mock('../steps/copy-templates.js', ...) — mock resolveTemplatesDir, listTemplateFiles (NOT copyTemplates — upgrade uses upgradeTemplates, a different export or inline function; mock the module boundary at sentinel-merge and copy-templates level)
    - vi.mock('../utils/sentinel-merge.js', ...) — mock extractVersion, versionGte, mergeClaude
    - vi.mock('fs-extra', ...) — mock pathExists
    - vi.mock('../utils/detect-project-type.js', ...) — mock detectProjectType

  In beforeEach: vi.resetAllMocks()

  Each test uses dynamic imports AFTER mock setup (copy the pattern from init.test.ts lines 34–43 exactly).

  For tests that invoke the upgrade command: instantiate new Command(), call program.exitOverride(), call registerUpgradeCommand(program) (dynamic import from './upgrade.js'), then await program.parseAsync(['node','goodvibes','upgrade',...]).

  Test: "skips upgrade when already up to date"
    - pathExists resolves true
    - extractVersion returns '1.0.0' (same call for both installed and bundled reads)
    - versionGte returns true
    - assert vi.mocked(outro) called with string containing 'up to date'

  Test: "--dry-run: prints change summary without writing files"
    - pathExists resolves false (no installed CLAUDE.md — null version triggers upgrade path)
    - versionGte returns false (older)
    - tasks mocked as vi.fn() (NOT executed, ensuring no writes)
    - assert outro called with string containing '--dry-run' or 'apply'
    - assert vi.mocked(tasks) NOT called

  Test: "runs full upgrade when CLAUDE.md is absent"
    - pathExists resolves false
    - versionGte returns false
    - tasks mocked via mockImplementation to execute task functions
    - assert vi.mocked(tasks) was called (upgrade proceeds when no installed version)

  Test: "prints diff summary before applying changes"
    - pathExists resolves true, versionGte returns false (needs upgrade)
    - tasks mocked to execute task functions
    - assert vi.mocked(note) called at least once (diff summary printed)

  Test: "upgrade --help exits zero and contains dry-run"
    - no mocks needed beyond baseline
    - try { await program.parseAsync(['node','goodvibes','upgrade','--help']) } catch {}
    - import { outro } only to confirm no crash path

  CRITICAL: upgrade.ts does not exist yet — all tests must import from './upgrade.js' inside try/catch or expect import to fail, OR simply write tests that will fail when the test runner tries to resolve the module. Use the same lazy-import-inside-test pattern from init.test.ts: the dynamic import is inside each test body, so the test will error when upgrade.js cannot be resolved, producing a RED result.

  Do NOT add fenced code blocks. Do NOT add try/catch around the dynamic imports — let them fail loud (RED = fail, not error catch).
  </action>

  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | grep -E "(upgrade|FAIL|fail|Cannot find)" | head -20</automated>
  </verify>

  <acceptance_criteria>
    - upgrade.test.ts file exists at packages/npm/src/commands/upgrade.test.ts
    - Running `cd packages/npm && npm test` shows failures referencing upgrade.test.ts (cannot resolve './upgrade.js' or assertion failures)
    - Test names are full English sentences (no test_1, no happy_path)
    - All 5 test cases present: skips-up-to-date, dry-run-no-write, absent-CLAUDE-runs-upgrade, diff-summary-printed, help-exits-zero
    - No fenced code blocks inside action (prose only in .md)
    - vi.resetAllMocks() in beforeEach
  </acceptance_criteria>

  <done>upgrade.test.ts exists with 5 RED failing tests; `npm test` output shows failures for upgrade.test.ts</done>
</task>

<task type="tdd">
  <name>Task 2: Write RED unit tests for upgrade command (Python) and verify-phase5.sh harness</name>
  <files>packages/pip/tests/test_upgrade_cmd.py, scripts/verify-phase5.sh</files>

  <read_first>
    - packages/pip/tests/test_main.py — exact runner setup, mocker.patch pattern, assertion style
    - packages/pip/tests/conftest.py — TEMPLATE_CONTENT, SENTINEL_START, SENTINEL_END, fixture definitions (tmp_dir, template_dir, sample_template_content)
    - packages/pip/src/goodvibes_cli/main.py — app object, how init_cmd is registered
    - packages/pip/src/goodvibes_cli/utils/sentinel_merge.py — extract_version, version_gte signatures
    - packages/pip/src/goodvibes_cli/steps/copy_templates.py — resolve_templates_dir, copy_templates signatures
    - scripts/verify-phase4.sh — full shell structure to copy verbatim
  </read_first>

  <behavior>
    - test_dry_run_shows_summary_without_writing: invoke app with ['upgrade','--dry-run'], assert exit_code==0, assert output contains text indicating dry-run (e.g. 'dry-run' or '--dry-run' or 'apply')
    - test_skips_when_already_up_to_date: patch _detect_installed_version to return '1.0.0', patch _detect_bundled_version to return '1.0.0', patch version_gte to return True, invoke app with ['upgrade'], assert 'up to date' in output
    - test_runs_upgrade_when_claude_md_absent: patch resolve_templates_dir, patch _detect_installed_version to return None, patch upgrade_templates to return [], invoke app with ['upgrade'], assert upgrade_templates called once
    - test_diff_summary_printed_before_apply: patch resolve_templates_dir, patch compute_changes to return [('CLAUDE.md','changed')], patch upgrade_templates to return [], invoke app with ['upgrade'], assert output contains '~' or 'change' or 'what will change' (case-insensitive)
    - test_upgrade_help_has_dry_run: invoke app with ['upgrade','--help'], assert exit_code==0, assert 'dry-run' in output
  </behavior>

  <action>
  Create packages/pip/tests/test_upgrade_cmd.py following the pattern of test_main.py.

  File structure:
    - Module docstring: "Upgrade command tests — mirrors test_main.py pattern."
    - Imports: from unittest.mock import patch; import pytest; from typer.testing import CliRunner; from goodvibes_cli.main import app
    - runner = CliRunner() at module level

  Each test that calls mocker.patch uses the path "goodvibes_cli.commands.upgrade_cmd.<function_name>" — the module that does not yet exist, so imports will fail and tests will error RED.

  test_upgrade_help_has_dry_run: invoke app with ['upgrade','--help']; assert exit_code==0 and 'dry-run' in result.output. This will fail because 'upgrade' subcommand is not registered yet.

  test_dry_run_shows_summary_without_writing(mocker):
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.resolve_templates_dir", return_value=None)
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.compute_changes", return_value=[])
    result = runner.invoke(app, ["upgrade", "--dry-run"])
    assert result.exit_code == 0
    assert "dry-run" in result.output.lower() or "apply" in result.output.lower()

  test_skips_when_already_up_to_date(mocker):
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.resolve_templates_dir", return_value=None)
    mocker.patch("goodvibes_cli.commands.upgrade_cmd._detect_installed_version", return_value="1.0.0")
    mocker.patch("goodvibes_cli.commands.upgrade_cmd._detect_bundled_version", return_value="1.0.0")
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.version_gte", return_value=True)
    result = runner.invoke(app, ["upgrade"])
    assert result.exit_code == 0
    assert "up to date" in result.output.lower()

  test_runs_upgrade_when_claude_md_absent(mocker):
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.resolve_templates_dir", return_value=None)
    mock_upgrade = mocker.patch("goodvibes_cli.commands.upgrade_cmd.upgrade_templates", return_value=[])
    mocker.patch("goodvibes_cli.commands.upgrade_cmd._detect_installed_version", return_value=None)
    mocker.patch("goodvibes_cli.commands.upgrade_cmd._detect_bundled_version", return_value="1.0.0")
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.version_gte", return_value=False)
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.compute_changes", return_value=[])
    result = runner.invoke(app, ["upgrade"])
    assert mock_upgrade.call_count == 1

  test_diff_summary_printed_before_apply(mocker):
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.resolve_templates_dir", return_value=None)
    mocker.patch("goodvibes_cli.commands.upgrade_cmd._detect_installed_version", return_value=None)
    mocker.patch("goodvibes_cli.commands.upgrade_cmd._detect_bundled_version", return_value="1.0.0")
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.version_gte", return_value=False)
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.compute_changes", return_value=[("CLAUDE.md", "changed")])
    mocker.patch("goodvibes_cli.commands.upgrade_cmd.upgrade_templates", return_value=[])
    result = runner.invoke(app, ["upgrade"])
    assert result.exit_code == 0
    # Diff summary printed — at least one change marker or "change" keyword in output
    assert any(marker in result.output for marker in ["~", "changed", "change", "will change"])

  All tests will be RED because goodvibes_cli.commands.upgrade_cmd does not exist and 'upgrade' is not registered in main.py.

  ---

  Create scripts/verify-phase5.sh by copying the structure of scripts/verify-phase4.sh verbatim, replacing Phase 4 content checks with Phase 5 checks:

  File existence checks:
    check "UPGRADE-TS"        "test -f packages/npm/src/commands/upgrade.ts"
    check "UPGRADE-TEST-TS"   "test -f packages/npm/src/commands/upgrade.test.ts"
    check "UPGRADE-PY"        "test -f packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py"
    check "UPGRADE-TEST-PY"   "test -f packages/pip/tests/test_upgrade_cmd.py"
    check "VERIFY-PHASE5"     "test -f scripts/verify-phase5.sh"

  Content correctness checks:
    check "UPGRADE-IN-INDEX"   "grep -v '^#' packages/npm/src/index.ts | grep -q 'registerUpgradeCommand'"
    check "UPGRADE-IN-MAIN"    "grep -v '^#' packages/pip/src/goodvibes_cli/main.py | grep -q 'upgrade_cmd'"
    check "UPGRADE-DRY-RUN-TS" "grep -q 'dry-run' packages/npm/src/commands/upgrade.ts"
    check "UPGRADE-DRY-RUN-PY" "grep -q 'dry.run' packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py"
    check "UPGRADE-MANAGED-TS" "grep -q '\\.claude/skills' packages/npm/src/commands/upgrade.ts"

  Unit test checks (only without --quick): same NPM-TESTS and PIP-TESTS checks as verify-phase4.sh.

  Summary block: "Phase 5 gate: PASS" / "Phase 5 gate: FAIL".

  Make verify-phase5.sh executable (chmod +x) via the shell after writing.
  </action>

  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run --extra dev pytest tests/test_upgrade_cmd.py -x -q 2>&1 | head -20 && bash /home/ygiokas/GoodVibes/scripts/verify-phase5.sh --quick 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - test_upgrade_cmd.py exists at packages/pip/tests/test_upgrade_cmd.py with 5 tests
    - Running `uv run --extra dev pytest tests/test_upgrade_cmd.py -x -q` shows errors/failures (RED — upgrade_cmd module missing)
    - verify-phase5.sh exists at scripts/verify-phase5.sh and is executable
    - Running `bash scripts/verify-phase5.sh --quick` exits non-zero (FAIL — upgrade.ts absent)
    - verify-phase5.sh output contains "Phase 5 gate: FAIL"
    - All test names are snake_case sentences beginning with "test_"
    - grep gates in verify-phase5.sh use `grep -v '^#' | grep -q` pattern (no bare grep on commented files)
  </acceptance_criteria>

  <done>test_upgrade_cmd.py and verify-phase5.sh both exist; pytest shows RED; verify-phase5.sh --quick exits 1</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → production module | Test imports resolve to real module paths; mocks isolate external I/O |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | verify-phase5.sh grep gates | mitigate | Use `grep -v '^#' \| grep -q` to exclude comment lines; bare `grep -q` on files with inline comments would self-invalidate |
| T-05-SC | Tampering | npm/pip installs | accept | No new installs in this plan; all packages pre-audited in Phases 2 and 3 |
</threat_model>

<verification>
Both test files exist and are RED:
- `cd packages/npm && npm test` — shows failures in upgrade.test.ts
- `cd packages/pip && uv run --extra dev pytest tests/test_upgrade_cmd.py -x -q` — shows failures
- `bash scripts/verify-phase5.sh --quick` — exits 1, output shows "Phase 5 gate: FAIL"
</verification>

<success_criteria>
- packages/npm/src/commands/upgrade.test.ts: 5 tests, all RED (import fails or assertion fails)
- packages/pip/tests/test_upgrade_cmd.py: 5 tests, all RED (module missing)
- scripts/verify-phase5.sh: executable, exits 1 on --quick run, uses correct grep-gate pattern
- No implementation code written in this plan
</success_criteria>

<output>
Create `.planning/phases/05-upgrade-command-template-repo/05-01-SUMMARY.md` when done
</output>
