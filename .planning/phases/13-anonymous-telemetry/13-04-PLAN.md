---
phase: 13-anonymous-telemetry
plan: 04
type: execute
wave: 2
depends_on: ["13-02"]
files_modified:
  - packages/npm/src/commands/init.ts
  - packages/npm/src/commands/init.test.ts
autonomous: true
requirements: [TEL-04, TEL-05]

must_haves:
  truths:
    - "Running goodvibes init shows disclosure note immediately after intro() and before any file operations"
    - "sendTelemetry() Promise is started after the dry-run early return — never called during --dry-run"
    - "Promise.race([telemetryPromise, sleep(1_000)]) is awaited after tasks() resolves — adds at most 1s extra wait"
    - "All existing init.test.ts tests still pass after modifications"
    - "New test confirms note() called with D-08 disclosure text"
    - "New test confirms sendTelemetry NOT called when --dry-run flag is set"
  artifacts:
    - path: "packages/npm/src/commands/init.ts"
      provides: "Disclosure note + fire-and-forget telemetry wired into init command"
      contains: "sendTelemetry"
    - path: "packages/npm/src/commands/init.test.ts"
      provides: "TEL-04 + dry-run guard tests"
      contains: "DO_NOT_TRACK"
  key_links:
    - from: "packages/npm/src/commands/init.ts"
      to: "packages/npm/src/steps/telemetry.ts"
      via: "import { sendTelemetry } from '../steps/telemetry.js'"
      pattern: "sendTelemetry"
    - from: "packages/npm/src/commands/init.ts"
      to: "Promise.race"
      via: "await Promise.race([telemetryPromise, sleep(1_000)]) after tasks()"
      pattern: "Promise\\.race"
---

<objective>
Wire telemetry disclosure and fire-and-forget timing into the npm init command.

Purpose: Makes TEL-04 (disclosure before file ops) and TEL-05 (non-blocking timing) observable from the CLI. The disclosure note appears for every run including --dry-run (informational only); sendTelemetry() is started only in the non-dry-run path. Promise.race caps the additional wait at 1 second after all tasks complete.

Output: Modified packages/npm/src/commands/init.ts (disclosure + telemetry wiring) and packages/npm/src/commands/init.test.ts (two new test cases + telemetry mock added to all existing tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-anonymous-telemetry/13-CONTEXT.md
@.planning/phases/13-anonymous-telemetry/13-RESEARCH.md
@.planning/phases/13-anonymous-telemetry/13-PATTERNS.md
@.planning/phases/13-anonymous-telemetry/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire telemetry disclosure and Promise.race into packages/npm/src/commands/init.ts</name>
  <files>packages/npm/src/commands/init.ts</files>
  <read_first>
    - packages/npm/src/commands/init.ts — full file (must read current state before any edit — intro() location line 50, dry-run guard lines 57-75, tasks() call lines 126-135, imports lines 1-7)
    - packages/npm/src/steps/telemetry.ts — confirm sendTelemetry() export exists (from Plan 13-02)
    - .planning/phases/13-anonymous-telemetry/13-CONTEXT.md — D-08 (exact disclosure text), D-09 (note() placement immediately after intro()), D-10 (Promise.race after tasks())
    - .planning/phases/13-anonymous-telemetry/13-PATTERNS.md — init.ts section (import insertion point, existing note() + guard pattern, dry-run guard pattern, tasks() + Promise.race placement)
    - .planning/phases/13-anonymous-telemetry/13-RESEARCH.md — Pitfall 6 (dry-run guard — disclosure note in both paths, sendTelemetry() in non-dry-run only), Pitfall 2 (Promise.race ensures Node stays alive)
  </read_first>
  <action>
    Make four surgical edits to packages/npm/src/commands/init.ts. Do not reformat, rename, or change anything outside these four insertion points.

    Edit 1 — Add import: After the existing import for configure-mcp.js (line 6), add: import { sendTelemetry } from '../steps/telemetry.js'

    Edit 2 — Add disclosure note: Immediately after the intro('goodvibes init') call (currently line 50), add: note('Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.', 'Privacy') — exact text from D-08; title is 'Privacy'. This note appears in BOTH dry-run and non-dry-run paths because it appears before the if (dryRun) block. The note is informational — no network call is made here.

    Edit 3 — Start telemetry Promise: After the closing brace of the if (dryRun) block (currently around line 74, after the return), and before const createdFiles: string[] = [], add: const telemetryPromise = sendTelemetry() — this starts the non-blocking Promise immediately in the non-dry-run path. Placement must be AFTER the dry-run return, NEVER before it.

    Edit 4 — Await Promise.race: After the try/catch block that calls await tasks(taskList) completes successfully (after process.exit(1) in the catch, meaning execution only continues on success), add: await Promise.race([telemetryPromise, sleep(1_000)]) — this is before the note(createdFiles...) call. The sleep() helper must be added as a private module function at the bottom of init.ts: function sleep(ms: number): Promise<void> { return new Promise(resolve => setTimeout(resolve, ms)) } — add the comment // ponytail: inline helper — keeps Promise.race readable above it.

    Verify the insertion order: (1) intro() → (2) note(disclosure, 'Privacy') → (3) existing non-empty check → (4) if (dryRun) return → (5) const telemetryPromise = sendTelemetry() → (6) taskList setup → (7) try { await tasks() } catch → (8) await Promise.race([telemetryPromise, sleep(1_000)]) → (9) note(createdFiles...) → (10) outro().
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - packages/npm/src/commands/init.ts contains: import { sendTelemetry } from '../steps/telemetry.js'
    - File contains: note('Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.', 'Privacy') — exact text from D-08
    - The disclosure note() call appears BEFORE the if (dryRun) block in the source (grep line numbers confirm ordering)
    - File contains: const telemetryPromise = sendTelemetry() — this line appears AFTER the closing brace of the if (dryRun) block
    - File contains: await Promise.race([telemetryPromise, sleep(1_000)]) — appears after the try/catch for tasks()
    - File contains: function sleep(ms: number): Promise&lt;void&gt; at the bottom of the file
    - npm run build exits 0 (TypeScript compiles without errors)
  </acceptance_criteria>
  <done>init.ts modified with 4 surgical edits: sendTelemetry import, disclosure note(), telemetryPromise start, Promise.race + sleep helper; TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add telemetry mock and TEL-04 tests to packages/npm/src/commands/init.test.ts</name>
  <files>packages/npm/src/commands/init.test.ts</files>
  <read_first>
    - packages/npm/src/commands/init.test.ts — full file (must read current state — existing vi.mock blocks lines 16-39, describe structure line 41, beforeEach line 43, existing tests)
    - packages/npm/src/commands/init.ts — the modified init.ts (from Task 1) to know what the mock must cover
    - .planning/phases/13-anonymous-telemetry/13-PATTERNS.md — init.test.ts section (vi.mock block for telemetry.js, dynamic import pattern, note() assertion pattern, dry-run sendTelemetry not-called assertion)
    - .planning/phases/13-anonymous-telemetry/13-CONTEXT.md — D-08 (exact disclosure text for assertion), D-11 (unit tests mock HTTP at module boundary)
  </read_first>
  <action>
    Make two edits to packages/npm/src/commands/init.test.ts.

    Edit 1 — Add telemetry module mock at top of file: After the existing vi.mock('../steps/configure-mcp.js', ...) block, add: vi.mock('../steps/telemetry.js', () => ({ sendTelemetry: vi.fn().mockResolvedValue(undefined) })) — this mock prevents any real fetch calls from firing during ALL existing and new tests in this file, and makes sendTelemetry a spy that can be asserted.

    Edit 2 — Add two new test cases inside the existing describe('init command') block:

    New test 1 (TEL-04): it('shows disclosure note with Privacy title before file operations', async () => { ... }) — Mock all dependencies as done in existing tests (resolveTemplatesDir, copyTemplates, tasks, installHeadroom, configureMcp). Also import sendTelemetry from the mocked telemetry module. Call program.parseAsync(['node', 'goodvibes', 'init']). Assert: vi.mocked(note) was called with 'Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.' as the first argument and 'Privacy' as the second argument. Use expect(vi.mocked(note)).toHaveBeenCalledWith('Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.', 'Privacy').

    New test 2 (dry-run guard): it('does not call sendTelemetry during --dry-run', async () => { ... }) — Mock listTemplateFiles, resolveTemplatesDir, tasks. Call program.parseAsync with '--dry-run'. Import sendTelemetry from the mocked module. Assert: expect(vi.mocked(sendTelemetry)).not.toHaveBeenCalled(). This verifies the dry-run guard from Pitfall 6 in RESEARCH.md.

    For both new tests, follow the existing test structure: dynamic imports inside the test function, vi.mocked() for assertions, program.exitOverride() already in place from the Command setup.

    Do not modify existing test cases — only add the telemetry mock and two new tests.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/npm/src/commands/init.test.ts contains: vi.mock('../steps/telemetry.js', () => ({ sendTelemetry: vi.fn().mockResolvedValue(undefined) }))
    - File contains a test named 'shows disclosure note with Privacy title before file operations' (or equivalent sentence)
    - File contains a test asserting sendTelemetry was NOT called during --dry-run
    - cd packages/npm && npm test exits 0
    - All previously-passing tests still pass (no regressions from adding the telemetry mock)
    - The TEL-04 test asserts note() was called with exact text 'Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.' and title 'Privacy'
  </acceptance_criteria>
  <done>init.test.ts updated: telemetry.js mock added (prevents real HTTP in all tests), two new tests added (TEL-04 disclosure assertion + dry-run guard); npm test exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init.ts action() → sendTelemetry() | Control flow; dry-run guard must prevent telemetry call in simulation mode |
| tasks() completion → Promise.race | Timing boundary; race must not block user if telemetry is slow |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04-DRY | Spoofing | const telemetryPromise = sendTelemetry() placement | mitigate | telemetryPromise assigned after the if (dryRun) { ... return } block — dry-run path exits before sendTelemetry is ever called; test in init.test.ts asserts this |
| T-13-04-RACE | Denial of Service | Promise.race missing or wrong timeout | mitigate | sleep(1_000) in the race ensures at most 1 extra second regardless of endpoint availability; AbortController in sendTelemetry() caps HTTP at 5s (internal fallback) |
| T-13-04-NOTE | Information Disclosure | Disclosure note content | accept | Disclosure text is informational only ('Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.'); contains no PII; meets TEL-04 transparency requirement |
</threat_model>

<verification>
After both tasks complete:
- cd packages/npm && npm test exits 0 (full suite including new TEL-04 tests)
- grep 'telemetryPromise' packages/npm/src/commands/init.ts confirms placement after dry-run block
- grep -n 'note.*Privacy\|note.*Anonymous' packages/npm/src/commands/init.ts shows the note() call
- grep 'Promise.race' packages/npm/src/commands/init.ts confirms await race after tasks()
</verification>

<success_criteria>
init.ts has disclosure note (D-08 text + 'Privacy' title) immediately after intro(); telemetryPromise starts after dry-run guard; Promise.race caps wait at 1s. init.test.ts has telemetry mock blocking real HTTP; TEL-04 and dry-run guard tests pass; full npm test suite green.
</success_criteria>

<output>
Create `.planning/phases/13-anonymous-telemetry/13-04-SUMMARY.md` when done
</output>
