---
phase: 13-anonymous-telemetry
plan: 05
type: execute
wave: 2
depends_on: ["13-03"]
files_modified:
  - packages/pip/src/goodvibes_cli/commands/init_cmd.py
  - packages/pip/tests/test_init_cmd.py
autonomous: true
requirements: [TEL-04, TEL-05]

must_haves:
  truths:
    - "Running goodvibes init shows Privacy panel with disclosure text immediately after console.rule() when telemetry is active (not opted out)"
    - "When DO_NOT_TRACK=1, GOODVIBES_NO_TELEMETRY=1, or CI=true is set, no Privacy panel is shown and start_telemetry_thread() is never called"
    - "start_telemetry_thread() is called only in the non-dry-run path — after the if dry_run: return block"
    - "tel_thread.join(timeout=1.0) is called after all task blocks complete — adds at most 1s extra wait"
    - "All existing test_init_cmd.py tests still pass after modifications"
    - "New test confirms Privacy panel is printed with D-08 disclosure text in non-opted-out path"
    - "New test confirms start_telemetry_thread NOT called when --dry-run flag is set"
    - "New test confirms Privacy panel NOT shown when DO_NOT_TRACK=1 is set"
  artifacts:
    - path: "packages/pip/src/goodvibes_cli/commands/init_cmd.py"
      provides: "Opt-out-gated disclosure panel + fire-and-forget thread wired into Python init command"
      contains: "start_telemetry_thread"
    - path: "packages/pip/tests/test_init_cmd.py"
      provides: "TEL-04 + dry-run guard + opt-out display suppression tests + telemetry mock in all existing tests"
      contains: "start_telemetry_thread"
  key_links:
    - from: "packages/pip/src/goodvibes_cli/commands/init_cmd.py"
      to: "packages/pip/src/goodvibes_cli/steps/telemetry.py"
      via: "from goodvibes_cli.steps.telemetry import start_telemetry_thread"
      pattern: "start_telemetry_thread"
    - from: "packages/pip/src/goodvibes_cli/commands/init_cmd.py"
      to: "tel_thread.join"
      via: "if tel_thread is not None: tel_thread.join(timeout=1.0) after task blocks"
      pattern: "tel_thread\\.join"
---

<objective>
Wire telemetry disclosure and fire-and-forget timing into the pip init command.

Purpose: Python-parity counterpart to Plan 13-04. Disclosure Panel appears only when telemetry is active (not opted out via DO_NOT_TRACK=1, GOODVIBES_NO_TELEMETRY=1, or CI=true); suppressed on opt-out per ROADMAP Phase 13 Success Criterion 2. start_telemetry_thread() is called only in the non-dry-run path; thread.join(timeout=1.0) caps additional wait at 1 second.

Output: Modified packages/pip/src/goodvibes_cli/commands/init_cmd.py (opt-out-gated disclosure + thread wiring) and packages/pip/tests/test_init_cmd.py (telemetry mock added to all tests + three new test cases).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-anonymous-telemetry/13-CONTEXT.md
@.planning/phases/13-anonymous-telemetry/13-RESEARCH.md
@.planning/phases/13-anonymous-telemetry/13-PATTERNS.md
@.planning/phases/13-anonymous-telemetry/13-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire opt-out-gated telemetry disclosure and thread join into packages/pip/src/goodvibes_cli/commands/init_cmd.py</name>
  <files>packages/pip/src/goodvibes_cli/commands/init_cmd.py</files>
  <read_first>
    - packages/pip/src/goodvibes_cli/commands/init_cmd.py — full file (must read current state — imports at top, console.rule() line 55, dry_run guard lines 57-73, try block line 83, except lines 108-115, post-tasks notes lines 117-130)
    - packages/pip/src/goodvibes_cli/steps/telemetry.py — confirm start_telemetry_thread() export exists (from Plan 13-03)
    - .planning/phases/13-anonymous-telemetry/13-CONTEXT.md — D-08 (exact disclosure text), D-09 (Panel immediately after console.rule()), D-10 (daemon thread + join(1.0))
    - .planning/phases/13-anonymous-telemetry/13-PATTERNS.md — init_cmd.py section (import insertion point, existing Panel pattern lines 76-78, dry-run guard, console.status() bracketing with thread start/join)
    - .planning/phases/13-anonymous-telemetry/13-RESEARCH.md — Pitfall 5 (daemon thread safe on early exit), Pitfall 6 (dry-run guard), Pattern 2 (Python integration example), ROADMAP Phase 13 SC2 (no disclosure on opt-out)
  </read_first>
  <action>
    Make four surgical edits to packages/pip/src/goodvibes_cli/commands/init_cmd.py. Do not reformat or change anything outside these insertion points.

    Edit 1 — Add import: After the existing import for install_headroom (currently line 11), add: from goodvibes_cli.steps.telemetry import start_telemetry_thread. Also confirm import os is present at the top of the file (add import os after the stdlib imports if it is not already there — it is needed for the opt-out check in Edit 2).

    Edit 2 — Add opt-out-gated disclosure Panel: Immediately after the console.rule("[bold]goodvibes init[/bold]") call (currently line 55), add the following two-statement block. First, compute the opt-out flag: _opted_out = (os.environ.get('DO_NOT_TRACK') == '1' or os.environ.get('GOODVIBES_NO_TELEMETRY') == '1' or os.environ.get('CI') == 'true'). Second, conditionally print the panel: if not _opted_out: console.print(Panel('Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.', title='Privacy')). The exact disclosure text from D-08 is the first argument; title='Privacy'. This block appears before the if dry_run: block. When any opt-out env var is set, the Panel is suppressed — per ROADMAP Phase 13 Success Criterion 2 ('shows no disclosure line' on opt-out).

    Edit 3 — Start telemetry thread: After the closing return of the if dry_run: block (currently around line 73), and before the non-empty directory notice comment, add: tel_thread = start_telemetry_thread() — this starts the daemon thread in the non-dry-run path only. Placement must be AFTER the if dry_run: block ends, NEVER before it. start_telemetry_thread() also checks opt-out internally and returns None when opted out, so no double-guard is needed here.

    Edit 4 — Join thread after tasks: After the try/except block (after the final except (OSError, Exception) handler, before the written_str = ... line), add: if tel_thread is not None: tel_thread.join(timeout=1.0) — this gives telemetry up to 1 second after all tasks complete. If an exception is raised and typer.Exit(1) is called, the daemon thread is killed automatically (Pitfall 5 — safe by design); the join is not reached but that is intentional.

    Verify the final insertion order in the non-dry-run path: (1) console.rule() → (2) _opted_out = ... → (3) if not _opted_out: console.print(Panel(disclosure, title='Privacy')) → (4) if dry_run: return → (5) tel_thread = start_telemetry_thread() → (6) existing = [e for e in cwd.iterdir()...] → (7) try: ... except → (8) if tel_thread is not None: tel_thread.join(timeout=1.0) → (9) console.print(Panel(written_str, ...)) → (10) console.rule("You're all set!").
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run python -c "from goodvibes_cli.commands.init_cmd import init_cmd; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - packages/pip/src/goodvibes_cli/commands/init_cmd.py contains: from goodvibes_cli.steps.telemetry import start_telemetry_thread
    - File contains: _opted_out = (os.environ.get('DO_NOT_TRACK') == '1' or os.environ.get('GOODVIBES_NO_TELEMETRY') == '1' or os.environ.get('CI') == 'true')
    - File contains: if not _opted_out: console.print(Panel('Anonymous usage stats are collected. Set DO_NOT_TRACK=1 to opt out.', title='Privacy'))
    - The _opted_out check and conditional Panel call appear BEFORE the if dry_run: block (grep line numbers confirm ordering)
    - File contains: tel_thread = start_telemetry_thread() — appears AFTER the dry_run return block
    - File contains: tel_thread.join(timeout=1.0) — appears after the try/except block, before post-tasks notes
    - uv run python import exits 0 and prints 'import OK'
  </acceptance_criteria>
  <done>init_cmd.py modified with 4 surgical edits: import, opt-out-gated disclosure Panel, tel_thread start after dry-run guard, tel_thread.join(1.0) after tasks; imports cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add telemetry mock and TEL-04 tests to packages/pip/tests/test_init_cmd.py</name>
  <files>packages/pip/tests/test_init_cmd.py</files>
  <read_first>
    - packages/pip/tests/test_init_cmd.py — full file (must read current state — existing mocker.patch calls, runner/app fixtures, assertion patterns)
    - packages/pip/src/goodvibes_cli/commands/init_cmd.py — the modified init_cmd.py (from Task 1) to know what the mock must cover
    - .planning/phases/13-anonymous-telemetry/13-PATTERNS.md — test_init_cmd.py section (mocker.patch for start_telemetry_thread, runner.invoke + output assertion, Privacy title assertion)
    - .planning/phases/13-anonymous-telemetry/13-CONTEXT.md — D-08 (exact disclosure text for assertion), D-11 (mock at module boundary)
  </read_first>
  <action>
    Make two edits to packages/pip/tests/test_init_cmd.py.

    Edit 1 — Add start_telemetry_thread mock to ALL existing test functions: In every existing test function that calls runner.invoke, add mocker.patch("goodvibes_cli.commands.init_cmd.start_telemetry_thread", return_value=None) as an additional patch before runner.invoke. This prevents real HTTP calls during tests and prevents thread leak. The mock path is "goodvibes_cli.commands.init_cmd.start_telemetry_thread" (matches the import statement in init_cmd.py). Returning None from the mock simulates the opted-out case — the tel_thread.join guard (if tel_thread is not None) skips cleanly.

    Edit 2 — Add three new test functions at the end of the file:

    New test 1 (TEL-04 non-opted-out): def test_shows_privacy_disclosure_panel_before_file_operations(runner, app, mocker, tmp_path): — Patch all dependencies (resolve_templates_dir, detect_project_type, copy_templates, install_headroom, configure_mcp, start_telemetry_thread). Call runner.invoke(app, ['--minimal']). Assert result.exit_code == 0. Assert 'Privacy' in result.output and 'DO_NOT_TRACK' in result.output. Both substrings come from the disclosure Panel added in Task 1.

    New test 2 (dry-run guard): def test_does_not_call_start_telemetry_thread_during_dry_run(runner, app, mocker, tmp_path): — Patch resolve_templates_dir and list_template_files and detect_project_type and start_telemetry_thread as a spy: mock_tel = mocker.patch("goodvibes_cli.commands.init_cmd.start_telemetry_thread", return_value=None). Call runner.invoke(app, ['--dry-run']). Assert result.exit_code == 0. Assert mock_tel.assert_not_called().

    New test 3 (opt-out display suppression): def test_does_not_show_privacy_panel_when_do_not_track_is_set(runner, app, mocker, monkeypatch, tmp_path): — Use monkeypatch.setenv('DO_NOT_TRACK', '1') at the start of the test (monkeypatch automatically restores env after the test — no manual cleanup needed). Patch all dependencies (resolve_templates_dir, detect_project_type, copy_templates, install_headroom, configure_mcp, start_telemetry_thread). Call runner.invoke(app, ['--minimal']). Assert result.exit_code == 0. Assert 'Privacy' not in result.output. This verifies ROADMAP Phase 13 Success Criterion 2 — no disclosure line when opted out.

    Test names must be sentences per CLAUDE.md naming convention. Do not modify any existing test functions beyond adding the start_telemetry_thread mock patch to each one.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_init_cmd.py -x -q 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/pip/tests/test_init_cmd.py contains: mocker.patch("goodvibes_cli.commands.init_cmd.start_telemetry_thread", return_value=None) in every test function that invokes runner
    - File contains a test named test_shows_privacy_disclosure_panel_before_file_operations (or equivalent sentence)
    - File contains a test asserting mock_tel.assert_not_called() for --dry-run
    - File contains a test asserting 'Privacy' not in result.output when DO_NOT_TRACK=1
    - cd packages/pip && uv run pytest tests/test_init_cmd.py -x -q exits 0
    - All previously-passing tests still pass (no regressions)
    - TEL-04 test asserts 'Privacy' in result.output and 'DO_NOT_TRACK' in result.output
  </acceptance_criteria>
  <done>test_init_cmd.py updated: start_telemetry_thread mock added to all existing tests, three new tests added (TEL-04 panel assertion + dry-run guard + opt-out display suppression); uv run pytest exits 0.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| init_cmd.py action → start_telemetry_thread() | Control flow; dry-run guard must prevent thread start in simulation mode |
| task blocks → tel_thread.join | Timing boundary; join(timeout=1.0) must not block user if telemetry is slow or if exception path taken |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-05-DRY | Spoofing | start_telemetry_thread() placement | mitigate | tel_thread = start_telemetry_thread() assigned after the if dry_run: return block — dry-run path exits before the call; test in test_init_cmd.py asserts mock_tel.assert_not_called() |
| T-13-05-JOIN | Denial of Service | thread.join without timeout | mitigate | tel_thread.join(timeout=1.0) — hard 1s cap; _fire() inside thread has urlopen(timeout=5); even if _fire() hangs, join returns after 1s |
| T-13-05-THREAD | Information Disclosure | daemon thread leaking on typer.Exit | accept | daemon=True; Python auto-kills daemon threads on process exit (RESEARCH.md Pitfall 5 — safe by design; join is best-effort not required cleanup) |
| T-13-05-NOTE | Information Disclosure | Disclosure panel content | accept | Panel text contains no PII; suppressed entirely on opt-out per ROADMAP SC2 |
</threat_model>

<verification>
After both tasks complete:
- cd packages/pip && uv run pytest tests/test_init_cmd.py -x -q exits 0
- grep 'start_telemetry_thread' packages/pip/src/goodvibes_cli/commands/init_cmd.py confirms import + two call sites (start and conditional join)
- grep -n '_opted_out\|DO_NOT_TRACK' packages/pip/src/goodvibes_cli/commands/init_cmd.py shows the opt-out guard before the Panel call
- grep 'Privacy' packages/pip/src/goodvibes_cli/commands/init_cmd.py confirms conditional disclosure Panel
- Full suite: cd packages/pip && uv run pytest tests/ -x -q exits 0
</verification>

<success_criteria>
init_cmd.py has opt-out-gated Privacy Panel (D-08 text) immediately after console.rule() — suppressed when DO_NOT_TRACK/GOODVIBES_NO_TELEMETRY/CI is set (ROADMAP SC2); start_telemetry_thread() called after dry-run guard; tel_thread.join(1.0) after tasks. test_init_cmd.py has telemetry mock in all tests; TEL-04, dry-run guard, and opt-out display suppression tests pass; full pip test suite green.
</success_criteria>

<output>
Create `.planning/phases/13-anonymous-telemetry/13-05-SUMMARY.md` when done
</output>
