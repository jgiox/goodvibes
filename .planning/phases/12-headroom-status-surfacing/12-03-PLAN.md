---
phase: 12-headroom-status-surfacing
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - packages/pip/src/goodvibes_cli/steps/install_headroom.py
  - packages/pip/src/goodvibes_cli/steps/configure_mcp.py
  - packages/pip/src/goodvibes_cli/commands/init_cmd.py
  - packages/pip/src/goodvibes_cli/commands/doctor_cmd.py
  - packages/pip/tests/test_install_headroom.py
  - packages/pip/tests/test_configure_mcp.py
  - packages/pip/tests/test_init_cmd.py
  - packages/pip/tests/test_doctor_cmd.py
autonomous: true
requirements:
  - HDR2-01
  - HDR2-02
  - HDR2-03
  - HDR2-04
  - HDR2-05

must_haves:
  truths:
    - "install_headroom() returns {'status': 'already-installed'} when subprocess.run(['headroom', 'compress', '--help'], timeout=10) exits 0"
    - "install_headroom() returns {'status': 'installed'} when uv/pipx/pip succeeds"
    - "install_headroom() returns {'status': 'skipped', 'reason': 'Python 3.10+ not found'} when detect_python returns None"
    - "install_headroom() returns {'status': 'failed'} when all installers are exhausted"
    - "configure_mcp() returns a dict with 'status' key for all code paths (already-registered/registered/skipped/failed)"
    - "Every subprocess.run call in install_headroom.py and configure_mcp.py passes timeout=10"
    - "subprocess.TimeoutExpired is caught alongside CalledProcessError in all except clauses that gained a timeout"
    - "goodvibes init (non-minimal) prints a Rich Panel titled 'Headroom' with actual install and MCP status strings"
    - "_check_headroom() in doctor_cmd.py probes with subprocess.run(['headroom', 'compress', '--help'], timeout=10)"
    - "_check_headroom() catches FileNotFoundError, CalledProcessError, and TimeoutExpired — label is 'headroom installed and working'"
  artifacts:
    - path: "packages/pip/src/goodvibes_cli/steps/install_headroom.py"
      provides: "install_headroom returning dict[str, str] with status key"
      contains: "def install_headroom"
    - path: "packages/pip/src/goodvibes_cli/steps/configure_mcp.py"
      provides: "configure_mcp returning dict[str, str] with status key"
      contains: "def configure_mcp"
    - path: "packages/pip/src/goodvibes_cli/commands/init_cmd.py"
      provides: "_format_headroom_status helper and Headroom Panel in outro"
      contains: "_format_headroom_status"
    - path: "packages/pip/src/goodvibes_cli/commands/doctor_cmd.py"
      provides: "functional probe replacing shutil.which in _check_headroom"
      contains: "compress.*--help"
    - path: "packages/pip/tests/test_install_headroom.py"
      provides: "subprocess.run probe mocks, return dict assertions, TimeoutExpired test"
    - path: "packages/pip/tests/test_configure_mcp.py"
      provides: "return dict assertions on every configure_mcp call"
    - path: "packages/pip/tests/test_init_cmd.py"
      provides: "return_value mocks and test_normal_run_shows_headroom_panel"
    - path: "packages/pip/tests/test_doctor_cmd.py"
      provides: "subprocess.run mocks, new label assertions, TimeoutExpired and CalledProcessError tests"
  key_links:
    - from: "packages/pip/src/goodvibes_cli/steps/install_headroom.py"
      to: "subprocess.run(['headroom', 'compress', '--help'])"
      via: "functional idempotency probe replacing shutil.which"
      pattern: "headroom.*compress.*--help"
    - from: "packages/pip/src/goodvibes_cli/commands/init_cmd.py"
      to: "packages/pip/src/goodvibes_cli/steps/install_headroom.py"
      via: "headroom_result = install_headroom(log_install)"
      pattern: "headroom_result"
    - from: "packages/pip/src/goodvibes_cli/commands/doctor_cmd.py"
      to: "subprocess.run(['headroom', 'compress', '--help'])"
      via: "_check_headroom functional probe"
      pattern: "compress.*--help"
---

<objective>
Python parity for Phase 12: mirror all TypeScript changes from Plans 12-01 and 12-02 in the Python pip package. Change install_headroom() and configure_mcp() from None-returning functions to discriminated dict returns, replace the shutil.which idempotency probe with subprocess.run(['headroom', 'compress', '--help'], timeout=10), add timeout=10 and subprocess.TimeoutExpired handling to all subprocess calls, wire results into init_cmd.py (Headroom Panel) and doctor_cmd.py (functional probe).

Purpose: HDR2-01 through HDR2-05 apply to both the npm and pip packages. Python users of goodvibes-cli deserve the same truthful init outro and functional doctor probe as TypeScript users (per D-03 Python parity).

Output: All 4 Python step/command files return structured results; all 4 test files updated; uv run pytest exits 0.

TDD discipline: Task 1 updates tests to the RED state and commits. Task 2 updates implementations to the GREEN state and commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-headroom-status-surfacing/12-RESEARCH.md
@.planning/phases/12-headroom-status-surfacing/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — update 4 Python test files for discriminated return dicts and compress --help probe</name>
  <files>
    packages/pip/tests/test_install_headroom.py,
    packages/pip/tests/test_configure_mcp.py,
    packages/pip/tests/test_init_cmd.py,
    packages/pip/tests/test_doctor_cmd.py
  </files>
  <read_first>
    packages/pip/tests/test_install_headroom.py (full file — know all mock patterns before editing)
    packages/pip/tests/test_configure_mcp.py (full file)
    packages/pip/tests/test_init_cmd.py (full file)
    packages/pip/tests/test_doctor_cmd.py (full file)
    .planning/phases/12-headroom-status-surfacing/12-PATTERNS.md sections:
      "test_install_headroom.py", "test_configure_mcp.py", "test_init_cmd.py", "test_doctor_cmd.py"
  </read_first>
  <action>
    Run tests first to confirm starting state:
      cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_install_headroom.py tests/test_configure_mcp.py tests/test_init_cmd.py tests/test_doctor_cmd.py -x -q

    ## test_install_headroom.py — 9 types of changes

    The probe changes from shutil.which("headroom") → subprocess.run(["headroom", "compress", "--help"]).
    This adds one subprocess.run call before the ONNX warning, shifting installer call indices by +1.

    (1) Remove every `mocker.patch("goodvibes_cli.steps.install_headroom.shutil.which", ...)` line.
    These appear in: test_uv_found_and_succeeds, test_pipx_fallback_when_uv_enoent,
    test_pip_fallback_when_uv_and_pipx_enoent, test_all_enoent_logs_warning,
    test_called_process_error_soft_fail, test_pipx_fallback_when_uv_called_process_error,
    test_already_installed_skips_installer, test_no_shell_true, test_onnx_warning_logged_before_subprocess.
    Remove only the shutil.which patch line in each; leave other mocks unchanged.

    (2) For tests that previously relied on shutil.which(return_value=None) to mean "headroom not on PATH,
    proceed to installer": the probe is now subprocess.run(["headroom", "compress", "--help"]).
    The probe must FAIL (FileNotFoundError) to fall through to the installer loop.
    For each test listed in step 1 EXCEPT test_already_installed_skips_installer: ensure the
    subprocess.run mock raises FileNotFoundError for the probe call as its first condition.

    For tests that already have a side_effect function (test_pipx_fallback_when_uv_enoent,
    test_pip_fallback_when_uv_and_pipx_enoent, test_called_process_error_soft_fail,
    test_pipx_fallback_when_uv_called_process_error): add at the TOP of the side_effect body:
      if cmd_list == ["headroom", "compress", "--help"]:
          raise FileNotFoundError("headroom not found")

    For tests that patch subprocess.run with a static return_value (test_uv_found_and_succeeds,
    test_all_enoent_logs_warning, test_no_shell_true): convert to side_effect. Example for
    test_uv_found_and_succeeds:
      def side_effect(cmd_list, **kwargs):
          if cmd_list == ["headroom", "compress", "--help"]:
              raise FileNotFoundError("headroom not found")
          return subprocess.CompletedProcess(args=cmd_list, returncode=0, stdout="", stderr="")
      run = mocker.patch("goodvibes_cli.steps.install_headroom.subprocess.run", side_effect=side_effect)

    For test_all_enoent_logs_warning (currently side_effect=FileNotFoundError("not found") globally):
    the global FileNotFoundError already applies to the probe too — no change needed to mock. The
    probe raises FileNotFoundError → falls through; installers raise FileNotFoundError → exhausted.

    For test_no_shell_true: add probe guard to the side_effect so the probe raises FileNotFoundError
    and the installer runs (so we can verify it never uses shell=True).

    (3) CRITICAL — installer call index shift in test_uv_found_and_succeeds:
    Before: call_args_list[0] was the uv installer call.
    After: call_args_list[0] is the probe call; call_args_list[1] is the uv installer call.
    Change `first_call_args = run.call_args_list[0][0][0]` to `run.call_args_list[1][0][0]`.

    (4) CRITICAL — call_count in test_called_process_error_soft_fail:
    Before: run.call_count == 3 (uv + pipx + pip — all CalledProcessError).
    After: probe (CalledProcessError) + uv + pipx + pip = 4. But wait — the probe raises
    CalledProcessError which is caught and causes fall-through to the installer loop.
    So: probe (1) + 3 installers (3) = 4 total.
    Change `assert run.call_count == 3` to `assert run.call_count == 4`.

    (5) test_already_installed_skips_installer: the probe now returns exit 0 (headroom working).
    Remove the shutil.which mock (done in step 1). Change the subprocess.run mock from a static
    return_value to a side_effect returning exit 0 for the probe:
      def side_effect(cmd_list, **kwargs):
          return subprocess.CompletedProcess(args=cmd_list, returncode=0, stdout="", stderr="")
      run = mocker.patch("goodvibes_cli.steps.install_headroom.subprocess.run", side_effect=side_effect)
    The probe returns exit 0 → already-installed → no installer calls.
    Replace `run.assert_not_called()` with `assert run.call_count == 1` (the probe IS a subprocess call).
    Add result capture and assertion:
      result = install_headroom(log_calls.append)
      assert result["status"] == "already-installed"

    (6) test_description_logged_before_idempotency_check: Replace the shutil.which instrumentation
    with subprocess.run instrumentation. The new recording function must raise FileNotFoundError
    for the probe (so execution continues past it and verifies ordering):
      def recording_run(cmd_list, **kwargs):
          event_log.append(f"subprocess:{cmd_list[0]}")
          if cmd_list == ["headroom", "compress", "--help"]:
              raise FileNotFoundError("headroom not found")
          return subprocess.CompletedProcess(args=cmd_list, returncode=0, stdout="", stderr="")
      mocker.patch("goodvibes_cli.steps.install_headroom.subprocess.run", side_effect=recording_run)
    Remove `mocker.patch("goodvibes_cli.steps.install_headroom.shutil.which", side_effect=recording_which)`.
    Change the find expression for `which_idx` (rename variable to `subprocess_idx`) to look for
    `e.startswith("subprocess:")`. The description ("headroom compresses") is still logged before
    the probe subprocess call. Assertion stays: `assert desc_idx < subprocess_idx`.

    (7) test_onnx_warning_logged_before_subprocess: Remove shutil.which mock. Update recording_run
    to raise FileNotFoundError for the probe but succeed for the installer:
      def recording_run(cmd_list, **kwargs):
          event_log.append(f"subprocess:{cmd_list[0]}")
          if cmd_list == ["headroom", "compress", "--help"]:
              raise FileNotFoundError("headroom not found")
          return subprocess.CompletedProcess(args=cmd_list, returncode=0, stdout="", stderr="")
    Event sequence after change: [log:headroom compresses, subprocess:headroom (probe), log:ONNX warning, subprocess:uv]
    The probe call is at index 1 in event_log; ONNX warning is at index 2; uv installer is at index 3.
    The existing assertion `assert onnx_idx < subprocess_idx` would FAIL because subprocess_idx points
    to the probe (index 1) which comes BEFORE onnx_idx (index 2). Update to find the INSTALLER subprocess
    call (not the probe):
      installer_idx = next(
          (i for i, e in enumerate(event_log)
           if e.startswith("subprocess:") and e != "subprocess:headroom"),
          None,
      )
    Replace `assert onnx_idx < subprocess_idx` with:
      assert installer_idx is not None, "installer subprocess.run must be called"
      assert onnx_idx < installer_idx, "ONNX warning must appear before the first installer subprocess call"

    (8) Add return value capture and assertion to every test that calls install_headroom():
      test_uv_found_and_succeeds:                        result["status"] == "installed"
      test_pipx_fallback_when_uv_enoent:                 result["status"] == "installed"
      test_pip_fallback_when_uv_and_pipx_enoent:         result["status"] == "installed"
      test_all_enoent_logs_warning:                      result["status"] == "failed"
      test_called_process_error_soft_fail:               result["status"] == "failed"
      test_pipx_fallback_when_uv_called_process_error:   result["status"] == "installed"
      test_python_absent_skips:                          result["status"] == "skipped"
      test_already_installed_skips_installer:            result["status"] == "already-installed" (done in step 5)
    For each: change `install_headroom(log_calls.append)` to `result = install_headroom(log_calls.append)`
    then add `assert result["status"] == "<expected>"` immediately after.

    (9) Add one new TimeoutExpired test after test_already_installed_skips_installer:
      def test_probe_timeout_falls_through_to_installer(mocker):
          """install_headroom falls through to installer when compress --help times out."""
          mocker.patch("goodvibes_cli.steps.install_headroom.detect_python", return_value="python3")
          def side_effect(cmd_list, **kwargs):
              if cmd_list == ["headroom", "compress", "--help"]:
                  raise subprocess.TimeoutExpired(cmd=cmd_list, timeout=10)
              return subprocess.CompletedProcess(args=cmd_list, returncode=0, stdout="", stderr="")
          run = mocker.patch("goodvibes_cli.steps.install_headroom.subprocess.run", side_effect=side_effect)
          from goodvibes_cli.steps.install_headroom import install_headroom
          log_calls: list[str] = []
          result = install_headroom(log_calls.append)
          assert result["status"] == "installed"
          called_cmds = [c[0][0][0] for c in run.call_args_list]
          assert "uv" in called_cmds

    ## test_configure_mcp.py — 2 types of changes

    (1) In every test function: change the bare call `configure_mcp(log_calls.append)` to
    `result = configure_mcp(log_calls.append)`.

    (2) Add result assertion immediately after each call using the mapping from 12-PATTERNS.md:
      test_already_registered_via_mcp_status:                      assert result == {"status": "already-registered", "reason": ""}
      test_already_in_claude_mcp_list:                             assert result == {"status": "already-registered", "reason": ""}
      test_claude_mcp_add_primary:                                  assert result["status"] == "registered"
      test_headroom_not_on_path:                                    assert result["status"] == "skipped"
      test_claude_not_found_fallback:                               assert result["status"] == "registered"
      test_headroom_enoent_in_fallback:                             assert result["status"] == "skipped"
      test_headroom_mcp_install_called_process_error_soft_fails:    assert result["status"] == "failed"
      test_claude_mcp_add_called_process_error_soft_fails:          assert result["status"] == "failed"

    ## test_init_cmd.py — 2 types of changes

    (1) For every bare `mocker.patch("goodvibes_cli.commands.init_cmd.install_headroom")` call:
    add `return_value={"status": "installed", "reason": ""}`.
    For every bare `mocker.patch("goodvibes_cli.commands.init_cmd.configure_mcp")` call:
    add `return_value={"status": "registered", "reason": ""}`.
    These appear in test_non_empty_dir_prints_notice_before_tasks and
    test_completion_shows_files_written_and_skipped_panels. Both tests run with --minimal so
    the functions are never called, but set return_value for correctness.

    (2) Add a new test function at the end of the file. This test will FAIL (RED) because init_cmd.py
    does not yet capture results or show a Headroom Panel:
      def test_normal_run_shows_headroom_panel(runner, app, mocker, tmp_path):
          """goodvibes init (non-minimal) shows Headroom Panel with actual install and MCP status."""
          mocker.patch("goodvibes_cli.commands.init_cmd.resolve_templates_dir", return_value=tmp_path)
          mocker.patch("goodvibes_cli.commands.init_cmd.detect_project_type", return_value="both")
          mocker.patch("goodvibes_cli.commands.init_cmd.copy_templates", return_value=(["CLAUDE.md"], []))
          mocker.patch(
              "goodvibes_cli.commands.init_cmd.install_headroom",
              return_value={"status": "installed", "reason": ""}
          )
          mocker.patch(
              "goodvibes_cli.commands.init_cmd.configure_mcp",
              return_value={"status": "registered", "reason": ""}
          )
          mocker.patch("pathlib.Path.iterdir", return_value=iter([]))
          result = runner.invoke(app, [])
          assert result.exit_code == 0
          assert "Headroom" in result.output
          assert "headroom: installed" in result.output
          assert "MCP: registered" in result.output

    ## test_doctor_cmd.py — 3 types of changes

    (1) Replace the two existing _check_headroom unit tests that mock shutil.which with new tests
    that mock subprocess.run. The old test names can be kept or updated to match the new behavior
    (per 12-PATTERNS.md "test_doctor_cmd.py" section for exact new test bodies):

    Replace test_check_headroom_returns_pass_when_headroom_on_path with:
      def test_check_headroom_returns_pass_when_headroom_working(mocker):
          mocker.patch(
              "goodvibes_cli.commands.doctor_cmd.subprocess.run",
              return_value=subprocess.CompletedProcess(
                  args=["headroom", "compress", "--help"], returncode=0, stdout="", stderr=""
              ),
          )
          result = _check_headroom()
          assert result.passed is True
          assert result.label == "headroom installed and working"

    Replace test_check_headroom_returns_fail_with_remedy_when_not_on_path with:
      def test_check_headroom_returns_fail_when_headroom_not_found(mocker):
          mocker.patch(
              "goodvibes_cli.commands.doctor_cmd.subprocess.run",
              side_effect=FileNotFoundError("headroom not found"),
          )
          result = _check_headroom()
          assert result.passed is False
          assert "uv tool install" in result.remedy

    (2) Add two new _check_headroom tests immediately after the two above:
      def test_check_headroom_returns_fail_when_headroom_broken(mocker):
          mocker.patch(
              "goodvibes_cli.commands.doctor_cmd.subprocess.run",
              side_effect=subprocess.CalledProcessError(1, ["headroom", "compress", "--help"]),
          )
          result = _check_headroom()
          assert result.passed is False

      def test_check_headroom_returns_fail_when_headroom_times_out(mocker):
          mocker.patch(
              "goodvibes_cli.commands.doctor_cmd.subprocess.run",
              side_effect=subprocess.TimeoutExpired(cmd=["headroom", "compress", "--help"], timeout=10),
          )
          result = _check_headroom()
          assert result.passed is False

    (3) Update the _check_headroom mock return value in all integration-style tests. In these tests,
    _check_headroom is patched directly with a CheckResult. Change the label from "headroom on PATH"
    to "headroom installed and working" in these test functions:
      test_doctor_cmd_raises_exit_1_when_any_check_fails (line ~77)
      test_doctor_cmd_does_not_raise_when_all_checks_pass (line ~89)
      test_doctor_cmd_collects_all_failures_before_exiting (line ~101)
      test_doctor_output_starts_with_version_line (line ~119)
    Change: CheckResult(label="headroom on PATH", ...) → CheckResult(label="headroom installed and working", ...)

    After all changes, run:
      cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_install_headroom.py tests/test_configure_mcp.py tests/test_init_cmd.py tests/test_doctor_cmd.py -x 2>&1 | tail -30
    MUST exit non-zero. Confirm failures are assertion mismatches (result["status"] KeyError or
    AssertionError on dict value), not import errors or syntax errors.
    Commit: `test(12-03): RED — update Python step/command tests for discriminated return dicts and compress --help probe`
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_install_headroom.py tests/test_configure_mcp.py tests/test_init_cmd.py tests/test_doctor_cmd.py -x 2>&1 | tail -30</automated>
  </verify>
  <done>All 4 test files updated with new assertions and mocks; uv run pytest exits non-zero confirming RED; RED commit created with message starting `test(12-03): RED`.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement discriminated returns, compress --help probe, timeout=10, Headroom Panel</name>
  <files>
    packages/pip/src/goodvibes_cli/steps/install_headroom.py,
    packages/pip/src/goodvibes_cli/steps/configure_mcp.py,
    packages/pip/src/goodvibes_cli/commands/init_cmd.py,
    packages/pip/src/goodvibes_cli/commands/doctor_cmd.py
  </files>
  <read_first>
    packages/pip/tests/test_install_headroom.py (already RED — see assertions that must pass)
    packages/pip/tests/test_configure_mcp.py (already RED)
    packages/pip/tests/test_init_cmd.py (already RED — note test_normal_run_shows_headroom_panel)
    packages/pip/tests/test_doctor_cmd.py (already RED)
    packages/pip/src/goodvibes_cli/steps/install_headroom.py (current implementation)
    packages/pip/src/goodvibes_cli/steps/configure_mcp.py (current implementation)
    packages/pip/src/goodvibes_cli/commands/init_cmd.py (current implementation)
    packages/pip/src/goodvibes_cli/commands/doctor_cmd.py (current implementation)
    .planning/phases/12-headroom-status-surfacing/12-PATTERNS.md sections:
      "install_headroom.py", "configure_mcp.py", "init_cmd.py", "doctor_cmd.py"
  </read_first>
  <action>
    ## install_headroom.py — 6 changes

    (1) Remove `import shutil` from imports. After this change shutil is not used in this file.
    Verify configure_mcp.py still uses shutil (it does — shutil.which for PATH check); no change there.

    (2) Change the function signature:
      def install_headroom(log: Callable[[str], None]) -> None:
    to:
      def install_headroom(log: Callable[[str], None]) -> dict[str, str]:

    (3) Python-absent early return: change bare `return` to
    `return {"status": "skipped", "reason": "Python 3.10+ not found"}`.

    (4) Replace the shutil.which probe block (the `if shutil.which("headroom") is not None:` block)
    with the functional probe. See 12-PATTERNS.md "install_headroom.py" section for the exact
    try-except block. The except clause catches
    (FileNotFoundError, subprocess.CalledProcessError, subprocess.TimeoutExpired). On probe success:
    `return {"status": "already-installed", "reason": ""}`.
    Remove the `import shutil` usage — the probe no longer uses shutil.which.

    (5) In the installer loop (for cmd_list in installers):
    - Add `timeout=10` to subprocess.run: `subprocess.run(cmd_list, capture_output=True, text=True, check=True, timeout=10)`
    - Change bare `return` on success to `return {"status": "installed", "reason": ""}`
    - Extend the CalledProcessError except to also catch TimeoutExpired:
        `except (subprocess.CalledProcessError, subprocess.TimeoutExpired) as e:`
      Use `getattr(e, 'stderr', '') or ""` to safely extract stderr (TimeoutExpired has no stderr):
        `lines = (getattr(e, 'stderr', '') or "").splitlines()`
    - The `continue` at the end of the except block stays (try next installer).

    (6) After the loop's final log call (all installers exhausted), add:
    `return {"status": "failed", "reason": "all installers exhausted"}`

    ## configure_mcp.py — 6 changes

    (1) Change the function signature from `-> None:` to `-> dict[str, str]:`.

    (2) Idempotency check (the headroom mcp status block, lines 18-28):
    - Add `timeout=10` to subprocess.run
    - Extend the except to catch TimeoutExpired:
        `except (FileNotFoundError, subprocess.CalledProcessError, subprocess.TimeoutExpired):`
    - Change bare `return` to `return {"status": "already-registered", "reason": ""}`

    (3) Primary strategy outer try block changes:
    - Add `timeout=10` to `subprocess.run(["claude", "mcp", "list"], ...)`
    - After `if "headroom" in list_result.stdout:` branch: change bare `return` to
      `return {"status": "already-registered", "reason": ""}`
    - The shutil.which check stays unchanged (PATH check, not functional probe)
    - The "not found on PATH" bare `return` becomes:
      `return {"status": "skipped", "reason": "headroom binary not found on PATH"}`
    - Add `timeout=10` to `subprocess.run(["claude", "mcp", "add", ...], ...)`
    - Change the success bare `return` after the log call to `return {"status": "registered", "reason": ""}`

    (4) Outer except CalledProcessError handler (lines 65-69):
    Keep the existing log call but replace the bare `return` with an explicit dict return:
      `return {"status": "failed", "reason": lines[0] if lines else "unknown error"}`
    (The current code has `return` without a value after the log; make it return the dict.)
    See 12-PATTERNS.md "configure_mcp.py" section for exact wording.

    (5) Fallback block (headroom mcp install, lines 72-91):
    - Add `timeout=10` to subprocess.run
    - Change bare `return` after the success log to `return {"status": "registered", "reason": ""}`
    - Change the FileNotFoundError bare `return` to
      `return {"status": "skipped", "reason": "headroom binary not found"}`
    - Extend the CalledProcessError except to `(subprocess.CalledProcessError, subprocess.TimeoutExpired)`:
        `except (subprocess.CalledProcessError, subprocess.TimeoutExpired) as e:`
      Use `getattr(e, 'stderr', '') or ""` for stderr. Change the log + bare `return` to
      `return {"status": "failed", "reason": first_line}`.

    (6) Verify the function has no code path that reaches the end without a return. Python does not
    enforce return types at runtime but the tests will catch any missing return (they assert on result["status"]).

    ## init_cmd.py — 4 changes

    (1) Add `_format_headroom_status` helper function immediately before `init_cmd`. See 12-PATTERNS.md
    "init_cmd.py" section for the exact body (uses dict.get() with labels dict per status key, joins
    two lines with '\n'). Add `# ponytail: inline helper — too small to justify a separate module` comment.

    (2) Initialize result defaults before the `if not minimal:` block (around line 70):
      # ponytail: default to skipped — minimal path never enters the block
      headroom_result: dict[str, str] = {"status": "skipped", "reason": ""}
      mcp_result: dict[str, str] = {"status": "skipped", "reason": ""}

    (3) Capture return values inside the `if not minimal:` block:
      headroom_result = install_headroom(log_install)
      mcp_result = configure_mcp(log_mcp)

    (4) Add Headroom Panel after the existing file list Panels and BEFORE the Next steps Panel
    (after line ~96):
      if not minimal:
          console.print(Panel(_format_headroom_status(headroom_result, mcp_result), title="Headroom"))

    ## doctor_cmd.py — 3 changes

    (1) Replace the entire body of `_check_headroom()` with the functional probe. See 12-PATTERNS.md
    "doctor_cmd.py" section for the exact new try-except body:
    `subprocess.run(["headroom", "compress", "--help"], capture_output=True, text=True, check=True, timeout=10)`
    Catch `(FileNotFoundError, subprocess.CalledProcessError, subprocess.TimeoutExpired)`.
    Both branches return `CheckResult(label="headroom installed and working", ...)`.

    (2) Remove `import shutil` from the import block. After _check_headroom is updated, shutil is
    not used anywhere else in doctor_cmd.py (confirm by grepping all function bodies).

    (3) The `subprocess` import is already present (used by _check_git_config). No new import needed.

    After all source changes, run:
      cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_install_headroom.py tests/test_configure_mcp.py tests/test_init_cmd.py tests/test_doctor_cmd.py -x
    MUST exit 0 — all tests pass.
    Commit: `feat(12-03): GREEN — Python parity: discriminated return dicts, compress --help probe, timeout=10`

    Update JOURNAL.md: add entry with today's date; summary: Phase 12-03 — Python parity for headroom
    status surfacing: discriminated return dicts in install_headroom.py and configure_mcp.py, functional
    compress --help probe replacing shutil.which, timeout=10 on all subprocess calls, Headroom Panel in
    init_cmd.py, functional _check_headroom in doctor_cmd.py; files changed: all 8 Python step/command/test
    files; reason: HDR2-01 through HDR2-05 apply to pip package (truthful init outro, functional doctor
    probe, compress --help probe, timeout enforcement); tests run: uv run pytest exits 0; docs: none.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_install_headroom.py tests/test_configure_mcp.py tests/test_init_cmd.py tests/test_doctor_cmd.py -x</automated>
  </verify>
  <done>
    install_headroom.py and configure_mcp.py return dict[str, str] for all code paths; every
    subprocess.run call in both step files passes timeout=10; subprocess.TimeoutExpired is caught
    alongside CalledProcessError in all relevant except clauses; shutil import removed from
    install_headroom.py and doctor_cmd.py; init_cmd.py shows Headroom Panel for non-minimal runs;
    doctor_cmd.py _check_headroom uses functional compress --help probe and reports "headroom installed
    and working"; uv run pytest exits 0; GREEN commit created with message starting `feat(12-03): GREEN`.
    JOURNAL.md updated with entry covering all 8 files and reason (HDR2-01 through HDR2-05 Python parity).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → subprocess | headroom/uv/pipx/pip/claude binaries are external; called with list args (shell=False by default) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-05 | Denial of Service | Python subprocess hangs | mitigate | HDR2-04: timeout=10 on all subprocess.run calls; subprocess.TimeoutExpired caught and treated as soft failure |
| T-12-06 | Tampering | Python subprocess args | accept | list-form args (no shell=True) already enforced; test_no_shell_true verifies; no new risk |
| T-12-07 | Information Disclosure | error messages in Headroom Panel | accept | reason comes from subprocess error output, not user input; no PII; displayed in local terminal only |
| T-12-SC | Tampering | pip installs | accept | no new packages installed in this plan; probe change is in-place refactor only |
</threat_model>

<verification>
After Task 2 GREEN:
- `cd packages/pip && uv run pytest tests/test_install_headroom.py tests/test_configure_mcp.py tests/test_init_cmd.py tests/test_doctor_cmd.py -x` exits 0
- `grep -n "compress.*--help" packages/pip/src/goodvibes_cli/steps/install_headroom.py` returns probe line
- `grep -n "compress.*--help" packages/pip/src/goodvibes_cli/commands/doctor_cmd.py` returns probe line
- `grep -c "timeout=10" packages/pip/src/goodvibes_cli/steps/install_headroom.py` returns at least 2
- `grep -c "timeout=10" packages/pip/src/goodvibes_cli/steps/configure_mcp.py` returns at least 4
- `grep -n "TimeoutExpired" packages/pip/src/goodvibes_cli/steps/install_headroom.py` returns except clause
- `grep -n "TimeoutExpired" packages/pip/src/goodvibes_cli/steps/configure_mcp.py` returns at least 2 except clauses
- `grep -n "shutil" packages/pip/src/goodvibes_cli/steps/install_headroom.py` returns empty
- `grep -n "shutil" packages/pip/src/goodvibes_cli/commands/doctor_cmd.py` returns empty
- `grep -n "Headroom" packages/pip/src/goodvibes_cli/commands/init_cmd.py` shows Panel call
- `grep -n "_format_headroom_status" packages/pip/src/goodvibes_cli/commands/init_cmd.py` shows helper def and call
- `grep -n "headroom installed and working" packages/pip/src/goodvibes_cli/commands/doctor_cmd.py` shows new label (both branches)
</verification>

<success_criteria>
- install_headroom() returns dict[str, str] with status key for all code paths
- configure_mcp() returns dict[str, str] with status key for all code paths
- Both step files: every subprocess.run call passes timeout=10
- Both step files: subprocess.TimeoutExpired caught alongside CalledProcessError in all relevant except clauses
- init_cmd.py: Headroom Panel printed for non-minimal runs using _format_headroom_status
- doctor_cmd.py: _check_headroom uses compress --help functional probe; catches FileNotFoundError, CalledProcessError, and TimeoutExpired; label is "headroom installed and working"
- uv run pytest (run from packages/pip/) exits 0
- Two commits on the branch: RED (test updates) and GREEN (implementation)
- JOURNAL.md updated
</success_criteria>

<output>
Create `.planning/phases/12-headroom-status-surfacing/12-03-SUMMARY.md` when done.
</output>
