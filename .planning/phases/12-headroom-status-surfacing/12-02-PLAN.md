---
phase: 12-headroom-status-surfacing
plan: 02
type: execute
wave: 2
depends_on:
  - 12-01
files_modified:
  - packages/npm/src/commands/init.ts
  - packages/npm/src/commands/doctor.ts
  - packages/npm/src/commands/init.test.ts
  - packages/npm/src/commands/doctor.test.ts
autonomous: true
requirements:
  - HDR2-01
  - HDR2-02
  - HDR2-05

must_haves:
  truths:
    - "goodvibes init (non-minimal) shows a 'Headroom' note after the file list notes displaying the actual install and MCP outcomes"
    - "The Headroom note content includes a headroom status line and an MCP status line"
    - "goodvibes doctor reports 'headroom installed and working' (not 'headroom on PATH')"
    - "goodvibes doctor headroom check probes with 'headroom compress --help' and 10-second timeout"
    - "goodvibes doctor soft-fails on any headroom probe error (ENOENT, CalledProcessError, timeout)"
  artifacts:
    - path: "packages/npm/src/commands/init.ts"
      provides: "formatHeadroomStatus inline helper + headroomResult/mcpResult closure capture + Headroom note"
      contains: "formatHeadroomStatus"
    - path: "packages/npm/src/commands/doctor.ts"
      provides: "checkHeadroom using compress --help with 10s timeout and soft-fail catch"
      contains: "compress.*--help"
  key_links:
    - from: "packages/npm/src/commands/init.ts"
      to: "packages/npm/src/steps/install-headroom.ts"
      via: "import { installHeadroom, type HeadroomResult }"
      pattern: "HeadroomResult"
    - from: "packages/npm/src/commands/init.ts"
      to: "packages/npm/src/steps/configure-mcp.ts"
      via: "import { configureMcp, type McpResult }"
      pattern: "McpResult"
    - from: "packages/npm/src/commands/doctor.ts"
      to: "execa"
      via: "execa('headroom', ['compress', '--help'], { timeout: 10_000 })"
      pattern: "compress.*--help.*timeout"
---

<objective>
Wire the HeadroomResult and McpResult types (from Plan 12-01) into the commands layer. init.ts captures step return values and displays them in a 'Headroom' note after the file list. doctor.ts changes its headroom probe from a PATH-only `--version` call to the functional `compress --help` probe with a 10-second timeout.

Purpose: HDR2-01 and HDR2-02 (truthful init outro) and HDR2-05 (functional doctor probe) are all in the commands layer. This plan is the user-visible half of the phase.

Output: init.ts shows actual install and MCP outcomes in the outro; doctor.ts reports real functional status; all tests GREEN.

TDD discipline: Task 1 updates tests to RED state and commits. Task 2 updates implementations to GREEN state and commits.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-headroom-status-surfacing/12-RESEARCH.md
@.planning/phases/12-headroom-status-surfacing/12-PATTERNS.md
@.planning/phases/12-headroom-status-surfacing/12-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: RED — update init.test.ts and doctor.test.ts for result capture and new probe</name>
  <files>
    packages/npm/src/commands/init.test.ts,
    packages/npm/src/commands/doctor.test.ts
  </files>
  <read_first>
    packages/npm/src/commands/init.ts (see current hardcoded returns and no capture)
    packages/npm/src/commands/doctor.ts (see current --version probe)
    packages/npm/src/commands/init.test.ts (see all locations that mock installHeadroom/configureMcp)
    packages/npm/src/commands/doctor.test.ts (see headroom mock sequence)
    .planning/phases/12-headroom-status-surfacing/12-PATTERNS.md sections:
      "init.test.ts" and "doctor.test.ts" for exact assertion shapes and mock patterns
  </read_first>
  <action>
    Update init.test.ts — 2 types of changes:

    (1) All mockResolvedValue(undefined) calls for installHeadroom and configureMcp:
        Search the file for every occurrence of `mockResolvedValue(undefined)` that applies to
        `installHeadroom` or `configureMcp`. Change each one:
          - `vi.mocked(installHeadroom).mockResolvedValue(undefined)` →
            `vi.mocked(installHeadroom).mockResolvedValue({ status: 'installed' })`
          - `vi.mocked(configureMcp).mockResolvedValue(undefined)` →
            `vi.mocked(configureMcp).mockResolvedValue({ status: 'registered' })`
        There are 6 locations (lines approximately 133–134, 188–189, 219–220, 263–264, 303–304, 336–337);
        change all of them.

    (2) In the 'normal run: calls all 3 tasks' test, after the existing assertions for file-list note
        and next-steps note, add a new assertion block that verifies a Headroom note was emitted:
          - `const noteCalls = vi.mocked(note).mock.calls`
          - `const headroomNoteCall = noteCalls.find(c => String(c[1]).toLowerCase().includes('headroom'))`
          - `expect(headroomNoteCall).toBeDefined()`
          - `expect(String(headroomNoteCall![0])).toMatch(/headroom.*installed|MCP.*registered/i)`
        This test will FAIL because init.ts does not yet emit a Headroom note.

    Update doctor.test.ts — 2 types of changes:

    (1) In the 'returns pass result when headroom is on PATH' test (the first test in the
        checkHeadroom describe block): the mock sequence seeds a first execa call with
        `{ stdout: '1.0.0' } // headroom --version`. Update the comment to
        `// headroom compress --help → exit 0`. Leave the mock value unchanged (`{ stdout: '' }` is
        more accurate — compress --help produces help text on stdout — but `{ stdout: '1.0.0' }` also
        works since execa shape is the same; updating the comment is the minimum needed).
        Add an assertion after `await capturedAction()` verifying the probe call used the correct args:
          - `const execaCalls = vi.mocked(execa).mock.calls`
          - `const headroomProbe = execaCalls[0]`
          - `expect(headroomProbe[0]).toBe('headroom')`
          - `expect(headroomProbe[1]).toEqual(['compress', '--help'])`
          - `expect(headroomProbe[2]).toMatchObject({ timeout: 10_000 })`
        Also assert the note content includes the new label:
          - `expect(firstNote).toMatch(/headroom installed and working/i)`
        (currently the test only checks `/headroom/i` — tighten it to the new label)
        This test will FAIL because doctor.ts still uses `--version` and the old label.

    (2) In the 'returns fail result... not found (ENOENT)' test: the mock rejects all execa calls
        with ENOENT. No mock change needed. Add assertion that the note contains the remedy
        and that it does NOT contain "headroom on PATH" (old label):
          - `expect(allNoteText).not.toMatch(/headroom on PATH/i)`
        This will FAIL because the old label is still used.

    (3) In the 'returns fail... git user.name not set' test: the first mock seeds
        `{ stdout: '1.0.0' } // headroom --version`. Update the comment to
        `// headroom compress --help → exit 0`. No assertion change needed for this test.

    After all changes, run `cd packages/npm && npm run test` — MUST EXIT NON-ZERO.
    Confirm failures include: "headroom note not found in init.test.ts", "compress --help" assertion
    mismatch in doctor.test.ts, "headroom installed and working" mismatch in doctor.test.ts.
    Commit the RED state:
    `test(12-02): RED — update init/doctor test assertions for result capture and functional probe`
  </action>
  <acceptance_criteria>
    - init.test.ts: all 6 mockResolvedValue(undefined) → result objects; no TypeScript errors on
      { status: 'installed' } / { status: 'registered' } values after Plan 12-01 types are live
    - init.test.ts: 'normal run' test has Headroom note assertion (headroomNoteCall.toBeDefined())
    - doctor.test.ts: headroom-pass test asserts probe called with ['compress', '--help'] and timeout 10_000
    - doctor.test.ts: headroom-pass test asserts note contains 'headroom installed and working'
    - `npm run test` exits non-zero — fails because init.ts and doctor.ts not yet updated
    - RED commit exists
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm run test 2>&1 | tail -30</automated>
  </verify>
  <done>Test files updated with new assertions; npm test exits non-zero; RED commit created.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — wire result capture into init.ts and functional probe into doctor.ts</name>
  <files>
    packages/npm/src/commands/init.ts,
    packages/npm/src/commands/doctor.ts
  </files>
  <read_first>
    packages/npm/src/commands/init.test.ts (already RED — see what assertions must pass)
    packages/npm/src/commands/doctor.test.ts (already RED — see what assertions must pass)
    packages/npm/src/commands/init.ts (current implementation)
    packages/npm/src/commands/doctor.ts (current implementation)
    packages/npm/src/steps/install-headroom.ts (HeadroomResult type to import)
    packages/npm/src/steps/configure-mcp.ts (McpResult type to import)
    .planning/phases/12-headroom-status-surfacing/12-PATTERNS.md sections:
      "init.ts" and "doctor.ts" for exact code shapes
  </read_first>
  <action>
    Update init.ts — 5 changes:

    (1) Add type imports at the top of the file alongside the existing step imports:
        Import `type HeadroomResult` from '../steps/install-headroom.js' and
        `type McpResult` from '../steps/configure-mcp.js'.
        The existing `installHeadroom` and `configureMcp` value imports stay on the same lines
        (add the type to the existing import statement with braces).

    (2) Add the `formatHeadroomStatus` inline helper function before `registerInitCommand`.
        It takes `hr: HeadroomResult | undefined` and `mr: McpResult | undefined` and returns
        a string. For hr: map each status variant to a human-readable line using an object literal
        (not a switch). For 'skipped' and 'failed' variants, include the `reason` field inline.
        For mr: same pattern. Join the two lines with '\n'. Add a
        `// ponytail: inline helper — too small to justify a separate module` comment.
        See 12-PATTERNS.md "init.ts" section for the exact helper shape.

    (3) Inside the `action` handler (after line `const skippedFiles: string[] = []`), declare two
        closure variables before the `taskList` definition:
          `let headroomResult: HeadroomResult | undefined`
          `let mcpResult: McpResult | undefined`

    (4) In the `!minimal` task block, update both task callbacks:
        - headroom install task: change `await installHeadroom(...)` to `headroomResult = await installHeadroom(...)`.
          Change the hardcoded return `'headroom ready'` to a dynamic label. Define a const
          `headroomLabels: Record<HeadroomResult['status'], string>` object with values for all four
          statuses ('installed', 'already-installed', 'skipped', 'failed'). Return
          `headroomLabels[headroomResult.status]`. Place headroomLabels before the taskList or inline.
          Keep it inline using a const inside the task function to avoid the labels being a module-level
          global — ponytail forbids speculative abstractions.
        - MCP config task: same pattern — capture `mcpResult = await configureMcp(...)`, define
          `mcpLabels: Record<McpResult['status'], string>`, return `mcpLabels[mcpResult.status]`.

    (5) After the existing `note(createdFiles..., 'Files written')` and `note(skippedFiles..., 'Files skipped')`
        calls and BEFORE the `note(nextSteps, 'Next steps')` call, add:
          `if (!minimal && headroomResult) {`
          `  note(formatHeadroomStatus(headroomResult, mcpResult), 'Headroom')`
          `}`
        This guard ensures minimal runs (where headroomResult stays undefined) do not emit the note.

    Update doctor.ts — 1 function change, minimal:

    Replace the entire body of `checkHeadroom()` with the functional probe pattern:
      - Call `execa('headroom', ['compress', '--help'], { timeout: 10_000 })`
      - On success: return `{ label: 'headroom installed and working', pass: true }`
      - Catch ALL errors (use empty catch — `catch {` with no binding): return
        `{ label: 'headroom installed and working', pass: false, remedy: 'Run: uv tool install "headroom-ai[all]"  (or re-run goodvibes init)' }`
      - Remove the ENOENT-only check and the `throw e` re-throw. Doctor's job is to report,
        not propagate; catching all errors is correct here (HDR2-05).

    After all changes, run `cd packages/npm && npm run test` — MUST EXIT ZERO.
    Commit the GREEN state:
    `feat(12-02): GREEN — init.ts result capture + Headroom note; doctor.ts functional probe`
  </action>
  <acceptance_criteria>
    - init.ts imports HeadroomResult and McpResult types from step files
    - init.ts has formatHeadroomStatus helper function before registerInitCommand
    - init.ts headroom install task captures headroomResult (not discarded)
    - init.ts MCP config task captures mcpResult (not discarded)
    - init.ts emits note(..., 'Headroom') guarded by `!minimal && headroomResult`
    - doctor.ts checkHeadroom uses execa('headroom', ['compress', '--help'], { timeout: 10_000 })
    - doctor.ts checkHeadroom catches ALL errors with empty catch — no throw e
    - doctor.ts label is 'headroom installed and working' in both pass and fail branches
    - `cd packages/npm && npm run test` exits 0 — all tests pass
    - GREEN commit exists
  </acceptance_criteria>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm run test</automated>
  </verify>
  <done>
    init.ts shows actual headroom/MCP outcomes in 'Headroom' note; doctor.ts reports functional status
    using compress --help probe with timeout; npm run test exits 0. Update JOURNAL.md: add entry with
    today's date; summary: Phase 12-02 — wire HeadroomResult/McpResult into init.ts commands layer
    and update doctor.ts to functional compress --help probe; files changed: init.ts, doctor.ts,
    init.test.ts, doctor.test.ts; reason: HDR2-01/02 truthful init outro and HDR2-05 functional
    doctor probe; tests run: npm run test (exits 0); docs: none.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → subprocess | execa calls in doctor.ts; no new binaries beyond what Plan 12-01 already touches |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03 | Denial of Service | doctor.ts headroom probe | mitigate | { timeout: 10_000 } added; empty catch ensures no hang propagates |
| T-12-04 | Information Disclosure | headroom reason strings in note() | accept | reason comes from execa error.message, not user input; no PII; shows in local terminal only |
</threat_model>

<verification>
After Task 2 GREEN:
- `cd packages/npm && npm run test` exits 0
- `grep -n "Headroom" packages/npm/src/commands/init.ts` shows note call and formatHeadroomStatus
- `grep -n "compress.*--help" packages/npm/src/commands/doctor.ts` shows probe
- `grep -n "headroom installed and working" packages/npm/src/commands/doctor.ts` shows new label
- `grep -n "throw" packages/npm/src/commands/doctor.ts` shows no throw in checkHeadroom
- `grep -c "HeadroomResult\|McpResult" packages/npm/src/commands/init.ts` returns at least 3 (import + types)
</verification>

<success_criteria>
- goodvibes init (non-minimal) shows 'Headroom' note with actual install and MCP status strings
- goodvibes doctor headroom check uses compress --help probe, not --version
- goodvibes doctor headroom label is 'headroom installed and working' — not 'headroom on PATH'
- npm run test exits 0 from packages/npm/
- Two commits on branch: RED (test updates) and GREEN (implementation)
</success_criteria>

<output>
Create `.planning/phases/12-headroom-status-surfacing/12-02-SUMMARY.md` when done.
</output>
