---
phase: 04-ci-cd-scaffolding
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - packages/npm/src/utils/detect-project-type.test.ts
  - packages/pip/tests/test_detect_project_type.py
  - scripts/verify-phase4.sh
autonomous: true
requirements: [CI-05]

must_haves:
  truths:
    - "Running `npm test` reports failing tests for detectProjectType (RED — implementation missing)"
    - "Running `uv run --extra dev pytest tests/test_detect_project_type.py` reports failing tests (RED)"
    - "scripts/verify-phase4.sh exists and exits non-zero when template files are missing"
  artifacts:
    - path: "packages/npm/src/utils/detect-project-type.test.ts"
      provides: "Unit tests for detectProjectType covering all 5 input cases"
      min_lines: 40
    - path: "packages/pip/tests/test_detect_project_type.py"
      provides: "Pytest tests for detect_project_type covering all 5 input cases"
      min_lines: 40
    - path: "scripts/verify-phase4.sh"
      provides: "Smoke harness that validates template files and key content"
      min_lines: 30
  key_links:
    - from: "packages/npm/src/utils/detect-project-type.test.ts"
      to: "packages/npm/src/utils/detect-project-type.ts"
      via: "import { detectProjectType } from './detect-project-type.js'"
      pattern: "detect-project-type"
    - from: "packages/pip/tests/test_detect_project_type.py"
      to: "packages/pip/src/goodvibes_cli/utils/detect_project_type.py"
      via: "from goodvibes_cli.utils.detect_project_type import detect_project_type"
      pattern: "detect_project_type"
---

<objective>
Write RED failing tests for project-type detection (TS and Python) and author the phase smoke harness.

Purpose: TDD wave — tests define the contract before implementation. These tests must fail until Plan 03 provides the implementation.
Output: detect-project-type.test.ts, test_detect_project_type.py, scripts/verify-phase4.sh
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-ci-cd-scaffolding/04-RESEARCH.md
@.planning/phases/04-ci-cd-scaffolding/04-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Write failing TS unit tests for detectProjectType</name>
  <files>packages/npm/src/utils/detect-project-type.test.ts</files>
  <read_first>
    packages/npm/src/utils/detect-python.test.ts
    packages/npm/src/steps/copy-templates.test.ts
  </read_first>
  <behavior>
    - returns 'node' when only package.json present in tmpdir
    - returns 'python' when only pyproject.toml present in tmpdir
    - returns 'python' when only requirements.txt present in tmpdir
    - returns 'both' when package.json AND pyproject.toml both present
    - returns 'both' when neither marker file exists (safe default — empty dir)
  </behavior>
  <action>
    Create packages/npm/src/utils/detect-project-type.test.ts. Import pattern from detect-python.test.ts line 1: `import { describe, it, expect, beforeEach, afterEach } from 'vitest'`. Do NOT mock node:fs — use a real tmpdir (mkdtempSync pattern from copy-templates.test.ts lines 39-44). Import mkdtempSync, writeFileSync, rmSync from 'fs'; join from 'path'; tmpdir from 'os'.

    The import under test is: `import { detectProjectType } from './detect-project-type.js'` — this file does not exist yet, so the test suite will fail to compile (RED state).

    Write one `describe('detectProjectType', () => { ... })` block containing exactly 5 `it()` tests matching the 5 behavior items above. Use `writeFileSync(join(tmpDir, 'package.json'), '{}')` to create marker files. The `beforeEach` creates tmpDir via mkdtempSync; `afterEach` calls rmSync with recursive+force. No vi.mock needed — existsSync uses real filesystem.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | grep -E "(FAIL|Cannot find module|detect-project-type)" | head -5</automated>
  </verify>
  <done>npm test output contains a failure referencing detect-project-type.test.ts — the module cannot be resolved (RED state confirmed). Test file exists with 5 test cases.</done>
</task>

<task type="tdd">
  <name>Task 2: Write failing Python tests for detect_project_type</name>
  <files>packages/pip/tests/test_detect_project_type.py</files>
  <read_first>
    packages/pip/tests/conftest.py
    packages/pip/tests/test_copy_templates.py
    packages/pip/src/goodvibes_cli/utils/detect_python.py
  </read_first>
  <behavior>
    - test_returns_node_when_only_package_json_present: (tmp_dir / "package.json").write_text("{}"), expect "node"
    - test_returns_python_when_only_pyproject_toml_present: (tmp_dir / "pyproject.toml").write_text("[project]"), expect "python"
    - test_returns_python_when_only_requirements_txt_present: (tmp_dir / "requirements.txt").write_text("pytest"), expect "python"
    - test_returns_both_when_package_json_and_pyproject_toml_present: create both, expect "both"
    - test_returns_both_when_neither_marker_exists: empty tmp_dir, expect "both"
  </behavior>
  <action>
    Create packages/pip/tests/test_detect_project_type.py. Module docstring: `"""Tests for detect_project_type — CI-05."""`. Import only `import pathlib` and `import pytest`. Use the `tmp_dir` fixture from conftest.py (it accepts `tmp_path`). Import pattern from test_copy_templates.py line 1-3 as structural reference.

    Each test function imports from `goodvibes_cli.utils.detect_project_type import detect_project_type` — this module does not exist yet, so all tests fail with ImportError (RED state).

    Write exactly 5 test functions using the names and behaviors listed above. No fixtures beyond `tmp_dir`. No mocking needed — Path.exists() uses real filesystem.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run --extra dev pytest tests/test_detect_project_type.py -x -q 2>&1 | head -10</automated>
  </verify>
  <done>pytest output shows ImportError or ModuleNotFoundError for goodvibes_cli.utils.detect_project_type (RED state confirmed). Test file exists with 5 test functions.</done>
</task>

<task type="auto">
  <name>Task 3: Author scripts/verify-phase4.sh smoke harness</name>
  <files>scripts/verify-phase4.sh, JOURNAL.md</files>
  <read_first>
    scripts/verify-phase3.sh
  </read_first>
  <action>
    Create scripts/verify-phase4.sh as an executable bash script (chmod +x). Model the structure on verify-phase3.sh: header comment, PASS/FAIL counters, check functions, final summary.

    The script checks:
    1. Template file existence (6 files):
       - templates/.github/workflows/ci-node.yml
       - templates/.github/workflows/ci-python.yml
       - templates/.github/workflows/ci-both.yml
       - templates/.github/workflows/security.yml
       - templates/.github/workflows/dependency-review.yml
       - templates/.github/dependabot.yml
    2. Content correctness (use `grep -q`):
       - ci-node.yml contains `--if-present`
       - ci-python.yml contains `--extra dev`
       - ci-python.yml contains `python-version: ${{ matrix.python }}`
       - security.yml contains `queries: security-extended`
       - dependency-review.yml contains `pull_request` and does NOT contain `push:`
       - dependabot.yml contains `github-actions`, `npm`, and `pip`
    3. Unit test suites pass:
       - `cd packages/npm && npm test` exits 0
       - `cd packages/pip && uv run --extra dev pytest tests/ -x -q` exits 0

    The script accepts `--quick` flag (skip unit test run, check only file existence + content). Exit code 0 if all checks pass, 1 otherwise. Use `set -euo pipefail` only after variable declarations.

    When called with no args or `--quick`, run file + content checks only. Without `--quick`, also run unit tests.

    Final step — append a JOURNAL.md entry at the bottom of JOURNAL.md (create if missing). Entry format:
    ```
    ## YYYY-MM-DD — Phase 4 Plan 01: TDD scaffolding + smoke harness
    - Added detect-project-type.test.ts (5 failing RED tests for CI-05)
    - Added test_detect_project_type.py (5 failing RED tests for CI-05)
    - Added scripts/verify-phase4.sh (smoke harness for template file + content checks)
    - Tests are intentionally RED — implementation ships in Plan 03
    ```
  </action>
  <verify>
    <automated>bash /home/ygiokas/GoodVibes/scripts/verify-phase4.sh --quick 2>&1 | tail -5</automated>
  </verify>
  <done>verify-phase4.sh exists, is executable, runs without syntax errors, and exits non-zero (template files missing) when called with --quick. Final line contains FAIL count > 0 indicating missing templates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → filesystem | mkdtempSync / tmp_path write marker files; no user input crosses this boundary |
| verify script → filesystem | grep reads template files; no user-controlled paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | verify-phase4.sh path args | accept | script uses hardcoded relative paths from repo root; no user path input |
| T-04-01-SC | Tampering | npm/pip installs | accept | no new packages in this plan; existing packages already in lockfile |
</threat_model>

<verification>
After completing all tasks:
- packages/npm/src/utils/detect-project-type.test.ts exists with 5 test cases
- packages/pip/tests/test_detect_project_type.py exists with 5 test functions
- scripts/verify-phase4.sh exists and is executable
- `npm test` fails with "Cannot find module './detect-project-type.js'" (RED)
- `uv run --extra dev pytest tests/test_detect_project_type.py` fails with ImportError (RED)
- `bash scripts/verify-phase4.sh --quick` exits non-zero (templates missing)
</verification>

<success_criteria>
All three files exist. Tests are RED (import failures, not logic failures). Smoke harness exits non-zero. No existing tests broken.
</success_criteria>

<output>
Create `.planning/phases/04-ci-cd-scaffolding/04-01-SUMMARY.md` when done
</output>
