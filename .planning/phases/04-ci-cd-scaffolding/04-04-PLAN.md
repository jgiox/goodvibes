---
phase: 04-ci-cd-scaffolding
plan: 04
type: execute
wave: 2
depends_on: ["04-03"]
files_modified: []
autonomous: false
requirements: [CI-06]

must_haves:
  truths:
    - "A real GitHub repository initialized with goodvibes init passes CI on first push for a Node project"
    - "A real GitHub repository initialized with goodvibes init passes CI on first push for a Python project"
    - "security.yml and dependency-review.yml run without errors in the target repo"
    - "dependabot.yml is recognized by GitHub and shows update channels in the Dependabot tab"
  artifacts:
    - path: ".planning/phases/04-ci-cd-scaffolding/04-04-SUMMARY.md"
      provides: "Phase 4 complete sign-off with GitHub Actions run URLs"
  key_links:
    - from: "goodvibes init output"
      to: "ci.yml in target repo"
      via: "copyTemplates + rename"
      pattern: "ci.yml"
---

<objective>
Human verification checkpoint: confirm that generated GitHub Actions workflows pass on first push for both Node and Python projects with zero user edits.

Purpose: CI-06 cannot be verified automatically — it requires a real GitHub Actions run. This plan pauses for the developer to create test repos, run goodvibes init, push, and observe CI results.
Output: Confirmation that all generated workflows pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-ci-cd-scaffolding/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify generated CI passes on first push (Node project)</name>
  <what-built>
    Plan 02 authored ci-node.yml with: npm install, --if-present on build/test/lint, matrix node 20/22.
    Plan 03 wired detectProjectType so goodvibes init writes ci.yml (not ci-node.yml) when package.json is present.
  </what-built>
  <how-to-verify>
    1. Create a test directory: `mkdir /tmp/test-gv-node && cd /tmp/test-gv-node`
    2. Create a minimal package.json: `echo '{"name":"test","version":"1.0.0"}' > package.json`
    3. Run: `goodvibes init` (or `npx goodvibes init` if testing npm CLI)
    4. Verify .github/workflows/ci.yml was written (not ci-node.yml): `cat .github/workflows/ci.yml`
    5. Confirm ci.yml contains `npm install` (not `npm ci`) and `--if-present`
    6. Initialize git and push to a new public GitHub repo
    7. Go to the repo's Actions tab and wait for the CI run to complete
    8. Confirm: "CI" workflow is green for both Node 20 and Node 22 matrix entries
    9. Confirm: no ci-node.yml, ci-python.yml, or ci-both.yml exist in the repo (only ci.yml)
  </how-to-verify>
  <resume-signal>Type "node-ci-green" if CI passed, or describe which step failed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify generated CI passes on first push (Python project)</name>
  <what-built>
    Plan 02 authored ci-python.yml with: setup-uv@v8, python-version: ${{ matrix.python }}, uv run --extra dev pytest, conditional test execution.
    Plan 03 wired detect_project_type so goodvibes init writes ci.yml for Python projects.
  </what-built>
  <how-to-verify>
    1. Create a test directory: `mkdir /tmp/test-gv-python && cd /tmp/test-gv-python`
    2. Create a minimal pyproject.toml: `echo '[project]\nname = "test"\nversion = "0.1.0"' > pyproject.toml`
    3. Run: `goodvibes init` (pip CLI)
    4. Verify .github/workflows/ci.yml was written: `cat .github/workflows/ci.yml`
    5. Confirm ci.yml contains `setup-uv@v8`, `python-version: ${{ matrix.python }}`, and `--extra dev`
    6. Initialize git and push to a new public GitHub repo
    7. Go to the repo's Actions tab and wait for CI to complete
    8. Confirm: "CI" workflow is green for Python 3.10, 3.11, 3.12 (no tests found message is fine — not a failure)
    9. Check the "Security scan" workflow ran (CodeQL) and is green or in progress
    10. Check the Dependabot tab shows update channels for github-actions, npm, and pip
  </how-to-verify>
  <resume-signal>Type "python-ci-green" if CI passed, or describe which step failed</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify security.yml and dependency-review.yml functional</name>
  <what-built>
    security.yml: CodeQL with security-extended queries, weekly cron + push + PR triggers.
    dependency-review.yml: dependency-review-action@v5, pull_request trigger only.
  </what-built>
  <how-to-verify>
    Using one of the test repos from Task 1 or 2:
    1. Confirm the Security scan workflow ran (check Actions tab — it may still be queued)
    2. If it completed: confirm it shows 0 critical findings (not a failure)
    3. Create a pull request in the test repo (even a trivial change)
    4. Confirm the "Dependency Review" workflow appears and completes on the PR
    5. Confirm dependency-review-action did NOT run on the push event (only on the PR)
  </how-to-verify>
  <resume-signal>Type "security-green" after confirming, or describe any failures</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → GitHub | Push from local test repo; OIDC tokens managed by GitHub |
| GitHub Actions runner → npm/pip registry | Workflow installs user project's deps; not goodvibes-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Information Disclosure | CodeQL findings in public test repo | accept | test repos are throwaway; no real source code |
| T-04-04-02 | Elevation of Privilege | workflow permissions in test repo | mitigate | verified in Plan 02: security.yml has explicit permissions; ci.yml uses GitHub defaults (contents: read) |
| T-04-04-SC | Tampering | npm/pip installs | accept | no new package installs in this plan |
</threat_model>

<verification>
Phase 4 is complete when:
- Node CI (Task 1) green on first push
- Python CI (Task 2) green on first push
- Security scan (Task 3) passes or is in progress without errors
- dependency-review workflow appears on PR
</verification>

<success_criteria>
All three human verification checkpoints approved. Phase 4 complete — generated CI passes on first push for Node and Python projects with zero user configuration.
</success_criteria>

<output>
Create `.planning/phases/04-ci-cd-scaffolding/04-04-SUMMARY.md` when done
</output>
