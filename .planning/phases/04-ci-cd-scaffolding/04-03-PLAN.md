---
phase: 04-ci-cd-scaffolding
plan: 03
type: execute
wave: 1
depends_on: ["04-01", "04-02"]
files_modified:
  - packages/npm/src/utils/detect-project-type.ts
  - packages/npm/src/steps/copy-templates.ts
  - packages/npm/src/steps/copy-templates.test.ts
  - packages/npm/src/commands/init.ts
  - packages/pip/src/goodvibes_cli/utils/detect_project_type.py
  - packages/pip/src/goodvibes_cli/steps/copy_templates.py
  - packages/pip/tests/test_copy_templates.py
  - packages/pip/tests/conftest.py
  - packages/pip/src/goodvibes_cli/commands/init_cmd.py
autonomous: true
requirements: [CI-01, CI-02, CI-03, CI-04, CI-05]

must_haves:
  truths:
    - "npm test passes (all tests GREEN including the new detectProjectType tests from Plan 01)"
    - "uv run --extra dev pytest tests/ -x -q passes (all Python tests GREEN)"
    - "copyTemplates(templateDir, tmpDir, false, false, 'node') writes ci.yml (not ci-node.yml) and does not write ci-python.yml or ci-both.yml"
    - "copy_templates(template_dir, tmp_dir, project_type='python') writes ci.yml and does not write ci-node.yml or ci-both.yml"
    - "goodvibes init in a Node project directory writes .github/workflows/ci.yml with Node-specific content (npm install, --if-present) and no ci-python.yml or ci-both.yml file is present"
    - "init_cmd.py passes project_type from detect_project_type(cwd) to copy_templates"
  artifacts:
    - path: "packages/npm/src/utils/detect-project-type.ts"
      provides: "ProjectType type + detectProjectType(cwd) function"
      exports: ["ProjectType", "detectProjectType"]
    - path: "packages/pip/src/goodvibes_cli/utils/detect_project_type.py"
      provides: "detect_project_type(cwd) function returning node/python/both"
      min_lines: 15
    - path: "packages/npm/src/steps/copy-templates.ts"
      provides: "copyTemplates with projectType param, CI variant filter + rename"
      contains: "ci-${projectType}.yml"
    - path: "packages/pip/src/goodvibes_cli/steps/copy_templates.py"
      provides: "copy_templates with project_type param, CI variant filter + rename"
      contains: "ci_variants"
  key_links:
    - from: "packages/npm/src/commands/init.ts"
      to: "packages/npm/src/utils/detect-project-type.ts"
      via: "import { detectProjectType } from '../utils/detect-project-type.js'"
      pattern: "detectProjectType"
    - from: "packages/npm/src/steps/copy-templates.ts"
      to: "templates/.github/workflows/ci-node.yml"
      via: "rename variantPath → ci.yml after copy()"
      pattern: "rename.*ci.yml"
    - from: "packages/pip/src/goodvibes_cli/commands/init_cmd.py"
      to: "packages/pip/src/goodvibes_cli/utils/detect_project_type.py"
      via: "from goodvibes_cli.utils.detect_project_type import detect_project_type"
      pattern: "detect_project_type"
---

<objective>
Implement detectProjectType utilities and extend copy-templates in both the npm and pip CLIs to select and rename the correct CI variant. Wire detection into init callers. Turn RED tests GREEN.

Purpose: This is the implementation wave — brings Plan 01's failing tests GREEN and connects the detection logic to the copy pipeline.
Output: 9 files created or modified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-ci-cd-scaffolding/04-RESEARCH.md
@.planning/phases/04-ci-cd-scaffolding/04-PATTERNS.md
@.planning/phases/04-ci-cd-scaffolding/04-01-SUMMARY.md
@.planning/phases/04-ci-cd-scaffolding/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement detectProjectType (TS) and extend copyTemplates with CI variant selection</name>
  <files>
    packages/npm/src/utils/detect-project-type.ts
    packages/npm/src/steps/copy-templates.ts
    packages/npm/src/steps/copy-templates.test.ts
    packages/npm/src/commands/init.ts
    JOURNAL.md
  </files>
  <read_first>
    packages/npm/src/utils/detect-project-type.test.ts
    packages/npm/src/steps/copy-templates.ts
    packages/npm/src/steps/copy-templates.test.ts
    packages/npm/src/commands/init.ts
    packages/npm/src/utils/detect-python.ts
    .planning/phases/04-ci-cd-scaffolding/04-PATTERNS.md
  </read_first>
  <behavior>
    detect-project-type.ts:
    - exports `type ProjectType = 'node' | 'python' | 'both'`
    - exports `function detectProjectType(cwd: string): ProjectType`
    - uses `existsSync` from 'node:fs' and `join` from 'node:path'
    - returns 'node' when only package.json present
    - returns 'python' when pyproject.toml or requirements.txt present (but not package.json)
    - returns 'both' when both node and python markers present
    - returns 'both' when neither marker present (safe default)

    copy-templates.ts changes:
    - adds `projectType: ProjectType = 'both'` param to copyTemplates signature
    - filter skips the two non-selected CI variant names: ci_variants = ['ci-node.yml', 'ci-python.yml', 'ci-both.yml']; skip any where filename matches a variant != selectedVariant
    - after copy(), renames selected variant (`ci-${projectType}.yml`) to `ci.yml` — uses `rename` from 'node:fs/promises'; check existsSync before renaming
    - return value change: currently returns `listTemplateFiles(templateDir)` which returns source template paths. Change behavior based on mode:
      - Non-dry-run (dryRun=false): walk destDir after copy and return relative paths from destDir. This ensures the completion output shows `ci.yml` not `ci-node.yml` (per RESEARCH.md Pitfall 6).
      - Dry-run (dryRun=true): destDir is empty/untouched, so walking it returns an empty list. Instead return `listTemplateFiles(templateDir)` filtered to exclude the two non-selected CI variants (i.e., include selectedVariant, exclude the other two from ciVariants). This preserves --dry-run behaviour (NPM-07) by returning the list of files that *would* be written.
  </behavior>
  <action>
    Step 1 — Create packages/npm/src/utils/detect-project-type.ts. Import `existsSync` from 'node:fs' and `join` from 'node:path'. Export `ProjectType` type and `detectProjectType` function per the PATTERNS.md Pattern 1 code excerpt exactly. No error handling (existsSync never throws). File is ~15 lines.

    Step 2 — Modify packages/npm/src/steps/copy-templates.ts:
    - Add import: `import { rename } from 'node:fs/promises'`
    - Add import: `import { detectProjectType, type ProjectType } from '../utils/detect-project-type.js'` (add type import — ProjectType is only needed for the signature)
    - Change `copyTemplates` signature: add `projectType: ProjectType = 'both'` as 5th param (optional with default)
    - Before the `copy()` call, define `const ciVariants = ['ci-node.yml', 'ci-python.yml', 'ci-both.yml']` and `const selectedVariant = \`ci-${projectType}.yml\``
    - Inside the `filter` callback, after the existing guards (CLAUDE.md, minimal+workflows, path traversal), add: for each variant in ciVariants, if src ends with that variant and variant !== selectedVariant, return false
    - After the `copy()` call (before mergeClaude), add: if `!minimal`, attempt rename `join(destDir, '.github', 'workflows', selectedVariant)` → `join(destDir, '.github', 'workflows', 'ci.yml')`; guard with existsSync check before rename
    - Change the return at the end: instead of `listTemplateFiles(templateDir)`, apply the dry-run conditional:
      - If `dryRun === true`: return `listTemplateFiles(templateDir).filter(p => !ciVariants.some(v => p.endsWith(v) && v !== selectedVariant))` — preserves --dry-run output without walking empty destDir.
      - If `dryRun === false`: walk destDir with the existing `walk()` pattern (or inline `readdirRecursive`) and return relative paths from destDir. This is a copy of the walk logic but targeting destDir, not templateDir.

    Step 3 — Add CI variant selection tests to packages/npm/src/steps/copy-templates.test.ts. Add a new `describe` block `'copyTemplates — CI variant selection'` with 3 `it()` tests:
    - `writes ci.yml (not ci-node.yml) when projectType is node` — call `copyTemplates(templateDir, tmpDir, false, false, 'node')`; assert existsSync ci.yml is true, ci-node.yml is false
    - `does not write ci-python.yml or ci-both.yml when projectType is node`
    - `writes ci.yml when projectType is python`

    The `templateDir` for these tests should be `resolveTemplatesDir()` — the real templates directory now contains ci-node.yml etc. (from Plan 02).

    Step 4 — Modify packages/npm/src/commands/init.ts:
    - Add import: `import { detectProjectType } from '../utils/detect-project-type.js'`
    - In the `.action()` handler, after `const cwd = process.cwd()`, add: `const projectType = detectProjectType(cwd)`
    - In the copyTemplates call inside the task: `await copyTemplates(templateDir, cwd, false, minimal, projectType)`

    Do NOT add fenced code blocks to this action. Read the current files first (listed in read_first) to understand exact line numbers before making edits.

    Final step — append a JOURNAL.md entry at the bottom of JOURNAL.md (create if missing). Entry format:
    ```
    ## YYYY-MM-DD — Phase 4 Plan 03 Task 1: detectProjectType (TS) + copyTemplates CI variant selection
    - Created packages/npm/src/utils/detect-project-type.ts (ProjectType + detectProjectType)
    - Modified packages/npm/src/steps/copy-templates.ts (projectType param, CI filter, rename, dest-walk return)
    - Added CI variant selection tests to copy-templates.test.ts
    - Wired detectProjectType into packages/npm/src/commands/init.ts
    - npm test GREEN (all detect-project-type and CI variant tests pass)
    ```
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | tail -5</automated>
  </verify>
  <done>npm test exits 0. All tests pass including the 5 new detectProjectType tests from Plan 01 and the 3 new CI variant selection tests added here.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement detect_project_type (Python) and extend copy_templates with CI variant selection</name>
  <files>
    packages/pip/src/goodvibes_cli/utils/detect_project_type.py
    packages/pip/src/goodvibes_cli/steps/copy_templates.py
    packages/pip/tests/test_copy_templates.py
    packages/pip/tests/conftest.py
    packages/pip/src/goodvibes_cli/commands/init_cmd.py
    JOURNAL.md
  </files>
  <read_first>
    packages/pip/tests/test_detect_project_type.py
    packages/pip/src/goodvibes_cli/utils/detect_python.py
    packages/pip/src/goodvibes_cli/steps/copy_templates.py
    packages/pip/tests/test_copy_templates.py
    packages/pip/tests/conftest.py
    packages/pip/src/goodvibes_cli/commands/init_cmd.py
    .planning/phases/04-ci-cd-scaffolding/04-PATTERNS.md
  </read_first>
  <behavior>
    detect_project_type.py:
    - module docstring: "Detect the target project type by inspecting marker files."
    - from __future__ import annotations
    - import pathlib
    - def detect_project_type(cwd: pathlib.Path) -> str: returns 'node', 'python', or 'both'
    - logic: has_node = (cwd / "package.json").exists(); has_python = (cwd / "pyproject.toml").exists() or (cwd / "requirements.txt").exists()
    - returns 'both' when both present, 'node' when only node, 'python' when only python, 'both' when neither

    copy_templates.py changes:
    - adds `project_type: str = "both"` param to copy_templates signature
    - ignore_fn skips the two non-selected CI variants: `ci_variants = {"ci-node.yml", "ci-python.yml", "ci-both.yml"}`; `selected_variant = f"ci-{project_type}.yml"`; inside loop if name in ci_variants and name != selected_variant: ignored.add(name)
    - after shutil.copytree(), renames selected variant to ci.yml: `variant_path = dest_dir / ".github" / "workflows" / selected_variant`; `ci_path = dest_dir / ".github" / "workflows" / "ci.yml"`; if variant_path.exists(): variant_path.rename(ci_path)
    - return value change: walk destDir and return relative paths (so output shows ci.yml, not ci-node.yml). Change `return list_template_files(template_dir)` to walk dest_dir and return sorted relative paths.
  </behavior>
  <action>
    Step 1 — Create packages/pip/src/goodvibes_cli/utils/detect_project_type.py per the PATTERNS.md Pattern 1 Python code excerpt. File starts with module docstring, then `from __future__ import annotations`, then `import pathlib`. Export only `detect_project_type`. File is ~15 lines.

    Step 2 — Modify packages/pip/src/goodvibes_cli/steps/copy_templates.py:
    - Add `project_type: str = "both"` as 5th param to `copy_templates` signature (after minimal)
    - Before `def ignore_fn`, define: `ci_variants = {"ci-node.yml", "ci-python.yml", "ci-both.yml"}` and `selected_variant = f"ci-{project_type}.yml"`
    - Inside `ignore_fn`, after the existing guards and before `return ignored`, add: `if name in ci_variants and name != selected_variant: ignored.add(name)`
    - After the `shutil.copytree()` call, before the CLAUDE.md merge block, add: `variant_path = dest_dir / ".github" / "workflows" / selected_variant`; `ci_path = dest_dir / ".github" / "workflows" / "ci.yml"`; `if variant_path.exists(): variant_path.rename(ci_path)`
    - Change the final return: instead of `return list_template_files(template_dir)`, return paths relative to dest_dir using the same rglob pattern but targeting dest_dir: `return sorted(str(f.relative_to(dest_dir)) for f in dest_dir.rglob("*") if f.is_file())`

    Step 3 — Update packages/pip/tests/conftest.py `template_dir` fixture: add stub CI variant files so copy_templates tests that use this fixture have variants to filter. Add to the fixture body after creating `workflows/ci.yml`:
    - `(workflows / "ci-node.yml").write_text("name: CI\n")`
    - `(workflows / "ci-python.yml").write_text("name: CI\n")`
    - `(workflows / "ci-both.yml").write_text("name: CI\n")`
    Also remove `(workflows / "ci.yml").write_text("name: CI\n")` from conftest — the fixture should no longer pre-create ci.yml; the copy logic creates it via rename.

    Step 4 — Update packages/pip/tests/test_copy_templates.py: add 3 new tests in a clearly labelled section `# --- CI variant selection tests ---`:
    - `test_copy_templates_writes_ci_yml_not_ci_node_yml_for_node_project`: call `copy_templates(template_dir, tmp_dir, project_type="node")`; assert `(tmp_dir / ".github" / "workflows" / "ci.yml").exists()` and not `(tmp_dir / ".github" / "workflows" / "ci-node.yml").exists()`
    - `test_copy_templates_does_not_write_other_variants_for_node_project`: assert ci-python.yml and ci-both.yml do not exist in tmpdir
    - `test_copy_templates_writes_ci_yml_for_python_project`: project_type="python", assert ci.yml exists

    Update the existing `test_copy_templates_skips_github_workflows_when_minimal` and `test_copy_templates_github_workflows_copied_when_not_minimal` tests: these reference `ci.yml` in the fixture. The fixture no longer pre-creates `ci.yml`. Update `test_copy_templates_github_workflows_copied_when_not_minimal` to call `copy_templates(template_dir, tmp_dir, minimal=False)` (no project_type — defaults to "both") and assert `(tmp_dir / ".github" / "workflows" / "ci.yml").exists()`.

    Step 5 — Modify packages/pip/src/goodvibes_cli/commands/init_cmd.py:
    - Add import: `from goodvibes_cli.utils.detect_project_type import detect_project_type`
    - In `init_cmd`, after `cwd = pathlib.Path.cwd()`, add: `project_type = detect_project_type(cwd)`
    - Change the `copy_templates(...)` call: pass `project_type=project_type`

    Do NOT place fenced code blocks in this action. Read all listed files before editing.

    Final step — append a JOURNAL.md entry at the bottom of JOURNAL.md (create if missing). Entry format:
    ```
    ## YYYY-MM-DD — Phase 4 Plan 03 Task 2: detect_project_type (Python) + copy_templates CI variant selection
    - Created packages/pip/src/goodvibes_cli/utils/detect_project_type.py (detect_project_type function)
    - Modified packages/pip/src/goodvibes_cli/steps/copy_templates.py (project_type param, CI filter, rename, dest-walk return)
    - Added CI variant selection tests to test_copy_templates.py; updated conftest.py fixtures
    - Wired detect_project_type into packages/pip/src/goodvibes_cli/commands/init_cmd.py
    - pytest GREEN (all detect_project_type and CI variant tests pass)
    ```
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run --extra dev pytest tests/ -x -q 2>&1 | tail -10</automated>
  </verify>
  <done>pytest exits 0. All tests pass including the 5 new detect_project_type tests from Plan 01 and the 3 new CI variant selection tests added here. No existing test broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user cwd → detectProjectType | cwd is process.cwd() / pathlib.Path.cwd() — controlled by the shell, not user-supplied string |
| detectProjectType → copy-templates | projectType is one of three literal values; no injection surface |
| copy-templates filter → dest filesystem | CI variant name is `ci-${projectType}.yml`; projectType is a controlled enum; no path traversal possible |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | detectProjectType cwd input | accept | cwd is process.cwd() — OS-level; not user-supplied; join(cwd, 'package.json') sanitizes any relative path components |
| T-04-03-02 | Tampering | CI variant rename path | mitigate | selectedVariant is constructed from `ci-${projectType}.yml` where projectType is one of three string literals; no path traversal possible; existsSync guard prevents rename of non-existent file |
| T-04-03-03 | Information Disclosure | copy-templates return value | accept | returning dest paths instead of template paths is a UX improvement; no secrets in file paths |
| T-04-03-SC | Tampering | npm/pip installs | accept | no new packages; existsSync and pathlib.Path.exists() are stdlib |
</threat_model>

<verification>
After both tasks:
- `cd packages/npm && npm test` exits 0 — all tests green including Plan 01 failing tests
- `cd packages/pip && uv run --extra dev pytest tests/ -x -q` exits 0 — all tests green including Plan 01 failing tests
- `bash scripts/verify-phase4.sh` exits 0 (unit tests pass, all template files present with correct content)
- detect-project-type.ts exports ProjectType and detectProjectType
- detect_project_type.py exports detect_project_type
- copyTemplates accepts 5th param `projectType: ProjectType = 'both'`
- copy_templates accepts `project_type: str = "both"`
</verification>

<success_criteria>
Full test suite (npm + pip) green. verify-phase4.sh exits 0. No regressions in existing tests.
</success_criteria>

<output>
Create `.planning/phases/04-ci-cd-scaffolding/04-03-SUMMARY.md` when done
</output>
