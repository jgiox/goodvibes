---
phase: "11"
plan: "04"
type: execute
wave: 3
depends_on:
  - "11-01"
  - "11-02"
  - "11-03"
files_modified:
  - packages/pip-tombstone/pyproject.toml
  - packages/npm-tombstone/package.json
  - JOURNAL.md
autonomous: false
requirements:
  - PKG-01
  - PKG-02

must_haves:
  truths:
    - "PyPI trusted publishing is configured for goodvibes-cli before any pip-v* tag publish is attempted"
    - "A deprecation stub for jgiox-goodvibes exists and auto-installs goodvibes-cli as a dependency"
    - "npm deprecate command is run against @jgiox/goodvibes"
    - "JOURNAL.md is updated with Phase 11 completion"
  artifacts:
    - path: "packages/pip-tombstone/pyproject.toml"
      provides: "Stub wheel for jgiox-goodvibes that depends on goodvibes-cli"
      contains: "goodvibes-cli"
    - path: "packages/npm-tombstone/package.json"
      provides: "Documentation of npm deprecate command to run manually"
      contains: "goodvibes-cli"
  key_links:
    - from: "packages/pip-tombstone/pyproject.toml"
      to: "PyPI jgiox-goodvibes project"
      via: "dependencies = ['goodvibes-cli']"
      pattern: "goodvibes-cli"
---

<objective>
Human checkpoint for PyPI OIDC trusted publisher configuration (blocking — must happen before first goodvibes-cli publish to PyPI), plus creation of tombstone stubs so users still on the old package names get auto-redirected. JOURNAL.md update closes the phase. Per PKG-01 (deprecation strategy) and PKG-02 (registry page completeness requires successful publish).

Purpose: The PyPI OIDC setup for goodvibes-cli is a one-time human action on pypi.org that cannot be automated. Without it, the publish-pip.yml workflow will fail with OIDC token mismatch. The tombstone stubs ensure users who installed @jgiox/goodvibes or jgiox-goodvibes before the rename get a clear redirect.

Output: PyPI OIDC configured, tombstone stubs authored, npm deprecate run, JOURNAL.md updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-publish-quality-discoverability/11-RESEARCH.md
@.planning/phases/11-publish-quality-discoverability/11-01-SUMMARY.md
@.planning/phases/11-publish-quality-discoverability/11-02-SUMMARY.md
@.planning/phases/11-publish-quality-discoverability/11-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-03 completed all automated changes: package rename, CI gates, smoke tests, UX patches. The next step requires PyPI to be configured for the new goodvibes-cli package before any pip-v* tag is pushed. This is a one-time manual action on pypi.org that Claude cannot perform.
  </what-built>
  <how-to-verify>
    1. Go to https://pypi.org and log in with the jgiox account.
    2. Navigate to Account Settings -> Publishing -> Add a new publisher.
    3. Select "GitHub Actions" as the publishing method.
    4. Fill in:
       - PyPI project name: goodvibes-cli
       - GitHub owner: jgiox
       - GitHub repository: goodvibes
       - Workflow filename: publish-pip.yml
       - Environment name: release
    5. Click "Add" to save the trusted publisher.
    6. Verify the publisher appears in the list for goodvibes-cli.
    7. Also run: `npm deprecate "@jgiox/goodvibes@*" "Renamed to goodvibes-cli. Install: npm install -g goodvibes-cli"` from a terminal where you are logged into npm (run `npm whoami` first to confirm auth).
  </how-to-verify>
  <resume-signal>Type "configured" when PyPI trusted publisher is set up and npm deprecate has been run</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Create pip tombstone stub and npm tombstone documentation</name>
  <files>packages/pip-tombstone/pyproject.toml, packages/npm-tombstone/package.json</files>
  <read_first>
    packages/pip/pyproject.toml
  </read_first>
  <action>
    Create directory packages/pip-tombstone/ and create packages/pip-tombstone/pyproject.toml with this exact content:

    ```toml
    # ponytail: minimal stub — redirects users from old PyPI name to goodvibes-cli
    [project]
    name = "jgiox-goodvibes"
    version = "2.0.0"
    description = "Renamed to goodvibes-cli. Run: pip install goodvibes-cli"
    dependencies = ["goodvibes-cli"]
    requires-python = ">=3.10"
    license = { text = "Apache-2.0" }

    [build-system]
    requires = ["hatchling"]
    build-backend = "hatchling.build"
    ```

    Version 2.0.0 signals a breaking change to users who have jgiox-goodvibes pinned. The `dependencies = ["goodvibes-cli"]` causes pip to auto-install goodvibes-cli when jgiox-goodvibes is installed. No source code, no hatch_build.py, no tests — this is a one-file package.

    Create directory packages/npm-tombstone/ and create packages/npm-tombstone/package.json with this exact content:

    ```json
    {
      "name": "@jgiox/goodvibes",
      "version": "2.0.0",
      "description": "DEPRECATED — Renamed to goodvibes-cli. Install: npm install -g goodvibes-cli",
      "_deprecation_note": "This stub is NOT published to npm. npm deprecate command was run manually: npm deprecate '@jgiox/goodvibes@*' 'Renamed to goodvibes-cli. Install: npm install -g goodvibes-cli'",
      "license": "Apache-2.0"
    }
    ```

    The npm-tombstone package.json is documentation only — the npm deprecate CLI command (run in the human checkpoint above) is the actual mechanism for npm. Unlike PyPI, npm's deprecation is a registry-level flag, not a stub package.

    Update JOURNAL.md with a Phase 11 completion entry including: date (today), summary (package rename to goodvibes-cli, CI gates PUB-01/PUB-02, doctor version POL-01, upgrade labels POL-02, PyPI OIDC configured, tombstone stubs), files changed (all 11-01 through 11-04 files), tests run (npm test, uv run pytest), docs updated (README badges, JOURNAL.md).
  </action>
  <verify>
    <automated>test -f /home/ygiokas/GoodVibes/packages/pip-tombstone/pyproject.toml && grep -c "goodvibes-cli" /home/ygiokas/GoodVibes/packages/pip-tombstone/pyproject.toml</automated>
  </verify>
  <done>
    - packages/pip-tombstone/pyproject.toml exists with name = "jgiox-goodvibes", version = "2.0.0", dependencies = ["goodvibes-cli"]
    - packages/npm-tombstone/package.json exists with deprecation documentation
    - JOURNAL.md has a new entry for Phase 11 completion
    - Both directories are committed to git
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → pypi.org | OIDC trusted publisher configuration requires authenticated human action on PyPI |
| tombstone stub → pip resolver | pip follows dependencies in the stub to install goodvibes-cli — pip's dependency resolver is the trust mechanism |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Spoofing | PyPI stub redirects users | accept | Redirect is opt-in via install; pip's dependency resolver handles goodvibes-cli install; no deception |
| T-11-04-02 | Tampering | OIDC configuration on pypi.org | mitigate | Human checkpoint ensures OIDC is configured before any publish tag is pushed; workflow fails loudly if not configured |
| T-11-04-SC | Tampering | npm/pip installs | accept | No new runtime packages installed; pip-tombstone is a separate one-off publish |
</threat_model>

<verification>
- PyPI goodvibes-cli trusted publisher configured (human-verified in checkpoint)
- npm deprecate run for @jgiox/goodvibes (human-verified in checkpoint)
- packages/pip-tombstone/pyproject.toml exists and contains "goodvibes-cli" in dependencies
- packages/npm-tombstone/package.json exists with deprecation note
- JOURNAL.md has Phase 11 entry dated today
- `grep "goodvibes-cli" packages/pip-tombstone/pyproject.toml` — found in dependencies line
</verification>

<success_criteria>
PyPI OIDC trusted publisher configured for goodvibes-cli under the jgiox account, matching the publish-pip.yml workflow and "release" environment. npm @jgiox/goodvibes marked deprecated with redirect message. Tombstone stubs in place. JOURNAL.md updated. Phase 11 complete — goodvibes is installable as goodvibes-cli from both public registries, with CI smoke tests gating every future publish.
</success_criteria>

<output>
Create `.planning/phases/11-publish-quality-discoverability/11-04-SUMMARY.md` when done.
</output>
