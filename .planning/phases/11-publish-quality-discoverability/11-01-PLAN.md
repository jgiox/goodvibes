---
phase: "11"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/npm/package.json
  - packages/npm/src/commands/upgrade.ts
  - packages/npm/src/commands/upgrade.test.ts
  - packages/pip/pyproject.toml
  - packages/pip/src/goodvibes_cli/main.py
  - packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py
  - packages/pip/tests/test_main.py
  - .github/workflows/publish-pip.yml
  - README.md
autonomous: true
requirements:
  - PKG-01

must_haves:
  truths:
    - "goodvibes is installable as goodvibes-cli on both npm and PyPI"
    - "Running goodvibes --version, goodvibes upgrade, and goodvibes init all work without PackageNotFoundError after rename"
    - "No in-repo source file references the old names @jgiox/goodvibes or jgiox-goodvibes (except uv.lock which is auto-generated)"
  artifacts:
    - path: "packages/npm/package.json"
      provides: "npm package manifest with name: goodvibes-cli"
      contains: "goodvibes-cli"
    - path: "packages/pip/pyproject.toml"
      provides: "pip package manifest with name = goodvibes-cli"
      contains: "goodvibes-cli"
    - path: "packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py"
      provides: "upgrade command with all jgiox-goodvibes references replaced"
      contains: "goodvibes-cli"
    - path: "packages/pip/src/goodvibes_cli/main.py"
      provides: "version callback using goodvibes-cli metadata key"
      contains: "goodvibes-cli"
    - path: "packages/npm/src/commands/upgrade.ts"
      provides: "upgrade command referencing goodvibes-cli on npm"
      contains: "goodvibes-cli"
  key_links:
    - from: "packages/pip/src/goodvibes_cli/main.py"
      to: "importlib.metadata.version"
      via: "version('goodvibes-cli')"
      pattern: "goodvibes-cli"
    - from: "packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py"
      to: "PyPI JSON API"
      via: "_PYPI_URL"
      pattern: "pypi.org/pypi/goodvibes-cli/json"
    - from: "packages/npm/src/commands/upgrade.ts"
      to: "npm registry"
      via: "npm view goodvibes-cli version"
      pattern: "goodvibes-cli"
---

<objective>
Rename both packages from personal-handle-prefixed names (@jgiox/goodvibes / jgiox-goodvibes) to goodvibes-cli on both registries. Update every in-repo call site atomically so the CLI continues to function without PackageNotFoundError or broken self-update URLs. This is the highest-risk task in Phase 11 — a partial rename breaks the CLI silently. Per PKG-01.

Purpose: goodvibes-cli is discoverable by name without knowing the maintainer's handle. The name is verified available on both npm (good-vibes-cli does not exist) and PyPI.

Output: Six source files updated, all tests updated to match new package name references, README badges updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-publish-quality-discoverability/11-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rename npm package and update all npm-side call sites</name>
  <files>packages/npm/package.json, packages/npm/src/commands/upgrade.ts, packages/npm/src/commands/upgrade.test.ts</files>
  <read_first>
    packages/npm/package.json
    packages/npm/src/commands/upgrade.ts
    packages/npm/src/commands/upgrade.test.ts
  </read_first>
  <behavior>
    - upgrade.test.ts line 184: assert execa called with 'npm', ['install', '-g', 'goodvibes-cli@1.0.1'] (was '@jgiox/goodvibes@1.0.1')
    - upgrade.test.ts line 214: assert execa NOT called with 'npm', ['view', 'goodvibes-cli', 'version'] (was '@jgiox/goodvibes')
    - All other upgrade.test.ts assertions remain unchanged
  </behavior>
  <action>
    In packages/npm/package.json:
    - Change "name" from "@jgiox/goodvibes" to "goodvibes-cli".
    - Add "homepage": "https://github.com/jgiox/goodvibes" (field not currently present).
    - Do not change version, description, keywords, repository, license, scripts, dependencies, or devDependencies.

    In packages/npm/src/commands/upgrade.ts:
    - Line 18: change `['view', '@jgiox/goodvibes', 'version']` to `['view', 'goodvibes-cli', 'version']`.
    - Line 36: change `` `@jgiox/goodvibes@${version}` `` to `` `goodvibes-cli@${version}` `` in the npm install -g call.
    - Do not change any other logic, imports, or exports.

    In packages/npm/src/commands/upgrade.test.ts:
    - Line 184: update the expected execa call from `'@jgiox/goodvibes@1.0.1'` to `'goodvibes-cli@1.0.1'`.
    - Line 214: update the not-called assertion from `['view', '@jgiox/goodvibes', 'version']` to `['view', 'goodvibes-cli', 'version']`.
    - Do not change any other test assertions.

    Run tests to confirm green: `cd packages/npm && npm test`.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test</automated>
  </verify>
  <done>
    - packages/npm/package.json "name" field equals "goodvibes-cli"
    - packages/npm/package.json has "homepage" field set to "https://github.com/jgiox/goodvibes"
    - packages/npm/src/commands/upgrade.ts contains "goodvibes-cli" in both npm view and npm install calls
    - npm test passes with no failures
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rename pip package and update all pip-side call sites</name>
  <files>packages/pip/pyproject.toml, packages/pip/src/goodvibes_cli/main.py, packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py, packages/pip/tests/test_main.py, .github/workflows/publish-pip.yml, README.md</files>
  <read_first>
    packages/pip/pyproject.toml
    packages/pip/src/goodvibes_cli/main.py
    packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py
    packages/pip/tests/test_main.py
    .github/workflows/publish-pip.yml
    README.md
  </read_first>
  <behavior>
    - test_main.py test_version_output_includes_installed_version: change `importlib.metadata.version("jgiox-goodvibes")` to `importlib.metadata.version("goodvibes-cli")` — this test uses the real metadata so the package name must be correct
    - All other test_main.py assertions remain unchanged
  </behavior>
  <action>
    In packages/pip/pyproject.toml:
    - Line 2: change `name = "jgiox-goodvibes"` to `name = "goodvibes-cli"`.
    - Add classifiers array immediately after the keywords line (before dependencies):
      ```
      classifiers = [
          "Development Status :: 5 - Production/Stable",
          "Intended Audience :: Developers",
          "Environment :: Console",
          "Operating System :: OS Independent",
          "Programming Language :: Python :: 3",
          "Programming Language :: Python :: 3.10",
          "Programming Language :: Python :: 3.11",
          "Programming Language :: Python :: 3.12",
          "Topic :: Software Development",
          "Topic :: Software Development :: Code Generators",
          "Topic :: Utilities",
      ]
      ```
    - Add under [project.urls]: `Repository = "https://github.com/jgiox/goodvibes"` (Homepage is already present).
    - Do not change version, description, keywords, requires-python, license, dependencies, build-system, hatch config, or pytest config.

    In packages/pip/src/goodvibes_cli/main.py:
    - Line 14: change `importlib.metadata.version("jgiox-goodvibes")` to `importlib.metadata.version("goodvibes-cli")`.
    - Do not change any other lines.

    In packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py:
    - Line 30: change `_PYPI_URL = "https://pypi.org/pypi/jgiox-goodvibes/json"` to `_PYPI_URL = "https://pypi.org/pypi/goodvibes-cli/json"`.
    - Line 36: change `importlib.metadata.version("jgiox-goodvibes")` to `importlib.metadata.version("goodvibes-cli")`.
    - Line 52: change `subprocess.run(["uv", "tool", "upgrade", "jgiox-goodvibes"], check=True)` to `subprocess.run(["uv", "tool", "upgrade", "goodvibes-cli"], check=True)`.
    - Line 55: change `[sys.executable, "-m", "pip", "install", "--upgrade", "jgiox-goodvibes"]` to `[sys.executable, "-m", "pip", "install", "--upgrade", "goodvibes-cli"]`.
    - Do not change any other lines.

    In packages/pip/tests/test_main.py:
    - Line 77: change `importlib.metadata.version("jgiox-goodvibes")` to `importlib.metadata.version("goodvibes-cli")`.
    - Do not change any other lines.

    In .github/workflows/publish-pip.yml:
    - Update the "Verify wheel name" step from:
      `test -f packages/pip/dist/jgiox_goodvibes-*.whl`
      to:
      `test -f packages/pip/dist/goodvibes_cli-*.whl`
    - Update the step name comment from `(D-01 — jgiox-goodvibes)` to `(PKG-01 — goodvibes-cli)`.
    - Do not change any other lines.

    In README.md, update these specific lines only:
    - Badge line 5: change `https://img.shields.io/npm/v/%40jgiox%2Fgoodvibes?style=flat-square` to `https://img.shields.io/npm/v/goodvibes-cli?style=flat-square` and the link href from `https://www.npmjs.com/package/@jgiox/goodvibes` to `https://www.npmjs.com/package/goodvibes-cli`.
    - Badge line 6: change `https://img.shields.io/pypi/v/jgiox-goodvibes?style=flat-square` to `https://img.shields.io/pypi/v/goodvibes-cli?style=flat-square` and the link href from `https://pypi.org/project/jgiox-goodvibes/` to `https://pypi.org/project/goodvibes-cli/`.
    - Line 13: change `npx @jgiox/goodvibes init` to `npx goodvibes-cli init`.
    - Line 19: change `pip install jgiox-goodvibes` to `pip install goodvibes-cli`.
    - Line 52: change `npx @jgiox/goodvibes init` to `npx goodvibes-cli init`.
    - Line 53: change `pip install jgiox-goodvibes` to `pip install goodvibes-cli`.
    - Do not change any other README lines (CI badge, license badge, all other content).

    Run tests: `cd packages/pip && uv run pytest tests/ -x -q`.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/ -x -q</automated>
  </verify>
  <done>
    - packages/pip/pyproject.toml name field equals "goodvibes-cli"
    - packages/pip/pyproject.toml has classifiers array with 11 entries
    - packages/pip/pyproject.toml [project.urls] has both Homepage and Repository
    - packages/pip/src/goodvibes_cli/main.py references "goodvibes-cli" in version callback
    - packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py has 0 occurrences of "jgiox-goodvibes"
    - packages/pip/tests/test_main.py references "goodvibes-cli" in the version test
    - publish-pip.yml wheel check matches goodvibes_cli-*.whl
    - README.md npm and PyPI badges and install commands reference goodvibes-cli
    - uv run pytest tests/ -x -q passes with no failures
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source files → registry | Package manifest names are read by npm/PyPI at publish time; wrong name = wrong registry entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | importlib.metadata.version call | mitigate | Update all 3 call sites atomically in this plan; tests fail if any are missed |
| T-11-01-02 | Tampering | upgrade self-update URLs | mitigate | Both npm view and PyPI JSON API URLs updated in same plan; test assertions verify new package name |
| T-11-01-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan; only package.json name field and pyproject.toml name field changed |
</threat_model>

<verification>
Run both test suites after the plan executes:
- `cd packages/npm && npm test` — all tests pass
- `cd packages/pip && uv run pytest tests/ -x -q` — all tests pass
- `grep -r "jgiox-goodvibes" packages/pip/src packages/npm/src` — no output (all replaced)
- `grep -r "@jgiox/goodvibes" packages/npm/src packages/npm/package.json` — no output (all replaced)
- `grep '"name"' packages/npm/package.json` — output: "name": "goodvibes-cli"
- `grep '^name' packages/pip/pyproject.toml` — output: name = "goodvibes-cli"
</verification>

<success_criteria>
Both packages renamed to goodvibes-cli. Zero in-repo source references to old names remain (uv.lock excluded — auto-generated). Both test suites pass. README install commands and badges updated to new names.
</success_criteria>

<output>
Create `.planning/phases/11-publish-quality-discoverability/11-01-SUMMARY.md` when done.
</output>
