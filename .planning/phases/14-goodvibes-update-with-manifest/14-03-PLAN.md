---
phase: 14
plan: 03
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - packages/pip/src/goodvibes_cli/steps/write_manifest.py
  - packages/pip/tests/test_write_manifest.py
  - packages/pip/src/goodvibes_cli/commands/init_cmd.py
  - packages/pip/tests/test_init_cmd.py
autonomous: true
requirements:
  - UPD-01
must_haves:
  truths:
    - "After goodvibes init completes (Python CLI), .goodvibes.json exists in the project root"
    - ".goodvibes.json contains 'version' string and 'files' dict mapping each written file path to its SHA-256 hex digest"
    - ".goodvibes.json does NOT list itself in its 'files' map"
    - "write_manifest hashes DEST file bytes (read_bytes), not template source"
    - "read_manifest returns None when .goodvibes.json is absent or contains malformed JSON"
    - "init_cmd calls write_manifest once after copy_templates succeeds, in the non-dry-run path only"
  artifacts:
    - path: "packages/pip/src/goodvibes_cli/steps/write_manifest.py"
      provides: "write_manifest() and read_manifest() plain functions + MANIFEST_PATH constant"
      contains: "hashlib.sha256"
    - path: "packages/pip/tests/test_write_manifest.py"
      provides: "Unit tests for write_manifest and read_manifest using tmp_dir fixture"
      contains: "test_write_manifest_creates_goodvibes_json"
    - path: "packages/pip/src/goodvibes_cli/commands/init_cmd.py"
      provides: "write_manifest call after copy_templates block + importlib.metadata version lookup"
      contains: "write_manifest"
    - path: "packages/pip/tests/test_init_cmd.py"
      provides: "mocker.patch for write_manifest + assertion it is called once on normal run"
      contains: "write_manifest"
  key_links:
    - from: "init_cmd.py after copy_templates block"
      to: "write_manifest(cwd, [f for f in created_files if f != '.goodvibes.json'], _version)"
      via: "direct call before tel_thread.join"
      pattern: "write_manifest.*created_files"
    - from: "write_manifest"
      to: "(dest_dir / rel).read_bytes() → hashlib.sha256(content).hexdigest()"
      via: "hashlib stdlib"
      pattern: "hashlib\\.sha256"
---

<objective>
Create the Python write_manifest step module (write_manifest.py + test_write_manifest.py) and wire write_manifest into init_cmd.py. Python parity for UPD-01: after every successful init run, .goodvibes.json is written with the SHA-256 of each managed file.

Purpose: Python CLI must produce the same .goodvibes.json as the npm CLI so that goodvibes update works regardless of which CLI was used to init.
Output: write_manifest.py, test_write_manifest.py, patched init_cmd.py, patched test_init_cmd.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-goodvibes-update-with-manifest/14-RESEARCH.md
@.planning/phases/14-goodvibes-update-with-manifest/14-PATTERNS.md
@.planning/phases/14-goodvibes-update-with-manifest/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create write_manifest.py + test_write_manifest.py</name>
  <files>
    packages/pip/src/goodvibes_cli/steps/write_manifest.py
    packages/pip/tests/test_write_manifest.py
  </files>
  <read_first>
    packages/pip/src/goodvibes_cli/steps/telemetry.py — analog for the step module shape: from __future__ import annotations at top, stdlib-only imports, no class, no @app.command() decorators, plain functions with return type annotations. Use this shape for write_manifest.py.
    packages/pip/tests/test_sentinel_merge.py — analog for real-filesystem test structure using the tmp_dir fixture. The tmp_dir fixture returns tmp_path directly (NOT a subdirectory) per CLAUDE.md gotcha.
    packages/pip/tests/conftest.py — confirm the tmp_dir fixture definition.
    .planning/phases/14-goodvibes-update-with-manifest/14-PATTERNS.md — sections "packages/pip/src/goodvibes_cli/steps/write_manifest.py" and "packages/pip/tests/test_write_manifest.py" contain the exact implementations and test names.
  </read_first>
  <behavior>
    write_manifest.py exports:
    - MANIFEST_PATH = ".goodvibes.json"
    - write_manifest(dest_dir: pathlib.Path, written_files: list[str], version: str) -> None
      - iterates written_files; for each rel reads (dest_dir / rel).read_bytes(); computes hashlib.sha256(content).hexdigest()
      - builds dict {"version": version, "files": {rel: hex, ...}}
      - writes json.dumps(manifest, indent=2) + "\n" to dest_dir / MANIFEST_PATH with encoding="utf-8"
    - read_manifest(dest_dir: pathlib.Path) -> dict | None
      - checks if dest_dir / MANIFEST_PATH exists; if not returns None
      - parses JSON with json.loads; on any Exception returns None

    test_write_manifest.py tests (all use tmp_dir fixture, no mocks for file I/O):
    - test_write_manifest_creates_goodvibes_json_with_sha256_per_file(tmp_dir)
    - test_read_manifest_returns_none_when_file_absent(tmp_dir)
    - test_read_manifest_returns_none_when_json_is_malformed(tmp_dir)
    - test_write_manifest_sets_version(tmp_dir)
    - test_write_manifest_hashes_actual_dest_content(tmp_dir) — writes two different files, confirms different hashes
  </behavior>
  <action>
    Create packages/pip/src/goodvibes_cli/steps/write_manifest.py with `from __future__ import annotations` at top, then imports: hashlib, json, pathlib (all stdlib). Define MANIFEST_PATH = ".goodvibes.json". Define write_manifest and read_manifest as plain functions — no class, no typer decorators.

    In write_manifest: use read_bytes() (not read_text()) when reading files for hashing — this avoids encoding round-trip differences. Build the files dict by iterating written_files with a for loop. Write the manifest with write_text(json.dumps(manifest, indent=2) + "\n", encoding="utf-8").

    In read_manifest: check p.exists() first, return None immediately if false. Wrap json.loads(p.read_text(encoding="utf-8")) in try/except Exception: return None. The except is intentional — malformed JSON must not crash the CLI.

    Create packages/pip/tests/test_write_manifest.py importing pathlib, and write_manifest + read_manifest from goodvibes_cli.steps.write_manifest. Import json inline in tests that need to parse the manifest. Use the tmp_dir fixture (pathlib.Path, not a subdirectory). Write test files directly into tmp_dir before calling write_manifest.

    Do NOT import pytest (the tests have no parametrize or raises assertions). The test file needs no mocker — all I/O is real using tmp_dir.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_write_manifest.py -v 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/pip && uv run pytest tests/test_write_manifest.py -v` exits 0
    - All 5 tests appear as PASSED in output
    - `grep -n "hashlib.sha256" packages/pip/src/goodvibes_cli/steps/write_manifest.py` returns a result
    - `grep -n "read_bytes" packages/pip/src/goodvibes_cli/steps/write_manifest.py` returns a result (not read_text for hashing)
    - `grep -n "return None" packages/pip/src/goodvibes_cli/steps/write_manifest.py` returns results for both the exists() check and the except block
  </acceptance_criteria>
  <done>write_manifest.py and test_write_manifest.py exist; all 5 new tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Wire write_manifest into init_cmd.py and patch test_init_cmd.py</name>
  <files>
    packages/pip/src/goodvibes_cli/commands/init_cmd.py
    packages/pip/tests/test_init_cmd.py
  </files>
  <read_first>
    packages/pip/src/goodvibes_cli/commands/init_cmd.py — read the full file. Key locations: existing imports (lines 1-14), the try/except block ending around line 127, the tel_thread.join call (line 129), the created_files list populated from copy_templates (line 100-101). The write_manifest call goes AFTER the except block closes and BEFORE the tel_thread.join check.
    packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py — lines 34-38: the _get_package_version() pattern using importlib.metadata.version("goodvibes-cli"). Replicate this inline in init_cmd.py, not as a helper function.
    packages/pip/tests/test_init_cmd.py — read the full file to understand existing mocker.patch calls and test structure. Find which tests exercise the normal (non-dry-run) run path to add write_manifest assertions.
    .planning/phases/14-goodvibes-update-with-manifest/14-PATTERNS.md — sections "packages/pip/src/goodvibes_cli/commands/init_cmd.py (MODIFY)" and "packages/pip/tests/test_init_cmd.py (MODIFY)" for exact patterns.
  </read_first>
  <action>
    Modify packages/pip/src/goodvibes_cli/commands/init_cmd.py:

    1. Add import at the top of the imports block (after the existing step imports, around line 13):
       `from goodvibes_cli.steps.write_manifest import write_manifest`

    2. Add `import importlib.metadata` if it is not already present (check the existing imports first).

    3. Insert the write_manifest call AFTER the closing line of the try/except block (after `raise typer.Exit(1)` in the OSError except clause, approximately line 127) and BEFORE the `if tel_thread:` check (approximately line 129). Insert:
       - `_version = importlib.metadata.version("goodvibes-cli")`
       - `write_manifest(cwd, [f for f in created_files if f != ".goodvibes.json"], _version)`

    The filter `f != ".goodvibes.json"` prevents the manifest from listing itself. The call is NOT inside the try/except block — it goes between the except block close and the tel_thread join. If write_manifest raises (it shouldn't given copyTemplates just succeeded in the same directory), the exception propagates to typer's top-level handler.

    The write_manifest call must NOT appear in the dry-run path. The dry-run early return is at approximately line 86 — the new call is well after that, so no guard needed.

    Modify packages/pip/tests/test_init_cmd.py:

    1. Add an autouse fixture or per-test mock for write_manifest to avoid real filesystem writes during unit tests. Using `mocker.patch("goodvibes_cli.commands.init_cmd.write_manifest")` per test is cleaner than autouse (follows existing mocker.patch style in the file).

    2. In tests that exercise the normal (non-dry-run) init path: add `mock_wm = mocker.patch("goodvibes_cli.commands.init_cmd.write_manifest")` and after invocation assert `mock_wm.call_count == 1`.

    3. In tests that exercise the --dry-run path: add `mock_wm = mocker.patch("goodvibes_cli.commands.init_cmd.write_manifest")` and assert `mock_wm.call_count == 0`.

    Also add `mocker.patch("goodvibes_cli.commands.init_cmd.importlib.metadata.version", return_value="1.2.0")` or patch at the module level `mocker.patch("goodvibes_cli.commands.init_cmd.importlib", ...)` in each test that patches write_manifest, so importlib.metadata.version("goodvibes-cli") does not fail in the test environment. A simpler approach: patch `importlib.metadata.version` itself — `mocker.patch("importlib.metadata.version", return_value="1.2.0")`.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/ -v 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/pip && uv run pytest tests/` exits 0 with no regressions against the 139-test baseline
    - `grep -n "write_manifest" packages/pip/src/goodvibes_cli/commands/init_cmd.py` returns at least 2 results (import + call)
    - `grep -n "write_manifest" packages/pip/tests/test_init_cmd.py` returns at least 2 results (mock + assertion)
    - The write_manifest call in init_cmd.py appears AFTER the except OSError block and BEFORE the tel_thread join check
    - The write_manifest call uses list comprehension filtering out ".goodvibes.json"
  </acceptance_criteria>
  <done>init_cmd.py calls write_manifest after tasks succeed; test_init_cmd.py mocks and asserts it; full pip suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| created_files list → write_manifest | File paths from copy_templates (already path-traversal-guarded by copytree ignore_fn) |
| write_manifest → disk | writes to pathlib.Path.cwd(), same dir where copy_templates just succeeded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03-01 | Tampering | written_files path list in write_manifest | accept | paths come from copy_templates which already rejects symlink-escape and '..' via resolve/relative_to check |
| T-14-03-02 | Tampering | manifest self-reference (.goodvibes.json in own files map) | mitigate | filter `f != ".goodvibes.json"` in init_cmd.py call site |
| T-14-SC | Tampering | pip/npm installs | accept | no new packages installed in this plan |
</threat_model>

<verification>
`cd packages/pip && uv run pytest tests/` exits 0.
`grep -n "write_manifest" packages/pip/src/goodvibes_cli/commands/init_cmd.py` shows import and call.
</verification>

<success_criteria>
- packages/pip/src/goodvibes_cli/steps/write_manifest.py: write_manifest(), read_manifest(), MANIFEST_PATH
- packages/pip/tests/test_write_manifest.py: 5 passing tests using real tmp_dir
- packages/pip/src/goodvibes_cli/commands/init_cmd.py: imports write_manifest, calls it after try/except block
- packages/pip/tests/test_init_cmd.py: mocker.patch for write_manifest with call count assertions
- `cd packages/pip && uv run pytest tests/` exits 0
</success_criteria>

<output>
Create `.planning/phases/14-goodvibes-update-with-manifest/14-03-SUMMARY.md` when done.
</output>
