---
phase: 14
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - packages/npm/src/utils/sentinel-merge.ts
  - packages/npm/src/utils/sentinel-merge.test.ts
autonomous: true
requirements:
  - UPD-06
must_haves:
  truths:
    - "TypeScript mergeClaude does not corrupt CLAUDE.md when SENTINEL_START is present but SENTINEL_END is absent"
    - "The malformed sentinel case is treated as Case B (append) — user content before the orphaned start marker is preserved"
    - "The UPD-06 test case in sentinel-merge.test.ts passes after the fix"
  artifacts:
    - path: "packages/npm/src/utils/sentinel-merge.ts"
      provides: "endIdx === -1 guard in mergeClaude after line 49"
      contains: "if (endIdx === -1)"
    - path: "packages/npm/src/utils/sentinel-merge.test.ts"
      provides: "UPD-06 test case: SENTINEL_START without SENTINEL_END does not corrupt file"
      contains: "SENTINEL_START without SENTINEL_END does not corrupt file"
  key_links:
    - from: "mergeClaude (sentinel-merge.ts)"
      to: "Case B handler (writeFile with trimEnd + append)"
      via: "endIdx === -1 guard"
      pattern: "if.*endIdx.*=== -1"
---

<objective>
Fix the TypeScript sentinel guard bug (UPD-06): when CLAUDE.md contains SENTINEL_START but no SENTINEL_END, the existing code computes `existing.slice(startIdx, -1 + 24)` = `existing.slice(startIdx, 23)` instead of aborting gracefully. The Python counterpart already handles this at `sentinel_merge.py` lines 64-68. Add the matching test case and apply the 4-line guard.

Purpose: Prevents CLAUDE.md data-loss on malformed files. This is Wave 0 — it must complete before the manifest and update commands are built.
Output: Patched sentinel-merge.ts + one new test case in sentinel-merge.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-goodvibes-update-with-manifest/14-RESEARCH.md
@.planning/phases/14-goodvibes-update-with-manifest/14-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add UPD-06 test case to sentinel-merge.test.ts (RED)</name>
  <files>packages/npm/src/utils/sentinel-merge.test.ts</files>
  <read_first>
    packages/npm/src/utils/sentinel-merge.test.ts — read the full file to understand the existing describe('mergeClaude') block structure, the SENTINEL_START and SENTINEL_END constants, the TEMPLATE_CONTENT fixture, and the tmpDir setup/teardown pattern. The new test appends inside the same describe block after the 'Case D2' test at line 137.
    packages/pip/tests/test_sentinel_merge.py — lines 127-138: the Python equivalent test that already passes. Mirror its assertions in TypeScript.
  </read_first>
  <behavior>
    - Test name: 'SENTINEL_START without SENTINEL_END does not corrupt file'
    - Given: a CLAUDE.md at destPath whose content is '# User content\n\n' + SENTINEL_START + '\norphaned start' (no SENTINEL_END)
    - When: mergeClaude(destPath, TEMPLATE_CONTENT) is called
    - Then:
      - result contains '# User content' (user content before the orphaned marker is preserved)
      - result contains SENTINEL_END (the template block was appended — treated as Case B)
      - result contains exactly 1 occurrence of SENTINEL_START (no duplication)
      - the substring after the last SENTINEL_END is empty or only whitespace (no garbage after the end marker)
  </behavior>
  <action>
    Append a new test case inside the existing describe('mergeClaude') block in packages/npm/src/utils/sentinel-merge.test.ts, after the 'Case D2' test at the bottom of the block (after line 136, before the closing brace on line 137).

    The test writes the malformed string — '# User content\n\n' + SENTINEL_START + '\norphaned start' — to join(tmpDir, 'CLAUDE.md') using writeFileSync. It then awaits mergeClaude(destPath, TEMPLATE_CONTENT). It reads the result with readFileSync and asserts:
    1. content.includes('# User content') is true
    2. content.includes(SENTINEL_END) is true
    3. (content.match(/<!-- goodvibes:start -->/g) ?? []).length === 1
    4. content.split(SENTINEL_END).at(-1)?.trim() === '' (nothing after the end marker)

    Use the existing SENTINEL_START, SENTINEL_END, TEMPLATE_CONTENT, tmpDir, writeFileSync, readFileSync, and join symbols — all already in scope. No new imports needed.

    This test will FAIL before the fix in Task 2 because the current code computes after = existing.slice(-1 + 24) = existing.slice(23), appending almost the entire file after the block.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test -- --reporter=verbose 2>&1 | grep -E "(SENTINEL_START without|FAIL|PASS)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - The test 'SENTINEL_START without SENTINEL_END does not corrupt file' appears in the vitest output
    - The test FAILS at this point (RED) — confirms the bug exists before the fix
    - No other existing tests are broken
  </acceptance_criteria>
  <done>New test case exists and is RED — the bug is reproduced.</done>
</task>

<task type="auto">
  <name>Task 2: Fix sentinel-merge.ts with endIdx === -1 guard (GREEN)</name>
  <files>packages/npm/src/utils/sentinel-merge.ts</files>
  <read_first>
    packages/npm/src/utils/sentinel-merge.ts — read the full file. The bug is at line 49: after `const endIdx = existing.indexOf(SENTINEL_END)`, the code immediately computes `existing.slice(startIdx, endIdx + SENTINEL_END.length)` without checking whether endIdx is -1. When endIdx === -1, SENTINEL_END.length is 24, so slice end becomes 23 — almost the entire file.
    packages/pip/src/goodvibes_cli/utils/sentinel_merge.py — lines 64-68: the Python fix that already exists. Mirror this guard in TypeScript.
  </read_first>
  <action>
    In packages/npm/src/utils/sentinel-merge.ts, insert a guard block immediately after line 49 (`const endIdx = existing.indexOf(SENTINEL_END)`), before line 50 (`const existingBlock = existing.slice(startIdx, endIdx + SENTINEL_END.length)`).

    The guard: if endIdx === -1, treat the malformed file as Case B. Write the file using writeFile with the content: existing.slice(0, startIdx).trimEnd() + '\n\n' + templateBlock + '\n'. Then return immediately. Do not fall through to the existingBlock computation.

    The SENTINEL_END constant value is '<!-- goodvibes:end -->' (22 characters). SENTINEL_END.length = 22, not 24 — double-check: '<!-- goodvibes:end -->' is exactly 22 chars. The arithmetic bug: when endIdx is -1, slice end = -1 + 22 = 21. Either way, the guard prevents incorrect slice computation.

    This is a 4-line insertion. Do not modify any other part of the file. The guard pattern mirrors Python sentinel_merge.py lines 64-68 exactly: check for end_idx == -1, write existing[:start_idx].rstrip() + '\n\n' + template_block + '\n', return.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/npm && npm test` exits 0
    - Test output includes 'SENTINEL_START without SENTINEL_END does not corrupt file' as PASS
    - All previously passing tests (132 baseline) continue to pass
    - The guard block `if (endIdx === -1)` appears in sentinel-merge.ts between the indexOf call and the existingBlock slice
    - Running `grep -n "endIdx === -1" packages/npm/src/utils/sentinel-merge.ts` returns a result
  </acceptance_criteria>
  <done>sentinel-merge.ts has the UPD-06 guard; npm test suite is fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template content → mergeClaude | Template files are repo-controlled; content is trusted |
| dest CLAUDE.md on disk → mergeClaude | Existing file may be malformed (this is the threat surface) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-01 | Tampering | mergeClaude (sentinel-merge.ts) | mitigate | UPD-06 guard: malformed file (orphaned start) treated as Case B; no garbage written after sentinel end |
| T-14-SC | Tampering | npm installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
Full npm suite green after Task 2: `cd packages/npm && npm test` exits 0 with no regressions.
UPD-06 test case added and passing.
</verification>

<success_criteria>
- `packages/npm/src/utils/sentinel-merge.ts` contains `if (endIdx === -1)` guard in mergeClaude
- `packages/npm/src/utils/sentinel-merge.test.ts` contains test 'SENTINEL_START without SENTINEL_END does not corrupt file'
- `cd packages/npm && npm test` exits 0
</success_criteria>

<output>
Create `.planning/phases/14-goodvibes-update-with-manifest/14-01-SUMMARY.md` when done.
</output>
