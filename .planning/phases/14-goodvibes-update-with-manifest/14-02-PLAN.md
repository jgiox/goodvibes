---
phase: 14
plan: 02
type: execute
wave: 1
depends_on:
  - 14-01
files_modified:
  - packages/npm/src/steps/write-manifest.ts
  - packages/npm/src/steps/write-manifest.test.ts
  - packages/npm/src/commands/init.ts
  - packages/npm/src/commands/init.test.ts
autonomous: true
requirements:
  - UPD-01
must_haves:
  truths:
    - "After goodvibes init completes, .goodvibes.json exists in the project root"
    - ".goodvibes.json contains a 'version' string and a 'files' object mapping each written file to its SHA-256 hex digest"
    - ".goodvibes.json does NOT list itself in its own 'files' map"
    - "writeManifest hashes the DEST file content (after sentinel merge), not the template source"
    - "readManifest returns null when .goodvibes.json is absent or contains malformed JSON"
    - "init.ts calls writeManifest once after await tasks(taskList) succeeds, in the non-dry-run path only"
  artifacts:
    - path: "packages/npm/src/steps/write-manifest.ts"
      provides: "writeManifest() and readManifest() exported functions + Manifest interface"
      exports: ["writeManifest", "readManifest", "Manifest"]
    - path: "packages/npm/src/steps/write-manifest.test.ts"
      provides: "Unit tests for writeManifest and readManifest using real tmpdir"
      contains: "writes .goodvibes.json with sha256"
    - path: "packages/npm/src/commands/init.ts"
      provides: "writeManifest call wired after tasks() + createRequire version lookup"
      contains: "writeManifest"
    - path: "packages/npm/src/commands/init.test.ts"
      provides: "vi.mock for write-manifest + assertion that writeManifest called once on normal run"
      contains: "write-manifest"
  key_links:
    - from: "init.ts after await tasks(taskList)"
      to: "writeManifest(cwd, createdFiles, version)"
      via: "direct call after try/catch block"
      pattern: "writeManifest.*createdFiles"
    - from: "writeManifest"
      to: "readFile(join(destDir, rel), 'utf-8') → createHash('sha256').update(content, 'utf8').digest('hex')"
      via: "node:crypto createHash"
      pattern: "createHash.*sha256"
---

<objective>
Create the TypeScript write-manifest step module (write-manifest.ts + write-manifest.test.ts) and wire writeManifest into init.ts after tasks() complete. This delivers UPD-01: after every successful init run, .goodvibes.json records the SHA-256 of each file written, indexed by relative path, plus the goodvibes version.

Purpose: The manifest is the foundation for goodvibes update — without it, update cannot distinguish managed-unmodified from user-modified files.
Output: write-manifest.ts, write-manifest.test.ts, patched init.ts, patched init.test.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-goodvibes-update-with-manifest/14-RESEARCH.md
@.planning/phases/14-goodvibes-update-with-manifest/14-PATTERNS.md
@.planning/phases/14-goodvibes-update-with-manifest/14-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create write-manifest.ts + write-manifest.test.ts</name>
  <files>
    packages/npm/src/steps/write-manifest.ts
    packages/npm/src/steps/write-manifest.test.ts
  </files>
  <read_first>
    packages/npm/src/steps/telemetry.ts — analog for the step module shape: no default export, named async function exports only, no project-internal imports, stdlib only. Use this shape for write-manifest.ts.
    packages/npm/src/utils/sentinel-merge.test.ts — analog for real-filesystem test structure: mkdtempSync, beforeEach/afterEach with rmSync, test naming conventions. Use this shape for write-manifest.test.ts.
    packages/npm/src/steps/copy-templates.ts — shows that written file paths are relative strings (e.g. 'CLAUDE.md', '.github/workflows/ci.yml'); these are the rel keys in writtenFiles.
    .planning/phases/14-goodvibes-update-with-manifest/14-PATTERNS.md — sections "packages/npm/src/steps/write-manifest.ts" and "packages/npm/src/steps/write-manifest.test.ts" provide the exact implementations and test names.
  </read_first>
  <behavior>
    write-manifest.ts exports:
    - `Manifest` interface: { version: string; files: Record<string, string> }
    - `MANIFEST_PATH` constant: '.goodvibes.json'
    - `writeManifest(destDir: string, writtenFiles: string[], version: string): Promise<void>` — for each rel in writtenFiles, reads join(destDir, rel) as utf-8 string, hashes with createHash('sha256').update(content, 'utf8').digest('hex'), builds files object, writes JSON.stringify(manifest, null, 2) + '\n' to join(destDir, MANIFEST_PATH)
    - `readManifest(destDir: string): Promise<Manifest | null>` — reads join(destDir, MANIFEST_PATH); if not found or JSON.parse throws, returns null (catch swallows the error — this is intentional per PATTERNS.md)

    write-manifest.test.ts tests:
    - 'writes .goodvibes.json with sha256 for each written file'
    - 'readManifest returns null when .goodvibes.json does not exist'
    - 'readManifest returns null when .goodvibes.json is malformed JSON'
    - 'writeManifest hashes the actual dest content, not the path string'
    - 'writeManifest sets version in the manifest'
  </behavior>
  <action>
    Create packages/npm/src/steps/write-manifest.ts with these static imports at the top: createHash from 'node:crypto', readFile and writeFile from 'node:fs/promises', join from 'node:path'. Export the Manifest interface, the MANIFEST_PATH constant ('.goodvibes.json'), and the two async functions writeManifest and readManifest as described in the behavior block above.

    In writeManifest: iterate writtenFiles with a for-of loop (not Promise.all — sequential is fine for <30 files); use readFile(join(destDir, rel), 'utf-8') then createHash('sha256').update(content, 'utf8').digest('hex') to get the SHA; build the manifest object; write with writeFile using JSON.stringify(manifest, null, 2) + '\n' and 'utf-8' encoding.

    In readManifest: wrap readFile + JSON.parse in a try/catch; the catch block returns null with a comment: 'ponytail: null signals missing or corrupt manifest; caller shows UPD-05 message'. No re-throw.

    Create packages/npm/src/steps/write-manifest.test.ts importing describe, it, expect, beforeEach, afterEach from 'vitest'; mkdtempSync, writeFileSync, readFileSync, rmSync from 'fs'; join from 'path'; tmpdir from 'os'; and writeManifest, readManifest from './write-manifest.js'. Use the real-filesystem tmpdir pattern from sentinel-merge.test.ts (mkdtempSync in beforeEach, rmSync recursive in afterEach). Import json natively to parse the written manifest in tests. Do not mock file I/O in this test file — use real tmpdir.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test -- --reporter=verbose 2>&1 | grep -E "(write-manifest|PASS|FAIL)" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/npm && npm test` exits 0
    - All 5 write-manifest tests appear as PASS in vitest output
    - Running `node -e "import('./src/steps/write-manifest.js').then(m => console.log(Object.keys(m)))"` in packages/npm/src/ would show ['writeManifest', 'readManifest', 'Manifest', 'MANIFEST_PATH'] (or similar — exports exist)
    - `grep -n "createHash.*sha256" packages/npm/src/steps/write-manifest.ts` returns a result
    - `grep -n "return null" packages/npm/src/steps/write-manifest.ts` returns a result in the readManifest catch block
  </acceptance_criteria>
  <done>write-manifest.ts and write-manifest.test.ts exist; all new tests pass; baseline npm test count increases by 5.</done>
</task>

<task type="auto">
  <name>Task 2: Wire writeManifest into init.ts and patch init.test.ts</name>
  <files>
    packages/npm/src/commands/init.ts
    packages/npm/src/commands/init.test.ts
  </files>
  <read_first>
    packages/npm/src/commands/init.ts — read the full file. Key locations: imports block (top), the try/catch around await tasks(taskList) (lines 132-141), the Promise.race call (line 143), the createdFiles array populated during tasks() (line 83). The writeManifest call goes between the catch block closing brace and the await Promise.race line.
    packages/npm/src/commands/init.test.ts — read the full file to understand the existing vi.mock() setup (lines 1-45 approx), especially the mock for '../steps/telemetry.js' and the test 'normal run' assertions. The write-manifest mock goes after the existing mocks; assertions go after existing assertions in the normal-run test.
    packages/npm/src/commands/upgrade.ts — lines 11-13: createRequire pattern used to read package.json. Replicate this inline in init.ts for the version lookup (not a module-level constant — inline at call site).
    .planning/phases/14-goodvibes-update-with-manifest/14-PATTERNS.md — section "packages/npm/src/commands/init.ts (MODIFY)" for exact insertion point and "packages/npm/src/commands/init.test.ts (MODIFY)" for mock and assertion patterns.
  </read_first>
  <action>
    Modify packages/npm/src/commands/init.ts:

    1. Add a static import at the top of the imports block: `import { writeManifest } from '../steps/write-manifest.js'`. Add it after the sendTelemetry import line.

    2. Add a static import for createRequire: `import { createRequire } from 'node:module'`. Check first — if createRequire is not already imported, add it alongside the existing node: imports.

    3. Between the catch block closing brace (after the process.exit(1) call, around line 141) and the `await Promise.race(...)` call (line 143), insert the writeManifest call:
       - Declare `const _req = createRequire(import.meta.url)`
       - Declare `const _ver = (_req('../../package.json') as { version: string }).version`
       - Call `await writeManifest(cwd, createdFiles.filter(f => f !== '.goodvibes.json'), _ver)`

    The filter `f !== '.goodvibes.json'` prevents the manifest from listing itself (A2 in RESEARCH.md). writeManifest is not inside any try/catch — if it fails, the error propagates. This is acceptable because writeManifest writes to the same directory where copyTemplates just succeeded; permission errors cannot arise here that didn't already abort.

    The writeManifest call must NOT appear in the dry-run path (the function returns early before tasks() for dry-run).

    Modify packages/npm/src/commands/init.test.ts:

    1. Add a vi.mock after the existing mocks (after the telemetry mock):
       - `vi.mock('../steps/write-manifest.js', () => ({ writeManifest: vi.fn().mockResolvedValue(undefined), readManifest: vi.fn().mockResolvedValue(null) }))`

    2. In the test that exercises the normal (non-dry-run) run path, add after the existing assertions:
       - Import writeManifest via `const { writeManifest } = await import('../steps/write-manifest.js')`
       - Assert `expect(vi.mocked(writeManifest)).toHaveBeenCalledTimes(1)`
       - Assert `expect(vi.mocked(writeManifest)).toHaveBeenCalledWith(expect.any(String), expect.any(Array), expect.any(String))`

    3. In the test that exercises the --dry-run path, add:
       - Assert `expect(vi.mocked(writeManifest)).not.toHaveBeenCalled()`
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/npm && npm test` exits 0 with no regressions
    - `grep -n "writeManifest" packages/npm/src/commands/init.ts` returns at least 2 results (import line + call line)
    - `grep -n "write-manifest" packages/npm/src/commands/init.test.ts` returns at least 1 result (vi.mock line)
    - The writeManifest call in init.ts is placed AFTER the try/catch block closing brace and BEFORE the await Promise.race line
    - The writeManifest call uses `createdFiles.filter(f => f !== '.goodvibes.json')` as the second argument
    - Tests that verify normal run assert writeManifest called once; tests for dry-run assert writeManifest not called
  </acceptance_criteria>
  <done>init.ts calls writeManifest after tasks succeed; init.test.ts mocks and asserts it; full npm suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| createdFiles list → writeManifest | written file paths come from copyTemplates (already path-traversal-guarded) |
| writeManifest → disk | writes to process.cwd() which is the same dir where copyTemplates just succeeded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-01 | Tampering | writtenFiles path list passed to writeManifest | accept | paths come from copyTemplates which already rejects '..' components; no additional guard needed |
| T-14-02-02 | Tampering | manifest self-reference (.goodvibes.json in own files map) | mitigate | filter `f !== '.goodvibes.json'` in init.ts call site prevents manifest listing itself |
| T-14-02-03 | Information Disclosure | SHA-256 hashes in .goodvibes.json | accept | SHA-256 of template files is not secret; manifest is per-project, not sent anywhere |
| T-14-SC | Tampering | npm/pip installs | accept | no new packages installed in this plan |
</threat_model>

<verification>
`cd packages/npm && npm test` exits 0.
`grep -n "writeManifest" packages/npm/src/commands/init.ts` shows import and call.
`grep -n "write-manifest" packages/npm/src/commands/init.test.ts` shows vi.mock.
</verification>

<success_criteria>
- packages/npm/src/steps/write-manifest.ts exported functions: writeManifest, readManifest; interface Manifest
- packages/npm/src/steps/write-manifest.test.ts: 5 passing tests using real tmpdir
- packages/npm/src/commands/init.ts: imports writeManifest; calls it after tasks() with filtered createdFiles and version
- packages/npm/src/commands/init.test.ts: vi.mock for write-manifest; assertions on call count
- `cd packages/npm && npm test` exits 0
</success_criteria>

<output>
Create `.planning/phases/14-goodvibes-update-with-manifest/14-02-SUMMARY.md` when done.
</output>
