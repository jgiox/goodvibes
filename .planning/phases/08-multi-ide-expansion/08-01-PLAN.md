---
phase: 08-multi-ide-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/.cursor/rules/goodvibes.mdc
  - templates/.github/copilot-instructions.md
  - templates/.windsurfrules
  - templates/.kiro/steering/goodvibes.md
autonomous: true
requirements: [IDE-01, IDE-02, IDE-03, IDE-04]

must_haves:
  truths:
    - "Running goodvibes init in a blank directory writes all four IDE rule files"
    - "Each IDE rule file activates on every AI request in its target IDE (alwaysApply/inclusion:always)"
    - "Each file encodes ponytail minimalism, fail-loud, surgical changes, security-first — not a CLAUDE.md copy"
    - "No-clobber: if the destination file already exists it is counted as skipped, not overwritten"
    - "--minimal writes Cursor, Windsurf, and Kiro rule files; skips copilot-instructions.md (already inside .github/)"
  artifacts:
    - path: "templates/.cursor/rules/goodvibes.mdc"
      provides: "Cursor MDC rule file with alwaysApply:true frontmatter"
      contains: "alwaysApply: true"
    - path: "templates/.github/copilot-instructions.md"
      provides: "GitHub Copilot repo-wide instructions (plain markdown, no frontmatter)"
    - path: "templates/.windsurfrules"
      provides: "Windsurf/Devin Desktop workspace rules (plain markdown, no frontmatter, under 12000 chars)"
    - path: "templates/.kiro/steering/goodvibes.md"
      provides: "Kiro steering file with inclusion:always frontmatter starting on line 1"
      contains: "inclusion: always"
  key_links:
    - from: "templates/.cursor/rules/goodvibes.mdc"
      to: "fs-extra copy() in copy-templates.ts"
      via: "recursive directory walk; .cursor/rules/ created automatically"
      pattern: "alwaysApply"
    - from: "templates/.github/copilot-instructions.md"
      to: "copy-templates.ts filter"
      via: "rel.startsWith('.github') guard excludes it under --minimal"
      pattern: "copilot-instructions"
    - from: "templates/.kiro/steering/goodvibes.md"
      to: "shutil.copytree in copy_templates.py"
      via: "recursive directory walk; .kiro/steering/ created automatically"
      pattern: "inclusion: always"
---

<objective>
Author the four IDE rule template files that goodvibes init will copy into every new project.

Purpose: Extend goodvibes to support Cursor, GitHub Copilot, Windsurf/Devin Desktop, and Kiro — the four leading VS Code-ecosystem AI coding IDEs. The existing copy machinery (fs-extra copy + shutil.copytree) automatically picks up new template files, creates subdirectories, and enforces no-clobber. No CLI code changes are needed. This plan is pure content authoring.

Output: Four new files in templates/ that are automatically walked by the existing copy step on every goodvibes init run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-multi-ide-expansion/08-RESEARCH.md
@templates/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author Cursor and Kiro IDE rule files</name>
  <files>
    templates/.cursor/rules/goodvibes.mdc
    templates/.kiro/steering/goodvibes.md
  </files>
  <read_first>
    - templates/CLAUDE.md — canonical source of engineering principles to adapt (NOT copy verbatim)
    - .planning/phases/08-multi-ide-expansion/08-RESEARCH.md — exact frontmatter schemas, format rules, pitfall list
  </read_first>
  <action>
Create templates/.cursor/rules/goodvibes.mdc (IDE-01, IDE-02 per Cursor MDC format):

File must start with YAML frontmatter block on line 1:
  ---
  alwaysApply: true
  ---

Then a blank line and the markdown body. The body encodes the same principles as CLAUDE.md but adapted for Cursor's always-in-context style (concise directives, no sentinel comments, no version stamp, no testing conventions):
- "Think before coding" section: state assumptions explicitly; stop if assumptions are security/schema-sensitive
- "Simplicity first" section: the 7-rung ponytail ladder written as a short numbered list
- "Surgical changes" section: narrow diffs, no opportunistic reformats, no renames unless required
- "Fail loud" section: no empty catch blocks, no silent retries, error messages must be actionable
- "Security" section: validate at boundary, no secrets in code/logs, flag: SQLi / XSS / command injection / path traversal

Omit: sentinel HTML comments, version stamp, journal requirement, push to remote rule, testing section (Claude Code-specific), ponytail /command references.

Keep body under 200 words total (token cost guidance for always-apply rules).

---

Create templates/.kiro/steering/goodvibes.md (IDE-01, IDE-02 per Kiro steering format):

File MUST start with YAML frontmatter on line 1 — no whitespace before the opening ---. Per RESEARCH.md Pitfall 3, any whitespace before frontmatter causes Kiro to ignore the inclusion mode.

  ---
  inclusion: always
  ---

Then a blank line and the markdown body. Same set of principles as the Cursor file but using Kiro's advisory tone (Kiro presents rules as steering instructions to its agent). Same omissions (no sentinel, version stamp, journal, push rule, testing section).

Keep body under 200 words total.
  </action>
  <verify>
    <automated>grep -c "alwaysApply: true" /home/ygiokas/GoodVibes/templates/.cursor/rules/goodvibes.mdc && grep -c "inclusion: always" /home/ygiokas/GoodVibes/templates/.kiro/steering/goodvibes.md && head -1 /home/ygiokas/GoodVibes/templates/.kiro/steering/goodvibes.md | grep -c "^---"</automated>
  </verify>
  <acceptance_criteria>
    - templates/.cursor/rules/goodvibes.mdc exists and has .mdc extension (not .md — Cursor ignores .md files in .cursor/rules/)
    - Line 1 of goodvibes.mdc is "---" (start of YAML frontmatter)
    - "alwaysApply: true" appears in the frontmatter block
    - templates/.kiro/steering/goodvibes.md exists
    - Line 1 of goodvibes.md is "---" (start of YAML frontmatter, no whitespace before it)
    - "inclusion: always" appears in the frontmatter block
    - Neither file contains "<!-- goodvibes:start -->" or "<!-- goodvibes:end -->" (no sentinel comments)
    - Neither file contains "# goodvibes: v" (no version stamp)
    - Both files mention: ponytail ladder (or simplicity/YAGNI), fail loud, surgical changes, security flag list
    - Both files are under 300 lines each
  </acceptance_criteria>
  <done>Both files exist with correct frontmatter on line 1, encoding the required principles in IDE-native format</done>
</task>

<task type="auto">
  <name>Task 2: Author Copilot and Windsurf rule files</name>
  <files>
    templates/.github/copilot-instructions.md
    templates/.windsurfrules
  </files>
  <read_first>
    - templates/CLAUDE.md — canonical source of principles to adapt
    - .planning/phases/08-multi-ide-expansion/08-RESEARCH.md — format specs, character limits, pitfall list
  </read_first>
  <action>
Create templates/.github/copilot-instructions.md (IDE-01, IDE-02 per GitHub Copilot format):

Format: plain markdown, NO frontmatter. Copilot reads the raw file content. Any YAML frontmatter block would appear as literal text in the instructions, which is wrong.

Start directly with a markdown heading. Example opening: "## Engineering Rules — goodvibes"

Body encodes the same principles as CLAUDE.md adapted for Copilot Chat's natural-language context:
- Think before coding: state assumptions before implementing
- Simplicity first: ponytail ladder as short numbered list
- Surgical changes: narrow diffs, no opportunistic reformats
- Fail loud: no empty catch, no silent retries, actionable errors
- Security: validate at boundary, flag SQLi/XSS/command injection/path traversal

Omit: sentinel comments, version stamp, journal, push rule, testing conventions.

No character limit concern (4,000-char hard limit was removed June 2026). Keep under 100 lines for quality.

---

Create templates/.windsurfrules (IDE-01, IDE-02 per Windsurf format):

Format: plain markdown, NO frontmatter. The file is read as freeform text by Windsurf's Cascade agent.

Hard character limit: 12,000 characters (Windsurf silently truncates beyond this). Content must stay well under 3,000 characters (ponytail ruleset per RESEARCH.md guidance).

Start with a markdown heading. Body is same principle set as above but in Windsurf's advisory tone (Cascade presents rules as persistent context for every conversation).

Same omissions: no sentinel, version stamp, journal, push rule, testing section.
  </action>
  <verify>
    <automated>head -1 /home/ygiokas/GoodVibes/templates/.github/copilot-instructions.md | grep -cv "^---" && head -1 /home/ygiokas/GoodVibes/templates/.windsurfrules | grep -cv "^---" && wc -c /home/ygiokas/GoodVibes/templates/.windsurfrules</automated>
  </verify>
  <acceptance_criteria>
    - templates/.github/copilot-instructions.md exists
    - Line 1 is NOT "---" (no frontmatter — plain markdown only)
    - File does not contain "<!-- goodvibes:start -->" or "# goodvibes: v"
    - File mentions: simplicity/ponytail, fail loud, surgical changes, security flag list
    - templates/.windsurfrules exists at the repo-root-relative path (not in a subdirectory)
    - Line 1 of .windsurfrules is NOT "---" (no frontmatter)
    - .windsurfrules file size is under 3,000 characters (wc -c output is less than 3000)
    - .windsurfrules mentions: simplicity/ponytail, fail loud, surgical changes, security flag list
    - Neither file contains sentinel HTML comments or version stamps
  </acceptance_criteria>
  <done>Both files exist as plain markdown with no frontmatter, under character limits, encoding required engineering principles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates/ → user project | Static files copied verbatim into user's codebase |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-01 | Tampering | templates/ static files | mitigate | Path traversal guard (`rel.includes('..')`) already in copy-templates.ts line 91 and ignore_fn in copy_templates.py lines 59-62; IDE template files are repo-controlled static content — guard is belt-and-suspenders |
| T-8-02 | Information Disclosure | .windsurfrules character limit | accept | Content stays under 3,000 chars; Windsurf 12,000-char truncation is silent but no sensitive data in rule files |
| T-8-SC | Tampering | npm/pip installs | accept | No new packages installed in Phase 8 |
</threat_model>

<verification>
After both tasks:

1. All four IDE rule files exist in templates/:
   - templates/.cursor/rules/goodvibes.mdc (with alwaysApply: true)
   - templates/.github/copilot-instructions.md (no frontmatter)
   - templates/.windsurfrules (no frontmatter, under 3000 chars)
   - templates/.kiro/steering/goodvibes.md (with inclusion: always, frontmatter on line 1)

2. Directory structure check:
   ls templates/.cursor/rules/
   ls templates/.kiro/steering/

3. No sentinel markers in any IDE file:
   grep -l "goodvibes:start" templates/.cursor/rules/goodvibes.mdc templates/.windsurfrules templates/.kiro/steering/goodvibes.md templates/.github/copilot-instructions.md
   (should return nothing — grep finds no matches)

4. Upgrade.ts MANAGED_FIXED does NOT contain IDE paths (verify no regression):
   grep -c "cursorrules\|windsurfrules\|kiro\|copilot-instructions" packages/npm/src/commands/upgrade.ts
   (should return 0)

5. Python upgrade_cmd.py _MANAGED_FIXED does NOT contain IDE paths:
   grep -c "cursorrules\|windsurfrules\|kiro\|copilot-instructions" packages/pip/src/goodvibes_cli/commands/upgrade_cmd.py
   (should return 0)
</verification>

<success_criteria>
- Four template files exist with correct formats and frontmatter (or no frontmatter for Copilot/Windsurf)
- Running goodvibes init in a blank directory would write all four files (verified by test assertions in plan 08-02)
- Running goodvibes init --minimal would skip copilot-instructions.md but write the other three (verified by test assertions in plan 08-02)
- MANAGED_FIXED in both CLIs does not contain IDE file paths
</success_criteria>

<output>
Create .planning/phases/08-multi-ide-expansion/08-01-SUMMARY.md when done
</output>
