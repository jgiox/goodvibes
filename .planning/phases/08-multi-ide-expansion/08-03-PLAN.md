---
phase: 08-multi-ide-expansion
plan: 03
type: execute
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - README.md
autonomous: true
requirements: [IDE-05]

must_haves:
  truths:
    - "README contains a multi-IDE compatibility table listing Cursor, GitHub Copilot, Windsurf, and Kiro"
    - "Each table row states: IDE name, file written, minimum version or setting to activate rules"
    - "The Flags section accurately reflects that --minimal skips copilot-instructions.md but writes Cursor/Windsurf/Kiro rule files"
  artifacts:
    - path: "README.md"
      provides: "Multi-IDE compatibility table in a new section"
      contains: "Cursor"
  key_links:
    - from: "README.md"
      to: "templates/.cursor/rules/goodvibes.mdc"
      via: "table row references the file path written by goodvibes init"
      pattern: "\\.cursor/rules/goodvibes\\.mdc"
---

<objective>
Add a multi-IDE compatibility table to README.md and update the Flags section to reflect Phase 8 --minimal behavior.

Purpose: IDE-05 requires the README and template repo to document each supported IDE, the file written, and the minimum version or setting needed to activate rules. This is a single additive section plus a one-line update to the existing Flags section. No code changes.

Output: Updated README.md with a new "IDE compatibility" section and an updated --minimal description.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-multi-ide-expansion/08-RESEARCH.md
@README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add multi-IDE compatibility table and update Flags section</name>
  <files>README.md</files>
  <read_first>
    - README.md — read the full file to understand existing section structure and prose style before editing
    - .planning/phases/08-multi-ide-expansion/08-RESEARCH.md — "State of the Art" and "Open Questions" sections for activation notes per IDE
  </read_first>
  <action>
Make two additive changes to README.md:

--- Change 1: New "IDE compatibility" section ---

Add a new section after the existing "Platform support" table and before the "Docs" section. Use this heading: "## IDE compatibility"

Insert this table (adapt prose to match existing README style):

| IDE | File written | Minimum version | Activation |
|-----|-------------|----------------|------------|
| Claude Code | `CLAUDE.md` | Any | Automatic — loaded on every session |
| Cursor | `.cursor/rules/goodvibes.mdc` | 0.45+ | Automatic — `alwaysApply: true` in frontmatter |
| GitHub Copilot | `.github/copilot-instructions.md` | VS Code Copilot extension | Automatic — applied to all Copilot Chat requests |
| Windsurf / Devin Desktop | `.windsurfrules` | Any (Windsurf or Devin Desktop) | Automatic — applied to every Cascade conversation |
| Kiro | `.kiro/steering/goodvibes.md` | Any | Automatic — `inclusion: always` in frontmatter |

Add a note below the table: "**Note for GitHub Copilot users:** If instructions do not activate, check that the VS Code setting `github.copilot.chat.codeGeneration.useInstructionFiles` is enabled (it is on by default in recent versions)."

--- Change 2: Update the Flags section ---

The current "Flags" section contains:

  `--minimal` skips: `.github/` (workflows, issue templates, PR template, dependabot) and `docs/`.

Update that sentence to:

  `--minimal` skips: `.github/` (workflows, issue templates, PR template, dependabot, Copilot instructions) and `docs/`. Cursor, Windsurf, and Kiro rule files are written by `--minimal` — they are AI configuration, not scaffolding.

Do NOT reformat any other section, rename headings, or change any other prose.
  </action>
  <verify>
    <automated>grep -c "IDE compatibility\|\.cursor/rules/goodvibes\.mdc\|windsurfrules\|\.kiro/steering" /home/ygiokas/GoodVibes/README.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a "## IDE compatibility" heading
    - Table contains exactly 5 rows: Claude Code, Cursor, GitHub Copilot, Windsurf / Devin Desktop, Kiro
    - Each row has: IDE name, file path written, minimum version/setting, activation description
    - `.cursor/rules/goodvibes.mdc` appears in the table as the Cursor file path
    - `.windsurfrules` appears in the table as the Windsurf file path
    - `.kiro/steering/goodvibes.md` appears in the table as the Kiro file path
    - `.github/copilot-instructions.md` appears in the table as the Copilot file path
    - Copilot VS Code setting note is present below the table
    - The Flags section mentions that Cursor, Windsurf, and Kiro are written by --minimal
    - The Flags section mentions that Copilot instructions are skipped by --minimal
    - No other existing README section is modified
  </acceptance_criteria>
  <done>README.md contains the IDE compatibility table with 5 rows and the updated --minimal description in the Flags section</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README.md → public GitHub viewers | Static documentation; no executable content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-03-01 | Information Disclosure | README IDE table | accept | Only public documentation about file paths; no secrets or credentials referenced |
| T-8-SC | Tampering | npm/pip installs | accept | No packages installed in this plan |
</threat_model>

<verification>
After task completes:

1. IDE compatibility section present:
   grep -A 20 "## IDE compatibility" README.md
   (Should show the 5-row table)

2. All four IDE file paths documented:
   grep -c "cursor/rules/goodvibes\.mdc\|copilot-instructions\|windsurfrules\|kiro/steering" README.md
   (Should return 4 or more matches)

3. Flags section updated:
   grep -A 3 "minimal.*skips" README.md
   (Should mention Cursor/Windsurf/Kiro are written by --minimal)

4. Copilot VS Code setting note present:
   grep -c "useInstructionFiles" README.md
   (Should return 1)
</verification>

<success_criteria>
- README.md contains "## IDE compatibility" section with a 5-row table
- All four new IDE file paths are documented with activation requirements
- Flags section accurately describes --minimal behavior for IDE files
- No existing README content is broken or removed
</success_criteria>

<output>
Create .planning/phases/08-multi-ide-expansion/08-03-SUMMARY.md when done
</output>
