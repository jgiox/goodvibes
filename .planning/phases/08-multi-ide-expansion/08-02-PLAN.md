---
phase: 08-multi-ide-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/npm/src/steps/copy-templates.test.ts
  - packages/pip/tests/test_copy_templates.py
  - packages/pip/tests/conftest.py
autonomous: true
requirements: [IDE-01, IDE-03, IDE-04]

must_haves:
  truths:
    - "npm test passes with new IDE file assertions covering IDE-01, IDE-03, IDE-04"
    - "pytest passes with new IDE file assertions covering the same requirements"
    - "Tests confirm no-clobber: existing IDE rule file is counted as skipped, not overwritten"
    - "Tests confirm --minimal skips .github/copilot-instructions.md but writes the other three IDE files"
    - "Tests confirm goodvibes init writes all four IDE rule files on a fresh directory"
  artifacts:
    - path: "packages/npm/src/steps/copy-templates.test.ts"
      provides: "TS test assertions for IDE-01, IDE-03, IDE-04"
      contains: "goodvibes.mdc"
    - path: "packages/pip/tests/test_copy_templates.py"
      provides: "Python test assertions for IDE-01, IDE-03, IDE-04"
      contains: "goodvibes.mdc"
    - path: "packages/pip/tests/conftest.py"
      provides: "Updated template_dir fixture with IDE file stubs"
      contains: ".cursor"
  key_links:
    - from: "packages/npm/src/steps/copy-templates.test.ts"
      to: "templates/.cursor/rules/goodvibes.mdc"
      via: "resolveTemplatesDir() returns repo-root templates/ under vitest"
      pattern: "existsSync.*cursor.*goodvibes\\.mdc"
    - from: "packages/pip/tests/test_copy_templates.py"
      to: "packages/pip/tests/conftest.py template_dir fixture"
      via: "fixture creates IDE file stubs; tests assert their presence in dest"
      pattern: ".cursor/rules/goodvibes.mdc"
---

<objective>
Extend the existing copy-templates test suites (TS + Python) with assertions covering the four new IDE rule files.

Purpose: The research identified a test coverage gap (Pitfall 6) — the existing tests do not assert IDE file presence. Since the copy machinery already handles IDE files without code changes, the test gap is the only thing standing between "it should work" and "we know it works." These tests also serve as regression guards against any future change that accidentally adds IDE files to MANAGED_FIXED.

Output: New test cases appended to existing test files. The TS tests use the real template directory (integration-style, same pattern as existing describe blocks). The Python tests extend the fixture and add unit-style tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/08-multi-ide-expansion/08-RESEARCH.md
@packages/npm/src/steps/copy-templates.test.ts
@packages/pip/tests/test_copy_templates.py
@packages/pip/tests/conftest.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add IDE file assertions to copy-templates.test.ts</name>
  <files>packages/npm/src/steps/copy-templates.test.ts</files>
  <read_first>
    - packages/npm/src/steps/copy-templates.test.ts — read the full file before editing; follow all existing patterns (describe/it/beforeEach/afterEach, mkdtempSync, resolveTemplatesDir, existsSync, join)
    - packages/npm/src/steps/copy-templates.ts — understand what copyTemplates returns and how the --minimal filter works
    - .planning/phases/08-multi-ide-expansion/08-RESEARCH.md — Pitfall 6 lists exact behaviors to test
  </read_first>
  <behavior>
    - Test: "writes .cursor/rules/goodvibes.mdc on fresh init (IDE-01)" — copyTemplates(templateDir, tmpDir, false, false) → existsSync(join(tmpDir, '.cursor', 'rules', 'goodvibes.mdc')) === true AND written array contains '.cursor/rules/goodvibes.mdc'
    - Test: "writes .windsurfrules on fresh init (IDE-01)" — copyTemplates(templateDir, tmpDir, false, false) → existsSync(join(tmpDir, '.windsurfrules')) === true
    - Test: "writes .kiro/steering/goodvibes.md on fresh init (IDE-01)" — copyTemplates(templateDir, tmpDir, false, false) → existsSync(join(tmpDir, '.kiro', 'steering', 'goodvibes.md')) === true
    - Test: "writes .github/copilot-instructions.md on fresh init (IDE-01)" — copyTemplates(templateDir, tmpDir, false, false) → existsSync(join(tmpDir, '.github', 'copilot-instructions.md')) === true
    - Test: "existing .cursor/rules/goodvibes.mdc is counted as skipped not overwritten (IDE-03)" — pre-create file with custom content; copyTemplates second run; file content unchanged AND path appears in skipped array
    - Test: "--minimal skips .github/copilot-instructions.md (IDE-04)" — copyTemplates(templateDir, tmpDir, false, true) → existsSync(join(tmpDir, '.github', 'copilot-instructions.md')) === false
    - Test: "--minimal writes .cursor/rules/goodvibes.mdc (IDE-04)" — copyTemplates(templateDir, tmpDir, false, true) → existsSync(join(tmpDir, '.cursor', 'rules', 'goodvibes.mdc')) === true
    - Test: "--minimal writes .windsurfrules (IDE-04)" — copyTemplates(templateDir, tmpDir, false, true) → existsSync(join(tmpDir, '.windsurfrules')) === true
    - Test: "--minimal writes .kiro/steering/goodvibes.md (IDE-04)" — copyTemplates(templateDir, tmpDir, false, true) → existsSync(join(tmpDir, '.kiro', 'steering', 'goodvibes.md')) === true
  </behavior>
  <action>
Append a new describe block "copyTemplates — IDE rule files" to packages/npm/src/steps/copy-templates.test.ts following the exact same structure as the existing describe blocks at the bottom of the file.

The describe block must include:
- beforeEach: mkdtempSync for tmpDir
- afterEach: rmSync(tmpDir, { recursive: true, force: true })
- const templateDir = resolveTemplatesDir() (outside beforeEach, same pattern as existing CI variant tests)

Add 9 it() tests matching the behavior list above.

Important: these tests use the REAL template directory (resolveTemplatesDir()). The TS tests are integration-style by convention in this project — no mocking needed. The new IDE template files authored in plan 08-01 must exist on disk before these tests pass. If running before plan 08-01 completes, the "fresh init" tests will fail (expected RED → GREEN ordering).

Do NOT modify any existing test. Only append.

Test naming convention: it('writes .cursor/rules/goodvibes.mdc on fresh init (IDE-01)')
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm && npm test -- --reporter verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - All 9 new it() tests are present in the file
    - npm test passes (all existing tests still green, new tests green after plan 08-01 template files exist)
    - No existing test case is modified or removed
    - Test names match the CLAUDE.md naming convention: it('describes expected behavior')
    - The describe block follows the beforeEach/afterEach tmpDir cleanup pattern
  </acceptance_criteria>
  <done>npm test passes with 9 new IDE assertion tests green; no pre-existing tests broken</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add IDE file assertions to test_copy_templates.py and update conftest fixture</name>
  <files>
    packages/pip/tests/test_copy_templates.py
    packages/pip/tests/conftest.py
  </files>
  <read_first>
    - packages/pip/tests/conftest.py — read the full template_dir fixture to understand what stub files it creates
    - packages/pip/tests/test_copy_templates.py — read the full file; follow all existing patterns (module imports inside test functions, mocker.patch for merge_claude, tmp_dir/template_dir fixtures)
    - packages/pip/src/goodvibes_cli/steps/copy_templates.py — understand ignore_fn and the .github check for --minimal
  </read_first>
  <behavior>
    - Fixture update: template_dir fixture in conftest.py must include IDE file stubs so Python tests can assert their presence without depending on the real templates directory
      - (template_dir / ".cursor" / "rules" / "goodvibes.mdc").write_text("---\nalwaysApply: true\n---\n# Rules\n")
      - (template_dir / ".github" / "copilot-instructions.md").write_text("# Copilot\n") — note .github/ dir already created in fixture
      - (template_dir / ".windsurfrules").write_text("# Rules\n")
      - (template_dir / ".kiro" / "steering" / "goodvibes.md").write_text("---\ninclusion: always\n---\n# Rules\n")
    - Test: "writes .cursor/rules/goodvibes.mdc on fresh init (IDE-01)" — copy_templates(template_dir, tmp_dir) → (tmp_dir / ".cursor" / "rules" / "goodvibes.mdc").exists() is True
    - Test: "writes .windsurfrules on fresh init (IDE-01)" — (tmp_dir / ".windsurfrules").exists() is True
    - Test: "writes .kiro/steering/goodvibes.md on fresh init (IDE-01)" → (tmp_dir / ".kiro" / "steering" / "goodvibes.md").exists() is True
    - Test: "writes .github/copilot-instructions.md on fresh init (IDE-01)" → (tmp_dir / ".github" / "copilot-instructions.md").exists() is True
    - Test: "existing .cursor/rules/goodvibes.mdc is counted as skipped not overwritten (IDE-03)" — pre-create file; second copy_templates run; file content unchanged AND path in skipped list
    - Test: "--minimal skips .github/copilot-instructions.md (IDE-04)" → (tmp_dir / ".github" / "copilot-instructions.md").exists() is False
    - Test: "--minimal writes .cursor/rules/goodvibes.mdc (IDE-04)" → (tmp_dir / ".cursor" / "rules" / "goodvibes.mdc").exists() is True
    - Test: "--minimal writes .windsurfrules (IDE-04)" → (tmp_dir / ".windsurfrules").exists() is True
    - Test: "--minimal writes .kiro/steering/goodvibes.md (IDE-04)" → (tmp_dir / ".kiro" / "steering" / "goodvibes.md").exists() is True
  </behavior>
  <action>
Step 1 — Update packages/pip/tests/conftest.py:

In the template_dir fixture, after the existing (issue_template / "bug_report.md").write_text("# Bug\n") line, add:

    # IDE rule file stubs (Phase 8 — IDE-01 through IDE-04)
    cursor_rules = d / ".cursor" / "rules"
    cursor_rules.mkdir(parents=True)
    (cursor_rules / "goodvibes.mdc").write_text("---\nalwaysApply: true\n---\n# Rules\n")
    (d / ".github" / "copilot-instructions.md").write_text("# Copilot\n")
    (d / ".windsurfrules").write_text("# Rules\n")
    kiro_steering = d / ".kiro" / "steering"
    kiro_steering.mkdir(parents=True)
    (kiro_steering / "goodvibes.md").write_text("---\ninclusion: always\n---\n# Rules\n")

Step 2 — Append 9 test functions to packages/pip/tests/test_copy_templates.py following the existing pattern (import inside function body, mocker.patch for merge_claude, using tmp_dir and template_dir fixtures):

Function names: test_copy_templates_writes_cursor_mdc_on_fresh_init, test_copy_templates_writes_windsurfrules_on_fresh_init, test_copy_templates_writes_kiro_steering_on_fresh_init, test_copy_templates_writes_copilot_instructions_on_fresh_init, test_copy_templates_skips_existing_cursor_mdc_and_counts_as_skipped, test_copy_templates_minimal_skips_copilot_instructions, test_copy_templates_minimal_writes_cursor_mdc, test_copy_templates_minimal_writes_windsurfrules, test_copy_templates_minimal_writes_kiro_steering.

Do NOT modify any existing test function or the conftest fixtures other than the template_dir addition described above.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && python -m pytest tests/test_copy_templates.py -v 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - conftest.py template_dir fixture creates all four IDE stub files
    - 9 new test functions exist in test_copy_templates.py following existing naming convention (test_describes_expected_behavior)
    - pytest tests/test_copy_templates.py passes (all green)
    - No existing test function is modified or removed
    - Each new test imports copy_templates inside the function body (existing convention)
  </acceptance_criteria>
  <done>pytest tests/test_copy_templates.py passes with 9 new IDE assertion tests green; no pre-existing tests broken</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → real templates/ | resolveTemplatesDir() resolves to repo-root templates/ under vitest — tests depend on plan 08-01 completing first |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-8-02-01 | Tampering | Test tmpdir | accept | Tests use OS tmpdir with unique prefix; rmSync cleanup in afterEach; no security surface |
| T-8-SC | Tampering | npm/pip installs | accept | No new packages; existing vitest and pytest are already installed |
</threat_model>

<verification>
After both tasks:

1. TS test suite green:
   cd packages/npm && npm test
   (All tests pass, including the 9 new IDE assertions)

2. Python test suite green:
   cd packages/pip && python -m pytest tests/test_copy_templates.py -v
   (All tests pass, including the 9 new IDE assertions)

3. Verify test count increased:
   grep -c "it('writes\|it('existing\|it('--minimal" packages/npm/src/steps/copy-templates.test.ts
   (Should be >= 9 matches from new tests)

   grep -c "def test_copy_templates_writes.*ide\|def test_copy_templates_minimal.*ide\|def test_copy_templates_skips_existing_cursor" packages/pip/tests/test_copy_templates.py
   (Should be >= 3 matches)

4. Conftest fixture includes IDE stubs:
   grep -c "cursor\|windsurfrules\|kiro" packages/pip/tests/conftest.py
   (Should be > 0)
</verification>

<success_criteria>
- npm test passes with 9 new IDE rule file tests
- pytest tests/test_copy_templates.py passes with 9 new IDE rule file tests
- IDE-03 (no-clobber) and IDE-04 (--minimal behavior) are covered by automated tests
- No pre-existing test is broken
</success_criteria>

<output>
Create .planning/phases/08-multi-ide-expansion/08-02-SUMMARY.md when done
</output>
