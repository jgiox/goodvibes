---
phase: 01-template-content-repo-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-phase1.sh
  - templates/CLAUDE.md
autonomous: true
requirements:
  - CLAUDEMD-01
  - CLAUDEMD-02
  - CLAUDEMD-03
  - CLAUDEMD-04
  - CLAUDEMD-05

must_haves:
  truths:
    - "D-02: templates/CLAUDE.md exists and is between 80 and 100 lines (curation rule: keep core rule + required behavior per section; strip all Good/Bad examples, enforcement blocks, and questions-to-ask callouts)"
    - "D-08: all template files live under templates/ at repo root, mirroring the exact target project layout; no path translation required at copy time"
    - "CLAUDE.md contains a <!-- goodvibes:start --> / <!-- goodvibes:end --> sentinel block"
    - "CLAUDE.md contains the version stamp '# goodvibes: v1.0.0' inside the sentinel block"
    - "CLAUDE.md contains all 6 required sections: Before you begin, Think before coding, Simplicity first, Surgical changes, Fail loud, Security, Journal"
    - "CLAUDE.md embeds the full ponytail minimalism ruleset inline (the ladder, rules, when-not-to-be-lazy sections — not paraphrased)"
    - "scripts/verify-phase1.sh exists, is executable, and all its check() invocations run without error syntax"
  artifacts:
    - path: "templates/CLAUDE.md"
      provides: "Template CLAUDE.md for user project installation"
      min_lines: 80
      contains: "goodvibes:start"
    - path: "scripts/verify-phase1.sh"
      provides: "Phase 1 smoke-test harness"
      contains: "verify-phase1"
  key_links:
    - from: "templates/CLAUDE.md"
      to: "<!-- goodvibes:start --> sentinel"
      via: "HTML comment markers"
      pattern: "goodvibes:start"
    - from: "templates/CLAUDE.md"
      to: "ponytail ruleset"
      via: "inline embedded text"
      pattern: "lazy senior developer"
---

<objective>
Create the phase smoke-test harness and the most constrained deliverable of Phase 1:
the canonical templates/CLAUDE.md file that gets installed into every user project.

Purpose: Establishes the verification infrastructure (verify-phase1.sh) so all subsequent
plan waves can self-check, and produces the template CLAUDE.md that auto-activates ponytail
minimalism rules and embeds the 6 curated engineering rule sections (per D-01 through D-06).

Output:
- scripts/verify-phase1.sh — batch smoke-test runner (14 checks, all 22 Phase 1 requirements)
- templates/CLAUDE.md — 80-100 line template; sentinel-wrapped; ponytail inline; 6 rule sections
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-template-content-repo-foundation/01-CONTEXT.md
@.planning/phases/01-template-content-repo-foundation/01-RESEARCH.md
@.planning/phases/01-template-content-repo-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/verify-phase1.sh smoke-test harness</name>
  <files>scripts/verify-phase1.sh</files>
  <read_first>
    - .planning/phases/01-template-content-repo-foundation/01-PATTERNS.md lines 645-689 — exact shell script pattern with check() function and all 14 check() invocations
    - .planning/phases/01-template-content-repo-foundation/01-VALIDATION.md — full requirement-to-command map
  </read_first>
  <action>
    Create scripts/verify-phase1.sh using the exact pattern from PATTERNS.md lines 650-688.

    The script must:
    - Start with #!/usr/bin/env bash and set -e
    - Define check() { if eval "$2" > /dev/null 2>&1; then echo "PASS [$1]" ... else echo "FAIL [$1]: $2" ... fi } with pass/fail counters
    - Include exactly these check() calls (in this order, matching VALIDATION.md): CLAUDEMD-01, CLAUDEMD-02, CLAUDEMD-03, CLAUDEMD-04, CLAUDEMD-05, CAV-01, CAV-02, HYG-01, HYG-03, DOCS-01, DOCS-02, DOCS-03, DOCS-04, DOCS-05, DOCS-06, DOCS-07, REPO-01, REPO-02, REPO-03
    - Use the exact commands from PATTERNS.md: `test -f templates/CLAUDE.md` for CLAUDEMD-01; `[ $(wc -l < templates/CLAUDE.md) -le 100 ] && [ $(wc -l < templates/CLAUDE.md) -ge 80 ]` for CLAUDEMD-02; `grep -q 'goodvibes: v1.0.0' templates/CLAUDE.md` for CLAUDEMD-03; `grep -q 'goodvibes:start' templates/CLAUDE.md` for CLAUDEMD-04; `grep -q 'Think before' templates/CLAUDE.md && grep -q 'Simplicity' templates/CLAUDE.md` for CLAUDEMD-05; `test -d templates/.claude/skills/caveman` for CAV-01; `grep -q 'Julius Brussee' NOTICE` for CAV-02; `test -d templates/.claude/skills/goodvibes-hygiene` for HYG-01; `grep -q 'plugin marketplace add' templates/.claude/skills/goodvibes-hygiene/SKILL.md` for HYG-03; `test -f templates/CONTRIBUTING.md && ! grep -q 'TODO\|YOUR_PROJECT' templates/CONTRIBUTING.md` for DOCS-01; `test -f templates/SECURITY.md` for DOCS-02; `test -f templates/JOURNAL.md` for DOCS-03; `test -f templates/CHANGELOG.md` for DOCS-04; `test -f templates/.github/ISSUE_TEMPLATE/bug_report.yml && test -f templates/.github/ISSUE_TEMPLATE/feature_request.yml` for DOCS-05; `test -f templates/.github/PULL_REQUEST_TEMPLATE.md` for DOCS-06; `test -f templates/docs/onboarding.md` for DOCS-07; `test -f LICENSE` for REPO-01; `test -f NOTICE` for REPO-02; `grep -q 'npx goodvibes init' README.md` for REPO-03
    - End with: echo "" / echo "Results: $pass passed, $fail failed" / [ $fail -eq 0 ] && echo "Phase 1 gate: PASS" || { echo "Phase 1 gate: FAIL"; exit 1; }

    After writing, make it executable: chmod +x scripts/verify-phase1.sh
  </action>
  <verify>
    <automated>bash -n scripts/verify-phase1.sh && echo "Syntax OK"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/verify-phase1.sh exists: `test -f scripts/verify-phase1.sh && echo PASS`
    - Script passes bash syntax check: `bash -n scripts/verify-phase1.sh` exits 0
    - Script is executable: `test -x scripts/verify-phase1.sh && echo PASS`
    - Script contains check for CLAUDEMD-01: `grep -q 'CLAUDEMD-01' scripts/verify-phase1.sh && echo PASS`
    - Script contains check for REPO-03: `grep -q 'REPO-03' scripts/verify-phase1.sh && echo PASS`
    - Script contains "Phase 1 gate": `grep -q 'Phase 1 gate' scripts/verify-phase1.sh && echo PASS`
  </acceptance_criteria>
  <done>scripts/verify-phase1.sh exists, is executable, passes syntax check, and contains all 19 check() invocations covering requirements CLAUDEMD-01 through REPO-03</done>
</task>

<task type="auto">
  <name>Task 2: Author templates/CLAUDE.md (80-100 lines, sentinel-wrapped, ponytail inline, 6 rule sections)</name>
  <files>templates/CLAUDE.md</files>
  <read_first>
    - /mnt/c/Users/YiannisGiokas/Downloads/Code Directions.md — the SOURCE for all 6 engineering rule sections; read all sections retained per D-03: "Before you begin", "Think before coding", "Simplicity first", "Surgical changes", "Security", "Fail loud", "Journal"
    - .planning/phases/01-template-content-repo-foundation/01-RESEARCH.md lines 176-227 — sentinel block structure, line budget, curation map
    - .planning/phases/01-template-content-repo-foundation/01-PATTERNS.md lines 57-176 — exact sentinel layout, curation map table, ponytail embed text verbatim
  </read_first>
  <action>
    Create templates/CLAUDE.md following the EXACT structure and constraints below.

    STRUCTURE (exact section order):
    1. File header: `# CLAUDE.md` (1 line)
    2. One blank line
    3. `<!-- goodvibes:start -->` (sentinel open — 1 line)
    4. `# goodvibes: v1.0.0` (version stamp — per D-04, CLAUDEMD-03)
    5. Blank line
    6. `## Engineering Rules` heading (1 line)
    7. Blank line
    8. Section: `### Before you begin` — from Code Directions.md "Before you begin" block: keep the 5-bullet checklist (the exact request, success criteria, files you expect to touch, tests you expect to run, docs you expect to update) and the "If you cannot state those things clearly, you are not ready to code." line. Strip nothing from this section.
    9. Section: `### Think before coding` — from Code Directions.md: keep the bold statement ("State assumptions explicitly before implementing.") and the 4 Required behavior bullets only. Strip Good/Bad examples, Enforcement block.
    10. Section: `### Simplicity first` — from Code Directions.md: keep the bold statement ("Use the minimum code that solves the problem.") and the 4 Required behavior bullets only. Strip Good/Bad, Questions to ask, Enforcement.
    11. Section: `### Surgical changes` — from Code Directions.md: keep the bold statement ("Touch only what the task requires.") and the 5 Required behavior bullets only. Strip Good/Bad, Enforcement.
    12. Section: `### Fail loud` — from Code Directions.md: keep the bold statement ("Do not fail silent.") and the 5 Required behavior bullets only. Strip Good/Bad, Enforcement.
    13. Section: `### Security` — from Code Directions.md: keep the bold statement ("Security is an engineering requirement, not a cleanup task.") and the 8 Required behavior bullets and the "Must flag immediately" list (9 items). Strip the Security review checklist and Enforcement.
    14. Section: `### Journal` — from Code Directions.md: keep the bold statement ("Update JOURNAL.md at the end of every task.") and the "Each entry must include" list (8 items) and the 3 Rules bullets. Strip Enforcement.
    15. Blank line
    16. `## Ponytail — Minimalism Ruleset` heading (1 line)
    17. Blank line
    18. Embed the FULL ponytail ruleset verbatim from PATTERNS.md lines 119-165. This includes: the opening paragraph ("You are a lazy senior developer..."), ## Persistence section, ## The ladder section (all 7 rungs), ## Rules section (5 bullets), ## Output section, ## Intensity section (table), ## When NOT to be lazy section. Do NOT omit any section. Do NOT paraphrase.
    19. `<!-- goodvibes:end -->` (sentinel close — 1 line)

    LINE BUDGET DISCIPLINE: After writing, count lines with `wc -l templates/CLAUDE.md`. Target is 80-100 lines. If over 100: shorten the Security "Required behavior" list to 5 bullets (keeping validate input, encode output, parameterized queries, least privilege, secrets out of code) and trim the Journal entry fields to 6 items. If under 80: the file is too sparse — check that ponytail embed is complete.

    PROHIBITED: Do NOT add user-instruction comments inside the sentinel block. Do NOT add sections from excluded topics (Goal-driven execution, ML drift, Push to GitHub, Deps current, MD files current, PR checklist, Red flags). Do NOT add placeholder text like "[YOUR_PROJECT_NAME]".
  </action>
  <verify>
    <automated>test -f templates/CLAUDE.md && lines=$(wc -l < templates/CLAUDE.md) && [ $lines -ge 80 ] && [ $lines -le 100 ] && grep -q 'goodvibes:start' templates/CLAUDE.md && grep -q 'goodvibes: v1.0.0' templates/CLAUDE.md && grep -q 'goodvibes:end' templates/CLAUDE.md && grep -q 'lazy senior developer' templates/CLAUDE.md && echo "PASS: $lines lines"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f templates/CLAUDE.md && echo PASS`
    - Line count 80-100: `lines=$(wc -l < templates/CLAUDE.md); [ $lines -ge 80 ] && [ $lines -le 100 ] && echo "PASS: $lines lines"`
    - Sentinel open present: `grep -q '<!-- goodvibes:start -->' templates/CLAUDE.md && echo PASS`
    - Sentinel close present: `grep -q '<!-- goodvibes:end -->' templates/CLAUDE.md && echo PASS`
    - Version stamp present: `grep -q 'goodvibes: v1.0.0' templates/CLAUDE.md && echo PASS`
    - All 6 required sections present: `grep -q 'Think before coding' templates/CLAUDE.md && grep -q 'Simplicity first' templates/CLAUDE.md && grep -q 'Surgical changes' templates/CLAUDE.md && grep -q 'Fail loud' templates/CLAUDE.md && grep -q 'Security' templates/CLAUDE.md && grep -q 'Journal' templates/CLAUDE.md && echo PASS`
    - Ponytail ladder present: `grep -q 'lazy senior developer' templates/CLAUDE.md && echo PASS`
    - Ponytail "when not to be lazy" present: `grep -q 'When NOT to be lazy' templates/CLAUDE.md && echo PASS`
    - No excluded sections: `grep -qv 'ML drift\|Push to GitHub\|Deps current\|Red flags' templates/CLAUDE.md && echo PASS`
    - No placeholder stubs: `! grep -q 'TODO\|YOUR_PROJECT\|\[PROJECT\]' templates/CLAUDE.md && echo PASS`
  </acceptance_criteria>
  <done>templates/CLAUDE.md is 80-100 lines, contains sentinel block with version stamp, all 6 required engineering rule sections from Code Directions.md (per D-02, D-03), and the full ponytail ruleset verbatim (per D-05, D-06). No excluded sections, no placeholders, no user-instruction comments inside sentinel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External content → templates/CLAUDE.md | Ponytail ruleset fetched from DietrichGebert/ponytail and embedded inline |
| Code Directions.md → templates/CLAUDE.md | User-owned source document curated into template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | templates/CLAUDE.md ponytail embed | mitigate | Embed from verified PATTERNS.md §Code Examples (GitHub API fetched 2026-06-23); PATTERNS.md lines 119-165 are the authoritative copy — executor reads from there, not from an arbitrary URL |
| T-01-02 | Information Disclosure | templates/CLAUDE.md examples | mitigate | Code Directions.md contains no credentials; curated content strips all examples and enforcement blocks per D-02; no credential-adjacent text survives curation |
| T-01-03 | Repudiation | templates/CLAUDE.md version stamp | accept | Version stamp `# goodvibes: v1.0.0` is plaintext metadata, not a signature; upgrade logic (Phase 5) relies on its presence, not its integrity; ASVS L1 does not require tamper-evident stamps for static template files |
| T-01-04 | Tampering | scripts/verify-phase1.sh | accept | Script is a local dev tool in source control; not executed in CI or in user environments; bash -n syntax check on write is sufficient at ASVS L1 |
| T-01-05 | Information Disclosure | scripts/verify-phase1.sh grep patterns | mitigate | All grep patterns reference file content only; no secrets or tokens referenced; no eval of external input |
| T-01-SC | Tampering | npm/pip installs | accept | Phase 1 installs NO packages. Package legitimacy gate is not applicable — no install tasks exist. |

**ASVS L1 Context:** Phase 1 creates static text files only. No code executes in production, no user input is processed, no network calls are made. Security surface is documentation quality (no credential leakage in examples) and license compliance (NOTICE file). Both are addressed above.
</threat_model>

<verification>
After both tasks complete, run the full verify script (it will report FAIL for files not yet created in later plans — that is expected at this stage):

```bash
bash scripts/verify-phase1.sh
```

Expected at end of Plan 01: CLAUDEMD-01 through CLAUDEMD-05 checks PASS; all other checks FAIL (files not yet created). Overall gate will FAIL — this is correct and expected.

To confirm Plan 01 specifically:
```bash
test -f templates/CLAUDE.md && wc -l templates/CLAUDE.md && grep -c 'goodvibes:start' templates/CLAUDE.md && grep -c 'lazy senior developer' templates/CLAUDE.md
```
</verification>

<success_criteria>
- scripts/verify-phase1.sh exists, is executable, passes bash -n syntax check
- templates/CLAUDE.md is 80-100 lines
- templates/CLAUDE.md contains sentinel block open (`<!-- goodvibes:start -->`) and close (`<!-- goodvibes:end -->`)
- templates/CLAUDE.md contains `# goodvibes: v1.0.0` inside the sentinel block
- templates/CLAUDE.md contains all 7 sections: Before you begin, Think before coding, Simplicity first, Surgical changes, Fail loud, Security, Journal
- templates/CLAUDE.md contains ponytail embed with "lazy senior developer" and "When NOT to be lazy" sections
- No excluded sections (ML drift, Push to GitHub, Deps current, Red flags) present
- No placeholder stubs (TODO, YOUR_PROJECT) present
</success_criteria>

<output>
Create .planning/phases/01-template-content-repo-foundation/01-01-SUMMARY.md when done
</output>
