---
phase: 01-template-content-repo-foundation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - LICENSE
  - NOTICE
  - README.md
  - packages/npm/.gitkeep
  - packages/pip/.gitkeep
autonomous: true
requirements:
  - REPO-01
  - REPO-02
  - REPO-03
  - REPO-04

must_haves:
  truths:
    - "LICENSE file exists at repo root with Apache 2.0 text and 'Copyright 2026 Ioannis Giokas' header"
    - "NOTICE file credits caveman (Julius Brussee, MIT), ponytail (DietrichGebert, MIT), and headroom (Headroom Contributors, Apache 2.0)"
    - "README.md has 'npx goodvibes init' as the first shell code block a visitor sees"
    - "README.md describes the four layers: caveman, headroom, ponytail, CLAUDE.md rules"
    - "packages/npm/ and packages/pip/ directories exist (placeholder for Phase 2/3 CLI source)"
  artifacts:
    - path: "LICENSE"
      provides: "Apache 2.0 license text for the goodvibes repo"
      contains: "Apache License"
    - path: "NOTICE"
      provides: "Third-party attribution for caveman, ponytail, headroom"
      contains: "Julius Brussee"
    - path: "README.md"
      provides: "Repo landing page with hero command"
      contains: "npx goodvibes init"
    - path: "packages/npm/.gitkeep"
      provides: "Placeholder for npm CLI source (Phase 2)"
    - path: "packages/pip/.gitkeep"
      provides: "Placeholder for pip CLI source (Phase 3)"
  key_links:
    - from: "NOTICE"
      to: "templates/.claude/skills/caveman/"
      via: "Apache 2.0 Section 4d attribution requirement"
      pattern: "Julius Brussee"
    - from: "README.md"
      to: "docs/onboarding.md"
      via: "Docs section link"
      pattern: "npx goodvibes init"
---

<objective>
Create the repo's legal foundation (LICENSE + NOTICE), landing page (README.md), and
placeholder package directories (packages/npm/, packages/pip/).

Purpose: Establishes Apache 2.0 compliance (REPO-01, REPO-02), the public-facing hero
action that converts first-time visitors (REPO-03), and the monorepo structure that
Phase 2/3 CLI packages will populate (REPO-04, D-09, D-10).

Output:
- LICENSE — official Apache 2.0 text from apache.org with 2026 copyright line
- NOTICE — Apache 2.0 Section 4d attribution for caveman (MIT), ponytail (MIT), headroom (Apache 2.0)
- README.md — hero command first, four-layers description, docs links
- packages/npm/.gitkeep — placeholder for npm CLI (Phase 2)
- packages/pip/.gitkeep — placeholder for pip CLI (Phase 3)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-template-content-repo-foundation/01-CONTEXT.md
@.planning/phases/01-template-content-repo-foundation/01-RESEARCH.md
@.planning/phases/01-template-content-repo-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create LICENSE and NOTICE files at repo root</name>
  <files>LICENSE, NOTICE</files>
  <read_first>
    - .planning/phases/01-template-content-repo-foundation/01-PATTERNS.md lines 548-595 — exact NOTICE pattern (verified against Apache 2.0 Section 4d) and LICENSE source instruction
    - .planning/phases/01-template-content-repo-foundation/01-RESEARCH.md lines 327-356 — NOTICE file pattern verified against headroom NOTICE via GitHub API
  </read_first>
  <action>
    NOTICE file: Create NOTICE at repo root using the exact pattern from PATTERNS.md lines 566-592.

    The NOTICE file must contain exactly:
    - Line 1: `goodvibes`
    - Line 2: `Copyright 2026 Ioannis Giokas (jgiox)`
    - Line 3: blank
    - Line 4: `This product includes software developed by the goodvibes contributors.`
    - Line 5: blank
    - Line 6: `Third-Party Attributions`
    - Line 7: `========================`
    - Line 8: blank
    - caveman block: name, dashes line, `Copyright 2026 Julius Brussee`, `Licensed under the MIT License`, `https://github.com/juliusbrussee/caveman`
    - blank line
    - ponytail block: name, dashes line, `Copyright 2026 DietrichGebert`, `Licensed under the MIT License`, `https://github.com/DietrichGebert/ponytail`
    - blank line
    - headroom block: name, dashes line, `Copyright 2025 Headroom Contributors`, `Licensed under the Apache License, Version 2.0`, `https://github.com/headroomlabs-ai/headroom`

    LICENSE file: Fetch the official Apache 2.0 license text from:
    https://www.apache.org/licenses/LICENSE-2.0.txt

    Insert the copyright line at the top of the fetched text:
    `Copyright 2026 Ioannis Giokas (jgiox)`
    followed by a blank line, then the full Apache 2.0 text verbatim.

    CRITICAL: Do NOT write the Apache 2.0 text from memory. Fetch the official text.
    Any deviation creates legal ambiguity (per RESEARCH.md §Don't Hand-Roll).
  </action>
  <verify>
    <automated>test -f LICENSE && test -f NOTICE && grep -q 'Apache License' LICENSE && grep -q 'Julius Brussee' NOTICE && grep -q 'DietrichGebert' NOTICE && grep -q 'Headroom Contributors' NOTICE && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - LICENSE exists: `test -f LICENSE && echo PASS`
    - LICENSE contains Apache 2.0 text: `grep -q 'Apache License' LICENSE && echo PASS`
    - LICENSE contains copyright line: `grep -q 'Ioannis Giokas' LICENSE && echo PASS`
    - NOTICE exists: `test -f NOTICE && echo PASS`
    - NOTICE credits caveman (CAV-02 requirement): `grep -q 'Julius Brussee' NOTICE && echo PASS`
    - NOTICE credits ponytail: `grep -q 'DietrichGebert' NOTICE && echo PASS`
    - NOTICE credits headroom: `grep -q 'Headroom Contributors' NOTICE && echo PASS`
    - NOTICE has GitHub URLs for all three: `grep -q 'juliusbrussee/caveman' NOTICE && grep -q 'DietrichGebert/ponytail' NOTICE && grep -q 'headroomlabs-ai/headroom' NOTICE && echo PASS`
  </acceptance_criteria>
  <done>LICENSE contains official Apache 2.0 text (fetched from apache.org) with copyright header for Ioannis Giokas. NOTICE credits caveman (Julius Brussee, MIT), ponytail (DietrichGebert, MIT), and headroom (Headroom Contributors, Apache 2.0) with GitHub URLs for each.</done>
</task>

<task type="auto">
  <name>Task 2: Create README.md with hero command and four-layers description; create packages/ placeholders</name>
  <files>README.md, packages/npm/.gitkeep, packages/pip/.gitkeep</files>
  <read_first>
    - .planning/phases/01-template-content-repo-foundation/01-PATTERNS.md lines 599-631 — README.md exact structure pattern with hero command placement, four-layers content, docs links
    - .planning/phases/01-template-content-repo-foundation/01-CONTEXT.md decisions D-14 — README scope: hero command + one-paragraph what-it-does + four layers + docs links, no detailed setup steps
    - .planning/phases/01-template-content-repo-foundation/01-CONTEXT.md decision D-10 — packages/ placeholder structure: packages/npm/ and packages/pip/ with .gitkeep
  </read_first>
  <action>
    README.md: Create README.md at repo root using the structure from PATTERNS.md lines 606-631.

    STRUCTURE (exact order, per D-14 and REPO-03):
    1. `# goodvibes` (h1)
    2. Blank line
    3. Shell code block containing ONLY: `npx goodvibes init`
       (This is the hero action — it MUST be the first code block, before any prose)
    4. Blank line
    5. One-paragraph description: "goodvibes is a single-command bootstrap for vibe coding
       projects. Run npx goodvibes init and it quietly installs four layers that keep your
       context window efficient and your code clean — no configuration required."
       (Adapt this prose, but keep it to 2-3 sentences maximum. Do not mention team workflows,
       stakeholders, or enterprise patterns.)
    6. Blank line
    7. `## How it works` heading
    8. Blank line
    9. Sentence: "goodvibes installs four layers into your project:"
    10. Blank line
    11. Bullet list — four layers (exact names and descriptions per PATTERNS.md lines 619-625):
        - `**caveman**` — compresses Claude's output tokens so you get more done per context window
        - `**headroom**` — compresses input tokens by summarizing what Claude reads
        - `**ponytail**` — keeps your code minimal (enforced via CLAUDE.md on every session)
        - `**CLAUDE.md rules**` — engineering discipline: think before coding, simplicity first, fail loud, security basics, keep a journal
    12. Blank line
    13. `## Docs` heading
    14. Blank line
    15. Doc links (relative markdown links, per D-14):
        - `[onboarding.md](docs/onboarding.md) — git and PR basics for beginners`
        - `[CONTRIBUTING.md](CONTRIBUTING.md)`
        - `[SECURITY.md](SECURITY.md)`
        - `[CHANGELOG.md](CHANGELOG.md)`

    DO NOT include: detailed setup instructions (CLI handles it), team/enterprise language,
    badge rows as the first element, or any placeholder text.

    packages/ placeholders: Create empty .gitkeep files at:
    - packages/npm/.gitkeep
    - packages/pip/.gitkeep
    (These are empty files — just create them. They exist so git tracks the directories
    per D-10: "Phase 1 creates the packages/ directory structure (empty) as placeholder.")
  </action>
  <verify>
    <automated>test -f README.md && grep -q 'npx goodvibes init' README.md && test -f packages/npm/.gitkeep && test -f packages/pip/.gitkeep && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - README.md exists: `test -f README.md && echo PASS`
    - Hero command present (REPO-03 smoke test): `grep -q 'npx goodvibes init' README.md && echo PASS`
    - Four layers mentioned — caveman: `grep -q 'caveman' README.md && echo PASS`
    - Four layers mentioned — headroom: `grep -q 'headroom' README.md && echo PASS`
    - Four layers mentioned — ponytail: `grep -q 'ponytail' README.md && echo PASS`
    - Docs section with links: `grep -q 'onboarding.md' README.md && echo PASS`
    - No placeholder stubs: `! grep -q 'TODO\|YOUR_PROJECT\|\[PROJECT\]' README.md && echo PASS`
    - packages/npm/.gitkeep exists: `test -f packages/npm/.gitkeep && echo PASS`
    - packages/pip/.gitkeep exists: `test -f packages/pip/.gitkeep && echo PASS`
  </acceptance_criteria>
  <done>README.md exists with npx goodvibes init as the first code block, one-paragraph description, four-layers section (caveman, headroom, ponytail, CLAUDE.md rules), and docs links. packages/npm/.gitkeep and packages/pip/.gitkeep exist as directory placeholders for Phase 2/3 CLI source.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| apache.org → LICENSE | Official license text fetched from external authoritative source |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | LICENSE (Apache 2.0 text) | mitigate | Fetch from https://www.apache.org/licenses/LICENSE-2.0.txt only — not from npm, GitHub mirrors, or memory. Any modification to license text creates legal ambiguity. |
| T-03-02 | Repudiation | NOTICE file attribution | mitigate | NOTICE content matches RESEARCH.md §NOTICE pattern verified against Apache 2.0 Section 4d and headroom's own NOTICE file via GitHub API (2026-06-23). Copyright years and names match upstream. |
| T-03-03 | Information Disclosure | README.md | accept | README is public-facing documentation. Contains no credentials, tokens, or PII. Placeholder check (`! grep TODO`) prevents accidental stubs that could mislead users. |
| T-03-04 | Spoofing | NOTICE — incorrect copyright holder names | mitigate | Names verified: Julius Brussee (juliusbrussee/caveman), DietrichGebert (DietrichGebert/ponytail), Headroom Contributors (headroomlabs-ai/headroom). Do not alter names or years from RESEARCH.md pattern. |
| T-03-SC | Tampering | npm/pip installs | accept | Phase 1 installs NO packages. No package install tasks in this plan. Package legitimacy gate not applicable. |

**ASVS L1 Context:** Legal files (LICENSE, NOTICE) carry compliance risk, not security risk per se. The mitigation is sourcing from authoritative upstream (apache.org for LICENSE) and using verified attribution data (RESEARCH.md for NOTICE). README.md presents no ASVS-relevant surface.
</threat_model>

<verification>
After both tasks complete:

```bash
# Legal compliance checks
test -f LICENSE && grep -c 'Apache License' LICENSE
test -f NOTICE && grep -c 'Julius Brussee' NOTICE && grep -c 'DietrichGebert' NOTICE && grep -c 'Headroom Contributors' NOTICE

# README hero action check
grep -c 'npx goodvibes init' README.md

# Package placeholders
test -f packages/npm/.gitkeep && test -f packages/pip/.gitkeep && echo "placeholders OK"
```
</verification>

<success_criteria>
- LICENSE exists with official Apache 2.0 text and Ioannis Giokas copyright header
- NOTICE exists crediting caveman (Julius Brussee, MIT), ponytail (DietrichGebert, MIT), headroom (Headroom Contributors, Apache 2.0)
- README.md has npx goodvibes init as the first code element; describes all four layers
- packages/npm/.gitkeep and packages/pip/.gitkeep exist
- No placeholder stubs in README.md
</success_criteria>

<output>
Create .planning/phases/01-template-content-repo-foundation/01-03-SUMMARY.md when done
</output>
