---
phase: 03-pip-cli
plan: 03
type: execute
wave: 1
depends_on:
  - "03-01"
files_modified:
  - packages/pip/src/goodvibes_cli/utils/detect_python.py
  - packages/pip/src/goodvibes_cli/steps/install_headroom.py
  - packages/pip/src/goodvibes_cli/steps/configure_mcp.py
  - packages/pip/tests/test_install_headroom.py
  - packages/pip/tests/test_configure_mcp.py
autonomous: true
requirements:
  - PIP-01
  - PIP-02
  - PIP-03

must_haves:
  truths:
    - "detect_python() returns None when all probes are ENOENT (Python not on PATH)"
    - "detect_python() returns None when python3 output is 'Python 2.7' (version < 3.10)"
    - "detect_python() returns 'python3' when output matches 'Python 3.12'"
    - "detect_python() skips commands whose output does not match /Python \\d+\\.\\d+/ (Windows Store guard)"
    - "install_headroom() tries uv first, then pipx, then pip --user — in that order"
    - "install_headroom() logs ONNX warning before starting any subprocess call"
    - "install_headroom() soft-fails on CalledProcessError without raising (never exits 1 on C++ failure)"
    - "install_headroom() returns gracefully when all installers are FileNotFoundError"
    - "install_headroom() never uses shell=True"
    - "configure_mcp() checks headroom mcp status first (idempotency)"
    - "configure_mcp() primary: claude mcp add -s user headroom <absolute-path>"
    - "configure_mcp() fallback: headroom mcp install when claude is FileNotFoundError"
    - "configure_mcp() returns gracefully when headroom binary is not on PATH"
    - "install_headroom() passes args as list: ['uv', 'tool', 'install', 'headroom-ai[all]']"
  artifacts:
    - path: "packages/pip/src/goodvibes_cli/utils/detect_python.py"
      provides: "detect_python() — probes python3/python/py, returns first 3.10+ cmd or None"
      exports: ["detect_python"]
    - path: "packages/pip/src/goodvibes_cli/steps/install_headroom.py"
      provides: "install_headroom(log) — uv→pipx→pip chain, soft-fail on all errors"
      exports: ["install_headroom"]
    - path: "packages/pip/src/goodvibes_cli/steps/configure_mcp.py"
      provides: "configure_mcp(log) — Strategy B primary, Strategy A fallback, ENOENT-safe"
      exports: ["configure_mcp"]
    - path: "packages/pip/tests/test_install_headroom.py"
      provides: "Mocked subprocess tests — no real uv/pip/pipx called in suite"
    - path: "packages/pip/tests/test_configure_mcp.py"
      provides: "Mocked subprocess tests — no real claude/headroom called in suite"
  key_links:
    - from: "packages/pip/src/goodvibes_cli/steps/install_headroom.py"
      to: "goodvibes_cli.utils.detect_python.detect_python"
      via: "from goodvibes_cli.utils.detect_python import detect_python"
      pattern: "from goodvibes_cli.utils.detect_python import detect_python"
    - from: "packages/pip/src/goodvibes_cli/steps/install_headroom.py"
      to: "subprocess.run"
      via: "subprocess.run(cmd_list, check=True)"
      pattern: "subprocess.run"
    - from: "packages/pip/src/goodvibes_cli/steps/configure_mcp.py"
      to: "shutil.which"
      via: "shutil.which('headroom') for absolute path resolution"
      pattern: "shutil.which"
---

<objective>
Wave 1b: implement detect_python.py, install_headroom.py, and configure_mcp.py — the headroom
integration layer. Runs in parallel with 03-02 (no file conflicts).

Purpose: These are the subprocess-heavy modules. All subprocess calls use list args and
FileNotFoundError guards. Tests use pytest-mock to mock subprocess.run — no real uv/pipx/
pip/claude/headroom processes run during the test suite.

Output: detect_python.py, install_headroom.py, configure_mcp.py, and their passing test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-pip-cli/03-RESEARCH.md
@packages/npm/src/utils/detect-python.ts
@packages/npm/src/steps/install-headroom.ts
@packages/npm/src/steps/configure-mcp.ts
@.planning/phases/03-pip-cli/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: detect_python.py + install_headroom.py</name>
  <files>
    packages/pip/src/goodvibes_cli/utils/detect_python.py,
    packages/pip/src/goodvibes_cli/steps/install_headroom.py,
    packages/pip/tests/test_install_headroom.py
  </files>
  <behavior>
    detect_python():
    - Probes ["python3", "python", "py"] in order
    - For each: subprocess.run([cmd, "--version"], capture_output=True, text=True, check=False)
    - Matches stdout or stderr against r"Python (\d+)\.(\d+)"
    - No match → continue (Windows Store guard)
    - major > 3 → return cmd (future Python 4)
    - major == 3 and minor >= 10 → return cmd
    - major < 3 OR minor < 10 → continue
    - FileNotFoundError → continue
    - Any other exception → continue
    - All probes exhausted → return None

    install_headroom(log: Callable[[str], None]) -> None:
    - Calls detect_python(); if None → log skip message; return
    - Logs ONNX warning BEFORE any subprocess.run call
    - Defines installers list (order matters):
        [["uv", "tool", "install", "headroom-ai[all]"],
         ["pipx", "install", "headroom-ai[all]"],
         [python_cmd, "-m", "pip", "install", "--user", "headroom-ai[all]"]]
    - For each installer: subprocess.run(cmd_list, capture_output=True, text=True, check=True)
    - FileNotFoundError → continue to next
    - CalledProcessError → log first line of e.stderr as error + manual-install hint; return
    - Success (no exception) → return
    - Loop exhausted (all FileNotFoundError) → log "no installer found" warning; return
    - NEVER raises; NEVER shell=True; NEVER exits non-zero from this function
    - headroom-ai[all] per D-02: runtime-only install (not declared dep); uses [all] in the
      subprocess arg list — brackets are passed as literal list element, no shell expansion needed

    Note on [all] vs [mcp]: The locked decision D-02 specifies runtime-only install matching
    Phase 2 pattern. The TypeScript uses headroom-ai[all] (see install-headroom.ts lines 28-30).
    The Python port uses the same headroom-ai[all] string, passed as a discrete list element
    to subprocess.run — no shell expansion risk.
  </behavior>
  <action>
    Implement packages/pip/src/goodvibes_cli/utils/detect_python.py as a direct port of
    packages/npm/src/utils/detect-python.ts. Use only Python stdlib: subprocess.

    detect_python() -> str | None:
      for cmd in ["python3", "python", "py"]:
        try:
          result = subprocess.run([cmd, "--version"], capture_output=True, text=True, check=False)
          output = result.stdout or result.stderr  # Python <3.4 used stderr
          match = re.search(r"Python (\d+)\.(\d+)", output)
          if not match:
            continue  # Windows Store guard or unexpected output
          major, minor = int(match.group(1)), int(match.group(2))
          if major > 3 or (major == 3 and minor >= 10):
            return cmd
          # version too old, try next
        except FileNotFoundError:
          continue
        except Exception:
          continue
      return None

    Implement packages/pip/src/goodvibes_cli/steps/install_headroom.py as a direct port of
    packages/npm/src/steps/install-headroom.ts. Use only Python stdlib: subprocess.

    import subprocess
    from typing import Callable
    from goodvibes_cli.utils.detect_python import detect_python

    def install_headroom(log: Callable[[str], None]) -> None:
      python_cmd = detect_python()
      if python_cmd is None:
        log("Python 3.10+ not found — skipping headroom install. Install Python 3.10+ and run `goodvibes init` again.")
        return

      # HDR-03: ONNX warning BEFORE any subprocess (so it shows even if installer is slow)
      log("Note: headroom will download its compression model on first use — this may take 1–3 minutes on a slow connection.")

      installers: list[list[str]] = [
        ["uv", "tool", "install", "headroom-ai[all]"],
        ["pipx", "install", "headroom-ai[all]"],
        [python_cmd, "-m", "pip", "install", "--user", "headroom-ai[all]"],
      ]

      for cmd_list in installers:
        try:
          subprocess.run(cmd_list, capture_output=True, text=True, check=True)
          return
        except FileNotFoundError:
          continue
        except subprocess.CalledProcessError as e:
          first_line = (e.stderr or "").splitlines()[0] if (e.stderr or "").splitlines() else "unknown error"
          log(f"headroom install failed: {first_line}")
          log('You can install headroom manually later: uv tool install "headroom-ai[all]"')
          return

      # All three installers were FileNotFoundError
      log("No package installer found (uv, pipx, pip). Install Python 3.10+ with uv or pipx and run `goodvibes init` again.")

    For tests: use pytest-mock mocker.patch("goodvibes_cli.utils.detect_python.subprocess.run")
    and mocker.patch("goodvibes_cli.steps.install_headroom.subprocess.run") to mock subprocess
    calls. Never allow real uv/pipx/pip to run during tests.

    Remove @pytest.mark.skip from test_install_headroom.py and implement:
    - test_uv_found_and_succeeds: mock uv subprocess.run returns CompletedProcess; verify log called with ONNX warning
    - test_pipx_fallback_when_uv_enoent: uv raises FileNotFoundError; pipx succeeds
    - test_pip_fallback_when_uv_and_pipx_enoent: both raise FileNotFoundError; pip succeeds
    - test_all_enoent_logs_warning: all three raise FileNotFoundError; log called with "No package installer found"
    - test_called_process_error_soft_fail: uv raises CalledProcessError; log called with error msg; no raise
    - test_python_absent_skips: detect_python returns None; log called with "Python 3.10+ not found"; returns immediately
    - test_onnx_warning_logged_before_subprocess: verify ONNX warning is first log call before subprocess
    - test_no_shell_true: confirm subprocess.run calls do NOT have shell=True in kwargs
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_install_headroom.py -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    uv run pytest tests/test_install_headroom.py -v exits 0; 8 tests PASSED, 0 failed, 0 skipped;
    no real subprocess calls made during test run (mock assertions pass)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: configure_mcp.py — Strategy B primary, Strategy A fallback, ENOENT-safe</name>
  <files>
    packages/pip/src/goodvibes_cli/steps/configure_mcp.py,
    packages/pip/tests/test_configure_mcp.py
  </files>
  <behavior>
    configure_mcp(log: Callable[[str], None]) -> None:
    - Step 1 idempotency: subprocess.run(["headroom", "mcp", "status"], check=True)
        success (exit 0) → log "headroom MCP already configured"; return
        FileNotFoundError or CalledProcessError → continue to registration
    - Step 2 primary: check claude mcp list for existing "headroom" entry
        subprocess.run(["claude", "mcp", "list"], capture_output=True, text=True, check=True)
        if "headroom" in stdout → log "already registered"; return
    - Step 2 resolve path: shutil.which("headroom") → absolute_path
        if None → log "headroom binary not found on PATH"; return
    - Step 2 register: subprocess.run(["claude", "mcp", "add", "-s", "user", "headroom", absolute_path], check=True)
        success → log "headroom registered as global MCP server"; return
        FileNotFoundError (claude not found) → fall through to Step 3
    - Step 3 fallback: subprocess.run(["headroom", "mcp", "install"], check=True)
        success → return
        FileNotFoundError → log "headroom binary not found"; return
        CalledProcessError → re-raise (unexpected failure in fallback)
    - CLAUDE_CONFIG_DIR warning: logged before headroom mcp install fallback
    - NEVER writes to ~/.claude/ or ~/.claude.json directly
    - NEVER shell=True
  </behavior>
  <action>
    Implement packages/pip/src/goodvibes_cli/steps/configure_mcp.py as a direct port of
    packages/npm/src/steps/configure-mcp.ts. Use only Python stdlib: subprocess, shutil.

    import subprocess
    import shutil
    from typing import Callable

    def configure_mcp(log: Callable[[str], None]) -> None:
      # Step 1: idempotency check
      try:
        subprocess.run(["headroom", "mcp", "status"], capture_output=True, text=True, check=True)
        log("headroom MCP already configured — skipping")
        return
      except (FileNotFoundError, subprocess.CalledProcessError):
        pass

      # Step 2: primary — claude mcp add
      try:
        list_result = subprocess.run(
          ["claude", "mcp", "list"], capture_output=True, text=True, check=True
        )
        if "headroom" in list_result.stdout:
          log("headroom already registered in claude MCP — skipping")
          return

        absolute_path = shutil.which("headroom")
        if not absolute_path:
          log("headroom binary not found on PATH — MCP registration skipped. "
              "Run `uv tool install \"headroom-ai[all]\"` then re-run `goodvibes init`.")
          return

        subprocess.run(
          ["claude", "mcp", "add", "-s", "user", "headroom", absolute_path],
          capture_output=True, text=True, check=True,
        )
        log("headroom registered as global MCP server")
        return
      except FileNotFoundError:
        # claude CLI not on PATH — fall back to headroom mcp install
        log("claude CLI not found — falling back to headroom mcp install")
        log("Warning: if you use CLAUDE_CONFIG_DIR, you may need to run `headroom mcp install` manually")

      # Step 3: fallback — headroom mcp install
      try:
        subprocess.run(["headroom", "mcp", "install"], capture_output=True, text=True, check=True)
      except FileNotFoundError:
        log("headroom binary not found — MCP registration skipped. "
            "Install headroom and run `headroom mcp install` manually.")
      except subprocess.CalledProcessError as e:
        raise  # unexpected failure — re-raise so caller can surface it

    Note on shutil.which vs subprocess which/where: Using shutil.which() is the Python stdlib
    cross-platform equivalent of running "which headroom" (Linux/macOS) or "where headroom"
    (Windows). It avoids running an additional subprocess and handles Windows PATH correctly.
    This is a deliberate improvement over the TypeScript port.

    For tests: mock subprocess.run and shutil.which.
    Remove @pytest.mark.skip from test_configure_mcp.py and implement:
    - test_already_registered_via_mcp_status: headroom mcp status exits 0; log contains "already"
    - test_already_in_claude_mcp_list: mcp status fails; claude mcp list shows "headroom"; returns
    - test_claude_mcp_add_primary: mcp status fails; list clean; shutil.which returns path; claude mcp add called
    - test_headroom_not_on_path: shutil.which returns None; logs "not found on PATH"; returns
    - test_claude_not_found_fallback: claude raises FileNotFoundError; headroom mcp install called; CLAUDE_CONFIG_DIR warning logged
    - test_headroom_enoent_in_fallback: claude FileNotFoundError; headroom also FileNotFoundError; log "not found"
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/test_configure_mcp.py tests/test_install_headroom.py -v 2>&amp;1 | tail -20</automated>
  </verify>
  <done>
    uv run pytest tests/test_configure_mcp.py tests/test_install_headroom.py -v exits 0;
    6 + 8 = 14 tests PASSED, 0 failed, 0 skipped; no real subprocess called in test suite
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → subprocess (uv/pipx/pip) | List args only; no user input flows into cmd_list |
| CLI → subprocess (claude/headroom) | shutil.which() result is a filesystem path; passed as discrete list element |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Tampering | subprocess.run cmd_list construction | mitigate | Args are hardcoded strings + shutil.which() result — no user input; never shell=True |
| T-03-03-02 | Tampering | shutil.which("headroom") path injection | mitigate | Path from shutil.which() is passed as a single discrete list element; no f-string interpolation |
| T-03-03-03 | Elevation of Privilege | pip install --user scope | accept | --user flag limits install to user site-packages; no sudo or system-wide changes |
| T-03-03-04 | Spoofing | Windows Store python stub | mitigate | detect_python() requires /Python \d+\.\d+/ match in output; Store stub produces no such output |
| T-03-03-SC | Tampering | npm/pip/cargo installs | mitigate | No new package installs in this plan; pytest-mock already in dev deps |
</threat_model>

<verification>
Run after both tasks complete:
  cd packages/pip && uv run pytest tests/test_install_headroom.py tests/test_configure_mcp.py -v
Must exit 0 with 14 passed, 0 failed, 0 skipped.

Also verify no shell=True in either implementation file:
  grep -r "shell=True" packages/pip/src/goodvibes_cli/steps/ packages/pip/src/goodvibes_cli/utils/detect_python.py
Must return no output (exit 1 from grep).
</verification>

<success_criteria>
1. uv run pytest tests/test_install_headroom.py -v: 8 passed, 0 failed
2. uv run pytest tests/test_configure_mcp.py -v: 6 passed, 0 failed
3. No shell=True in detect_python.py, install_headroom.py, or configure_mcp.py
4. install_headroom.py imports detect_python from goodvibes_cli.utils.detect_python
5. configure_mcp.py uses shutil.which (not subprocess which/where)
6. headroom-ai[all] appears as literal list element string in install_headroom.py (D-02 runtime install)
7. ONNX warning logged before any subprocess.run call in install_headroom()
</success_criteria>

<output>
Create .planning/phases/03-pip-cli/03-03-SUMMARY.md when done
</output>
