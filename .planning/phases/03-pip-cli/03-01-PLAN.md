---
phase: 03-pip-cli
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - scripts/verify-phase3.sh
  - packages/pip/pyproject.toml
  - packages/pip/src/goodvibes_cli/__init__.py
  - packages/pip/src/goodvibes_cli/__main__.py
  - packages/pip/tests/conftest.py
  - packages/pip/tests/test_sentinel_merge.py
  - packages/pip/tests/test_copy_templates.py
  - packages/pip/tests/test_install_headroom.py
  - packages/pip/tests/test_configure_mcp.py
autonomous: true
requirements:
  - PIP-04
  - PIP-05

must_haves:
  truths:
    - "bash scripts/verify-phase3.sh --quick exits 0"
    - "uv run pytest tests/ -x -q exits 0 (stubs pass)"
    - "packages/pip/pyproject.toml declares name = 'jgiox-goodvibes' with requires-python = '>=3.10'"
    - "goodvibes_cli/__init__.py exports __version__ = '1.0.0'"
    - "goodvibes_cli/__main__.py checks sys.version_info < (3, 10) before any other import"
  artifacts:
    - path: "scripts/verify-phase3.sh"
      provides: "Phase 3 smoke-test harness"
    - path: "packages/pip/pyproject.toml"
      provides: "Package metadata, build config, scripts entry point"
      contains: "jgiox-goodvibes"
    - path: "packages/pip/src/goodvibes_cli/__init__.py"
      provides: "Version string"
      exports: ["__version__"]
    - path: "packages/pip/src/goodvibes_cli/__main__.py"
      provides: "Python version guard (runs before any import)"
    - path: "packages/pip/tests/conftest.py"
      provides: "Shared pytest fixtures (tmp_dir, template_dir, sample_template_content)"
    - path: "packages/pip/tests/test_sentinel_merge.py"
      provides: "13 sentinel merge test stubs (matching Phase 2 coverage)"
    - path: "packages/pip/tests/test_copy_templates.py"
      provides: "9 copy_templates test stubs"
    - path: "packages/pip/tests/test_install_headroom.py"
      provides: "Mocked subprocess test stubs for headroom install chain"
    - path: "packages/pip/tests/test_configure_mcp.py"
      provides: "Mocked subprocess test stubs for MCP registration"
  key_links:
    - from: "pyproject.toml"
      to: "goodvibes_cli.main:app"
      via: "[project.scripts] goodvibes entry point"
      pattern: "goodvibes.*goodvibes_cli"
    - from: "packages/pip/src/goodvibes_cli/__main__.py"
      to: "sys.version_info"
      via: "guard before imports"
      pattern: "sys.version_info"
---

<objective>
Wave 0 foundation: create the verify-phase3.sh smoke harness, scaffold the packages/pip/ package
with pyproject.toml (jgiox-goodvibes, D-01 name), version guard in __main__.py (D-05), and
all test stub files that Wave 1 plans will flesh out.

Purpose: Every Wave 1 plan can immediately run `uv run pytest tests/test_<module>.py -x` and get
meaningful red results. The smoke harness provides a CI-equivalent gate for the whole phase.

Output: scripts/verify-phase3.sh, packages/pip/{pyproject.toml, src/goodvibes_cli/__init__.py,
__main__.py, tests/conftest.py, tests/test_*.py x4}
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-pip-cli/03-RESEARCH.md
@scripts/verify-phase2.sh
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify-phase3.sh smoke harness</name>
  <files>scripts/verify-phase3.sh</files>
  <action>
    Create scripts/verify-phase3.sh mirroring the structure of verify-phase2.sh exactly:
    set -e, cd to repo root from dirname, --quick flag parsing, pass/fail counters,
    check() function, static checks section, build checks section (--quick skips), summary.

    Static checks to include (run every time, --quick safe):
    - PIP-PKG-01: test -f packages/pip/pyproject.toml
    - PIP-PKG-02: pyproject.toml name field is "jgiox-goodvibes" (python3 -c "import tomllib; ..."
      or grep-based: grep -q 'name = "jgiox-goodvibes"' packages/pip/pyproject.toml)
    - PIP-PKG-03: pyproject.toml has 'goodvibes = "goodvibes_cli.main:app"' script entry
    - PIP-PKG-04: requires-python contains >=3.10 (grep -q '>=3.10' packages/pip/pyproject.toml)
    - PIP-05-GUARD: packages/pip/src/goodvibes_cli/__main__.py contains sys.version_info
    - PIP-05-ORDER: version check appears before 'import typer' in __main__.py
      (line-number comparison: grep -n 'sys.version_info' | head -1 line < grep -n 'import typer' | head -1 line)
    - PIP-05-EXIT: __main__.py contains sys.exit(1) in the version guard block
    - PIP-TEST-CFG: pyproject.toml contains [tool.pytest.ini_options]
    - HDR-06-PY: pyproject.toml does NOT contain 'headroom-ai' in [project.dependencies]
      (headroom is runtime-only per D-02; grep -v returns non-zero if found)
    - SENTINEL-FORMAT: grep -q 'goodvibes:start' templates/CLAUDE.md (sentinel markers exist in template)
    - PIP-BUILD-01: test -f packages/pip/src/goodvibes_cli/__init__.py
    - PIP-BUILD-02: test -f packages/pip/src/goodvibes_cli/main.py (will be created in Wave 2)
      — use a soft check (no exit on fail in static section); mark as SKIP-UNTIL-WAVE2

    Build checks (full mode only, --quick=0):
    - PIP-UV-BUILD: cd packages/pip && uv build --no-sources 2>/dev/null && test -f dist/jgiox_goodvibes-*.whl
      (verifies hatchling builds the wheel correctly with the jgiox-goodvibes name)
    - PIP-INSTALL: cd packages/pip && uv run python -c "import goodvibes_cli" 2>/dev/null
    - PIP-HELP: cd packages/pip && uv run goodvibes --help 2>&1 | grep -q goodvibes
    - PIP-DRYRUN: cd packages/pip && uv run goodvibes init --help 2>&1 | grep -q dry-run
    - PIP-MINIMAL: cd packages/pip && uv run goodvibes init --help 2>&1 | grep -q minimal
    - PIP-TEST: cd packages/pip && uv run pytest tests/ -x -q 2>/dev/null

    PIP-BUILD-02 (main.py) should print "SKIP [PIP-BUILD-02]: not yet created (Wave 2)" rather
    than FAIL during Wave 0 — use a conditional check: if file exists, run the real check,
    otherwise print the skip note and increment pass counter. This prevents false FAIL during
    Wave 0 execution.

    PIPESTATUS note: all subprocess calls inside check() use bash -c "..." which captures the
    exit code directly. Pipe masking is not a concern because check() tests the exit code of the
    whole bash -c block, not individual pipe stages. For any multi-pipe command inside a check,
    use set -o pipefail at the top of the bash -c string.

    Make executable: chmod +x scripts/verify-phase3.sh

    Verify the script runs without error before finishing (bash scripts/verify-phase3.sh --quick).
  </action>
  <verify>
    <automated>bash scripts/verify-phase3.sh --quick 2>&amp;1 | grep -E "(PASS|FAIL|Results)"</automated>
  </verify>
  <done>
    bash scripts/verify-phase3.sh --quick exits 0, prints "Results: N passed, 0 failed"
    (a few SKIP lines for not-yet-created files are acceptable), and "Phase 3 gate: PASS"
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: packages/pip scaffold + test stubs</name>
  <files>
    packages/pip/pyproject.toml,
    packages/pip/src/goodvibes_cli/__init__.py,
    packages/pip/src/goodvibes_cli/__main__.py,
    packages/pip/tests/conftest.py,
    packages/pip/tests/test_sentinel_merge.py,
    packages/pip/tests/test_copy_templates.py,
    packages/pip/tests/test_install_headroom.py,
    packages/pip/tests/test_configure_mcp.py
  </files>
  <behavior>
    - pyproject.toml: name = "jgiox-goodvibes" (D-01); requires-python = ">=3.10" (D-05);
      hatchling build backend; [tool.hatch.build.targets.wheel.force-include] maps
      ../../templates to goodvibes_cli/templates (D-03); goodvibes script entry point (D-04);
      typer>=0.15 and rich>=14 deps; headroom NOT in [project.dependencies] (D-02 runtime-only);
      [tool.pytest.ini_options] with testpaths = ["tests"]
    - __init__.py: contains __version__ = "1.0.0", nothing else
    - __main__.py: import sys as first statement; sys.version_info < (3, 10) check before any
      other import; prints clear error to stderr naming 3.10 as minimum; sys.exit(1);
      then 'from goodvibes_cli.main import app' and 'app()' after the guard
    - Test stubs: all tests decorated with @pytest.mark.skip(reason="stub — implement in Wave 1")
      except for basic structural tests that pass immediately (import checks, fixture sanity)
    - conftest.py: provides tmp_path-based fixtures and TEMPLATE_CONTENT constant
      matching Phase 2 sentinel-merge.test.ts TEMPLATE_CONTENT (same sentinel markers, v1.0.0)
    - test_sentinel_merge.py: 13 stubs matching Phase 2: versionGte equal/newer/older/minor,
      extractVersion present/absent/full-block, mergeClaude case A/B/B-idempotent/C/D/D2
    - test_copy_templates.py: 9 stubs — copies files, skips CLAUDE.md in copy,
      calls sentinel merge for CLAUDE.md, skips .github/workflows when minimal=True,
      path traversal guard, dry_run returns list without writing, second run no overwrite,
      handles empty template dir
    - test_install_headroom.py: stubs for uv-found, pipx-found, pip-found, all-ENOENT,
      CalledProcessError soft-fail, Python-absent skip, ONNX warning logged, no-shell-true
    - test_configure_mcp.py: stubs for already-registered idempotency, claude-mcp-add primary,
      headroom-mcp-install fallback, headroom-not-found ENOENT-safe, CLAUDE_CONFIG_DIR warning
  </behavior>
  <action>
    Create the directory tree packages/pip/src/goodvibes_cli/ and packages/pip/tests/.

    pyproject.toml content (per D-01 through D-05):
      [project]
      name = "jgiox-goodvibes"
      version = "1.0.0"
      description = "One-command bootstrap for vibe coding projects"
      readme = "README.md"
      requires-python = ">=3.10"
      license = { text = "Apache-2.0" }
      keywords = ["scaffold", "vibe-coding", "claude", "llm", "starter-kit", "cli"]
      dependencies = ["typer>=0.15", "rich>=14"]

      [project.scripts]
      goodvibes = "goodvibes_cli.main:app"

      [build-system]
      requires = ["hatchling"]
      build-backend = "hatchling.build"

      [tool.hatch.build.targets.wheel.force-include]
      "../../templates" = "goodvibes_cli/templates"

      [tool.hatch.build.targets.wheel]
      packages = ["src/goodvibes_cli"]

      [tool.hatch.build.targets.sdist]
      include = ["src/goodvibes_cli/**", "tests/**"]

      [tool.pytest.ini_options]
      testpaths = ["tests"]
      addopts = "-x -q"

    headroom is NOT declared in [project.dependencies] — per D-02 (runtime-only, uv→pipx→pip
    chain at runtime, same as Phase 2 npm pattern).

    __main__.py must have sys.version_info check as lines 1-10, before ANY other import.
    The error message must say: "goodvibes requires Python 3.10 or higher. You have
    Python {major}.{minor}." to stderr, then sys.exit(1). After the guard block:
    'from goodvibes_cli.main import app' then 'app()' in an if __name__ == "__main__" block.

    Test stubs: use @pytest.mark.skip on every test function that imports the modules
    not yet created (sentinel_merge, copy_templates, install_headroom, configure_mcp).
    conftest.py MUST be importable immediately with no missing module errors.

    After creating all files, initialize the uv project (run in packages/pip/):
      uv sync
    to generate uv.lock and validate pyproject.toml parses correctly.

    Do NOT run uv build yet — that is a Wave 2 / full-mode verify-phase3.sh check.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip && uv run pytest tests/ -x -q 2>&amp;1 | tail -5</automated>
  </verify>
  <done>
    uv run pytest tests/ -x -q exits 0; all stubs are skipped (skip count = total test count);
    no ImportError or SyntaxError in output; pyproject.toml name = "jgiox-goodvibes" confirmed
    by grep; __main__.py has sys.version_info check before import typer on line order check.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo root → scripts/ | Shell script reads repo files; no user input, no network |
| pyproject.toml → build system | Hatchling reads pyproject.toml at build time; trusted developer artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | verify-phase3.sh check() inputs | accept | Script inputs are hardcoded grep/test commands, no user-controlled strings |
| T-03-01-02 | Tampering | pyproject.toml force-include path | mitigate | Path "../../templates" is relative to package dir, controlled by developer; hatchling sandboxes traversal |
| T-03-01-SC | Tampering | npm/pip/cargo installs | mitigate | slopcheck passed for all packages; no [ASSUMED]/[SUS] packages in this plan |
</threat_model>

<verification>
Run after both tasks complete:
  bash scripts/verify-phase3.sh --quick
  cd packages/pip && uv run pytest tests/ -x -q
Both must exit 0.
</verification>

<success_criteria>
1. bash scripts/verify-phase3.sh --quick exits 0 with "Phase 3 gate: PASS"
2. uv run pytest tests/ exits 0 (all stubs skip, none error)
3. grep 'name = "jgiox-goodvibes"' packages/pip/pyproject.toml returns 0 (D-01)
4. grep 'goodvibes = "goodvibes_cli.main:app"' packages/pip/pyproject.toml returns 0 (D-04)
5. grep '>=3.10' packages/pip/pyproject.toml returns 0 (D-05)
6. packages/pip/src/goodvibes_cli/__main__.py has sys.version_info before import typer
7. headroom-ai does NOT appear in packages/pip/pyproject.toml [project.dependencies] (D-02)
8. packages/pip/uv.lock exists (uv sync ran successfully)
</success_criteria>

<output>
Create .planning/phases/03-pip-cli/03-01-SUMMARY.md when done
</output>
