---
phase: 09-vibe-platform-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - "09-01"
  - "09-02"
files_modified:
  - README.md
  - CHANGELOG.md
  - JOURNAL.md
  - packages/npm/package.json
  - packages/pip/pyproject.toml
  - templates/CLAUDE.md
autonomous: true
requirements:
  - VPE-01
  - VPE-02
  - VPE-03
  - VPE-04
  - VPE-05
  - VPE-06
must_haves:
  truths:
    - "README IDE compatibility table has rows for Codex CLI, Lovable, Replit, and Bolt.new"
    - "CHANGELOG.md has a [1.5.0] entry documenting Phase 9 additions"
    - "JOURNAL.md has a Phase 9 entry (2026-07-01)"
    - "Both packages are version 1.5.0"
    - "templates/CLAUDE.md version header reads v1.5.0"
  artifacts:
    - path: "README.md"
      provides: "Updated IDE compatibility table with 4 new platform rows"
      contains: "Codex CLI"
    - path: "CHANGELOG.md"
      provides: "[1.5.0] release entry"
      contains: "[1.5.0]"
    - path: "packages/npm/package.json"
      provides: "npm package version"
      contains: "\"version\": \"1.5.0\""
    - path: "packages/pip/pyproject.toml"
      provides: "pip package version"
      contains: "version = \"1.5.0\""
  key_links:
    - from: "README.md IDE table"
      to: "templates/replit.md, templates/.bolt/prompt"
      via: "table rows reference file paths that must exist (written by 09-01)"
---

<objective>
Documentation and version bump to close Phase 9: update the README IDE compatibility
table, write CHANGELOG + JOURNAL entries, and bump both packages to 1.5.0.

Purpose: Users discover platform coverage through the README table. Version 1.5.0
communicates the new platform additions as a minor feature release.

Output: Updated README, CHANGELOG, JOURNAL; both packages at 1.5.0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-vibe-platform-expansion/09-CONTEXT.md
@.planning/phases/09-vibe-platform-expansion/09-01-SUMMARY.md
@.planning/phases/09-vibe-platform-expansion/09-02-SUMMARY.md

# Files being modified
@README.md
@CHANGELOG.md
@JOURNAL.md
@packages/npm/package.json
@packages/pip/pyproject.toml
@templates/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README IDE compatibility table</name>
  <files>README.md</files>
  <action>
Add four new rows to the `## IDE compatibility` table. Insert them after the existing
`| AGENTS.md (cross-tool) | ... |` row (line ~79 in the current file), keeping
alphabetical-ish ordering by platform name or logical grouping.

New rows to append after the AGENTS.md row:

    | OpenAI Codex CLI | `AGENTS.md` (already written) | Any | Automatic — Codex reads AGENTS.md from project root |
    | Lovable | `AGENTS.md` + `CLAUDE.md` (already written) | Any | Automatic — Lovable reads both from repo root |
    | Replit Agent | `replit.md` | Any | Automatic — Replit Agent reads replit.md from project root |
    | Bolt.new | `.bolt/prompt` | Any | Automatic — read when project is opened in Bolt |

Also update the `goodvibes init` description lines in the README that list supported IDEs.
The line at ~line 30 reads "Supports 10 IDEs out of the box: Claude Code, Cursor, ...".
Update the count to 14 and append the 4 new platforms:
"Supports 14 AI coding tools out of the box: Claude Code, Cursor, GitHub Copilot, Windsurf,
Devin Desktop, Kiro, Antigravity, Cline, Amazon Q, Continue.dev, OpenAI Codex CLI, Lovable,
Replit Agent, and Bolt.new"

Also update the --minimal line (~line 44) to add: "replit.md, .bolt/prompt" to the list
of files written under --minimal. Pattern: append ", Replit, Bolt.new" after
"Continue.dev" in the existing list.

Surgical: touch only these three locations (table, IDE count line, --minimal line).
No other reformatting.
  </action>
  <verify>
    <automated>grep -c "Codex CLI\|Lovable\|Replit Agent\|Bolt.new" /home/ygiokas/GoodVibes/README.md</automated>
  </verify>
  <done>README IDE compatibility table has 4 new rows (Codex CLI, Lovable, Replit Agent, Bolt.new). IDE count updated. --minimal description updated. No other lines changed.</done>
</task>

<task type="auto">
  <name>Task 2: CHANGELOG entry, JOURNAL entry, and version bump to 1.5.0</name>
  <files>CHANGELOG.md, JOURNAL.md, packages/npm/package.json, packages/pip/pyproject.toml, templates/CLAUDE.md</files>
  <action>
**CHANGELOG.md** — Replace the `## [Unreleased]` section with a `## [1.5.0] - 2026-07-01`
entry. Keep the Unreleased header above it (empty). Add under Added:

    ## [1.5.0] - 2026-07-01

    ### Added
    - `replit.md` — Replit Agent reads this from project root automatically; encodes goodvibes engineering rules
    - `.bolt/prompt` — Bolt.new reads this when the project opens; encodes the same rules in plain text
    - `docs/platform-setup/chatgpt.md` — beginner guide for pasting goodvibes rules into ChatGPT Projects custom instructions
    - `docs/platform-setup/base44.md` — beginner guide for Base44 AI controls
    - README IDE compatibility table: added Codex CLI, Lovable, Replit Agent, Bolt.new

Also add a `[1.5.0]` diff link at the bottom of CHANGELOG.md alongside the existing
version links, in the same format as [1.4.0].

**JOURNAL.md** — Append a new entry at the top (after the existing most-recent entry).
Format matches existing entries exactly:

    ## 2026-07-01 — Phase 9: Vibe Platform Expansion (v1.5.0)

    Added two new template files (replit.md, .bolt/prompt) and two platform setup guides
    (docs/platform-setup/chatgpt.md, docs/platform-setup/base44.md). Updated README IDE
    compatibility table to cover Codex CLI, Lovable, Replit Agent, and Bolt.new.
    No CLI code changes — copy machinery picks up new template files automatically.

    Files changed: templates/replit.md (new), templates/.bolt/prompt (new),
    templates/docs/platform-setup/chatgpt.md (new), templates/docs/platform-setup/base44.md (new),
    README.md, CHANGELOG.md, packages/npm/package.json, packages/pip/pyproject.toml, templates/CLAUDE.md

    Tests run: cd packages/pip && uv run pytest tests/ (all passing)
               cd packages/npm && npm run test:integration (all passing)

    Docs updated: README.md (IDE table + count), CHANGELOG.md, JOURNAL.md

**packages/npm/package.json** — Change `"version": "1.4.0"` to `"version": "1.5.0"`.
Surgical: only the version field.

**packages/pip/pyproject.toml** — Change `version = "1.4.0"` to `version = "1.5.0"`.
Surgical: only the version field.

**templates/CLAUDE.md** — Change the version header comment from `# goodvibes: v1.4.0`
to `# goodvibes: v1.5.0`. This is the sentinel-controlled block; only the version string
in the header line changes, not the surrounding content.

After all edits, run prebuild to sync template files into the npm package, then run both test suites to confirm GREEN:
    cd packages/npm && npm run prebuild
    cd packages/pip && uv run pytest tests/
    cd packages/npm && npm run test:integration
  </action>
  <verify>
    <automated>grep "\"version\": \"1.5.0\"" /home/ygiokas/GoodVibes/packages/npm/package.json &amp;&amp; grep "version = \"1.5.0\"" /home/ygiokas/GoodVibes/packages/pip/pyproject.toml &amp;&amp; grep "\[1.5.0\]" /home/ygiokas/GoodVibes/CHANGELOG.md &amp;&amp; grep "v1.5.0" /home/ygiokas/GoodVibes/templates/CLAUDE.md</automated>
  </verify>
  <done>CHANGELOG has [1.5.0] entry. JOURNAL has 2026-07-01 Phase 9 entry. Both packages are version 1.5.0. templates/CLAUDE.md header reads v1.5.0. Both test suites pass GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Version strings in package.json / pyproject.toml | Edited in-repo; no network action until publish |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Tampering | package.json version bump | accept | Version bump is a local edit; publishing is a separate manual step not in scope for this plan |
| T-09-03-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan |
</threat_model>

<verification>
    grep "\"version\": \"1.5.0\"" packages/npm/package.json
    grep 'version = "1.5.0"' packages/pip/pyproject.toml
    grep "\[1.5.0\]" CHANGELOG.md
    grep "v1.5.0" templates/CLAUDE.md
    grep -c "Codex CLI\|Lovable\|Replit\|Bolt.new" README.md   # expect 4+
    cd packages/pip && uv run pytest tests/ 2>&1 | tail -5
    cd packages/npm && npm run test:integration 2>&1 | tail -5
</verification>

<success_criteria>
- README IDE compatibility table has 4 new rows: Codex CLI (via AGENTS.md), Lovable (via AGENTS.md + CLAUDE.md), Replit Agent (replit.md), Bolt.new (.bolt/prompt)
- README IDE count updated to 14
- CHANGELOG [1.5.0] entry documents all four new template files
- JOURNAL has Phase 9 entry dated 2026-07-01
- packages/npm/package.json version = 1.5.0
- packages/pip/pyproject.toml version = 1.5.0
- templates/CLAUDE.md header = v1.5.0
- Both test suites GREEN
</success_criteria>

<output>
Create `.planning/phases/09-vibe-platform-expansion/09-03-SUMMARY.md` when done
</output>
