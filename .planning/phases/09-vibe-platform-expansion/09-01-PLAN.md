---
phase: 09-vibe-platform-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - templates/replit.md
  - templates/.bolt/prompt
  - templates/docs/platform-setup/chatgpt.md
  - templates/docs/platform-setup/base44.md
autonomous: true
requirements:
  - VPE-01
  - VPE-04
must_haves:
  truths:
    - "templates/replit.md exists, is plain markdown (no frontmatter), and encodes the goodvibes engineering rules"
    - "templates/.bolt/prompt exists, is plain text (no markdown formatting), and encodes the same rules"
    - "templates/docs/platform-setup/chatgpt.md exists with beginner-friendly instructions for pasting rules into ChatGPT Projects"
    - "templates/docs/platform-setup/base44.md exists with beginner-friendly instructions for Base44 AI controls"
    - "All four files are under 3000 characters each"
  artifacts:
    - path: "templates/replit.md"
      provides: "Replit Agent configuration — auto-read from project root"
    - path: "templates/.bolt/prompt"
      provides: "Bolt.new persistent instructions — read when project opens"
    - path: "templates/docs/platform-setup/chatgpt.md"
      provides: "Step-by-step guide for ChatGPT Projects custom instructions"
    - path: "templates/docs/platform-setup/base44.md"
      provides: "Step-by-step guide for Base44 AI controls"
  key_links:
    - from: "templates/replit.md"
      to: "existing templates/CLAUDE.md rule set"
      via: "same engineering rules, Replit-native section headers"
    - from: "templates/.bolt/prompt"
      to: "existing templates/AGENTS.md rule set"
      via: "same rules in plain text, no markdown"
---

<objective>
Author the four new template files for Phase 9: two platform rule files (replit.md,
.bolt/prompt) and two platform setup guides (chatgpt.md, base44.md).

Purpose: goodvibes init will automatically copy these to the user's project via the
existing fs-extra / shutil.copytree machinery. No CLI code changes required.

Output: Four static template files in templates/ that encode the goodvibes engineering
rules for vibe-coding platforms.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-vibe-platform-expansion/09-CONTEXT.md
@.planning/phases/09-vibe-platform-expansion/09-RESEARCH.md

# Existing template files to match in style and content
@templates/AGENTS.md
@templates/GEMINI.md
@templates/docs/onboarding.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author templates/replit.md and templates/.bolt/prompt</name>
  <files>templates/replit.md, templates/.bolt/prompt</files>
  <action>
Create two new template files. Both encode the same goodvibes engineering rules as
AGENTS.md and CLAUDE.md, adapted to each platform's expected format.

**templates/replit.md**
Format: plain markdown, NO YAML frontmatter. Replit Agent reads this automatically from
project root. Use Replit's documented section headers: Project Overview, Coding Style,
Simplicity First, Surgical Changes, Fail Loud, Security, Testing. Add a one-line comment
at the very top telling users that Replit Agent may regenerate this file after its first
session — commit this file to git to preserve it (per D-03 / pitfall A2 in CONTEXT.md).

Keep the content faithful to the existing rule set but concise enough to fit in Replit's
recommended "focused and concise" guidance. Target ~600–900 characters total.

**templates/.bolt/prompt**
Format: plain text ONLY — no markdown headers, no frontmatter, no fenced code blocks.
Bolt.new reads `.bolt/prompt` (not `.bolt/promptfile`) when the project is opened and
applies these instructions to every prompt (per D-03). The file lives in a `.bolt/`
subdirectory — create the subdirectory as needed.

Mirror the same goodvibes rules (think before coding, simplicity ladder, surgical changes,
fail loud, security) in prose form. One section per rule, no markdown syntax. Target
~600–900 characters total.

Neither file adds new abstractions beyond what is already in AGENTS.md / CLAUDE.md.
Ponytail: shortest text that fully represents the rules.
  </action>
  <verify>
    <automated>test -f /home/ygiokas/GoodVibes/templates/replit.md &amp;&amp; test -f /home/ygiokas/GoodVibes/templates/.bolt/prompt &amp;&amp; echo "files exist" &amp;&amp; ! grep -q "^---" /home/ygiokas/GoodVibes/templates/replit.md &amp;&amp; echo "replit.md has no frontmatter" &amp;&amp; wc -c &lt; /home/ygiokas/GoodVibes/templates/replit.md &amp;&amp; wc -c &lt; /home/ygiokas/GoodVibes/templates/.bolt/prompt</automated>
  </verify>
  <done>Both files exist. replit.md is valid plain markdown with no YAML frontmatter. .bolt/prompt is plain text. Both are under 3000 characters.</done>
</task>

<task type="auto">
  <name>Task 2: Author templates/docs/platform-setup/chatgpt.md and base44.md</name>
  <files>templates/docs/platform-setup/chatgpt.md, templates/docs/platform-setup/base44.md</files>
  <action>
Create two beginner-friendly setup guides for UI-only platforms. These go in
`templates/docs/platform-setup/` alongside the existing `docs/onboarding.md`.

**templates/docs/platform-setup/chatgpt.md**
Guide users to paste the goodvibes engineering rules into ChatGPT Projects custom
instructions. ChatGPT has no file-based rule mechanism; users must use the Projects UI.

Structure:
1. What this is (one sentence)
2. Step-by-step: Open ChatGPT → Create or open a Project → click the project name →
   Project Instructions → paste the text below → Save
3. The text to paste: the goodvibes rule set (same content as AGENTS.md / CLAUDE.md, under
   "several thousand characters" limit of ChatGPT Projects)
4. A "last verified" date line at the bottom (set to 2026-07-01)
5. A note that the UI may change — if steps differ, look for "Project Instructions" or
   "Custom Instructions" in the project settings

Tone: assume the reader has never opened ChatGPT before. Short sentences. No jargon.
Target ~800–1500 characters for steps + ~1200 characters for the paste block.

**templates/docs/platform-setup/base44.md**
Guide users to paste goodvibes rules into Base44's AI controls panel. Base44 has no
file-based rule mechanism; instructions live in the AI controls UI.

Structure:
1. What this is (one sentence)
2. Step-by-step: Open Base44 → Open your project → AI controls (top-right panel or
   Settings) → Custom Instructions → paste the text below → Save
3. The text to paste: same goodvibes rule set
4. A "last verified" date line (2026-07-01)
5. A note about Base44 Skills (separate feature for saved instructions; optional, out of
   scope for goodvibes)

Tone and target length: same as chatgpt.md.

Both files write `## Last verified: 2026-07-01` at the bottom.
Both files note that if the UI steps differ from the guide, look for
"Custom Instructions" or "AI controls" in project settings — the paste text stays the same.
  </action>
  <verify>
    <automated>test -f /home/ygiokas/GoodVibes/templates/docs/platform-setup/chatgpt.md &amp;&amp; test -f /home/ygiokas/GoodVibes/templates/docs/platform-setup/base44.md &amp;&amp; echo "guides exist" &amp;&amp; grep -l "Last verified" /home/ygiokas/GoodVibes/templates/docs/platform-setup/chatgpt.md /home/ygiokas/GoodVibes/templates/docs/platform-setup/base44.md</automated>
  </verify>
  <done>Both guide files exist under templates/docs/platform-setup/. Each contains step-by-step instructions, a paste block with the goodvibes rules, and a Last verified date. Both are under 3000 characters.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| template files → user project | Static authored content; copied verbatim by existing machinery |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | templates/replit.md, templates/.bolt/prompt | accept | Files are static authored content in the goodvibes repo. Existing path traversal guard (`rel.includes('..')` in copy-templates.ts, `ignore_fn` in copy_templates.py) remains in place. New files introduce no new threat surface. |
| T-09-02 | Information Disclosure | templates/docs/platform-setup/*.md | accept | Guides contain only public engineering rule text. No secrets, no PII, no credentials. |
| T-09-SC | Tampering | npm/pip installs | accept | No new packages installed in this plan. Package legitimacy gate not applicable. |
</threat_model>

<verification>
After both tasks complete:
- `ls templates/replit.md templates/.bolt/prompt templates/docs/platform-setup/chatgpt.md templates/docs/platform-setup/base44.md` — all four exist
- `grep -c "^---" templates/replit.md` returns 0 (no frontmatter)
- Each file is under 3000 characters: `wc -c templates/replit.md templates/.bolt/prompt templates/docs/platform-setup/chatgpt.md templates/docs/platform-setup/base44.md`
</verification>

<success_criteria>
- templates/replit.md: plain markdown, no frontmatter, contains the goodvibes engineering rules, under 3000 chars
- templates/.bolt/prompt: plain text (no markdown syntax), contains the same rules, under 3000 chars
- templates/docs/platform-setup/chatgpt.md: beginner step-by-step guide with paste block, last-verified date
- templates/docs/platform-setup/base44.md: beginner step-by-step guide with paste block, last-verified date
- No CLI code changes made (copy machinery picks these up automatically)
</success_criteria>

<output>
Create `.planning/phases/09-vibe-platform-expansion/09-01-SUMMARY.md` when done
</output>
