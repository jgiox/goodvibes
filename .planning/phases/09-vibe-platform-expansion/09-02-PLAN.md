---
phase: 09-vibe-platform-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/pip/tests/conftest.py
  - packages/pip/tests/test_copy_templates.py
  - packages/npm/src/steps/copy-templates.integration.test.ts
autonomous: true
requirements:
  - VPE-01
  - VPE-02
  - VPE-03
  - VPE-04
  - VPE-05
  - VPE-06
  - VPE-07
  - VPE-08
must_haves:
  truths:
    - "Python test suite passes GREEN with 6 new test cases covering replit.md and .bolt/prompt"
    - "TS integration test suite passes GREEN with 6 new test cases covering replit.md and .bolt/prompt"
    - "conftest.py template_dir fixture includes stubs for replit.md and .bolt/prompt"
    - "All new tests follow the 3-test pattern: fresh init writes, skip existing, write under --minimal"
  artifacts:
    - path: "packages/pip/tests/conftest.py"
      provides: "Updated template_dir fixture with replit.md and .bolt/prompt stubs"
    - path: "packages/pip/tests/test_copy_templates.py"
      provides: "6 new Python test cases (3 per file)"
    - path: "packages/npm/src/steps/copy-templates.integration.test.ts"
      provides: "6 new TS integration test cases (3 per file)"
  key_links:
    - from: "packages/pip/tests/conftest.py"
      to: "packages/pip/tests/test_copy_templates.py"
      via: "template_dir fixture must have stubs or tests will have false positives"
    - from: "packages/npm/src/steps/copy-templates.integration.test.ts"
      to: "templates/ (real files via resolveTemplatesDir)"
      via: "TS integration tests use resolveTemplatesDir() — real template files must exist"
---

<objective>
Extend both test suites with the 3-test pattern for replit.md and .bolt/prompt.

Purpose: Every new template file must be covered by integration tests verifying (1) fresh
init writes the file, (2) existing file is skipped, (3) --minimal also writes the file.

Output: 6 new Python tests + 6 new TS integration tests, all passing GREEN.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-vibe-platform-expansion/09-CONTEXT.md

# Existing test patterns to follow exactly
@packages/pip/tests/conftest.py
@packages/pip/tests/test_copy_templates.py
@packages/npm/src/steps/copy-templates.integration.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend Python conftest.py and test_copy_templates.py</name>
  <files>packages/pip/tests/conftest.py, packages/pip/tests/test_copy_templates.py</files>
  <behavior>
    - test_copy_templates_writes_replit_md_on_fresh_init: copy_templates to empty dir → (tmp_dir / "replit.md").exists() is True
    - test_copy_templates_skips_existing_replit_md_and_counts_as_skipped: pre-write "# custom" to replit.md → copy_templates → file still reads "# custom" (no-clobber honored)
    - test_copy_templates_minimal_writes_replit_md: copy_templates(minimal=True) → (tmp_dir / "replit.md").exists() is True
    - test_copy_templates_writes_bolt_prompt_on_fresh_init: copy_templates to empty dir → (tmp_dir / ".bolt" / "prompt").exists() is True
    - test_copy_templates_skips_existing_bolt_prompt_and_counts_as_skipped: pre-write ".bolt/prompt" → copy_templates → content preserved
    - test_copy_templates_minimal_writes_bolt_prompt: copy_templates(minimal=True) → (tmp_dir / ".bolt" / "prompt").exists() is True
  </behavior>
  <action>
**conftest.py** — add stubs for the two new files to the template_dir fixture. Follow the
exact pattern of existing stubs (one write_text call per file). For .bolt/prompt:

    bolt_dir = d / ".bolt"
    bolt_dir.mkdir()
    (bolt_dir / "prompt").write_text("Engineering rules\n")

For replit.md:

    (d / "replit.md").write_text("## Project\n")

Add these two blocks after the existing Phase 8 stubs (after the kiro_steering block,
before `return d`).

**test_copy_templates.py** — append 6 new test functions after the existing tests. Follow
the naming pattern exactly:

    def test_copy_templates_writes_replit_md_on_fresh_init(tmp_dir, template_dir, mocker):
    def test_copy_templates_skips_existing_replit_md_and_counts_as_skipped(tmp_dir, template_dir, mocker):
    def test_copy_templates_minimal_writes_replit_md(tmp_dir, template_dir, mocker):
    def test_copy_templates_writes_bolt_prompt_on_fresh_init(tmp_dir, template_dir, mocker):
    def test_copy_templates_skips_existing_bolt_prompt_and_counts_as_skipped(tmp_dir, template_dir, mocker):
    def test_copy_templates_minimal_writes_bolt_prompt(tmp_dir, template_dir, mocker):

Each test patches merge_claude (same as all existing tests). The skip tests pre-write the
file with "# custom\n" content before calling copy_templates, then assert the content is
unchanged. For .bolt/prompt, pre-create the .bolt/ directory before writing the file.

Run from packages/pip/ to confirm GREEN before finishing.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/pip &amp;&amp; uv run pytest tests/test_copy_templates.py -v --tb=short 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>All tests in test_copy_templates.py pass, including the 6 new ones. No existing tests broken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend TS integration test suite</name>
  <files>packages/npm/src/steps/copy-templates.integration.test.ts</files>
  <behavior>
    - 'writes replit.md on fresh init (VPE-01)': copyTemplates to empty tmpDir → existsSync(join(tmpDir, 'replit.md')) is true, written array contains 'replit.md'
    - 'skips existing replit.md and counts it as skipped (VPE-02)': writeFileSync replit.md with '# custom\n' → copyTemplates → content unchanged, skipped array contains 'replit.md'
    - 'writes replit.md under --minimal (VPE-03)': copyTemplates(templateDir, tmpDir, false, true) → existsSync(join(tmpDir, 'replit.md')) is true
    - 'writes .bolt/prompt on fresh init (VPE-04)': copyTemplates → existsSync(join(tmpDir, '.bolt', 'prompt')) is true, written array contains '.bolt/prompt' or 'bolt'
    - 'skips existing .bolt/prompt and counts it as skipped (VPE-05)': mkdirSync + writeFileSync → copyTemplates → content unchanged, skipped contains '.bolt/prompt'
    - 'writes .bolt/prompt under --minimal (VPE-06)': copyTemplates(templateDir, tmpDir, false, true) → existsSync(join(tmpDir, '.bolt', 'prompt')) is true
  </behavior>
  <action>
Append 6 new test cases to the existing `copyTemplates — minimal filter scope` describe
block (the last `describe` block before `copyTemplates — workflow conflict guard`). This
keeps all the new-file tests in one logical group, consistent with where GEMINI.md,
AGENTS.md, .clinerules, .amazonq, .continue, and .devin tests were added in Phase 8.

Import mkdirSync from 'fs' is already present at line 3 of the file. No new imports needed.

Pattern for the skip test (copy exactly from AGENTS.md / devin pattern):

    it('skips existing replit.md and counts it as skipped (VPE-02)', async () => {
      writeFileSync(join(tmpDir, 'replit.md'), '# custom\n')
      const { skipped } = await copyTemplates(templateDir, tmpDir, false, false)
      expect(readFileSync(join(tmpDir, 'replit.md'), 'utf8')).toBe('# custom\n')
      expect(skipped.some((f: string) => f.includes('replit.md'))).toBe(true)
    })

Pattern for .bolt/prompt skip test — pre-create the directory:

    it('skips existing .bolt/prompt and counts it as skipped (VPE-05)', async () => {
      mkdirSync(join(tmpDir, '.bolt'), { recursive: true })
      writeFileSync(join(tmpDir, '.bolt', 'prompt'), 'custom\n')
      const { skipped } = await copyTemplates(templateDir, tmpDir, false, false)
      expect(readFileSync(join(tmpDir, '.bolt', 'prompt'), 'utf8')).toBe('custom\n')
      expect(skipped.some((f: string) => f.includes('.bolt'))).toBe(true)
    })

TS integration tests use the real templates/ directory via resolveTemplatesDir(). This
means templates/replit.md and templates/.bolt/prompt MUST exist before these tests pass.
If 09-01 has not been executed yet, these tests will fail at the "writes on fresh init"
assertions — that is expected RED state (Wave 1 plans run in parallel; execute 09-01
before running 09-02's verify command, or run both plans then verify).

Run from packages/npm/ to confirm GREEN.
  </action>
  <verify>
    <automated>cd /home/ygiokas/GoodVibes/packages/npm &amp;&amp; npm run test:integration 2&gt;&amp;1 | tail -20</automated>
  </verify>
  <done>All integration tests pass, including the 6 new ones for replit.md and .bolt/prompt. No existing tests broken.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → real filesystem tmpdir | Integration tests use real tmpdir; no network, no external calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | test tmpdir cleanup | accept | afterEach/teardown cleans tmpdir; existing pattern, no new risk |
| T-09-02-SC | Tampering | npm/pip installs | accept | No new packages. uv run pytest and npm run test:integration use existing toolchain. |
</threat_model>

<verification>
After both tasks:

Python:
    cd packages/pip && uv run pytest tests/test_copy_templates.py -v 2>&1 | grep -E "PASSED|FAILED|ERROR"

TS:
    cd packages/npm && npm run test:integration 2>&1 | grep -E "pass|fail|error" -i

All 6 new tests must show PASSED / pass. Zero regressions.
</verification>

<success_criteria>
- conftest.py template_dir fixture has stubs for replit.md and .bolt/prompt
- 6 new Python tests pass (3 per file): writes on fresh init, skips existing, writes under --minimal
- 6 new TS integration tests pass (3 per file): writes on fresh init, skips existing, writes under --minimal
- Full test suites remain GREEN (no regressions in existing tests)
</success_criteria>

<output>
Create `.planning/phases/09-vibe-platform-expansion/09-02-SUMMARY.md` when done
</output>
