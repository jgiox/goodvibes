import { describe, it } from 'vitest'

describe('init command', () => {
  it.todo('--dry-run: prints files, writes nothing')
  it.todo('--minimal: skips headroom and CI files')
  it.todo('prints file list on completion (NPM-04)')
  it.todo('prints next-steps block with max 3 steps (NPM-05)')
  it.todo('zero-config: runs without stdin input (NPM-02)')
})
