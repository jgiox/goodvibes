# NOTE: No rate limiting configured — the endpoint is open to the public internet.
# Counter values are for directional guidance only and cannot be relied upon for precise numbers.
# Add Cloudflare Rate Limiting via the dashboard or wrangler.toml [[unsafe.bindings]] if accuracy matters.
name = "goodvibes-telemetry"
main = "worker.js"
compatibility_date = "2025-01-01"
workers_dev = true

[[kv_namespaces]]
binding = "INSTALLS"
# created via: wrangler kv namespace create INSTALLS
id = "b8a19ad579f343a794771d7809486f7a"
